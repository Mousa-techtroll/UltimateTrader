# Phase 2.7 — Delete dead engine EvaluateSetupQuality overloads + orphan include

## Phase
- **Phase id:** 2.7
- **Fix ids:** 2.7
- **Touches trading logic:** N (dead-code removal; no live path touched)
- **depends_on:** 2.1 (orchestrator-honors-engine-scorer) and 2.4-GATE — both COMMITTED. The deadness of these overloads is a CONSEQUENCE of 2.1.
- **Binding stok param/option used:** n/a (no-op; dead-code removal)

## GREP-VERIFICATION (done FIRST, per task instruction)
Repo-wide `grep -rn "EvaluateSetupQuality"` (--include=*.mqh,*.mq5):
- **Exactly ONE real call site:** `Include/Core/CSignalOrchestrator.mqh:746`. Reading 700–779 shows the Fix-2.1 branch (lines 743–748):
  - `is_engine` → `quality = signal.setupQuality;` (line 744) — the engine's own pre-computed scorer tier; does NOT call EvaluateSetupQuality at all.
  - else → `m_evaluator.EvaluateSetupQuality(daily_trend, h4_trend, regime, macro_score, signal.comment, isBearRegime, sig_type)` (746–748) — this resolves to the **LEGACY** `(ENUM_TREND_DIRECTION daily, ENUM_TREND_DIRECTION h4, …)` overload (CSetupEvaluator:67), NOT the engine `(EntrySignal&, IMarketContext*)` overload.
- **`Tests/TestQualityScoring.mq5:60`** is a *local replica* ("We replicate CSetupEvaluator scoring logic locally" — line 11); it does NOT `#include` CSetupEvaluator (only Enums/Structs) and does NOT call the engine overload. Not compiled into the EA. Not a caller.
- **Zero callers** of `EvaluateSetupQuality(const EntrySignal&, IMarketContext*, ...)` repo-wide. → both engine overloads (CSetupEvaluator 402 + 410) are provably DEAD after 2.1.
- **Orphan include check:** `#include "CConfluenceScorer.mqh"` (CSetupEvaluator:16) was used ONLY at line 404 (the `CConfluenceScorer scorer;` inside the 402 overload). Lines 271/287/397 are COMMENT mentions only. After deleting the overload, the include is orphaned WITHIN this file. CConfluenceScorer the CLASS stays alive — independently included by `UltimateTrader.mq5:101`, the 4 engines (CExpansionEngine/CRangeReversionEngine/CTrendContinuationEngine/CReversalSweepEngine), and `CMajorStrategyEngine.mqh`. Removing it from CSetupEvaluator only drops a now-unused include.

## What changed (DELETED vs KEPT)
- **Files touched (verified file:line against current source):**
  - `Include/Validation/CSetupEvaluator.mqh`:
    - **DELETED** the two dead engine overloads `EvaluateSetupQuality(const EntrySignal&, IMarketContext*, int &out_score)` (was 402–407) and the convenience `EvaluateSetupQuality(const EntrySignal&, IMarketContext*)` (was 410–414), plus their header comment block. Replaced with a Fix-2.7 explanatory comment.
    - **DELETED** the orphan `#include "CConfluenceScorer.mqh"` (was line 16). Replaced with a Fix-2.7 explanatory comment.
- **KEPT (provably still referenced — NOT deleted):**
  - The **legacy** `EvaluateSetupQuality(ENUM_TREND_DIRECTION daily, …)` overload (line 67) — IT is the live non-engine call from CSignalOrchestrator:746. Untouched.
  - The `CConfluenceScorer` class itself and every other `#include` of it (UltimateTrader.mq5, 4 engines, CMajorStrategyEngine) — those are the live scoring path for engines via `SetScorer`.
- **Diff summary:** removed ~13 lines of unreachable code (two overloads) + one orphan include from CSetupEvaluator only; replaced with comments documenting the removal. No live path altered.
- **Logic-cut check:** NO trading logic deleted. The removed overloads were unreachable (zero callers after 2.1). The live scoring paths (legacy overload for non-engine; engine `signal.setupQuality` set by each engine's own shared CConfluenceScorer) are untouched.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 10364 ms elapsed, cpu='X64 Regular'
```
- Errors: 0 | New warnings vs 38-baseline: 0
- Error 106 / include issues: **NONE** — the orphan-include removal compiled clean; no "undeclared identifier" (CConfluenceScorer is not referenced in CSetupEvaluator after the overload deletion). This is the dedicated risk the plan flagged; verified safe.
- `.ex5` regenerated: yes (10364 ms compile; verified at the combined-build step below)

## QA backtest (decoded, ACTUAL — never estimated)
- COMBINED 2.6+2.7+2.8 byte-identity A/B run (logged in 2-8.md) = **A==B PASS** (all 5 CSVs md5-match + cmp IDENTICAL).
- **No-op / byte-identical claim:** YES — confirmed. Removing unreachable code cannot move any executed path; combined A==B (raw CSV md5 + cmp clean).

## stok review + verdict
- PENDING (per task instruction).

## Kill-criteria check
- **Phase kill-criterion:** byte-identical backtest regression guard (FIX-SOUND dead code); 0 errors; no error 106.
- **Result:** PASS — 0 err / 0 new warn, no error 106; combined A==B byte-identical (2-8.md).

## mt5-developer sign-off
- Grep-verified zero callers of the engine overloads + include used only by the deleted overload; compile clean (0 err / 0 new warn, no error 106); only dead code removed; legacy overload + CConfluenceScorer class kept. — SIGNED

## stok sign-off
- **SIGN-OFF (2026-06-27, INDEPENDENT stok no-op gate — verified at SOURCE, not the write-up).** Re-ran repo-wide `grep -rn EvaluateSetupQuality` (*.mqh/*.mq5): exactly ONE live call site, `CSignalOrchestrator.mqh:746`; remaining hits are the kept legacy declaration (CSetupEvaluator:72), the Fix-2.7 deletion comments (17/401/402), and `Tests/TestQualityScoring.mq5:60` (a standalone local replica — no include of CSetupEvaluator, not compiled into the EA, not a caller). Read CSignalOrchestrator:735–748 at source: the Fix-2.1 branch is correct — `is_engine → quality = signal.setupQuality` (no call); `else → EvaluateSetupQuality(daily_trend, h4_trend, regime, macro_score, signal.comment, isBearRegime, sig_type)`, whose 7-arg signature resolves unambiguously to the KEPT legacy `(ENUM_TREND_DIRECTION, …)` overload at CSetupEvaluator:72 (verified intact 72–76), NOT the deleted `(EntrySignal&, IMarketContext*)` engine overloads. The deleted overloads built a LOCAL non-shared CConfluenceScorer and had zero callers; engines score via their OWN shared scorer (CMajorStrategyEngine::SetScorer). CConfluenceScorer the class + every other include (UltimateTrader.mq5:101, 4 engines, CMajorStrategyEngine) preserved. Orphan-include removal compiled clean — no error 106. No live path lost. Byte-identical on production config (combined A==B). SIGNED.

## Decision
- **PASS → COMMITTED** — dead-code removal, byte-identical, zero callers verified at source. BOTH agents signed (mt5-developer QA + INDEPENDENT stok).

## Commit
- **Hash:** (not committed — task instruction: do NOT commit)
- **Message:** n/a
