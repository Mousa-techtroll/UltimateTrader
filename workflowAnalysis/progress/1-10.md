# Phase 1.10 — CRangeBoxDetector width-stability tautology → real Bars() guard

> Implemented and validated JOINTLY with Phase 1.11 (fix 1.12) in a single combined byte-identity A/B, because both fixes are no-ops on the XAUUSD gold production config. See Phase 1.11 log (`1-11.md`) for the same shared byte-identity result.

## Phase
- **Phase id:** 1.10
- **Fix ids:** 1.11
- **Touches trading logic:** N (NOT-A-TRADING-PROBLEM — cosmetic robustness; identical steady-state)
- **depends_on (phases that must be COMMITTED first):** 1.9 (COMMITTED)
- **Binding stok param/option used:** n/a (no-op; the literal guard text `Bars(_Symbol,PERIOD_H1) >= m_box_lookback + 11` is specified verbatim in the plan)

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/MarketAnalysis/CRangeBoxDetector.mqh` (168–172) — replaced the constant-true width-stability guard `if(m_box_lookback + 10 <= 100)` with the real data-availability guard `if(Bars(_Symbol, PERIOD_H1) >= m_box_lookback + 11)`. Added a 3-line explanatory comment (the older-box reads start at shift 11 and span `m_box_lookback` bars, so `11 + m_box_lookback` closed H1 bars must exist).
- **Diff summary:** The old guard was a tautology — with the default `m_box_lookback` (≈20), `20 + 10 = 30 <= 100` is always true, so it never actually verified there was enough history for the `CopyHigh/CopyLow(_Symbol, PERIOD_H1, 11, m_box_lookback, …)` older-box reads at shift 11. The new guard ties the gate to the actual bar count required by those reads. The inner width-stability math (older-box hh/ll over `m_box_lookback` bars, `width_change > m_max_width_change_pct` → invalidate) is UNCHANGED; only the entry condition to that block changed.
- **Logic-cut check:** NO trading logic deleted. A guard condition was *tightened* from always-true to a genuine bar-count check. The body of the width-stability branch (and every other validation check 1/2/4) is byte-for-byte unchanged. No disable, no short-circuit, no toggle park.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 7807 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (exactly 38; none reference CRangeBoxDetector.mqh).
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — `UltimateTrader.ex5` 890,494 bytes, md5 `a1ace2aede1337eab767a3fc6bbb45f3` (B-build, contains BOTH Phase-1.10 and Phase-1.11 edits) ≠ A-baseline `1d78fa96ff70bd203059a58fb345ff38`. A-baseline copied aside to `/tmp/UT_phase1_10_11_baseline.ex5` BEFORE any edit.

## QA backtest (decoded, ACTUAL — never estimated) — SHARED combined A/B (1.10 + 1.11)
- **Config:** XAUUSD+ H1, production `UltimateTrader.set` (`InpSymbolProfile=0` gold, `InpSignalSource=2` BOTH, `InpEnableMultiStrategy=false`, `InpEnablePullbackCont=true`, `InpEnableCrashDetector=true`, `InpRubberBandAPlusOnly=true`, `InpMaxPositions=5`, `InpMaxTradesPerDay=5`, `InpDXYSymbol=USDX`, `InpVIXSymbol=VIX`). Deposit 10000 / leverage 100. Same `.set` + `.ini` for A and B → any delta is purely the binary. Terminal `C:\Program Files\Vantage Markets MT5 Terminal\terminal64.exe` / user `nullkuhl` / terminal ID `725B72F25E46C780EF59F57016D58156`, WITHOUT `/portable` (proven prior-phase harness; the committed `backtest_symbol.sh` path/ID/data-dir are STALE — Vantage International path + `010E…` ID + `ahmed` dir — and were NOT used).
- **Mode:** Model=1 (1-min OHLC), same as Phases 1.1–1.9.
- **Window:** 2024 (acceptance = byte-identical).
- **A (committed HEAD baseline `.ex5`):** trades 140 | WR 46.43% | net $1190.84 | totR 11.76 | avgR 0.0840 | L/S 109/31. A reproduced the EXACT Phase-1.9 committed-HEAD baseline (Stats.csv md5 `04d116972696f905c6ea5fa6701a7155`), confirming A IS HEAD and the harness is deterministic.
- **B (this build — BOTH 1.10 + 1.11 fixes):** trades 140 | WR 46.43% | net $1190.84 | totR 11.76 | avgR 0.0840 | L/S 109/31.
- **Delta + explanation:** ZERO delta. **A == B BYTE-IDENTICAL.** All 5 produced CSVs are raw-byte identical (`cmp` clean, md5-matching): Stats `04d116972696f905c6ea5fa6701a7155`, TradeEvents `825489b61fbf3db5cb2302361de66bb1`, Risk `8babda0f7b846cc6fa94941ecb39c815`, Candidates `bcc44924d5d3fc31a5f4d35e5142b0a7`, backtest_results `177ca6a9f38c32459643e894a8ba195d`. Mechanism (1.10): on gold the old guard was already always-true (`m_box_lookback`≈20 → `30<=100`) AND a full 2024 run always has `Bars(H1) >> lookback+11`, so the width-stability branch executes identically under both guards; additionally CRangeBoxDetector-consuming engines are OFF by default (`InpEnableMultiStrategy=false`) so the path is off the production trade path entirely.
- **No-op / byte-identical claim:** YES — trade-for-trade, SL-for-SL, R-for-R IDENTICAL on XAUUSD+ H1 2024. The `.ex5` changed; the production trade path did not.
- **Bisection:** NOT REQUIRED — A==B held with both fixes applied together, so neither fix introduced any divergence. The no-op claim is confirmed, not contradicted.

## stok review + verdict
- **Numbers handed to stok:** A==B byte-identical (5/5 CSVs cmp-clean + md5-matching) on XAUUSD+ H1 2024; A reproduces the committed-HEAD Phase-1.9 md5; the mechanism (tautology guard was already always-true on gold; full-year bar count far exceeds `lookback+11`; RangeBox engines OFF by default).
- **stok assessment:** PENDING (independent stok agent).
- **stok verdict:** PENDING.

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** NOT-A-TRADING-PROBLEM — acceptance = byte-identical XAUUSD+ H1 backtest.
- **Result:** PASS — A==B byte-identical (5/5 CSVs cmp-clean). No criterion tripped.

## mt5-developer sign-off
- Compile clean (0 errors, exactly 38 warnings = baseline, 0 new). Fix implements the validated spec exactly: the constant-true tautology `m_box_lookback + 10 <= 100` is replaced by the real guard `Bars(_Symbol, PERIOD_H1) >= m_box_lookback + 11` matching the shift-11/`m_box_lookback`-span older-box reads. NO logic cut (guard tightened only; width-stability body unchanged). file:line verified against current source (168–172). A==B byte-identical on XAUUSD+ H1 2024 (5/5 CSVs cmp-clean, A reproduces committed-HEAD md5). **SIGNED.**

## stok sign-off
- **SIGN-OFF (independent stok agent).** Verified at source (not the write-up): guard at CRangeBoxDetector.mqh:172 is now `Bars(_Symbol,PERIOD_H1) >= m_box_lookback + 11`, correctly matching the shift-11 / `m_box_lookback`-span older-box `CopyHigh/CopyLow` reads at 177–178; width-stability body unchanged — guard tightened, no logic cut. A==B byte-identical on XAUUSD+ H1 2024 (5/5 CSVs cmp-clean + md5-match) is the necessary and expected outcome: the old tautology was already always-true on gold AND a full-year run has `Bars(H1) >> lookback+11`, and the RangeBox-consuming engines are OFF by default. Zero trading impact on the production config. ACCEPT.

## Decision
- **PASS (mt5 side)** — compile clean, A==B byte-identical, kill-criterion not tripped. Status → **mt5-QA-DONE** (byte-identical, validated jointly with Phase 1.11). stok PENDING. Do NOT commit; do NOT advance NEXT ACTION until the independent stok agent signs off.

## Commit
- **Hash:** (not committed — main session runs git; awaiting stok sign-off)
- **Message:** (pending)
