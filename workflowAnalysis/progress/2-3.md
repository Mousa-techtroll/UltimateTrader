# Phase 2.3 — Scorer L3 objective spine + real engine_confluence + expansion-only killzone

## Phase
- **Phase id:** 2.3
- **Fix ids:** 2.3 (BETTER-OPTION)
- **Touches trading logic:** Y (but unreachable on the production `.set` — see byte-identity)
- **depends_on (phases that must be COMMITTED first):** 2.2 (spine freshness; COMMITTED b6228cb), 1.2 (closed-bar BOS anchors; COMMITTED)
- **Binding stok param/option used:** `InpSpineMinConfluence = 25` (0–100 SMC scale; below the SMC hard-reject floor of 40 → spine necessary-but-weaker). Killzone (axis 7) awarded ONLY for a real expansion/breakout `engine_mode`, NOT a clock window.

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/Validation/CConfluenceScorer.mqh`
    - Added member `int m_spine_min_confluence;` (Phase 2.3 objective spine floor) + ctor default `= 25`; added defaulted 6th `Configure(...)` param `int spine_min_confluence = 25` (wired exactly like the Phase-2.2 `bos_freshness_bars` defaulted param).
    - L3 HARD GATE spine: `bool engine_spine = (signal.engine_confluence > 0);` → `bool engine_spine = (ctx.GetSMCConfluenceScore(dir) >= m_spine_min_confluence);`. The spine is now OBJECTIVE — engines can no longer self-certify their way past the gate by setting `engine_confluence>0`. 0 remains possible.
    - Killzone axis 7: `if(signal.engine_confluence >= 70) points += 1;` → award only for a real expansion/breakout `engine_mode` (`MODE_DISPLACEMENT || MODE_LONDON_BREAKOUT || MODE_COMPRESSION_BO || MODE_INSTITUTIONAL_CANDLE || MODE_PANIC_MOMENTUM`). The pure session-clock modes (`MODE_SILVER_BULLET / MODE_NY_CONTINUATION / MODE_LONDON_CLOSE`) and the engine's self-certified `engine_confluence` no longer earn the killzone point.
  - `Include/EntryPlugins/CReversalSweepEngine.mqh` (AdoptSignal ~199-205)
    - `signal.engine_confluence = 60; if(has_shift) signal.engine_confluence = 75;` → `signal.engine_confluence = (m_context != NULL) ? m_context.GetSMCConfluenceScore(dir) : 0;` (real objective SMC confluence in the signal direction; `dir` in scope at L163; `has_shift` still used by the early-return gate at L127 so no unused-variable warning).
  - `Include/EntryPlugins/CRangeReversionEngine.mqh`
    - `TagAndRetarget(...)` (~131-139, the single chokepoint all four range sub-paths funnel through — Range Edge Fade, Range Box Bounce, BB-Touch long, BB-Touch short): removed the `if(engine_confluence<=0) engine_confluence=60;` fabrication floor; now derives `dir` from `signal.action` and sets `signal.engine_confluence = (m_context!=NULL && dir!=SIGNAL_NONE) ? m_context.GetSMCConfluenceScore(dir) : 0;` UNCONDITIONALLY.
    - Removed the two pre-set fabricated constants `signal.engine_confluence = 55;` at the BB-Touch long/short sites (TagAndRetarget now sets the real value; the pre-sets were dead + misleading).
  - `UltimateTrader_Inputs.mqh` (MULTI-STRATEGY ENGINES group, after `InpBOSFreshnessBars`)
    - Added `input int InpSpineMinConfluence = 25;` (Phase 2.3 objective spine floor; comment notes 0-100 scale + below SMC hard-reject floor 40).
  - `UltimateTrader.mq5` (~720-723, inside `if(InpEnableMultiStrategy)`)
    - `g_confluenceScorer.Configure(...)` extended to pass `InpSpineMinConfluence` as the 6th arg (alongside the Phase-2.2 `InpBOSFreshnessBars`).
- **Diff summary:** The scorer's L3 engine-confluence spine path is now OBJECTIVE (`GetSMCConfluenceScore(dir) >= InpSpineMinConfluence`) instead of trusting an engine-fabricated number; the two named engines now report the REAL `GetSMCConfluenceScore(dir)` (0 possible) instead of hardcoded 55/60/75; and the killzone axis is keyed off a real expansion/breakout `engine_mode` instead of a self-certified confluence threshold. `git diff --stat`: 5 files, +60/-21.
- **Logic-cut check:** NO trading logic deleted. Every change repairs *which* number the gate/axis reads. No sub-path removed, no toggle disabled. The removed `55`/`60`/`75`/`<=0→60` assignments are fabricated-constant repairs (replaced with the objective source), not logic cuts.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 8177 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (all 38 are pre-existing — Logger/RecoveryManager/HealthMonitor/CVolatilityBreakoutEntry/Series/CEnhancedTradeExecutor; NONE in CConfluenceScorer / CReversalSweepEngine / CRangeReversionEngine / mq5 / Inputs).
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — `/mnt/c/Trading/UltimateTrader/UltimateTrader.ex5` 893874 bytes, 2026-06-26_19:59:37, md5 `13fa45273181759a12a063df40bb7f00` (differs from the A-build baseline `9efea8565f10c527f47ebfc0114e2c99` because source changed, even though dormant on the production `.set`).

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, production `.set` = `UltimateTrader.set` (Profiles/Tester; `InpEnableMultiStrategy=false`, `InpSignalSource=2` BOTH, `InpSymbolProfile=0`; `InpSpineMinConfluence` not in `.set` → uses default 25). Years 2024 and 2023. Deposit 10000 USD, Leverage 100.
- **Mode:** 1-min OHLC (Model=1).
- **A (baseline = current committed HEAD):** `/tmp/UT_phase2_3_baseline.ex5` (md5 `9efea856…`), the working-tree `.ex5` copied aside BEFORE editing. Verified == committed HEAD behaviorally: A 2024 Stats md5 `04d116972696f905c6ea5fa6701a7155`, A 2023 Stats md5 `b11e07675cf4d577e27663ffde0aca19` — both EXACT matches to the committed Phase 2.2 anchors (and TradeEvents/Risk/Candidates anchors match too).
  - 2024: 140t / $1190.84 net / 0.0840 avg-R / L109 S31.
  - 2023: 129t / $714.29 net / 0.0264 avg-R / L77 S52.
- **B (this phase):** `/tmp/UT_phase2_3_B.ex5` (md5 `13fa4527…`).
  - 2024 Stats md5 `04d116972696f905c6ea5fa6701a7155` (= A). 2023 Stats md5 `b11e07675cf4d577e27663ffde0aca19` (= A).
- **Delta + explanation:** ZERO. On the production `.set`, `InpEnableMultiStrategy=false` → `g_confluenceScorer` is never `new`'d (mq5:719 is inside `if(InpEnableMultiStrategy)` at :717) and the four engines' `SetScorer` (729/735/741/751) never run → the scorer object is constant-NULL, so the edited spine, the new `InpSpineMinConfluence`/`Configure` arg, the expansion-only killzone, and the two engines' scorer-consumed `engine_confluence` are all UNREACHABLE on the default path. The engines' `engine_confluence` is only ever *consumed* by `CConfluenceScorer.Score(...)`, which is never invoked when `m_scorer==NULL`. Byte-identity is STRUCTURAL, not coincidental. (ExpansionEngine signals still appear in the CSV via the LEGACY evaluator path — `routed_engine=false`, per Phase 2.1 — unaffected by Phase 2.3.)
- **No-op / byte-identical claim:** YES — trade-for-trade, SL-for-SL, R-for-R IDENTICAL. A==B BYTE-IDENTICAL across ALL 8 CSVs (Stats/TradeEvents/Risk/Candidates × 2024+2023): raw md5 MATCH on all 8 AND `cmp` byte-for-byte clean on all 8.
  - 2024: Stats `04d11697…` / TradeEvents `825489b6…` / Risk `8babda0f…` / Candidates `bcc44924…` (A==B).
  - 2023: Stats `b11e0767…` / TradeEvents `7ef40d5f…` / Risk `b21a2999…` / Candidates `2fba7df1…` (A==B).
- **Iter-3 note:** the objective-spine / real-confluence / expansion-only-killzone behavior is exercised only when `InpEnableMultiStrategy=true` (Iter 3, after the 2.4-GATE tier re-derivation). Tuning `InpSpineMinConfluence` now would be blind (gate dormant). 25 ships un-tuned per the binding stok param.

## Inspection note (wiring confirmation for Iter 3)
- **Objective spine** — `CConfluenceScorer.mqh` L3 gate: `bool engine_spine = (ctx.GetSMCConfluenceScore(dir) >= m_spine_min_confluence);`. `m_spine_min_confluence` defaults 25 (ctor + Configure default) and is wired from `InpSpineMinConfluence` via the mq5 Configure call. The spine is `structure_shift || engine_spine`; no engine can pass by fabricating `engine_confluence`. CONFIRMED.
- **Real engine_confluence** — CReversalSweepEngine.mqh:204 `signal.engine_confluence = (m_context!=NULL) ? m_context.GetSMCConfluenceScore(dir) : 0;`; CRangeReversionEngine.mqh:139 (in `TagAndRetarget`, covering all 4 range sub-paths) `signal.engine_confluence = (m_context!=NULL && dir!=SIGNAL_NONE) ? m_context.GetSMCConfluenceScore(dir) : 0;`. No hardcoded passing constant; 0 remains possible. CONFIRMED.
- **Expansion-only killzone** — axis 7 awards `+1` only for `engine_mode ∈ {MODE_DISPLACEMENT, MODE_LONDON_BREAKOUT, MODE_COMPRESSION_BO, MODE_INSTITUTIONAL_CANDLE, MODE_PANIC_MOMENTUM}`. The clock-window modes (`MODE_SILVER_BULLET / MODE_NY_CONTINUATION / MODE_LONDON_CLOSE`) and self-certified `engine_confluence` no longer earn it. CONFIRMED.
- **Scope note for stok / Iter 3 (NOT in Phase-2.3 scope, but flagged):** the OTHER major engines still self-certify `engine_confluence` constants — `CExpansionEngine` (65/70), `CSessionEngine` (50), `CLiquidityEngine` (computed), and `CTrendContinuationEngine` (151 uses a computed `confluence`; 413/438 already use the REAL `GetSMCConfluenceScore`). The plan scopes Phase 2.3 to ONLY `CReversalSweepEngine` + `CRangeReversionEngine`, which is what was edited. BENEFICIAL SIDE EFFECT of the objective spine: even an un-migrated engine that fabricates `engine_confluence` can no longer satisfy the L3 spine (it now reads `GetSMCConfluenceScore(dir)`), and a fabricated `engine_confluence` no longer earns the killzone point either. When those engines turn ON in Iter 3, their fabricated `engine_confluence` is decorative w.r.t. the scorer gate — stok should decide in Iter 3 whether to migrate them to the real score too (out of this phase's binding scope).
- **engine_mode population for the two edited engines:** CReversalSweepEngine AdoptSignal sets `engine_mode = mode` (MODE_SFP for sweep paths — NOT an expansion mode, so it will NOT earn the killzone point, which is the intended honest behavior; rubber-band/failed-break mode split is Phase 3.2). CRangeReversionEngine sets MODE_FVG_MITIGATION / MODE_SFP / MODE_OB_RETEST (none expansion → no killzone point — correct for a range/MR fade engine).

## stok review + verdict
- **Numbers handed to stok:** A==B byte-identity across all 8 CSVs (md5 + cmp) on the production `.set` both years; A reproduces the committed Phase-2.2 anchors (2024 `04d11697…`, 2023 `b11e0767…`); the objective-spine/real-confluence/expansion-only-killzone source wiring; the beneficial-side-effect note that the objective spine also neutralizes fabrication by the un-migrated engines.
- **stok assessment:** ACCEPT — verified at SOURCE, not the write-up.
- **stok verdict:** SIGN-OFF.

### stok independent verification (at source, not the write-up)
Read directly and confirmed every load-bearing claim:
- **Objective spine (CConfluenceScorer.mqh:118):** `bool engine_spine = (ctx.GetSMCConfluenceScore(dir) >= m_spine_min_confluence);` — the spine no longer reads `signal.engine_confluence` AT ALL. `m_spine_min_confluence` defaults 25 (ctor :57 + Configure 6th defaulted param :64), wired from `InpSpineMinConfluence` at mq5:723. Spine = `structure_shift || engine_spine` (:119). CONFIRMED objective; no engine can self-certify past the L3 gate.
- **Real engine_confluence, ZERO fabricated constants:** `grep -nE 'engine_confluence\s*=\s*[0-9]+'` over both named engines returns NOTHING — every hardcoded 55/60/75 and the `<=0→60` floor are gone. The real assignments are present: CReversalSweepEngine.mqh:204-205 `(m_context!=NULL) ? GetSMCConfluenceScore(dir) : 0`; CRangeReversionEngine.mqh:139-140 in `TagAndRetarget` (the single chokepoint for all 4 range sub-paths) `(m_context!=NULL && dir!=SIGNAL_NONE) ? GetSMCConfluenceScore(dir) : 0`. 0 remains possible. CONFIRMED.
- **GetSMCConfluenceScore is a real method:** `IMarketContext.mqh:45` virtual (base returns 0), overridden `CMarketContext.mqh:510`. Not a fabricated call. `GetRecentBOSTime()` (Phase-2.2 dep the BOS spine path reads) present `IMarketContext.mqh:74`. CONFIRMED.
- **Expansion-only killzone (CConfluenceScorer.mqh:180-186):** axis 7 `+1` keyed off `engine_mode ∈ {MODE_DISPLACEMENT, MODE_LONDON_BREAKOUT, MODE_COMPRESSION_BO, MODE_INSTITUTIONAL_CANDLE, MODE_PANIC_MOMENTUM}`. All 11 ENUM_ENGINE_MODE members exist (Enums.mqh:282-292); the awarded set is a correct subset of the 5 expansion/breakout modes; the 3 clock modes (SILVER_BULLET/NY_CONTINUATION/LONDON_CLOSE) and self-certified `engine_confluence` are excluded. CONFIRMED.
- **Byte-identity is STRUCTURAL (not coincidental):** verified at source — `g_confluenceScorer = NULL` (mq5:155); `new` ONLY inside `if(InpEnableMultiStrategy)` (mq5:717/719); all 4 `SetScorer` (730/736/742/752) in that same block; `delete` :1290. Production `.set` runs `InpEnableMultiStrategy=false` and omits `InpSpineMinConfluence` (→ default 25). Scorer object constant-NULL → `Score()` never invoked → the edited spine, the new Configure arg, the expansion-only killzone, and the engines' scorer-CONSUMED `engine_confluence` are all unreachable. Same proof class signed for Phases 2.1 and 2.2. A==B across all 8 CSVs (md5 + cmp, both years) is the expected, forced outcome. CONFIRMED.

### Q1 — Byte-identity correct (scorer dormant)
CONFIRMED. Structural consequence of the NULL-gating, verified at source. Not coincidental.

### Q2 — Scope ruling on the other (un-migrated) engines — BINDING
**ACCEPT leaving CExpansionEngine (65/70), CSessionEngine (50), CLiquidityEngine (computed) self-certified for now; migration to the real `GetSMCConfluenceScore` is recorded as an Iter-3 item. Do NOT migrate them in Phase 2.3.** Reasons (load-bearing, not deferral-by-convenience):
1. **The spine change already neutralizes them.** The objective spine reads `ctx.GetSMCConfluenceScore(dir)`, NOT `signal.engine_confluence`. A fabricated `engine_confluence` from an un-migrated engine therefore CANNOT pass the L3 hard gate (it no longer feeds it) and CANNOT earn the killzone point (axis 7 keys off `engine_mode`, verified :180-186). The fabricated number is now purely decorative w.r.t. every scorer decision — it sits in the struct as telemetry only.
2. **Out of the plan's binding scope.** The fix-2.3 row scopes the migration to exactly the two named engines. Both are migrated. Touching three more engines without a behavioral test widens the diff for zero measurable effect today.
3. **Blind + un-validatable now.** All three are dormant until the router turns ON in Iter 3 (`InpEnableMultiStrategy`). Any change to them today is byte-identical and cannot be A/B'd — the right place to migrate-and-verify is Iter 3, on the 2.4-GATE-re-derived score distribution.
- **Iter-3 item RECORDED:** migrate CExpansionEngine / CSessionEngine / CLiquidityEngine `engine_confluence` to the real `GetSMCConfluenceScore(dir)` for telemetry cleanliness, AND (carrying the Phase-2.1 open invariant forward) confirm with the router ON that no computed/legacy confluence path (incl. CTrendContinuationEngine's computed value at :151 alongside its real `GetSMCConfluenceScore` at 413/438) double-counts into a scorer decision and that every routed engine sets `routed_engine` in its non-NULL-scorer block.

### Q3 — InpSpineMinConfluence=25 floor + expansion-mode killzone set — BINDING
- **Floor = 25: CONFIRMED as the right Iter-3 starting value, NOT to be locked.** It is internally coherent: below the SMC hard-reject floor of 40 (`CSignalValidator`) and the configurable `m_smc_min_confluence` default of 60, so the spine is a NECESSARY-BUT-WEAKER pre-filter, not the binding constraint. A floor ≥40 would be redundant with the validator; too high would starve engines before the validator sees them. 25 is the stok binding param and ships un-tuned. **Iter-3 condition (RECORDED):** sweep `InpSpineMinConfluence` alongside `InpBOSFreshnessBars {4,6,8,12}`, pick by avg-R per engine, on the 2.4-GATE-re-derived distribution. Blind today (gate dormant) — every value yields the identical byte-identical run until the router turns ON. Do NOT lock 25.
- **Expansion-only killzone set: CONFIRMED and the EXCLUSION is final.** Excluding the 3 pure clock modes directly honors the repo's MEASURED finding (Silver Bullet −2.1R/6yr; London Breakout & NY Continuation 0% WR) — ICT killzones have NO gold-H1 edge, so awarding a confluence point for merely being in a clock window would manufacture confluence the data rejects. Keying axis 7 off a measured expansion/breakout displacement instead is the honest mapping. **Iter-3 condition (RECORDED):** the EXCLUSION is final; the INCLUSION of the 5 expansion modes is correct in principle but its marginal +1 contribution to avg-R is itself un-validated until the router is ON — confirm in the per-engine Iter-3 validation that the expansion killzone point earns positive avg-R rather than just being a non-clock default.

### stok Iter-3 items (carried forward, none blocking 2.3)
1. Migrate the 3 un-migrated engines' `engine_confluence` to the real `GetSMCConfluenceScore` (telemetry cleanliness; spine already neutralizes the gate effect).
2. Sweep `InpSpineMinConfluence` (with `InpBOSFreshnessBars {4,6,8,12}`) by avg-R per engine on the 2.4-GATE-re-derived distribution; do not lock 25.
3. Confirm the expansion-mode killzone point contributes positive avg-R per engine (exclusion of clock modes is final).
4. Re-check (Phase-2.1 invariant) that no computed/legacy confluence double-counts into a scorer decision and every routed engine sets `routed_engine` in its non-NULL-scorer block.

### stok verdict
**SIGN-OFF.** Mechanism implements the validated fix exactly; byte-identity is structural and verified at source; the scope ruling (neutralize-now, migrate-in-Iter-3) is sound; the 25 floor and expansion-only killzone are coherent and honor the repo's measured no-killzone-edge finding. All behavioral validation correctly defers to Iter 3 (router ON, post-2.4-GATE). No change requested.

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** no-op claim on the production path → must be byte-identical; touches-logic correctness verified by inspection for Iter 3. (Iter-3 spine/killzone tuning + per-engine validation is where the behavioral effect is judged.)
- **Result:** PASS — A==B byte-identical (raw md5 + cmp) across all 8 CSVs both years; the "unreachable on default path" claim holds (scorer constant-NULL when `InpEnableMultiStrategy=false`).

## mt5-developer sign-off
- Compile clean (0 errors / 0 new warnings, verbatim Result quoted); mechanism implements the validated fix exactly (objective spine `>= InpSpineMinConfluence=25`; real `GetSMCConfluenceScore(dir)` in both named engines, 0 possible; expansion-only killzone); file:line verified against current source; NO logic cut; A==B byte-identical proven (md5 + cmp, both years). — SIGNED

## stok sign-off
- **SIGNED** (independent agent — verified at SOURCE, not the write-up). (1) `InpSpineMinConfluence=25` CONFIRMED as the right un-tuned Iter-3 floor (below SMC hard-reject 40 → necessary-but-weaker; sweep by avg-R in Iter 3, do not lock). (2) Expansion-only killzone set CONFIRMED and the exclusion of the 3 clock modes is FINAL — directly honors the measured no-killzone-edge finding (Silver Bullet −2.1R/6yr; London BO & NY Cont 0% WR). (3) Other-engines scope ruling: ACCEPT neutralize-now / migrate-in-Iter-3 — the objective spine already makes their fabricated `engine_confluence` decorative w.r.t. the L3 gate and the killzone point; migration recorded as an Iter-3 item, NOT done in 2.3. Byte-identity is STRUCTURAL (scorer constant-NULL on the production path). Four Iter-3 items recorded above. No change requested. — SIGNED

## Decision
- **QA-PASS — BOTH agents signed (mt5-developer + INDEPENDENT stok).** Phase 2.3 → COMMITTED (separately-measurable commit run by the main session). NEXT ACTION → Phase 2.4. No loop.

## Commit
- **Hash:** (not committed per task instruction — main session runs git when it advances)
- **Message:** Phase 2.3: scorer L3 objective spine + real engine_confluence + expansion-only killzone (fix 2.3, InpSpineMinConfluence=25)
