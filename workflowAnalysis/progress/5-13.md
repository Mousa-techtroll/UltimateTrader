# Phase 5.13 — Verify fix 5.12 (major_engine best-signal copy) + remove dead CSignalManager / g_consecutiveErrors

## Phase
- **Phase id:** 5.13 (Iteration 5 — LAST phase; CLOSES Iteration 5)
- **Fix ids:** 5.12 (Core-Orch-1, EntrySignal.major_engine dropped in best-signal copy — VERIFY ONLY) + 5.13 (Core-Orch-2 dead `CSignalManager` + TopLevel-7 dead `g_consecutiveErrors` — dead-code removal)
- **Touches trading logic:** N (5.12 telemetry-only + already covered under 2.1; 5.13 provably-dead non-trading scaffolding)
- **depends_on:** 5.11 COMMITTED (HEAD 21957b4). 5.12 also covered by Phase 2.1's routed_engine/major_engine plumbing.
- **Binding stok param/option used:** FIX-SOUND (dead-code removal). No stok param — the gate is "grep-confirmed truly dead BEFORE removal" (the 2.7 discipline) + byte-identical A/B.

## Fix 5.12 — VERIFY result (already covered under Phase 2.1; NO edit)
- GREP/READ of `CSignalOrchestrator.mqh` best-signal field-by-field copy block (`CheckForNewSignals`, ~lines 799-835).
- `best_signal.major_engine = signal.major_engine;` is **ALREADY present at line 830**, with the verbatim comment `// Fix 2.1 (also fix 5.12): carry the major_engine tag into the best-signal copy ...`. (The Phase-2.1 OPTION-2 redo added both `best_signal.major_engine` :830 and `best_signal.routed_engine` :833.)
- **Conclusion: 5.12 is already covered — NO edit made.** Telemetry-only today; inert on the production path (engines off → `major_engine==ENGINE_NONE`, `routed_engine==false`), so byte-identical either way.

## Fix 5.13 — GREP-CONFIRMED-DEAD evidence (every hit classified)
Repo-wide grep (`--include=*.mq5 --include=*.mqh`) for `CSignalManager`, `g_signalManager`, `g_consecutiveErrors`:

**`CSignalManager` (class) / `g_signalManager` (instance):**
| Hit | Classification |
|---|---|
| `UltimateTrader.mq5:96` `#include "Include/Core/CSignalManager.mqh"` | DECLARATION (include) — dead |
| `UltimateTrader.mq5:343` `CSignalManager *g_signalManager = NULL;` | global DECLARATION — dead |
| `UltimateTrader.mq5:884` `g_signalManager = new CSignalManager(InpConfirmationStrictness, InpTP1Distance, InpTP2Distance);` | ALLOCATION — dead |
| `UltimateTrader.mq5:1277` `if(g_signalManager != NULL) { delete g_signalManager; g_signalManager = NULL; }` | TEARDOWN — dead |
| `Include/Core/CSignalManager.mqh:2/16/18/33` | the class definition itself |
| `Include/Common/Structs.mqh:488` (comment "from Stack 1.7 SignalManager") | COMMENT only |

- **Method-invocation grep `g_signalManager *-> | g_signalManager *.` → ZERO HITS.** The object is allocated + deleted but **NEVER has a single method called** on any path (live or dead). `HasPendingSignal/IncrementBarCount/IsExpired/StorePendingSignal/CheckPatternConfirmation/RecalculateTPs/...` are all unreached.
- **The LIVE pending/confirmation path uses a DIFFERENT object — `g_signalOrchestrator` (class `CSignalOrchestrator`)**, which carries its OWN `SPendingSignal m_pending_signal` (`CSignalOrchestrator.mqh:108`) and its OWN methods (`HasPendingSignal()`, `IncrementPendingBarCount()`, `CheckPendingConfirmation()`, `GetPendingSignal()`, `ClearPendingSignal()`, `RevalidatePending()`/`SoftRevalidatePending()`) invoked at `UltimateTrader.mq5:1585-1721`. `CSignalManager` is a parallel never-invoked duplicate that carried a divergent 2-bar `m_pending_expiry_bars` expiry trap — removing it cuts nothing live.
- **`SPendingSignal` STRUCT is heavily used live** (`UltimateTrader.mq5:1352/1598/1721`, `CSignalOrchestrator.mqh:108/225`, `CTradeOrchestrator.mqh:786`) — it lives in `Structs.mqh:490`, NOT in `CSignalManager.mqh`. PRESERVED; only the dead `CSignalManager` consumer removed.

**`g_consecutiveErrors`:**
| Hit | Classification |
|---|---|
| `UltimateTrader.mq5:356` `int g_consecutiveErrors = 0;` | global DECLARATION (with orphan `// Phase 3.3` comment) |

- **The ONLY occurrence is the declaration.** ZERO reads, ZERO writes/increments, ZERO branches anywhere in the repo. Never read on any trading (or non-trading) branch → provably dead.

**Verdict: BOTH provably dead. No live read/call affects trading → safe to remove.**

## What changed (file:line)
- `UltimateTrader.mq5:96` — removed `#include "Include/Core/CSignalManager.mqh"`.
- `UltimateTrader.mq5:343` — removed `CSignalManager *g_signalManager = NULL;` (global declaration).
- `UltimateTrader.mq5:355-356` — removed the `// Phase 3.3: Consecutive error tracking` comment + `int g_consecutiveErrors = 0;`.
- `UltimateTrader.mq5:884` — removed `g_signalManager = new CSignalManager(...)` allocation in `OnInit()`.
- `UltimateTrader.mq5:1277` — removed `if(g_signalManager != NULL){ delete g_signalManager; ... }` teardown in `OnDeinit()`. **The reverse-order teardown of every OTHER (live) global is PRESERVED untouched** — only the dead `g_signalManager` line was excised (live deletes at the prior lines unchanged: signalOrchestrator → tradeOrchestrator → posCoordinator → riskMonitor → regimeScaler → adaptiveTP, then Execution layer).
- `Include/Core/CSignalManager.mqh` — **deleted** (`git rm`) the whole dead class file.
- **Diff:** `UltimateTrader.mq5` 8 deletions; `CSignalManager.mqh` removed. NO logic cut (post-removal grep for all three identifiers across `*.mq5`/`*.mqh` = ZERO hits).

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 8469 ms elapsed, cpu='X64 Regular'
```
- Errors: 0 | Warnings: **38** (== the 38 pre-existing baseline; 0 new, 0 dropped). The dead `g_signalManager`/`g_consecutiveErrors`/`CSignalManager` produced no warnings, so the count is unchanged at 38 (it did NOT drop below 38).
- Warnings in the edited file (`UltimateTrader.mq5`): the 2 pre-existing `warning 43` at 783/784 remain (unrelated; lot-math cast). No NEW warning introduced.
- Error 106 / include issues: NONE (nothing else `#include`'d `CSignalManager.mqh`; the only consumer was the removed code).
- `.ex5` regenerated: yes (repo `UltimateTrader.ex5` md5 `d4aaabcc80f9db24c6b59eafffc0f018` = the tested B binary).

## 3b binary-binding md5 assertions (MANDATORY — RESUME.md step 3b)
- **Leg A (committed HEAD 21957b4):** built in a Windows-accessible git worktree `C:\Trading\_ut_p513A_wt` (detached @21957b4, `CSignalManager.mqh` PRESENT = committed-HEAD dead-code state). Compiled `Result: 0 errors, 38 warnings, 8961 ms`. Deleted data-dir load-path `UltimateTrader_p513A.ex5` before compile; copied fresh; **load-path md5 == fresh build md5 = `8364f77b48370d5857312145ff42384f`** (load==fresh ASSERTED). Worktree removed after run.
- **Leg B (post-fix 5.13, FINAL shipping binary):** compiled in-tree `Result: 0 errors, 38 warnings, 8469 ms`. Deleted data-dir load-path `UltimateTrader_p513B.ex5` before compile; copied fresh; **load-path md5 == fresh build md5 = `d4aaabcc80f9db24c6b59eafffc0f018`** (load==fresh ASSERTED).
- **A and B md5 DISTINCT** (`8364f77b…` ≠ `d4aaabcc…`) — expected: B removed the dead class → different bytecode. (Neither equals the stale pre-4.6 `9dbe9052`.)
- **A reproduces the binding baseline byte-for-byte:** all 4 EA-CSV md5 == post-5.9 binding baseline (Stats `3b13ea8f…` / TradeEvents `2222e2f9…` / Risk `c152fb4a…` / Candidates `77b07fb1…`); MT5-report headline **$17,455.34 / PF 1.28 / 1742 trades / Eq-DD 12.64%** → fresh-HEAD load binary, stale-binary risk closed.
- `.ini` `Expert=` names the phase-specific binaries (`UltimateTrader_p513A.ex5` / `UltimateTrader_p513B.ex5`), NEVER the bare `UltimateTrader.ex5`.

## QA backtest (decoded, ACTUAL)
- **Config:** XAUUSD+ H1, Model=1, full **2019.01.01–2026.06.27**, 397-line `[TesterInputs]` production block (`InpEnableMultiStrategy=false`, `InpSignalSource=2`=BOTH) — the same `.ini` template as Phase 5.11's p511A.ini, A/B differing only in `Expert=`/`Report=`. Clean state (Common\Files state file relocated before each leg). 2 runs total (1 A + 1 B), within the ≤2 bound.
- **A (committed HEAD 21957b4):** Net **$17,455.34** / PF **1.28** / **1742** trades / Eq-DD **12.64%** (== binding baseline; report `p513_A.htm`).
- **B (post-fix 5.13):** Net **$17,455.34** / PF **1.28** / **1742** trades / Eq-DD **12.64%** (report `p513_B.htm`) — identical headline.
- **BYTE-IDENTITY PROOF — all 4 output CSVs md5-EQUAL AND `cmp` byte-for-byte CLEAN:**

```
Stats        A=3b13ea8feded6f2faec6b5a157ab05db B=3b13ea8feded6f2faec6b5a157ab05db  MD5-EQUAL  cmp-CLEAN
TradeEvents  A=2222e2f955ea0eef99b50f7b4c52ebff B=2222e2f955ea0eef99b50f7b4c52ebff  MD5-EQUAL  cmp-CLEAN
Risk         A=c152fb4a1ebf586de37b36274ce6506e B=c152fb4a1ebf586de37b36274ce6506e  MD5-EQUAL  cmp-CLEAN
Candidates   A=77b07fb1fc2982aaed4899b823bb0e26 B=77b07fb1fc2982aaed4899b823bb0e26  MD5-EQUAL  cmp-CLEAN
```

All 4 also equal the RESUME.md binding-baseline targets. (Archived under `Logs/p513_ab/{A,B}_*.csv`.)

## Mechanism (WHY byte-identical)
- **CSignalManager:** its methods are NEVER called on any path (zero `->`/`.` invocations repo-wide). Removing an allocated-but-never-invoked object — and its class — changes no executed instruction. The live pending/confirmation logic is entirely in `CSignalOrchestrator` (its own `SPendingSignal m_pending_signal` + its own methods), which is untouched. So the EA's behavior is unchanged → byte-identical.
- **g_consecutiveErrors:** declared but never read on any branch (let alone a trading branch). A global that is never read can be deleted with no behavioral effect → byte-identical.
- **major_engine (5.12):** already copied (line 830, since 2.1) and inert on the production path (engines off → `major_engine==ENGINE_NONE`, `routed_engine==false`), consumed only as telemetry/`is_engine==routed_engine` which is constant-false here. No edit this phase, no behavior change.
- Net: no live path changed → A==B byte-identical, exactly as expected for dead-code removal + an already-present telemetry copy.

## Kill-criterion read
- Iteration-5 QA(c)/(e): **default byte-identical for 5.13** — PASS (4/4 CSV md5-equal + cmp-clean; A reproduces baseline). The "expected A==B byte-identical" prediction held → confirms the removed code was genuinely dead. No kill-criterion tripped.

## Sign-offs
- **mt5-developer:** SIGNED — 5.12 verified already-covered (no edit); 5.13 grep-confirmed-dead at SOURCE before removal (zero method invocations / zero reads); compile 0-err/38-warn (0 new); reverse-order teardown of live globals preserved; A reproduces binding baseline via 3b; A==B byte-identical (4/4 md5 + cmp). Mechanism correct.
- **stok:** **SIGNED — verdict ACCEPT** (binding, clean; meets the Phase-2.7 dead-code standard). Independently re-grepped to exit-1:
  - **Truly dead-only:** CSignalManager/g_signalManager = ZERO hits repo-wide (the committed pre-removal file showed a complete self-contained NEVER-WIRED duplicate with its own m_pending_expiry_bars=2 / StorePendingSignal / CheckPatternConfirmation). The LIVE pending/confirmation path is g_signalOrchestrator's SEPARATE mechanism (mq5:1577-1718, its own m_pending_signal at CSignalOrchestrator:108) — different object/methods/expiry, untouched. g_consecutiveErrors = ZERO hits (decl was the sole occurrence). Removing the duplicate is a genuine improvement: it hard-coded a divergent 2-bar expiry trap and its ctor signature made it look invokable = a latent footgun if ever wired.
  - **SPendingSignal correctly PRESERVED** (Structs.mqh:490, NOT in the deleted file; live consumers at mq5:1344/1590/1713, CTradeOrchestrator:786, orchestrator member :108). The struct ≠ the dead class.
  - **Byte-identical proof sufficient (2.7 standard):** 4/4 EA-CSV md5-equal + cmp-clean, A reproduces baseline; A/B binaries correctly DISTINCT (8364f77b≠d4aaabcc — removing a class changes bytecode while leaving every EXECUTED instruction identical); shipped .ex5 == tested B (d4aaabcc); reverse-order teardown of live globals intact (mq5:1260-1301, no g_signalManager line, no gap).
  - **5.12 already-covered under 2.1** confirmed (CSignalOrchestrator:830, telemetry-only + inert on prod) — no action correct.
  - **Verdict: ACCEPT. Iteration 5 COMPLETE on this accept.**

## Follow-up (logged by stok; cosmetic, Iter-6 hygiene pass, NOT a loop)
- Structs.mqh:7 + :488 still carry comments referencing "SignalManager" / "Stack 1.7 SignalManager" — harmless documentation lag now the class is gone; sweep in Iteration 6's comment-correction pass. NOT a logic defect.

## Status
- **DONE — both agents signed (mt5 implementation-correct + byte-identical proven; stok ACCEPT, binding, source-verified truly-dead). Committed source-only on `feat/multi-strategy`.** Dead CSignalManager (never-invoked duplicate with a divergent 2-bar-expiry trap) + g_consecutiveErrors removed; SPendingSignal preserved; teardown intact; A/B byte-identical (3b: distinct binaries, A reproduced baseline, shipped==tested-B). 5.12 already under 2.1. 1 cosmetic Iter-6 follow-up (stale Structs.mqh comments). **THIS IS THE LAST ITERATION-5 PHASE — ITERATION 5 COMPLETE.** NEXT ACTION → Iteration 6 (infra/hygiene 6.1–6.9).

## Commit
- **Hash:** `<filled at commit>` (source: UltimateTrader.mq5 + `git rm Include/Core/CSignalManager.mqh` + 5-13.md + 00-STATUS.md; `.ex5` intentionally uncommitted). Branch `feat/multi-strategy`.
