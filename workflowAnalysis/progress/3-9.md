# Phase 3.9 — Range/Mean-Reversion engine validation

**Status: CUT (decided at the 2.4-GATE; no separate isolated run needed).**

Per the binding 2.4-GATE verdict (`2-4-GATE-verdict.md`, stok-signed), the Range/Mean-Reversion engine was the one routed engine that clearly tripped the kill-criterion in the engines-ON derivation:

- **RangeReversion: avg-R −0.688 (full 2019–2026) / −0.530 (OOS 2024–2026), n≈4.** Trips the kill-criterion (PF < 1.0 AND avg-R < 0 over full AND OOS) outright.

The corrected engine (BB-touch + RSI reimplemented on the closed bar in Phase B; DAY_DATA news-flat from Phase 3.6; objective spine + real `engine_confluence` from Phase 2.3) **still has no gold-H1 edge** — consistent with the audit's broader finding that band/mean-reversion fading is fragile on a structurally-trending metal (bands expand violently into the trend). This is a *concept* outcome on gold, not an implementation defect: the engine is correctly implemented and correctly off.

**Decision:** Range/Mean-Reversion engine **stays CUT / OFF-by-default** (`InpEnableEngineRange=false`). Production byte-identical (engine never registered when `InpEnableMultiStrategy=false`). No production source change.

**QA gate:** mt5 (2.4-GATE derivation data) + stok (2.4-GATE verdict) both signed; this phase records that decision rather than re-running it (the GATE verdict explicitly states 3.9 is decidable from GATE data with no separate run).

**Per-engine validation tally (Iteration 3b):** ① Trend-Cont = KEEP-BUT-TINY (3.7); ② Reversal/Sweep = KEEP-FOR-SFP-LONG (3.8); ③ Range/MR = **CUT** (this phase); ④ Expansion = pending (3.10).
