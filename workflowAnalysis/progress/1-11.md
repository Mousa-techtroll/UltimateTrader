# Phase 1.11 — CMarketContext IsPriceAboveMA200 no-data branch → configurable default flag

> Implemented and validated JOINTLY with Phase 1.10 (fix 1.11) in a single combined byte-identity A/B, because both fixes are no-ops on the XAUUSD gold production config (Phase 1.11 keeps the gold default TRUE → byte-identical). See Phase 1.10 log (`1-10.md`) for the same shared byte-identity result.

## Phase
- **Phase id:** 1.11
- **Fix ids:** 1.12
- **Touches trading logic:** Y (directional gate — but the gold default is TRUE, reproducing the prior hardcoded `return true` exactly, so it is byte-identical on XAUUSD)
- **depends_on (phases that must be COMMITTED first):** 1.9 (COMMITTED); landed jointly with 1.10
- **Binding stok param/option used:** `m_ma200_default_bullish` DEFAULT TRUE for the gold profile (per plan fix 1.12 + RESUME §(c) intent: "Keep gold default true — preserves the realized warmup long bias"). No new `Inp*` input added (the flag is a member defaulted true; the plan said "configurable default flag", which this satisfies — exposing it as an input is deferred as out of minimal scope and unnecessary for gold).

## Caller grep BEFORE changing the contract (mandatory)
`grep -rn "IsPriceAboveMA200"` over `*.mqh`/`*.mq5`:
- `Include/Core/CMarketStateManager.mqh:117` — pass-through wrapper `{ return (m_context != NULL) ? m_context.IsPriceAboveMA200() : false; }` — no-arg bool, unaffected.
- `Include/Display/CDisplay.mqh:88` — `bool above_200 = m_context.IsPriceAboveMA200();` — display only, no-arg bool, unaffected.
- `Include/EntryPlugins/CTrendContinuationEngine.mqh:71` — `if(!m_context.IsPriceAboveMA200())` — directional gate (engine OFF by default on this `.set`), no-arg bool, unaffected.
- `Include/MarketAnalysis/CMarketContext.mqh:447/459` — the definition itself.
- `Include/MarketAnalysis/IMarketContext.mqh:34` — interface default `virtual bool IsPriceAboveMA200() { return false; }` — signature unchanged.
**Conclusion:** every caller invokes `IsPriceAboveMA200()` with no arguments and a `bool` return. The fix PRESERVES that exact signature (only the internal no-data branch changed; the new `IsMA200Available()` is an *additive* concrete helper, NOT added to the interface, so no interface-default consumer is affected). NO caller breaks.

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/MarketAnalysis/CMarketContext.mqh` — (1) member decl (66–73): added `bool m_ma200_default_bullish;` next to the cached MA200 members, with a comment that TRUE = gold profile (preserves realized warmup long bias), FALSE for symbols/history-gapped feeds where a no-data bullish assumption would be unsafe. (2) constructor init (150): `m_ma200_default_bullish = true;` (gold default). (3) added `IsMA200Available() const` helper (454–457): `return (m_ma200_value > 0);`. (4) `IsPriceAboveMA200()` (459–467): the no-data branch changed from hardcoded `if(m_ma200_value <= 0) return true;` to `if(!IsMA200Available()) return m_ma200_default_bullish;`. The data-available branch (`bid > m_ma200_value` via live `SYMBOL_BID`) is UNCHANGED.
- **Diff summary:** Replace the silent hardcoded bullish default in the MA200-unavailable branch with a configurable member `m_ma200_default_bullish` (defaulted TRUE for gold), and add an `IsMA200Available()` predicate so the availability test is named and reusable. On gold this is exactly equivalent to the old `return true`; the value is correctness/portability for non-gold/history-gapped symbols that can now opt out of the no-data bullish assumption.
- **Logic-cut check:** NO trading logic deleted. The hardcoded literal `true` was *replaced* by a member whose default IS `true` (a configurability/wiring change, not a removal). The live-BID `bid > m_ma200_value` comparison is untouched. No disable, no short-circuit, no toggle park.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 7807 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (exactly 38; none reference the edited CMarketContext.mqh lines — the new member / helper / branch are warning-clean).
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — `UltimateTrader.ex5` 890,494 bytes, md5 `a1ace2aede1337eab767a3fc6bbb45f3` (B-build, contains BOTH Phase-1.10 and Phase-1.11 edits) ≠ A-baseline `1d78fa96ff70bd203059a58fb345ff38`. A-baseline copied aside to `/tmp/UT_phase1_10_11_baseline.ex5` BEFORE any edit.

## QA backtest (decoded, ACTUAL — never estimated) — SHARED combined A/B (1.10 + 1.11)
- **Config:** XAUUSD+ H1, production `UltimateTrader.set` (`InpSymbolProfile=0` gold, `InpSignalSource=2` BOTH, `InpEnableMultiStrategy=false`, `InpEnablePullbackCont=true`, `InpEnableCrashDetector=true`, `InpRubberBandAPlusOnly=true`, `InpMaxPositions=5`, `InpMaxTradesPerDay=5`, `InpDXYSymbol=USDX`, `InpVIXSymbol=VIX`). Deposit 10000 / leverage 100. Same `.set` + `.ini` for A and B → any delta is purely the binary. Terminal `C:\Program Files\Vantage Markets MT5 Terminal\terminal64.exe` / user `nullkuhl` / terminal ID `725B72F25E46C780EF59F57016D58156`, WITHOUT `/portable` (proven prior-phase harness; committed `backtest_symbol.sh` path/ID/data-dir are STALE and were NOT used).
- **Mode:** Model=1 (1-min OHLC), same as Phases 1.1–1.9.
- **Window:** 2024 (acceptance = byte-identical).
- **A (committed HEAD baseline `.ex5`):** trades 140 | WR 46.43% | net $1190.84 | totR 11.76 | avgR 0.0840 | L/S 109/31. A reproduced the EXACT Phase-1.9 committed-HEAD baseline (Stats.csv md5 `04d116972696f905c6ea5fa6701a7155`), confirming A IS HEAD and the harness is deterministic.
- **B (this build — BOTH 1.10 + 1.11 fixes):** trades 140 | WR 46.43% | net $1190.84 | totR 11.76 | avgR 0.0840 | L/S 109/31.
- **Delta + explanation:** ZERO delta. **A == B BYTE-IDENTICAL.** All 5 produced CSVs are raw-byte identical (`cmp` clean, md5-matching): Stats `04d116972696f905c6ea5fa6701a7155`, TradeEvents `825489b61fbf3db5cb2302361de66bb1`, Risk `8babda0f7b846cc6fa94941ecb39c815`, Candidates `bcc44924d5d3fc31a5f4d35e5142b0a7`, backtest_results `177ca6a9f38c32459643e894a8ba195d`. Mechanism (1.11): `m_ma200_default_bullish` defaults to TRUE → the no-data branch returns exactly the same value the old hardcoded `return true` did, so any time the branch is reached the result is identical; and on XAUUSD the H1 MA200 is available after warmup so the data-available `bid > m_ma200_value` path (unchanged) dominates. The behavioral change (a FALSE default) is only realizable on non-gold / history-gapped symbols, which is out of this production XAUUSD config's scope.
- **No-op / byte-identical claim:** YES — trade-for-trade, SL-for-SL, R-for-R IDENTICAL on XAUUSD+ H1 2024. The `.ex5` changed; the production trade path did not. Confirmed at the gold default TRUE per the plan's mandate.
- **Bisection:** NOT REQUIRED — A==B held with both fixes applied together, so neither fix introduced any divergence. The no-op claim is confirmed, not contradicted.

## stok review + verdict
- **Numbers handed to stok:** A==B byte-identical (5/5 CSVs cmp-clean + md5-matching) on XAUUSD+ H1 2024; A reproduces committed-HEAD Phase-1.9 md5; the caller-grep result (all 5 callers are no-arg bool, none break); the mechanism (gold default TRUE = prior hardcoded `return true`; MA200 available after warmup so data-available branch dominates).
- **stok assessment:** PENDING (independent stok agent).
- **stok verdict:** PENDING.
- **stok-facing note (directional gate):** the only behavioral delta this fix introduces is for symbols where MA200 is unavailable AND an operator sets `m_ma200_default_bullish=false` — i.e. a no-data LONG-bias is withheld for non-gold/history-gapped feeds. On gold the flag stays TRUE so the realized warmup long bias is preserved exactly (byte-identical). The directional gate `CTrendContinuationEngine:71` that consumes it is OFF by default on this `.set` (`InpEnableMultiStrategy=false`), so even the warmup window is off the production trade path here.

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** NEEDS-CARE — directional gate; keep gold default TRUE; prove XAUUSD reproduces baseline trade-by-trade.
- **Result:** PASS — gold default kept TRUE; A==B byte-identical (5/5 CSVs cmp-clean) → XAUUSD reproduces baseline trade-for-trade / SL-for-SL / R-for-R. No criterion tripped.

## mt5-developer sign-off
- Compile clean (0 errors, exactly 38 warnings = baseline, 0 new). Fix implements the validated spec exactly: added `IsMA200Available()` helper; changed the no-data branch to the configurable `m_ma200_default_bullish` (defaulted TRUE for gold). Caller grep performed BEFORE the change — all 5 callers are no-arg bool, signature preserved, none break. NO logic cut (hardcoded `true` replaced by a member defaulted `true`; live-BID compare untouched). file:line verified against current source (66–73 member, 150 ctor, 454–457 helper, 459–467 branch). A==B byte-identical on XAUUSD+ H1 2024 with the gold default TRUE (5/5 CSVs cmp-clean, A reproduces committed-HEAD md5). **SIGNED.**

## stok sign-off
- **SIGN-OFF (independent stok agent).** Verified at source: member `m_ma200_default_bullish` (CMarketContext.mqh:73) defaults TRUE in ctor (:150); the no-data branch (:465) returns that flag = exact prior hardcoded `return true` on gold; live-BID `bid > m_ma200_value` path unchanged; `IsMA200Available()` is additive on the concrete class only (NOT on IMarketContext:34, so no interface-default consumer changes). Independent caller-grep confirms 5 refs, all no-arg bool, signature preserved, none break — and the only directional consumer (CTrendContinuationEngine:71) is OFF by default on this `.set`. A==B byte-identical on XAUUSD+ H1 2024 (5/5 CSVs cmp-clean + md5-match) is correct by construction: gold flag TRUE reproduces the prior bias exactly. The single realizable behavioral delta (flag flipped FALSE) lives only on non-gold / history-gapped feeds, off the gold production trade path. KILL-CRITERION (gold default TRUE; XAUUSD trade-for-trade identical) PASS. ACCEPT.

## Decision
- **PASS (mt5 side)** — compile clean, A==B byte-identical (gold default TRUE preserved), caller-grep clean, kill-criterion not tripped. Status → **mt5-QA-DONE** (byte-identical, validated jointly with Phase 1.10). stok PENDING. Do NOT commit; do NOT advance NEXT ACTION until the independent stok agent signs off.

## Commit
- **Hash:** (not committed — main session runs git; awaiting stok sign-off)
- **Message:** (pending)
