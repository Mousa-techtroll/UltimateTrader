# Phase 2.1 — Orchestrator HONORS the engine CConfluenceScorer (fix 2.1)

> STATUS: **✅ QA-PASS → COMMITTED (Option 2). BOTH agents signed — mt5-developer + INDEPENDENT stok re-sign-off.**
> The Option-1 spec-literal impl below was REJECTED by stok (it honored an UNINITIALIZED SETUP_NONE
> on the legacy path because `m_scorer==NULL` there) and re-implemented per stok's binding Option-2.
> This top section is the Option-2 redo of record; the Option-1 history is preserved further down.

---

## OPTION-2 REDO (stok-binding — the implementation of record)

### Root cause of the Option-1 failure (verified at source)
`CMajorStrategyEngine::m_scorer` defaults `NULL` (CMajorStrategyEngine.mqh:42) and `SetScorer(g_confluenceScorer)`
is called ONLY inside `if(InpEnableMultiStrategy)` (UltimateTrader.mq5:728/734/740/750). On the legacy path
(`InpEnableMultiStrategy=false`) the legacy-registered major engines (Expansion/Session/Liquidity, registered at
mq5:686/674/665 on their own `InpEnable*Engine` toggles) have `m_scorer==NULL`, so their per-engine
`if(m_scorer != NULL)` scoring block is SKIPPED and `signal.setupQuality` stays at `Init()`'s `SETUP_NONE`.
Option-1's `is_engine = (major_engine != ENGINE_NONE)` honored that uninitialized SETUP_NONE → silently cut
those 3 live engines. `m_scorer != NULL` is the exact "router-wired" discriminator; the `routed_engine` flag
(set inside the non-NULL-scorer block) captures it.

### What changed (Option 2 — verified file:line against current source)
- `Include/Common/Structs.mqh` — added `bool routed_engine;` to `EntrySignal` (line 549, after `major_engine`); defaulted `routed_engine = false;` in `Init()` (line 587). (`SPosition` intentionally NOT changed — not needed; stok spec scoped the flag to EntrySignal.)
- `Include/Core/CSignalOrchestrator.mqh`:
  - `bool is_engine = (signal.major_engine != ENGINE_NONE);` → **`bool is_engine = signal.routed_engine;`** (line 566). All downstream gates unchanged in placement: `if(!is_engine) regime_risk_multiplier=1.0` (600), engine-vs-legacy `quality` select (720), `if(!is_engine) qualityScore=GetQualityScore` (758).
  - added `best_signal.routed_engine = signal.routed_engine;` next to the major_engine copy (line 810) so the flag survives ranking.
- The four engines set `signal.routed_engine = true;` INSIDE their existing `if(m_scorer != NULL)` scoring block (NOTE: there is no shared `CMajorStrategyEngine::TagAndScore` — the repo has the non-NULL-scorer block in four per-engine sites; the flag is set in ALL four so every routed engine is covered when the router is on in Iter 3):
  - `CExpansionEngine.mqh:595` (inside TagAndScore's `if(m_scorer != NULL)`),
  - `CTrendContinuationEngine.mqh:176`,
  - `CReversalSweepEngine.mqh:217`,
  - `CRangeReversionEngine.mqh:164` (ScoreSignal, after the `if(m_scorer==NULL) return;` guard).
- KEPT from the prior impl: the CSetupEvaluator engine-token Factor-4 WARN, the `best_signal.major_engine` copy (also fix 5.12), and the `regime_risk_multiplier` gating (now keyed off `routed_engine` via `is_engine`).
- **NO logic cut. NOT gated on raw major_engine.**

### Compile result (verbatim — Option 2)
```
Result: 0 errors, 38 warnings, 7600 ms elapsed, cpu='X64 Regular'
```
- Errors: 0 | New warnings vs 38-baseline: 0 (38==38; none in any of the 7 edited files — verified by grep of the decoded log).
- `.ex5`: Option-2 build `/tmp/UT_phase2_1_opt2.ex5` (md5 63cca71018766e8c130e726fa2b13ee8), installed to repo `UltimateTrader.ex5` + tester `MQL5/Experts/UltimateTrader.ex5`. A-baseline (committed HEAD) `/tmp/UT_phase2_1_baseline.ex5` (md5 8c249a98a530460a94d9f96bae17c570) still valid.

### Re-QA byte-identity (BINDING acceptance — ACTUAL decoded)
- **Config:** XAUUSD+ H1, production `UltimateTrader.set` (default — `InpEnableMultiStrategy=false`; legacy engine plugins `InpEnableExpansionEngine`/`SessionEngine`/`LiquidityEngine`/`PullbackCont`/`DisplacementEntry=true`). Model=1. Same `.set`+`.ini` for A and B. Terminal Vantage Markets / nullkuhl / 725B72F25E46C780EF59F57016D58156, no `/portable`.
- **A == B BYTE-IDENTICAL for BOTH 2024 AND 2023** — all 5 CSVs cmp-CLEAN + md5-match, A reproduces committed-HEAD:
  - 2024: Stats `04d116972696f905c6ea5fa6701a7155` (= committed-HEAD baseline), TradeEvents `825489b61fbf3db5cb2302361de66bb1`, Risk `8babda0f7b846cc6fa94941ecb39c815`, Candidates `bcc44924d5d3fc31a5f4d35e5142b0a7`, backtest_results `177ca6a9f38c32459643e894a8ba195d` — every md5 identical A/B.
  - 2023: Stats `b11e07675cf4d577e27663ffde0aca19` (= committed-HEAD baseline), TradeEvents `7ef40d5fcff7b79675867f8d43653ec6`, Risk `b21a2999181a770a7893a9e61f9161cf`, Candidates `2fba7df14c87f7abd53530d6ee67b59c`, backtest_results `07d903dea907388f0fd320f919738512` — every md5 identical A/B.
- **Expansion trades RETURNED:** 2024 ExpansionEngine Stats rows A=12 / B=12 (all 6 trades back, the 6 Option-1 dropped); 2023 A=2 / B=2 (the 1 trade back). Because every Stats CSV is byte-identical, the prior sub-0.25R exit ripple is GONE too — zero divergence, trade-for-trade / SL-for-SL / R-for-R IDENTICAL.
- **Headline (both years = committed HEAD):** 2024 140t / $1190.84 / 11.76R; 2023 129t / $714.29 / 3.40R — A and B identical.

### Why this is correct (routed_engine semantics)
`routed_engine` is true ONLY when an engine scored the signal through a non-NULL `m_scorer`, which happens ONLY under `InpEnableMultiStrategy`. On the default production path no engine is router-wired → `routed_engine=false` for every signal → `is_engine=false` everywhere → the legacy comment-token evaluator path runs unchanged → byte-identical. The engine-scorer consumption (the actual behavioral change of fix 2.1) is therefore inert on the default path and is exercised only when the router is ON — i.e. deferred to Iteration 3, scored on the 2.4-GATE-re-derived thresholds, exactly where the plan says it belongs. The wiring is still CORRECT for Iter 3: when the router is on, every routed engine sets `routed_engine=true` inside its non-NULL-scorer block, the orchestrator then honors that engine's CConfluenceScorer tier/native-0-10 score/router weight, and `best_signal.routed_engine` survives ranking.

### Kill-criterion (Option 2)
- **Criterion:** production default path byte-identical both years AND the previously-dropped Expansion trades return. **Result: PASS** — A==B byte-identical (5/5 CSVs, 2024+2023), Expansion trades returned (12/12, 2/2), zero ripple.

### Sign-off (Option 2)
- **mt5-developer:** Compile clean (0-err/0-new-warn, none in edited files); `routed_engine` keyed off the non-NULL-scorer block in all 4 engines + orchestrator + struct + best-signal copy; is_engine NOT gated on raw major_engine; A==B byte-identical both years (5/5 CSVs cmp-clean, A==committed-HEAD); Expansion trades returned; NO logic cut. **SIGNED.**
- **stok (independent re-sign-off — verified at SOURCE, not the write-up): SIGNED → SIGN-OFF.**

  **Adjudication point 1 — binding Option-2 acceptance MET (production path byte-identical both years + the 3 legacy engines' trades restored).** Confirmed at the level required: byte-identity here is not merely a measured result, it is a *structural consequence* I verified at source. `CMajorStrategyEngine::m_scorer` defaults `NULL` (`CMajorStrategyEngine.mqh:42`); `SetScorer(g_confluenceScorer)` is called ONLY inside `if(InpEnableMultiStrategy)` (`UltimateTrader.mq5:717–750`, 4 call sites). `routed_engine` is set `true` strictly inside each engine's `if(m_scorer != NULL)` scoring block — all 4 sites confirmed: `CExpansionEngine.mqh:595`, `CTrendContinuationEngine.mqh:176`, `CReversalSweepEngine.mqh:217`, `CRangeReversionEngine.mqh:164` (the last via the inverse early-return guard `if(m_scorer==NULL) return;` then set — semantically equivalent: only reached when the scorer is non-NULL). `EntrySignal::Init()` defaults `routed_engine=false` (`Structs.mqh:587`). Therefore on the production `.set` (`InpEnableMultiStrategy=false`) no engine is ever given a scorer → `routed_engine` stays false for EVERY signal → `is_engine=signal.routed_engine` (`CSignalOrchestrator.mqh:566`) is false everywhere → all three downstream gates take the unchanged legacy branch: `if(!is_engine) regime_risk_multiplier=1.0` (:600), the `quality` select (:720, legacy `EvaluateSetupQuality`), and `if(!is_engine) qualityScore=GetQualityScore` (:758). The orchestrator's executed code path is literally unchanged on this config, so byte-identity is the ONLY possible outcome and the Option-1 sub-0.25R exit ripple cannot recur. I accept mt5's re-QA md5-match to committed-HEAD both years (2024 Stats `04d116972696f905c6ea5fa6701a7155`, 2023 `b11e07675cf4d577e27663ffde0aca19` — the same committed-HEAD anchor I independently verified across Phases 1.6–1.11) and the returned 6/2024 + 1/2023 ExpansionEngine trades (rows 12/12, 2/2) as satisfying the binding acceptance. (Note: the controlled A/B artifacts lived in `/tmp` and are ephemeral; the on-disk `Logs/` CSVs are from a later/different run with non-matching hashes, as expected — they are not the A/B output. The structural argument + the repeatedly-verified committed-HEAD anchor is the level of confirmation I require, and it holds.)

  **Adjudication point 2 — `routed_engine` semantic is CORRECT and the Iter-3 migration is COHERENT.** The flag captures exactly "the orchestrator's CConfluenceScorer ran for this signal," which on this codebase is true *iff* the engine was wired through the multi-strategy router — the precise discriminator Option 2 demanded (`true ⟺ m_scorer!=NULL ⟺ SetScorer ran ⟺ InpEnableMultiStrategy on ⟺ router-wired`). It is the right property because it travels WITH the signal (no `Inp*` global re-read inside the orchestrator) and survives the `best_signal` field-copy (`best_signal.routed_engine = signal.routed_engine;`, :810) so the honored-scorer routing persists through ranking. Iter-3 migration is coherent: when `InpEnableMultiStrategy` turns ON, the same engine objects receive `SetScorer`+`RegisterEngine` (the `mq5:717–750` block), so `TagAndScore`/ScoreSignal runs the scorer, sets `routed_engine=true`, and the orchestrator HONORS that engine's tier/native-0-10 score/router activation weight — on the **2.4-GATE-re-derived `InpPoints*` thresholds**, which is exactly where the plan sequences engine-scorer consumption. The engine-scorer behavior change of fix 2.1 is therefore inert on the default path and exercised only in the engines-ON, thresholds-re-derived context — correct.

  **KILL-CRITERION: PASS** (production default path byte-identical BOTH 2024 and 2023, the 3 legacy engines' previously-dropped trades restored, zero divergence including the prior sub-0.25R ripple). NO logic cut.

### Adjudication point 3 — ITER-3 VERIFICATION ITEM (RECORDED, binding for Iter 3)
mt5 correctly flagged that the `routed_engine` flag is set at **4 separate per-engine sites**, NOT in a single shared base `CMajorStrategyEngine::TagAndScore` (the repo has the non-NULL-scorer scoring block duplicated per engine). This makes the invariant **"every routed engine sets `routed_engine=true` inside its own `if(m_scorer!=NULL)` scoring block"** a maintenance hazard rather than a structural guarantee. **Binding Iter-3 verification item:**
- When the router is turned ON in Iter 3 (per-engine validation 3.7–3.10, on the 2.4-GATE-re-derived thresholds), RE-VERIFY that each routed engine actually emits `routed_engine=true` and is honored by the orchestrator (a router-wired engine that scored but failed to set the flag would be silently mis-treated as a legacy comment-token signal — a false-legacy leak that would distort attribution and tiering).
- Whenever a NEW major engine is added or an existing engine's scoring block is refactored, CONFIRM it sets `routed_engine=true` inside its non-NULL-scorer block. A new engine that scores via `m_scorer` but omits the flag would be silently scored on the legacy evaluator path — exactly the kind of by-side-effect mis-routing this fix exists to prevent.
- Recommended hardening for Iter 3 (non-blocking for 2.1): consider lifting the flag-set into a shared helper (e.g. a base `TagAndScore`/`ScoreAndStamp`) so the invariant is enforced in one place rather than relied upon at 4 sites. Out of 2.1 scope; flag for the Iter-3 engine-correctness work (3.1–3.6).

---

## (OPTION-1 HISTORY BELOW — REJECTED by stok; preserved as the loop record)

## Phase
- **Phase id:** 2.1
- **Fix ids:** 2.1
- **Touches trading logic:** Y (FIX-SOUND plumbing, but it is the PREREQUISITE for every engine fix in Iter 3)
- **depends_on (phases that must be COMMITTED first):** Iter 1 (all COMMITTED). 2.1 is the start of Iter 2.
- **Binding stok param/option used:** n/a (no tuning constant). Literal spec: `is_engine = (signal.major_engine != ENGINE_NONE)`.

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/Core/CSignalOrchestrator.mqh` (CheckForNewSignals loop):
    - After `signal.plugin_name = ...` (was ~551): added `bool is_engine = (signal.major_engine != ENGINE_NONE);` (the engine stamps `major_engine` BEFORE the loop runs).
    - At the `signal.regime_risk_multiplier = 1.0;` reset (was ~580): gated to `if(!is_engine) signal.regime_risk_multiplier = 1.0;` — preserves the engine's router activation weight (engines set `regime_risk_multiplier = m_activation_weight` in TagAndScore; the unconditional reset discarded it). Non-engine path identical (still resets to 1.0).
    - At the "Score quality" call (was ~692): for engine signals `quality = signal.setupQuality` (the engine's own CConfluenceScorer tier, already computed); for non-engine signals the UNCHANGED legacy `m_evaluator.EvaluateSetupQuality(daily,h4,regime,macro_score,comment,isBearRegime,sig_type)`.
    - At the populate block (was ~722-723): `signal.setupQuality = quality;` kept (no-op for engines, since quality==setupQuality); `signal.qualityScore = GetQualityScore(quality)` gated to `if(!is_engine)` so engines keep the scorer's native 0-10 qualityScore for ranking instead of the legacy bucket {10/7/5/3}.
    - In the best-signal field copy (was ~767): added `best_signal.major_engine = signal.major_engine;` (also satisfies fix 5.12) so the engine identity survives ranking.
  - `Include/Validation/CSetupEvaluator.mqh` (legacy `EvaluateSetupQuality(daily,h4,...)` overload, Factor-4 block):
    - Added `int points_before_factor4 = points;` before the Factor-4 if/else cascade and a diagnostic WARN after it: if `points == points_before_factor4` (no Factor-4 token matched) AND the comment contains a known engine token (OB Retest / FVG Mitigation / SFP / Silver Bullet / London Close Rev / Compression / Institutional Candle / Panic Momentum / FailedBreak / Failed Break / Rubber Band / Displacement / Mitigation), `LogPrint` a WARN. Pure diagnostic — no scoring math changed (LogPrint is off the trade-decision path).
  - `Include/Common/Structs.mqh` — INSPECTED, NO change needed. `EntrySignal.major_engine` (line 548) and `SPosition.major_engine` (line 134) already exist; `EntrySignal::Init()` (line 580) already defaults `major_engine = ENGINE_NONE`. The plan's "Structs.mqh ~134" reference was to verify the field exists — it does.
- **Diff summary:** One scorer of record per signal class. Engine signals (`major_engine != ENGINE_NONE`) now HONOR their own CConfluenceScorer tier/score and keep their router weight; legacy/pattern/file signals (`major_engine == ENGINE_NONE`) take the byte-identical legacy comment-token evaluator path. `best_signal.major_engine` is now copied.
- **Logic-cut check:** NO trading logic deleted/short-circuited. No toggle/registration change. The legacy `EvaluateSetupQuality` overload is untouched (additive WARN only). The change is conditional routing, not removal.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 7812 ms elapsed, cpu='X64 Regular'
```
- Errors: 0 | New warnings vs 38-baseline: 0 (38 == 38; none in CSignalOrchestrator.mqh or CSetupEvaluator.mqh — verified by grep of the decoded log).
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — B build `/tmp/UT_phase2_1_fix.ex5` (md5 56ebad539b2dc63e076a04f148a05b5a), copied to repo `UltimateTrader.ex5` and to the tester `MQL5/Experts/UltimateTrader.ex5`. A-baseline snapshot (committed-HEAD source rebuilt): `/tmp/UT_phase2_1_baseline.ex5` (`Result: 0 errors, 38 warnings, 7691 ms`).

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, production `UltimateTrader.set` — `InpSymbolProfile=0` (gold), `InpSignalSource=2` (BOTH), `InpEnableMultiStrategy=false`, `InpEnableEngineTrend/Reversal/Range/Expansion=false` (the router gates). **BUT the legacy "major-engine" plugin gates are ON:** `InpEnableExpansionEngine=true`, `InpEnableSessionEngine=true`, `InpEnableLiquidityEngine=true`, `InpEnablePullbackCont=true`, `InpEnableDisplacementEntry=true`. Deposit 10000 / leverage 100. Same `.set` + same `.ini` for A and B (only the binary differs). Terminal: Vantage Markets MT5 Terminal / user nullkuhl / terminal ID 725B72F25E46C780EF59F57016D58156, WITHOUT `/portable`. The tester loads `MQL5/Experts/UltimateTrader.ex5` (a SEPARATE copy from the repo build dir — each build is copied into Experts before its run).
- **Mode:** Model=1 (1-min OHLC), matching all prior phases.
- **A (committed-HEAD source rebuilt):** A REPRODUCED the committed Phase-1.11 baseline EXACTLY in both windows — 2024 Stats md5 `04d116972696f905c6ea5fa6701a7155` (+ TradeEvents/Risk/Candidates/backtest_results md5 all matching the recorded 1.11 values); 2023 Stats md5 `b11e07675cf4d577e27663ffde0aca19`. Confirms A IS committed HEAD and the harness is deterministic.
  - 2024: 140 trades | net $1190.84 | totR 11.76
  - 2023: 129 trades | net $714.29  | totR 3.40
- **B (fix 2.1):**
  - 2024: 134 trades | net $946.50 | totR 10.30  (Stats md5 843aee2a49a28ae2deffb37a6d59a223)
  - 2023: 128 trades | net $825.20 | totR 4.47   (Stats md5 c6c96a8f4bee0db3e939cb5bb9c43cb0)
- **No-op / byte-identical claim:** **NOT byte-identical.** A != B in BOTH windows. THIS IS THE FAIL-LOOP TRIGGER (see Decision).

### Exact divergence (SignalID-keyed, robust CSV parse)
- **2024:** 6 trades DROPPED, 0 added. ALL 6 are `ExpansionEngine` signals (`major_engine = ENGINE_EXPANSION`): Asian Breakout London ×2, London Continuation NY ×1, IC Breakout Long ×3.
- **2023:** 1 trade DROPPED, 0 added. The 1 is an `ExpansionEngine` SETUP_A LONG (`major_engine = ENGINE_EXPANSION`).
- **The non-engine (`major_engine == ENGINE_NONE`) ENTRY set is BYTE-IDENTICAL** in both years: 2024 = 134/134 identical SignalID/Pattern/Dir/Quality/Entry/SL (0 diffs); 2023 = 128/128 (0 diffs). The truly-legacy pattern/file path (Engulfing/PinBar/MACross/FailedBreak/Pullback/File) is structurally untouched.
- **Only realized P&L ripples on the surviving non-engine trades** (exit-side, driven by the changed open-position/equity population): 2024 non-engine PnL_R ripple = 27 identical / 82 <0.05R / 20 [0.05,0.1) / 5 [0.1,0.25) / **0 ≥0.25R** (max |dR| 0.180R); 2023 = 66 identical / 59 <0.05 / 1 [0.05,0.1) / 2 [0.1,0.25) / **0 ≥0.25R** (max 0.220R). Same sub-0.25R exit-ripple class documented in Phases 1.4/1.7.

### ROOT CAUSE (mechanism, confirmed at source + via Candidates audit)
The TASK's acceptance ("legacy path = engines OFF = byte-identical, engines off by default") rests on a premise that is FALSE for this repo's production `.set`. `CExpansionEngine` is major engine #4 (`m_engine_id = ENGINE_EXPANSION`, CExpansionEngine.mqh:95) and is registered as a **legacy standalone entry plugin** at `UltimateTrader.mq5:686` gated on `InpEnableExpansionEngine` (the LEGACY toggle) — NOT on `InpEnableMultiStrategy` (the router gate, fix 5.7's orphan `InpEnableEngineExpansion`). `InpEnableExpansionEngine=true` on the production `.set`, so the Expansion engine runs in the engine-OFF "legacy" path and stamps `signal.major_engine = ENGINE_EXPANSION` + runs its own `m_scorer.Score(...)` (TagAndScore, CExpansionEngine.mqh:574-598). Its TagAndScore comment even documents the bug: "the orchestrator re-derives the ranking score from the evaluator/tier" — that re-derive IS the discard fix 2.1 removes.

Candidates-audit proof (2024): in **A** the orchestrator DISCARDED the engine scorer and re-scored these via the legacy comment-token evaluator → "Asian Breakout"/"IC Breakout" match Factor-4 (+2) plus trend/regime/macro → SETUP_A_PLUS/SETUP_A, QUALIFIED, executed. In **B** the orchestrator HONORS the engine's own CConfluenceScorer, which returns **SETUP_NONE (score 0)** for every one of these → QUALITY_BELOW_THRESHOLD → rejected. So fix 2.1 is operating EXACTLY as specified ("for engine signals use the engine's own setupQuality"); the engine scorer is simply far stricter than the legacy evaluator and rejects these expansion signals at the current (un-re-derived) thresholds.

Same applies to `CSessionEngine`/`CLiquidityEngine` (also `major_engine`-stamping, also legacy-registered ON), though in 2024/2023 the dropped set happened to be Expansion-tagged. `CPullbackContinuationEngine` does NOT stamp major_engine (it `Init()`s and leaves ENGINE_NONE), so `is_engine=false` for it → it stayed byte-identical (verified: its 5/2024 + Pullback/2023 trades unchanged).

## Inspection note (engine-scorer wiring correctness)
The wiring is correct by inspection AND proven by the Candidates audit: engine signals now carry their own scorer tier (engine's `m_scorer.Score()` result), the legacy comment-token overwrite is skipped for them, their native 0-10 `qualityScore` is preserved for ranking, their router `regime_risk_multiplier` is preserved, and `best_signal.major_engine` survives the ranking copy. The engine tier is now CONSUMED rather than discarded — exactly the prerequisite Iter 3 needs. (It will be exercised with engines ON, on the re-derived 2.4-GATE thresholds, in Iteration 3.)

## stok review + verdict
- **Numbers handed to stok:** the divergence above (2024 140→134t / $1190.84→$946.50; 2023 129→128t / $714.29→$825.20), the SignalID-keyed isolation (non-engine ENTRY set byte-identical both years; only ExpansionEngine signals dropped; sub-0.25R exit ripple), and the Candidates audit (A scored these A/A+ via the legacy evaluator; B's honored engine scorer returns SETUP_NONE).
- **stok assessment:** REVIEWED at source (independent stok agent — verified the registration architecture in `UltimateTrader.mq5:665/674/686` and `717-755`, `CMajorStrategyEngine.mqh:30-53`, `CExpansionEngine.mqh:570-598`, `Structs.mqh:550-581`, NOT the write-up). The write-up's mechanism is correct AND under-stated. The decisive source fact the spec author lacked: **the engine's CConfluenceScorer NEVER RAN on the legacy path.** `CMajorStrategyEngine::m_scorer` is `NULL` by default (base ctor, `CMajorStrategyEngine.mqh:42`) and is set ONLY by `SetScorer(g_confluenceScorer)`, which is called EXCLUSIVELY inside the `if(InpEnableMultiStrategy)` block (`UltimateTrader.mq5:728/734/740/751`). On the legacy-registered path (multi-strategy OFF — the production `.set`), `TagAndScore`'s `if(m_scorer != NULL)` branch (`CExpansionEngine.mqh:589`) is FALSE, so `signal.setupQuality` is never computed — it stays at `EntrySignal::Init()`'s default `SETUP_NONE` (`Structs.mqh:570`). So Option 1 does NOT "honor the engine's own stricter scorer"; it honors an **UNINITIALIZED `SETUP_NONE`** because no scorer ever ran. That is not a stricter judgment — it is reading a field that was never populated. Honoring it silently zeroes the tier of three CURRENTLY-TRADING engines. This is unambiguously a logic cut by side-effect and a violation of "legacy path byte-identical."
- **stok verdict: OPTION 2 — SCOPE-TO-ROUTER. BINDING.** Rationale, in priority order: (1) **No-logic-cut is non-negotiable and Option 1 cuts three live engines** (Expansion confirmed in the A/B; Session/Liquidity identically exposed) by honoring a never-computed `SETUP_NONE`. (2) **It prejudges engine scoring before the 2.4-GATE** — the gate exists precisely to forbid scoring on an un-re-derived distribution; here it is worse than "un-re-derived," the scorer literally did not execute. (3) The 2.1 plan-row's own acceptance text says "**legacy path byte-identical to v18**" and labels this **FIX-SOUND plumbing** — a FIX-SOUND item that drops 6 live trades is mis-classified, not accepted. The correct discriminator is NOT "did this signal carry `major_engine`" (a structural identity true on BOTH paths since `TagAndScore` always stamps `m_engine_id`) but "**did the orchestrator's scorer actually run for this signal**" — i.e. was the engine wired through the router. That is true iff `InpEnableMultiStrategy` is on. The honoring then bites in Iteration 3 (engines-ON) on the re-derived 2.4-GATE thresholds — exactly where the plan sequences it.

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS + task):** legacy (engine-off) path must be byte-identical; "if NOT identical, the legacy path was disturbed — STOP and reconcile."
- **Result:** FAIL-as-stated / RECONCILE. The TRULY-legacy (`major_engine==ENGINE_NONE`) ENTRY set IS byte-identical both years (0 entry/SL/quality diffs). The non-identity is confined to (a) ExpansionEngine signals — `major_engine != ENGINE_NONE` — which fix 2.1 is DESIGNED to rescore, and which the production `.set` registers ON as legacy plugins; and (b) sub-0.25R exit ripple on the unchanged co-existing legacy trades. The task's "engines are off by default" premise does not hold for this `.set` (InpEnableExpansionEngine/SessionEngine/LiquidityEngine=true). A correct implementation of the literal spec (`is_engine = major_engine != ENGINE_NONE`) therefore CANNOT be byte-identical on this config. **This requires stok adjudication — see RECONCILIATION QUESTION.**

## RECONCILIATION QUESTION FOR stok (binding decision needed)
The literal plan spec `is_engine = (signal.major_engine != ENGINE_NONE)` was implemented faithfully. On the production `.set` it changes the scoring of the legacy-registered major engines (Expansion/Session/Liquidity), which were previously getting A/A+ from the legacy comment-token evaluator and are now getting SETUP_NONE from their own (un-re-derived) CConfluenceScorer → 6 (2024) / 1 (2023) Expansion trades removed. Two readings:
1. **Spec-literal (current impl):** any signal carrying `major_engine` is an engine signal → honored by its scorer. This makes the production path NON-byte-identical NOW (Expansion/Session/Liquidity rescored to SETUP_NONE), effectively disabling those legacy-registered engines until the 2.4-GATE re-derives the engine-scorer thresholds. Net effect 2024 −$244 / 2023 +$111 (bidirectional; the dropped trades are a mix of winners and losers — the engine scorer is just stricter).
2. **Scope-to-router (alternative):** restrict `is_engine` to engines registered via the multi-strategy router (`InpEnableMultiStrategy`), so the legacy-registered Expansion/Session/Liquidity plugins keep the legacy evaluator and the production path STAYS byte-identical now; the honoring then only bites when `InpEnableMultiStrategy=true` (Iter 3). This matches the task's stated "engines off by default → byte-identical" expectation.

mt5 recommendation for stok: reading (2) reconciles the byte-identity acceptance and defers the engine-scorer consumption to Iter 3 (with re-derived thresholds), which is where the plan says it belongs ("until the engine tier is consumed, no Iter-3 engine can be judged" — and Iter 3 is engines-ON). Reading (1) is more literal but silently disables three currently-trading legacy engines on a NON-re-derived scorer (the exact thing the 2.4-GATE exists to prevent — scoring on an un-re-derived distribution). stok to pick the binding option.

## mt5-developer sign-off
- Compile clean (0 errors, exactly 38 warnings = baseline, 0 new, none in the two edited files). Fix implements the literal validated spec (is_engine discriminator; engine scorer honored; regime_risk_multiplier preserved for engines; best_signal.major_engine copied; CSetupEvaluator engine-token WARN). file:line verified against current source. NO logic cut. A reproduces committed-HEAD baseline exactly (deterministic harness). Engine-scorer wiring correct by inspection + Candidates audit. **SIGNED on compile + mechanism correctness.** NOT signing a byte-identity claim — the production path is NOT byte-identical (mechanism fully explained above; needs stok adjudication of the is_engine scope).

## stok sign-off
- **NOT SIGNED on the current (Option-1 / spec-literal) implementation** — it cuts live trading. **stok issues the BINDING redo spec below (Option 2). Re-QA after mt5 re-implements; stok sign-off is owed on the byte-identical-on-production redo, NOT pre-asserted here.**

## ===== BINDING stok DECISION + REDO SPEC FOR mt5 (Option 2) =====

**DECISION: OPTION 2 (scope-to-router). BINDING. Implement exactly the predicate below.**

### The gating predicate to use INSTEAD of `is_engine = (signal.major_engine != ENGINE_NONE)`
The discriminator must capture "this signal's tier was produced by the orchestrator's `CConfluenceScorer`," which on this codebase is true iff the engine was wired through the multi-strategy router. Implement via a router-set flag on the signal — **do NOT gate on the raw `major_engine` field, and do NOT gate on `InpEnableMultiStrategy` alone read inside the orchestrator** (the orchestrator should not re-read the global; the property must travel with the signal):

1. **Add `bool routed_engine;` to `EntrySignal`** (after `major_engine`, `Structs.mqh:548`), defaulted `false` in `EntrySignal::Init()` (`Structs.mqh:581`).
2. **Set it true ONLY where the scorer actually ran.** In `CMajorStrategyEngine::TagAndScore` (the shared base path used by all four engines via `CExpansionEngine.mqh:589` and siblings), inside the existing `if(m_scorer != NULL)` block, add `signal.routed_engine = true;`. Because `m_scorer` is NULL unless `SetScorer()` ran, and `SetScorer()` runs ONLY inside `if(InpEnableMultiStrategy)` (`UltimateTrader.mq5:728/734/740/751`), this flag is true iff the engine was router-wired. This is robust to future engines and needs no global read in the orchestrator.
3. **In `CSignalOrchestrator::CheckForNewSignals`, replace** `bool is_engine = (signal.major_engine != ENGINE_NONE);` **with** `bool is_engine = signal.routed_engine;`. Every existing downstream use of `is_engine` (the `regime_risk_multiplier` reset gate ~580, the quality-scoring branch ~692, the `qualityScore` gate ~722) is then correctly scoped: routed engines honor their CConfluenceScorer tier; legacy-registered engines (`m_scorer==NULL`, `routed_engine==false`) take the UNCHANGED legacy comment-token evaluator path — byte-identical to v18.
   - **Acceptable equivalent if a struct field is undesired:** gate on `(InpEnableMultiStrategy && signal.major_engine != ENGINE_NONE)`. This is behaviorally identical on the production `.set` (multi-strategy OFF → `is_engine` always false → byte-identical). The `routed_engine` flag is PREFERRED because it (a) keeps the orchestrator from reading an Inp* global, (b) survives the `best_signal` field-copy cleanly, and (c) is the exact "scorer ran" semantic, not a proxy. **mt5's choice between the two, but the `routed_engine` flag is the recommendation.**
4. **KEEP `best_signal.major_engine = signal.major_engine;`** (the ~767 copy — also fix 5.12) AND add `best_signal.routed_engine = signal.routed_engine;` in the same field-copy block so the flag survives ranking.

### What MUST stay byte-identical (the redo acceptance gate)
On the production `.set` (`InpEnableMultiStrategy=false`; `InpEnableExpansionEngine/SessionEngine/LiquidityEngine=true`): **A == B trade-for-trade, SL-for-SL, R-for-R for BOTH 2024 and 2023** (and prove it — the full Stats/TradeEvents/Risk/Candidates CSV md5s must match the committed-HEAD baseline: 2024 Stats `04d116972696f905c6ea5fa6701a7155`, 2023 `b11e07675cf4d577e27663ffde0aca19`). The 6/2024 + 1/2023 ExpansionEngine trades MUST return. There must be ZERO divergence — not even the sub-0.25R exit ripple, because with `is_engine=routed_engine=false` for every signal on this config, the orchestrator path is literally unchanged. If ANY divergence remains, the predicate is wrong — reconcile before re-QA.

### Iter-3 migration note (one line, confirmed coherent)
When `InpEnableMultiStrategy` turns ON in Iteration 3, the same `CExpansionEngine`/`CSessionEngine`(via its engine)/`CLiquidityEngine` objects get `SetScorer(g_confluenceScorer)` + `RegisterEngine(...)` (the existing `if(InpEnableMultiStrategy)` block, `UltimateTrader.mq5:717-755`), so `TagAndScore` runs the scorer, sets `routed_engine=true`, and the orchestrator HONORS their tier — on the **2.4-GATE-re-derived `InpPoints*` thresholds** (mandatory hard gate before any Iter-3 engine validation). This is coherent: the engine-scorer consumption that Option 1 forced NOW (on a never-run / un-re-derived scorer) is deferred to exactly the engines-ON, thresholds-re-derived context the plan sequences it into. NOTE for Iter 3: the legacy standalone registration of Expansion (`mq5:686`) and the router registration of engine-4 (`mq5:744-752`, gated on `InpEnableEngineExpansion` per fix 5.7) point at the SAME `g_expansionEngine` object — confirm no double-registration/double-emit when both `InpEnableExpansionEngine` and `InpEnableMultiStrategy+InpEnableEngineExpansion` are on (out of 2.1 scope; flagged for 3.10/5.7).

## Decision
- **LOOP — QA-FAIL-LOOP, redo per the BINDING Option-2 spec above.** stok ADJUDICATED: Option 1 (current impl) honors a never-computed default `SETUP_NONE` on three live legacy-registered engines = a logic cut by side-effect + prejudges engine scoring before the 2.4-GATE; REJECTED. Option 2 (scope-to-router via the `routed_engine` flag) keeps the production path byte-identical NOW and defers engine-scorer consumption to Iter 3 on re-derived thresholds. mt5 to re-implement the predicate, re-compile (0-err/0-new-warn), re-run the A/B, and PROVE byte-identity (md5-match both years). Do NOT advance to Phase 2.2 until the redo passes and BOTH agents sign. Do NOT commit (main session runs git). stok sign-off is owed on the redo, not pre-asserted.

## Commit
- **Hash:** n/a (Do NOT commit — per task instruction; and the gate is not green)
- **Message:** n/a
