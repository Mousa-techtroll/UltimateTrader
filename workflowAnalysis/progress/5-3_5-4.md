# Phase 5.3+5.4 — Multi-symbol file-position trail + closed-bar swing/ATR (Iteration 5, bundled)

## Phase
- **Phase id:** 5.3+5.4 (bundled — same trail-block lines, land together in ONE commit)
- **Fix ids:** 5.3 (Core-Pos-1 == XCut-3, multi-symbol file trail) + 5.4 (XCut-5, closed-bar swing/ATR in the file trail)
- **Touches trading logic:** Y (file-signal trail path) — but **inert in the Strategy Tester** (see STEP-0 path determination + byte-identity proof)
- **depends_on (phases that must be COMMITTED first):** 5.1 (symbol-match recovery, COMMITTED `90e4d82`) — landed first to shrink the trail-fix blast radius. HEAD = `90e4d82` on `feat/multi-strategy`.
- **Binding stok param/option used:** n/a (no new input; multi-symbol risk stays ONE aggregate bucket by design — NOT changed; count stays 5 — NOT widened to 6)

---

## STEP 0 — PATH DETERMINATION (done FIRST, before implementing)
**Finding (stated explicitly): BOTH trail blocks are the FILE-SIGNAL-position trail path, not the general trail path. In the Strategy Tester, file-signal positions NEVER open (`telegram_signals.csv` is absent). Therefore both 5.3 and 5.4 are BYTE-IDENTICAL in backtest.**

Evidence from current source (`Include/Core/CPositionCoordinator.mqh`, `UpdatePositions()` loop):
- The whole TP-ladder + trailing region is inside `if(m_positions[i].signal_source == SIGNAL_SOURCE_FILE)` which **opens at line 1694** and `continue`s at line 1972 for normal file signals (only `best_effort_mode && InpBestEffortFullManagement` falls through to the pattern path).
- The `pos_symbol` derivation (the strong hint from the prompt) is at **lines 1697–1698**: `string pos_symbol = PositionGetString(POSITION_SYMBOL); if(pos_symbol=="") pos_symbol = _Symbol;` — confirming these are file-position blocks.
- **Trail block 1** = the TP3/Runner ATR trail under `else if(InpFileTrailAfterTP2)` (lines ~1825–1866).
- **Trail block 2** = "PART A: Trailing after TP1" under `if(InpFileSignalTrailing && m_positions[i].tp0_closed)` (lines ~1882–1927).
- Plan cited ~1799/1812/1814/1818/1820 + 1864/1876/1877/1882; **lines drifted post-5.1** — verified actual lines against current source (block 1 at 1828/1841/1843/1847/1849; block 2 at 1893/1905/1906/1910/1911).

**Do file-signal positions EVER open in the tester?** NO. The golden ini runs `InpSignalSource=2` (BOTH), so the file path IS wired and runs every `InpFileCheckInterval`, but `telegram_signals.csv` is **ABSENT** from the tester Files dir (`.../MQL5/Files/telegram_signals.csv` — confirmed missing; matches Phases 4.4/4.5/4.7). `CFileEntry` finds no signals → 0 file-signal positions → the `signal_source==SIGNAL_SOURCE_FILE` branch is never entered → both trail blocks never execute.

**Conclusion stated before implementing:** XAUUSD-only tester ⇒ 5.3 and 5.4 are BYTE-IDENTICAL vs HEAD. The helper changes (CalculatePositionRiskDollars / GetCurrentMarketPrice) ARE on the pattern path (~15 call sites), but on a single-symbol tester `PositionGetString(POSITION_SYMBOL)` always returns the chart symbol → they resolve to `_Symbol` → still byte-identical. Proven below.

---

## What changed (per-fix, verified file:line against current source)

### Fix 5.3 — thread the position's OWN symbol (multi-symbol file trail)
- **`Include/Core/CPositionCoordinator.mqh` trail block 1 (TP3/Runner, `InpFileTrailAfterTP2`):** `iATR(_Symbol,…)`→`iATR(pos_symbol,…)` (1836/1901-region); `iHighest(_Symbol,…)`→`iHighest(pos_symbol,…)`, `iHigh(_Symbol,…)`→`iHigh(pos_symbol,…)`, `iLowest`/`iLow` likewise. `pos_symbol` is the in-scope derivation from lines 1697–1698.
- **trail block 2 (PART A, `InpFileSignalTrailing`):** same `_Symbol`→`pos_symbol` on iATR/iHighest/iHigh/iLowest/iLow.
- **`CalculatePositionRiskDollars(const SPosition &pos)` (helper, ~290):** derive symbol from the live position — `PositionSelectByTicket(pos.ticket)` then `PositionGetString(POSITION_SYMBOL)` into `risk_symbol` (fallback `_Symbol`); `SYMBOL_TRADE_TICK_VALUE`/`SYMBOL_TRADE_TICK_SIZE` now read `risk_symbol`. (Note: `SPosition` has **no** `symbol` field — verified Structs.mqh; the line-517 `symbol` belongs to `EntrySignal`. So derivation is from the broker position, not the struct — this is what caused the first compile to fail; fixed.)
- **`GetCurrentMarketPrice(const SPosition &pos)` (helper, ~318):** capture `POSITION_SYMBOL` into `price_symbol` (fallback `_Symbol`) inside the existing `PositionSelectByTicket`; the BID/ASK fallback now reads `price_symbol`.
- Deriving inside the two helpers means **the ~15 call sites are untouched** (per the validated fix). **Multi-symbol risk stays ONE aggregate bucket** — `CalculateTotalRiskExposure()` (994) still sums all positions' risk-dollars / equity; NOT changed.

### Fix 5.4 — closed-bar swing/ATR in the file trail (XCut-5, same lines as 5.3)
- In BOTH trail blocks: `iHighest(...,MODE_HIGH,5,0)`→`iHighest(...,MODE_HIGH,5,1)` and `iLowest(...,MODE_LOW,5,0)`→`iLowest(...,MODE_LOW,5,1)` — **last arg 0→1 (closed bars). Count STAYS 5 (NOT widened to 6).**
- ATR: `CopyBuffer(atr_h,0,0,1,buf)` → `CopyBuffer(atr_h,0,1,1,buf)` with `buf[0]` (the closed bar [1]) in both blocks.
- Same closed-bar repaint-correctness class as fix 4.8 (forming-bar `[0]` saw intrabar wick extremes / a still-forming ATR → intrabar wick-driven SL ratcheting on gold's wide H1 wicks).

- **Diff summary:** 1 file, +27/−16 lines. Both file-trail blocks: `_Symbol`→`pos_symbol` (5.3) + `0`→`1` closed-bar on iHighest/iLowest/ATR (5.4). Two helpers derive symbol-from-position so the ~15 call sites are untouched.
- **Logic-cut check:** NO trading logic deleted. `is_better` ratchet (tighten-only) preserved in both blocks; TP cascade, BE/SL-to-TP1 moves, exit-plugin loop all untouched. Aggregate risk bucket unchanged. No new input.

---

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 9236 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (exactly 38; zero warnings reference `CPositionCoordinator.mqh`)
- **First attempt FAILED** `4 errors, 42 warnings` — `error 256: undeclared identifier 'symbol'` from a wrong `pos.symbol` fallback (SPosition has no `symbol` field). Removed the `else if(pos.symbol != "")` fallback in both helpers → recompiled clean. (Recorded for the durable trail.)
- Error 106 / include issues: none
- `.ex5` regenerated: yes — `UltimateTrader.ex5` (912466 bytes). A/B binaries staged in the data-dir Experts folder: `UltimateTrader_p53A.ex5` (clean HEAD `90e4d82`, fix git-stashed; compiled `0 errors, 38 warnings`; md5 `3f8d9831…`), `UltimateTrader_p53B.ex5` (with fix; md5 `880897790341…`). **Negative control: A.ex5 ≠ B.ex5** (binaries genuinely differ → byte-identical CSV result is a real behavioral test, not a same-binary artifact).

---

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, full **2019.01.01–2026.06.27**, golden `backtest.ini` [TesterInputs] 395 production defaults (`InpEnableMultiStrategy=false`, `InpSignalSource=2`/BOTH, `InpTrailStrategy=4`, `InpMagicNumber=999999`), Deposit 10000 USD, Currency USD, Leverage 100, Optimization=0, Visual=0, ShutdownTerminal=1. A/B inis differ from the golden ini ONLY in `Expert=` + `Report=` (`diff` = exactly those 2 lines). **2 terminal runs total (within the ≤2 bound).**
- **Mode:** Model=1 (1-minute OHLC).
- **A (committed HEAD `90e4d82`, fix stashed → `UltimateTrader_p53A.ex5`):** Net **$16,685.59** | PF **1.28** | Trades **1742** | Sharpe **2.20** | Equity-DD **3,941.64 (12.88%)**.
- **B (BOTH fixes → `UltimateTrader_p53B.ex5`):** Net **$16,685.59** | PF **1.28** | Trades **1742** | Sharpe **2.20** | Equity-DD **3,941.64 (12.88%)**. IDENTICAL to A.
- **Harness soundness:** A's 4 EA CSVs reproduce the recorded Phase-5.1 committed-HEAD (`90e4d82`) baseline byte-for-byte (same md5s: Stats `fc9f934e…`, TradeEvents `40b897c1…`, Risk `49876b51…`, backtest_results `0424aa9f…`); A's headline == the binding baseline (Net $16,685.59 / PF 1.28 / 1742 / Sharpe 2.20 / Eq-DD 12.88%). Harness sound.
- **Delta + explanation:** ZERO delta. Both trail blocks are file-only; file positions never open in the tester; the two helpers resolve to `_Symbol` on a single-symbol tester. Byte-identical by construction.
- **No-op / byte-identical claim:** **CONFIRMED — A == B BYTE-IDENTICAL on all 4 output CSVs (md5-equal AND cmp-clean).**

## Byte-identity proof (4 CSV md5 + cmp; `/tmp/p53/A` vs `/tmp/p53/B`)
| Output CSV | A md5 | B md5 | md5 | cmp |
|---|---|---|---|---|
| `UltTrader_Stats_XAUUSD+_20190101_0000.csv` | `fc9f934ea78025d30c079bdd8fd67a80` | `fc9f934ea78025d30c079bdd8fd67a80` | IDENTICAL | CLEAN |
| `UltTrader_TradeEvents_XAUUSD+_20190101_0000.csv` | `40b897c19fbfaa9dec08e962f2b569a2` | `40b897c19fbfaa9dec08e962f2b569a2` | IDENTICAL | CLEAN |
| `UltTrader_Risk_XAUUSD+_20190101_0000.csv` | `49876b51edd0540a4f08f16eb88f5c3b` | `49876b51edd0540a4f08f16eb88f5c3b` | IDENTICAL | CLEAN |
| `backtest_results_20260626.csv` | `0424aa9ff3c89a0867e25be78527e4de` | `0424aa9ff3c89a0867e25be78527e4de` | IDENTICAL | CLEAN |

All 4 `cmp -s` clean. These md5s == the Phase-5.1 recorded committed-HEAD baseline → A AND B both reproduce HEAD `90e4d82` byte-for-byte.

## File-positions-never-open confirmation
- `telegram_signals.csv` ABSENT in the tester Files dir → 0 file-signal positions.
- B's output CSVs: **0** `FileTP/FileSignal/FILE_INDEPENDENT/FileTrail` markers in Stats.csv; **0** in TradeEvents.csv; **0** `FileSignal`-engine rows in the per-strategy `backtest_results` breakdown.
- B's tester journal (`20260627.log`): `Signal Source: SIGNAL_SOURCE_BOTH` (file path IS wired+running), `telegram_signals` appears 27,613× (the EA repeatedly attempts to read the CSV) but finds none → **0** `[FileTP*]`/`[FileTrail]`/`[FileExit]`/`[FileSignal]` log lines across the recent tester logs. Pattern engines fire (Engulfing/Rubber Band Short present). → the `signal_source==SIGNAL_SOURCE_FILE` branch (and thus both trail blocks) never executed.

## Byte-identity MECHANISM (stated)
The trail blocks live inside `if(m_positions[i].signal_source == SIGNAL_SOURCE_FILE)`; with `telegram_signals.csv` absent, that branch is never entered, so the 5.3 symbol-threading + 5.4 closed-bar reads inside it are never reached. The two helper changes ARE reached on the pattern path, but a single-symbol tester only ever instantiates XAUUSD+ positions → `PositionGetString(POSITION_SYMBOL)` == `_Symbol` → `risk_symbol`/`price_symbol` resolve to `_Symbol` → identical math. Both effects ⇒ byte-identical output. This is the expected, designed result.

## 5.4 live-only improvement (note for stok)
Because the file-trail path is file-only and unexercised in the tester, the byte-identical A/B proves **NO production regression**. The closed-bar improvement itself (5.4) is **live-only** and rests on the SAME repaint-correctness argument as fix 4.8 (forming-bar `[0]` swing/ATR repaints intrabar; closed-bar `[1]` is the honest real-time basis that eliminates intrabar wick-driven ratcheting on gold's wide H1 wicks). The plan's 5.4 KILL-CRITERION (runner premature-stop count must DROP without TP3-hit count dropping) is **not exercisable here** — it requires live/file signals. A synthetic file-signal validation backtest (injecting a `telegram_signals.csv`) would be needed to measure it, which is **OUT of the ≤2-run scope** → flagged as a follow-up for stok to decide if needed.

## stok review + verdict
- **Numbers handed to stok:** A vs B headline (identical: Net $16,685.59 / PF 1.28 / 1742t / Sharpe 2.20 / Eq-DD 12.88%) + 4-CSV byte-identity proof + STEP-0 file-only-path determination + 0-file-positions confirmation + the 5.4 live-only / KILL-criterion-not-exercisable note.
- **stok assessment:** PENDING.
- **stok verdict:** PENDING.

## Kill-criteria check
- **Phase kill-criteria (from plan §Iteration 5 / 00-STATUS):** (1) XAUUSD-only 2019–2026 byte-identical for 5.3 (no-op on production path); (2) 5.4 TP3-hit count must NOT drop alongside premature-stop count.
- **Result:** (1) **PASS** — all 4 CSVs byte-identical (md5+cmp); A reproduces committed HEAD. (2) **NOT TRIPPED / NOT EXERCISABLE in the tester** — file path unexercised (0 file positions), so no TP3/premature-stop counts to compare; 5.4 is live-only. No regression possible on the production path (byte-identical).

## mt5-developer sign-off
- Compile clean (0 errors / 38 warnings, 0 new); both trail blocks + both helpers verified file:line against current source; STEP-0 path determination established BEFORE implementing (file-only blocks, 0 file positions in tester); 5.3 threads `pos_symbol` into all iATR/iHighest/iHigh/iLowest/iLow + derives symbol-from-position inside the 2 helpers (~15 call sites untouched); aggregate risk bucket unchanged; 5.4 closed-bar `0→1` on iHighest/iLowest + ATR `CopyBuffer(...,0,1,1,buf)`+`buf[0]`, count stays 5; no logic cut; A==B byte-identical PROVEN (4-CSV md5+cmp) with A reproducing committed HEAD; negative control A.ex5≠B.ex5. — **SIGNED (mt5-developer)**

## stok sign-off
- **SIGNED — verdict: ACCEPT-WITH-FOLLOWUP** (binding). stok verified every claim at source (trail block1 :1836–1877, block2 :1903–1934; pos_symbol :1708–1709 inside the SIGNAL_SOURCE_FILE guard :1705; helpers CalculatePositionRiskDollars :290–313, GetCurrentMarketPrice :325–341; Structs.mqh :517 confirms `symbol` is EntrySignal's not SPosition's). Ruling:
  - **(1) 5.3 CORRECT + COMPLETE.** OLD code trails a FILE position on symbol X against the chart _Symbol → structurally wrong SL on TWO axes: ATR-scale mismatch (gold's ~$10–20 ATR applied as trail_dist to a 1.08-priced EURUSD = SL thousands of pips away / nonsensical), swing-level mismatch (sets symbol-X's SL to gold's recent-high − gold-ATR = a price on a different instrument's scale → broker-reject or instant hit → runner UNMANAGED), and risk-telemetry corruption (gold tick-value applied to symbol-X risk_dist → poisons CalculateTotalRiskExposure, the aggregate gate). Threading pos_symbol + deriving symbol inside the 2 helpers fixes trail + risk at all ~15 call sites untouched. **PositionGetString is the RIGHT (only correct) symbol source** since SPosition has none — broker position is ground truth; dropping the failed pos.symbol fallback (rather than inventing a struct field) is the right call. Empty-string fallback to _Symbol is sound (only on a failed select, which already `continue`s upstream).
  - **(2) ONE aggregate risk bucket = correct conservative policy.** The gold complex is heavily correlated (GC↔XAUUSD↔SI, all inverse-DXY); an aggregate bucket cannot under-count a coordinated move the way per-symbol budgets would (3 "independent" 1% gold-complex trades = a 3% bet on one DXY move). Per-symbol would be LESS safe. Keep it.
  - **(3) 5.4 CORRECT — same repaint class as 4.8.** Forming-bar [0] swing/ATR lets the trail ratchet on intrabar wick extremes that don't survive the close (gold H1 = wide wick-heavy candles → premature runner stop-out); closed-bar [1] references only settled price = honest. is_better tighten-only ratchet preserved (block1 :1866–1874, block2 :1926–1933) → can only make the trail MORE honest. Count rightly STAYS 5 (widening to 6 is a strategy-behavior change, not the repaint fix).
  - **(4) Sufficient to ACCEPT without the synthetic backtest:** byte-identical A/B proves ZERO production regression on the only tester-exercised path; the live-only 5.4 improvement rests on the same 4.8 argument (independently accepted) → consistency demands the same standard. The synthetic-injection validation is where to MEASURE the edge, not a gate on a commit that already proves no-regression.
  - **Verdict: ACCEPT-WITH-FOLLOWUP.**

## Follow-ups (logged by stok; deferred, NONE blocks this commit, NONE a loop)
- **FU-A (validation, before file/multi-symbol signals are relied on LIVE):** run the synthetic file-signal validation — inject a `telegram_signals.csv` into the tester Files dir and measure 5.4's KILL-CRITERION: runner-premature-stop count must DROP WITHOUT TP3-hit count dropping (if TP3 hits also fall → looser SL lets winners round-trip → reconsider). OUT of the ≤2-run scope; the place to measure the edge.
- **FU-B (latent footgun):** the 2 helpers call PositionSelectByTicket internally → mutate the GLOBAL position-select state. Current trail-path ordering is SAFE (helpers reselect the same ticket), but it's a latent ordering footgun if call ordering ever changes. Consider passing symbol explicitly / documenting the select-state contract.
- **FU-C (risk enhancement, NOT 5.3's lane):** the aggregate bucket is correlation-BLIND (right conservative default, but no correlation-aware haircuts for the gold complex). Future enhancement only.
- **FU-D (cadence):** `PERIOD_H1` is HARDCODED in both file-trail blocks' iATR/iHighest/iLowest — symbol is now correct (5.3) but a non-H1-intended file signal would still trail on gold's H1 cadence. Acceptable for an H1-primary EA; flag it.

## Decision
- **DONE — both agents signed (mt5 implementation-correct + byte-identical proven; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified). Committed source-only on `feat/multi-strategy`.** Multi-symbol file-trail correctness (5.3) + closed-bar file-trail swing/ATR (5.4, 4.8 repaint class) landed together; A/B BYTE-IDENTICAL on the production XAUUSD-only path (file-trail blocks never execute in the tester — telegram_signals.csv absent, 0 file positions despite 27,613 read-attempts). 4 deferred follow-ups (FU-A synthetic validation, FU-B select-side-effect, FU-C correlation-blind bucket, FU-D hardcoded H1). NEXT ACTION → Phase 5.5 (session_quality_factor per-bar reset + floor).

## Commit
- **Hash:** `e27b903` (source-only: CPositionCoordinator.mqh + 5-3_5-4.md + 5-1.md hash-note + 00-STATUS.md; `.ex5` intentionally uncommitted). Branch `feat/multi-strategy`.
