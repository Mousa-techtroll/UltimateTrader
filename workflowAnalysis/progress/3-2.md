# Phase 3.2 — Split CReversalSweepEngine reversal sub-edges into 3 distinct modes (fix 3.4)

---

## ✅ OPTION-(b) REDO — RESOLVED (stok BINDING decision, re-QA byte-identical)

**stok adjudicated the A!=B loop (below). BINDING decision = OPTION (b), Option-A implementation:** DELETE the two Factor-4 else-if branches; KEEP everything else (enum modes, Path-1/2/3 token+mode relabel, Fix-2.1 WARN, enum-ordinal-safety). Rationale verified at source: the legacy comment-token overload `CSetupEvaluator::EvaluateSetupQuality` is invoked ONLY on the non-engine else-branch (`CSignalOrchestrator:723`); routed engines take `quality = signal.setupQuality` at `:721` and never reach it. So the two branches fed NOTHING to the engine/GATE path — they only un-starved the LIVE legacy standalone S6 (CFailedBreakReversal, PF 0.78 marginal loser) and Crash/Rubber-Band (CCrashBreakoutEntry) plugins on production, promoting S6 ahead of its own Iter-6/6.9 kill-gate. That un-starving is DEFERRED to Iter-6/6.9 with its own kill-criteria, NOT here.

**Redo what-changed (file:line):**
- `Include/Validation/CSetupEvaluator.mqh` (Factor-4, ~233–247) — **DELETED** the two else-if branches `"FailedBreak"`/`"Failed Break"`→+2 and `"Rubber Band"`→+1 (plus their comment block). Replaced with a comment recording the stok Option-b rationale. The Fix-2.1 WARN list (still naming these tokens diagnostically) and every other Factor-4 branch are UNCHANGED.
- **KEPT** (unchanged from the original 3.2 work): `Enums.mqh` MODE_RUBBER_BAND (12) + MODE_FAILED_BREAK (13) appended at end; `CReversalSweepEngine.mqh` Path-1 S6→`MODE_FAILED_BREAK`/'FailedBreak' (:369), Path-2 sweep→`MODE_SFP`/'LiquiditySweep' (:378), Path-3 rubber-band→`MODE_RUBBER_BAND`/'Rubber Band' (:392); the Fix-2.1 WARN; enum-ordinal-safety.
- **Logic-cut check:** NO cut. Deleting an additive scoring branch only RESTORES the prior (byte-identical) production behavior; the GATE/engine attribution (the actual purpose of fix 3.4) is fully retained via the enum + token relabel.

**Redo compile result (verbatim):**
```
Result: 0 errors, 38 warnings, 7801 ms elapsed, cpu='X64 Regular'
```
- Errors: 0 | New warnings vs 38-baseline: 0. Error 106: none. `.ex5` regenerated — md5 `3943b42b01e4e7191ebcf95f54571376` (B'); A-baseline committed-HEAD `/tmp/UT_phase3_2_baseline.ex5` `445ae15b…` valid (reproduces anchors).

**Redo re-QA (BINDING acceptance) — A==B BYTE-IDENTICAL both years:** XAUUSD+ H1 Model=1 production `.set`. A (committed HEAD `445ae15b…`) vs B' (Option-b redo `3943b42b…`). **ALL 8 CSVs (Stats/TradeEvents/Candidates/Risk × 2024+2023) md5 MATCH + `cmp` byte-for-byte clean**, and A reproduces the committed anchors EXACTLY:
- 2024: Stats `04d116972696f905c6ea5fa6701a7155`, TradeEvents `825489b6…`, Candidates `bcc44924…`, Risk `8babda0f…` (A==B').
- 2023: Stats `b11e07675cf4d577e27663ffde0aca19`, TradeEvents `7ef40d5f…`, Candidates `2fba7df1…`, Risk `b21a2999…` (A==B').
- **Promoted-trade reversal CONFIRMED gone:** 2024 B' 140t/$1190.84/11.76R (was first-B 144t — the +4 'S6: Failed Break Long' promotions REMOVED; 'Failed Break' count 7→3, back to A's 3); 2023 B' 129t/$714.29/3.40R (was first-B 131t — the +2 'S6: Failed Break' + 'Rubber Band Short' promotions REMOVED; 'Failed Break' 2→1, 'Rubber Band' 19→18, back to A). Δtrades=+0 both years.

**Redo verdict:** byte-identity acceptance MET; the deliberate-but-misplaced legacy un-starving is removed; the 3-bucket GATE attribution is preserved. mt5-developer SIGNED (Option-b). **stok independent re-sign-off PENDING.** Status → `mt5-QA-DONE (Option b, re-QA byte-identical)`. Do NOT commit; do NOT advance until stok re-signs.

---

## Phase
- **Phase id:** 3.2
- **Fix ids:** 3.4 (mislabeled mode/token — rubber-band death-cross tagged MODE_SFP/'LiquiditySweep')
- **Touches trading logic:** Y (BETTER-OPTION — three distinct MODES for clean per-mode attribution; the Factor-4 starvation fix is reachable on the production path via the LEGACY standalone S6/Crash plugins — see A/B)
- **depends_on (COMMITTED first):** 3.1 (COMMITTED). Iter-3 ordering: 3.1–3.6 land before the 2.4-GATE derivation.
- **Binding stok param/option used:** n/a (no numeric param). Binding spec = plan fix 3.4 / 00-STATUS Phase 3.2: add MODE_RUBBER_BAND + MODE_FAILED_BREAK; Path1 S6→MODE_FAILED_BREAK/'FailedBreak', Path2 sweep→MODE_SFP/'LiquiditySweep', Path3 rubber-band→MODE_RUBBER_BAND/'Rubber Band' (spaced); add CSetupEvaluator Factor-4 branches for 'FailedBreak' AND 'Rubber Band' so neither is starved at 0.

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/Common/Enums.mqh` (ENUM_ENGINE_MODE, 279–300) — **APPENDED** `MODE_RUBBER_BAND` (ordinal 12) and `MODE_FAILED_BREAK` (ordinal 13) AFTER `MODE_PANIC_MOMENTUM` (11). Existing ordinals 0..11 unchanged.
  - `Include/EntryPlugins/CReversalSweepEngine.mqh` (CheckForEntrySignal cascade, 364–393):
    - Path 1 (S6, `m_s6`): `AdoptSignal(raw, MODE_SFP, "SFP")` → `AdoptSignal(raw, MODE_FAILED_BREAK, "FailedBreak")`.
    - Path 2 (standalone Liquidity Sweep, `m_liqSweep`): UNCHANGED — `AdoptSignal(raw, MODE_SFP, "LiquiditySweep")`.
    - Path 3 (rubber-band, `m_rubberBand`): `AdoptSignal(raw, MODE_SFP, "LiquiditySweep")` → `AdoptSignal(raw, MODE_RUBBER_BAND, "Rubber Band")` (spaced spelling).
  - `Include/Validation/CSetupEvaluator.mqh` (Factor-4 engine-native block, after the `SFP` branch ~234): added two else-if branches — `"FailedBreak"`/`"Failed Break"` → +2; `"Rubber Band"` → +1. Both <=2 (engine guardrail). Placed before `"Silver Bullet"`; no listed token is a substring of these comments, so cascade order is preserved.
- **Diff summary:** The rubber-band death-cross short no longer shares MODE_SFP/'LiquiditySweep' with the genuine liquidity sweep — it now has its OWN mode (MODE_RUBBER_BAND) + comment token ('Rubber Band'), and S6 has its own (MODE_FAILED_BREAK / 'FailedBreak'). Per-mode auto-disable and the orchestrator GATE can now attribute the +0.130R rubber-band short, the 0%-WR SFP, and S6 to three separate buckets. The new Factor-4 branches stop 'FailedBreak'/'Rubber Band' from being starved at 0 points.
- **Logic-cut check:** NO trading logic deleted. All three cascade paths still present and fire; only mode/token labels changed + two additive Factor-4 branches + two appended enum values. The A/B entry-set diff shows **0 trades dropped** in either year (only additions) — confirming no cut.

## Mode / token / Factor-4 wiring (for the GATE / Phase 3.8)
- **Three distinct buckets confirmed:** Path1 S6 = `MODE_FAILED_BREAK`/'FailedBreak'; Path2 genuine sweep = `MODE_SFP`/'LiquiditySweep'; Path3 rubber-band = `MODE_RUBBER_BAND`/'Rubber Band'. Each `AdoptSignal` prepends its token to `signal.comment` (CReversalSweepEngine:216) and stamps `signal.engine_mode = mode` (:201).
- **Orchestrator Rubber-Band A/A+ gate (CSignalOrchestrator:738–739)** does `StringFind(signal.comment, "Rubber Band") >= 0`. Before this fix the engine's rubber-band path adopted with token 'LiquiditySweep' → the comment did NOT contain "Rubber Band" → the engine's rubber-band signals were INVISIBLE to that A/A+ gate. The spaced 'Rubber Band' token now wires the engine path to that gate (matters when the engine turns ON at the GATE; on production the engine is OFF so it is moot there).
- **Factor-4 branches:** `"FailedBreak"`/`"Failed Break"` → +2 (structural failed-break reversal, matches False Breakout / OB Retest siblings); `"Rubber Band"` → +1 (thin A+-gated death-cross momentum short, matches SFP / London Close Rev). Both reachable on the LEGACY production path via the standalone plugins (see A/B). The Fix-2.1 WARN list (CSetupEvaluator:259–260) already named these tokens; they now have real branches so the WARN no longer fires for them.

## Enum-ordinal-safety check (CRITICAL — adding enum values)
- **Appended at the END.** `MODE_NONE=0 … MODE_PANIC_MOMENTUM=11` are NOT renumbered; new `MODE_RUBBER_BAND=12`, `MODE_FAILED_BREAK=13`.
- **Persisted/serialized state is SAFE:** `PersistedModePerformance.mode_id` (`int`, "ENUM_ENGINE_MODE as int", Structs.mqh:443) is the only serialized carrier of the enum. Only `CExpansionEngine`/`CLiquidityEngine`/`CSessionEngine` write/read it; write = `(int)m_mode_perf[i].mode`, read matches by **value** (`if((int)m_mode_perf[j].mode == in[i].mode_id)`), and none of those three engines track MODE_RUBBER_BAND/MODE_FAILED_BREAK — their tracked modes keep their old ordinals. `CReversalSweepEngine` has NO mode-perf array / no persistence, so the two new ordinals are never serialized by it.
- **Per-mode auto-disable** (RecordModeResult/IsModeDisabled in the three engines) indexes by **value comparison** (`m_mode_perf[i].mode == mode`), NOT by ordinal array index → unaffected by appended values.
- **No `switch` on ENUM_ENGINE_MODE** in the repo (grep confirmed); CTradeLogger:1522 uses `EnumToString((ENUM_ENGINE_MODE)mode_id)` — display-only, value-mapped. No hardcoded int relies on the old ordinals.
- **Conclusion:** appending is ordinal-safe; the A≠B divergence below is NOT an ordinal/serialization artifact — it is the Factor-4 scoring change on the legacy plugins (proven by trade-set diff).

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 8962 ms elapsed, cpu='X64 Regular'
```
- Errors: 0 | New warnings vs 38-baseline: 0 (all 38 are pre-existing; none in Enums.mqh / CReversalSweepEngine.mqh / CSetupEvaluator.mqh).
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — 896864 bytes, md5 `201819630344f229811eaaf84fcbf056` (B). A-baseline (committed HEAD) backed up to `/tmp/UT_phase3_2_baseline.ex5`, md5 `445ae15b4488c9f167e05306779a3f11`.

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, production `.set` = `MQL5/Profiles/Tester/UltimateTrader.set` (decoded: `InpEnableMultiStrategy=false`, all 4 `InpEnableEngine*=false`, **`InpEnableS3S6=true`**, **`InpEnableCrashDetector=true`**, `InpSignalSource=2` BOTH, `InpSymbolProfile=0` gold, `InpEnablePullbackCont=true`). Windows 2024 + 2023.
- **Mode:** Model=1 (1-min OHLC), Deposit 10000 USD, Leverage 100. Terminal `C:\Program Files\Vantage Markets MT5 Terminal\terminal64.exe` (NO `/portable`), data dir `nullkuhl`/`725B72F25E46C780EF59F57016D58156`; CSVs → `MetaQuotes/Terminal/Common/Files/`.
- **A (baseline = backed-up committed-HEAD `.ex5`):** reproduces the committed anchors EXACTLY — 2024 Stats `04d116972696f905c6ea5fa6701a7155` (TradeEvents `825489b6…` / Candidates `bcc44924…` / Risk `8babda0f…`); 2023 Stats `b11e07675cf4d577e27663ffde0aca19` (TradeEvents `7ef40d5f…` / Candidates `2fba7df1…` / Risk `b21a2999…`).
- **B (this phase, fix build, `.ex5` `201819630344…`):** 2024 Stats `e6373f08ec3876cb03d3e428df97e830`; 2023 Stats `fe324637ccd7d900f7c270dedcfd352b` (all 8 CSVs differ from A).
- **BYTE-IDENTITY RESULT: A != B (NOT byte-identical) — all 8 CSVs differ both years.** This is NOT the structural no-op of Phase 3.1.
- **Decoded headline:**
  - **2024:** A 140t / net $1190.84 / totR 11.76 / avgR 0.0840 / WR 46.4% → B **144t / net $1155.77 / totR 11.44 / avgR 0.0794 / WR 46.5%** (Δ +4t, **−$35.07 / −0.32R**).
  - **2023:** A 129t / net $714.29 / totR 3.40 / avgR 0.0264 / WR 35.7% → B **131t / net $632.59 / totR 2.20 / avgR 0.0168 / WR 35.9%** (Δ +2t, **−$81.70 / −1.20R**).
- **Mechanism (proven by entry-set diff, NOT estimated):** the divergence is the deliberate **Factor-4 starvation fix** working — but reaching the production path through the **LEGACY standalone plugins**, not the (OFF) CReversalSweepEngine:
  - `CFailedBreakReversal` (g_failedBreakRev) is registered standalone via `RegisterEntryPlugin(g_failedBreakRev, register_patterns)` (UltimateTrader.mq5:604, gated only on `InpEnableS3S6=true`) and emits `"S6: Failed Break Long | …"`.
  - `CCrashBreakoutEntry` (g_crashEntry) is registered standalone (mq5:638, gated on `InpEnableCrashDetector && g_profileEnableCrashBreakout && register_patterns`) and emits `"Rubber Band Short (Death Cross)"`.
  - Both are LEGACY signals (`routed_engine=false`) → scored by the **legacy CSetupEvaluator Factor-4** path (CSignalOrchestrator:723). Before this fix their comments matched NO Factor-4 branch → +0 → more often SETUP_NONE/lower tier. After: `"Failed Break"`→+2, `"Rubber Band"`→+1 → promoted into tradeable tiers.
  - **Entry-set diff confirms it precisely:** 2024 +4 NEW trades, ALL `"S6: Failed Break Long"` (2024.05.13 A, 06.13 A, 06.26 A, 08.27 B+); 2023 +2 NEW — one `"S6: Failed Break Long"` (2023.04.06 A) + one `"Rubber Band Short (Death Cross)"` (2023.10.19 A). **Zero trades dropped either year** (no cut). Common-entry Quality/RiskPct shifts (2024: 2 Q / 22 risk; 2023: 13 Q / 26 risk) are the secondary sequential ripple (the inserted trades' outcomes move the consecutive-loss/risk-scaler state, re-sizing later trades) — expected sequence-dependency, not a scoring bug.
- **No-op / byte-identical claim:** **NO — A != B.** The task's "expect BYTE-IDENTICAL on the production config" premise (engine OFF) did NOT account for the legacy standalone S6/Crash plugins emitting the same tokens through the legacy Factor-4 evaluator. The change is small (2024 −$35/−0.32R, 2023 −$82/−1.20R) and 100% mechanism-attributed to the deliberate starvation fix admitting previously-sub-tradeable S6/Rubber-Band setups (which net slightly losing in these two specific years).
- **Iter-3 note:** the engine-side mode/token relabeling (Path 1/2/3) is itself unreachable on production (engine OFF) — only the Factor-4 branches are reachable here (via legacy plugins). The 3-bucket attribution earns its value when the engine turns ON at the GATE / Phase 3.8.

## stok review + verdict
- **Numbers handed to stok:** the A != B decoded headline above (2024 +4t −$35.07/−0.32R; 2023 +2t −$81.70/−1.20R), the entry-set diff (all new trades = S6 'Failed Break' + one 'Rubber Band'; 0 dropped), and the mechanism (legacy standalone S6/Crash plugins un-starved by the new Factor-4 branches via the legacy evaluator).
- **stok assessment:** REVIEWED at SOURCE (independent stok agent — verified the registration + scoring architecture, NOT the write-up).
  - Confirmed the two new branches (`CSetupEvaluator.mqh:243` `"FailedBreak"`/`"Failed Break"`→+2; `:245` `"Rubber Band"`→+1) live in the **legacy comment-token overload** `EvaluateSetupQuality(daily,h4,regime,macro_score,pattern,isBearRegime,signal)` (`:67`). That overload is invoked at `CSignalOrchestrator.mqh:723` **only on the `else` (non-`is_engine`) branch** — i.e. ONLY for legacy/pattern/file signals. Routed-engine signals take `quality = signal.setupQuality` at `:721` and NEVER touch this overload.
  - The live S6 plugin (`CFailedBreakReversal`, registered `mq5:604` on `InpEnableS3S6`) emits `"S6: Failed Break Long | …"` (`CFailedBreakReversal.mqh:183`) and the live Crash plugin (`CCrashBreakoutEntry`, registered `mq5:638` on `InpEnableCrashDetector && g_profileEnableCrashBreakout`) emits `"Rubber Band Short (Death Cross)"` (`CCrashBreakoutEntry.mqh:250`). Both are `routed_engine=false` → legacy overload → the new branches match their comment strings (`"Failed Break"` ⊂ "S6: Failed Break Long"; `"Rubber Band"` ⊂ "Rubber Band Short (Death Cross)"). The A/B mechanism is **proven correct at source.**
  - **DECISIVE ARCHITECTURAL FACT:** the new branches, AS PLACED, are reachable by definition ONLY on the legacy production path. They contribute **nothing** to the routed-engine/GATE path (which reads `signal.setupQuality`, already set by the engine's own `CConfluenceScorer`, not by this overload). So this is NOT "a fix that also happens to touch production" — on the production `.set` it is the ENTIRE behavior of the change; the engine-attribution value of fix 3.4 lives ENTIRELY in the 3-mode split + tokens (already byte-identical/inert on production, engine OFF), NOT in these two Factor-4 branches.
  - This makes the situation a near-exact repeat of **Phase 2.1**: the literal plan wording ("so neither is starved at 0") reads as engine-scoring hygiene, but implemented literally it re-scores LIVE legacy-registered plugins on the production path — the same class of by-side-effect production change 2.1 caught. 2.1's binding ruling was scope-to-router / keep production byte-identical until engine-on validation. The same logic applies here, and MORE strongly: in 2.1 the side-effect *cut* live trades (silently zeroing a never-run scorer); here it *promotes* a known marginal-LOSER (S6, audit PF 0.78) into tradeable tiers on its comment token alone, BEFORE S6's own Iter-6/6.9 kill-criterion run.
  - **Trading-edge read of the delta:** the net negative is not noise — it is mechanism-consistent and directionally exactly what the edge predicts. The +trades are dominated by **S6 'Failed Break Long'** (6 of the 6 across both years; only 1 'Rubber Band'). S6-long is the repo's KNOWN marginal/loser (PF 0.78, exhaustion-filter already tightened in the 6 committed fixes); Rubber-Band-short is the ONE profitable short (+0.130R). Promoting the loser far more often than the winner produces a net drag — which is precisely the 2024 −$35 / 2023 −$82 we see. Un-starving S6 here, on its comment token, is promoting negative-expectancy trades AHEAD of the validation gate built to judge them. That is the wrong order.
- **stok verdict: OPTION (b) — SCOPE the Factor-4 branches OFF the legacy production path; keep production BYTE-IDENTICAL now. BINDING.** (Mirrors the Phase-2.1 Option-2 precedent.) Rationale in priority order: (1) **No-logic-cut's twin — no-silent-production-shift — is the governing principle**, and the plan's own structure DEFERS legacy-plugin comment/edge re-validation (incl. PBC/S6 kill-criteria) to Iter-6/6.9; promoting S6 on its token NOW front-runs that gate. (2) The branches' INTENDED purpose (per fix 3.4 / the 3-mode split) is to feed the engine/GATE path so the +0.130R rubber-band short and the 0%-WR SFP and S6 are each their own bucket — and that purpose is served entirely by the engine path, which does NOT route through this legacy overload. So scoping the branches to NOT fire for the legacy S6/Crash comment strings costs the GATE NOTHING. (3) Promoting a known marginal-loser (S6, PF 0.78) before its own kill-criterion run is premature and inverts the plan's validation order. (4) Identical-acceptance to 2.1: production `.set` must return to byte-identical (md5-match committed-HEAD both years); the engine-attribution value is preserved for Iter-3/GATE where it belongs.
- **Open question for stok (binding decision needed):** the plan fix 3.4 mandates "Add CSetupEvaluator Factor-4 branches for 'FailedBreak' and 'Rubber Band' so neither is starved at 0." Implemented literally, those branches also re-score the LIVE legacy standalone S6 (CFailedBreakReversal) and Crash/Rubber-Band (CCrashBreakoutEntry) plugins on the production path (both ON: InpEnableS3S6 / InpEnableCrashDetector), admitting +4 (2024) / +2 (2023) previously-starved setups (slightly net-negative in these years). stok to decide: (a) ACCEPT; (b) SCOPE to engine path / keep production byte-identical; (c) other.

## ===== BINDING stok DECISION + SCOPING PREDICATE FOR mt5 (Option b) =====

**DECISION: OPTION (b) — SCOPE the two new Factor-4 branches OFF the legacy production path. Keep production BYTE-IDENTICAL now. BINDING. (Mirrors the Phase-2.1 Option-2 precedent.)**

### The core fact mt5 must work with
The two new branches live in the **legacy comment-token overload** `EvaluateSetupQuality(daily,h4,regime,macro_score,pattern,isBearRegime,signal)` (`CSetupEvaluator.mqh:67`), invoked at `CSignalOrchestrator.mqh:723` ONLY for non-`is_engine` (legacy/pattern/file) signals. Routed-engine signals take `quality=signal.setupQuality` (`:721`) and never reach this overload. Therefore: as currently placed, the branches fire ONLY for legacy signals (which is the problem), and they do NOTHING for the engine/GATE path. The 3-mode split + tokens (Paths 1/2/3) already deliver the clean per-mode attribution the GATE needs; these two legacy-overload branches are NOT how the engine path gets scored. The fix's engine-attribution goal does not require them to fire on production at all.

### The required predicate (pick ONE; option A is the recommendation)

**Option A — REMOVE the two new branches from the legacy overload (cleanest; fully restores byte-identity).**
Delete the two `else if` branches at `CSetupEvaluator.mqh:243-246`:
```
else if(StringFind(pattern, "FailedBreak") >= 0 || StringFind(pattern, "Failed Break") >= 0)
   points += 2;
else if(StringFind(pattern, "Rubber Band") >= 0)
   points += 1;
```
Rationale this is correct and sufficient: the engine path (the ONLY path that benefits from un-starving 'FailedBreak'/'Rubber Band') does NOT route through this overload — it reads `signal.setupQuality` from the engine's own `CConfluenceScorer`. So removing these branches costs the GATE/engine path NOTHING. The legacy S6/Crash plugins return to their PRE-fix-3.4 scoring (they matched no Factor-4 branch before = +0 = the pre-fix-3.4 production behavior, which is the committed baseline). The legacy S6/Crash comment-token (re-)scoring is then handled where the plan already puts it: the audit's comment-token fixes / Iter-6/6.9, WITH per-plugin validation + kill-criteria (S6 carries its kill-criterion there).
- KEEP the `MODE_RUBBER_BAND`/`MODE_FAILED_BREAK` enum values, the Path-1/2/3 token relabel, AND the existing Fix-2.1 WARN list (`:268-272`) — the WARN naming these tokens is the correct breadcrumb that an ENGINE-emitted 'FailedBreak'/'Rubber Band' (spaceless engine token form, engine ON) is unscored; it does NOT change scoring (pure LogPrint). When engines turn ON at the GATE, the engine path scores via `CConfluenceScorer`, not this overload, so the WARN firing is informational, not a starvation.

**Option B — GUARD the branches so they fire ONLY for the engine token form, never for the legacy plugin comment strings (use if mt5 wants the branches present for any future engine-emitted legacy-path edge case).**
The engine emits the SPACELESS adopted token `"FailedBreak"` (Path 1, `AdoptSignal(raw, MODE_FAILED_BREAK, "FailedBreak")`) and `"Rubber Band"` (Path 3). The LIVE legacy plugins emit DISTINCT longer strings: S6 = `"S6: Failed Break Long | …"` (note the SPACE: "Failed Break", and the "S6:" prefix); Crash = `"Rubber Band Short (Death Cross)"`. To scope OFF the legacy plugins while leaving the engine-token-form scorable:
```
else if(StringFind(pattern, "FailedBreak") >= 0 && StringFind(pattern, "S6:") < 0)
   points += 2;   // engine spaceless 'FailedBreak' only; exclude legacy "S6: Failed Break ..."
else if(StringFind(pattern, "Rubber Band") >= 0 && StringFind(pattern, "Death Cross") < 0)
   points += 1;   // engine 'Rubber Band' only; exclude legacy "Rubber Band Short (Death Cross)"
```
Note Option B MUST drop the spaced `"Failed Break"` OR-clause from the `FailedBreak` branch — the spaced form is exactly what the legacy S6 plugin emits, so keeping it would re-admit S6. Option B is more fragile (string-coupling to plugin comment internals) and gains nothing on production vs Option A, because no engine signal reaches this overload on the production `.set` (engines OFF) — so under Option B the branches are dead-but-present on production. **Option A is preferred** (simpler, no string-coupling, identical production result). mt5's choice between A and B, but A is the recommendation.

### Redo acceptance gate (BINDING — same standard as 2.1)
On the production `.set` (`InpEnableMultiStrategy=false`, all 4 `InpEnableEngine*=false`, `InpEnableS3S6=true`, `InpEnableCrashDetector=true`): **A == B trade-for-trade, SL-for-SL, R-for-R for BOTH 2024 AND 2023**, and PROVE it — the Stats/TradeEvents/Risk/Candidates CSV md5s must match committed-HEAD: 2024 Stats `04d116972696f905c6ea5fa6701a7155` (TradeEvents `825489b6…`, Candidates `bcc44924…`, Risk `8babda0f…`); 2023 Stats `b11e07675cf4d577e27663ffde0aca19` (TradeEvents `7ef40d5f…`, Candidates `2fba7df1…`, Risk `b21a2999…`). The +4 (2024) / +2 (2023) S6/Rubber-Band trades MUST disappear (return to the committed baseline). ZERO divergence — not even sub-0.25R exit ripple — because with the branches scoped off, the legacy overload's executed code path on this config is literally unchanged. If ANY divergence remains, the predicate is wrong — reconcile before re-QA. The enum-ordinal-safety PASS and the Path-1/2/3 token relabel are UNAFFECTED (they are inert on production, engine OFF) and STAY.

### Iter-3 / GATE coherence (one line)
When engines turn ON at the GATE (Phase 3.8), the rubber-band death-cross short, the genuine SFP, and S6 each carry their own `engine_mode` (MODE_RUBBER_BAND / MODE_SFP / MODE_FAILED_BREAK) and their own adopted token, and are scored by the engine's `CConfluenceScorer` on the 2.4-GATE-re-derived thresholds — clean per-mode attribution + per-mode auto-disable, exactly as fix 3.4 intends. The legacy S6/Crash comment-token edge re-validation happens in Iter-6/6.9 with per-plugin kill-criteria. Both objectives are preserved; only the premature on-production promotion is removed.

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** "Must land before any engine A/B (clean attribution)"; the implicit no-op expectation was byte-identical on production (engine OFF). Touches-logic=Y, so the broader Iter-3 / RESUME(d) rule applies: a `touches_logic` change with A≠B must be explainable by the fix's mechanism and stok must sign off the behavioral shift.
- **Result:** Mechanism fully explained (deliberate starvation fix on legacy S6/Crash plugins; 0 trades cut; small mechanism-bounded negative). NOT byte-identical → loop-back decision belongs to stok. Enum-ordinal-safety: PASS (appended at end, value-keyed serialization, no switch).

## mt5-developer sign-off
- **(Option-b redo — FINAL):** Compile clean (`Result: 0 errors, 38 warnings, 7801 ms`, 0 new warnings); the three modes + token relabel remain correctly wired for the GATE (enum appended at end / ordinal-safe; Path-1/2/3 relabel intact); the two legacy Factor-4 branches DELETED per stok Option-b so production is byte-identical; NO logic cut (deleting an additive branch only restores prior behavior; GATE attribution preserved). **A==B byte-identical on all 8 CSVs both 2024+2023 (md5 MATCH + cmp clean, reproduces committed anchors `04d11697…`/`b11e0767…`), and the +4 (2024) / +2 (2023) promoted S6/Rubber-Band trades are confirmed GONE (counts back to A: 2024 'Failed Break' 3, 'Rubber Band' 0; 2023 'Failed Break' 1, 'Rubber Band' 18; Δtrades=+0 both years).** — SIGNED.
- (Original-impl history:) the original 3.2 added the two Factor-4 branches per the literal plan; A!=B on production (the live legacy S6/Crash un-starving); I did NOT assert byte-identity and looped to stok. stok ruled Option-b → branches removed.

## stok sign-off
- **SIGN-OFF (independent re-sign-off on the Option-b byte-identical redo — verified at SOURCE, not the write-up). SIGNED.**

### Q1 — binding Option-(b) acceptance MET (production byte-identical both years + promoted S6/RB trades gone)
- **Re-verified at source, stronger than md5-matching:** the working-tree diff of `Include/Validation/CSetupEvaluator.mqh` vs committed-HEAD is **COMMENT-ONLY** — a single inert `//` rationale block inserted between the `SFP` branch and `Silver Bullet` (`git diff HEAD` shows `+9` lines, all `//`; ZERO executable change). I confirmed committed-HEAD itself had **NO** `FailedBreak`/`Rubber Band` scoring branch in the Factor-4 region — the only HEAD mentions of those tokens are the Fix-2.1 WARN list (HEAD:259-260, pure LogPrint). So the legacy S6 (`"S6: Failed Break Long | …"`) and Crash (`"Rubber Band Short (Death Cross)"`) comments score **+0** in the legacy overload, exactly the committed baseline.
- **Therefore production byte-identity is STRUCTURAL by construction**, not a fitted re-run: the executed legacy scoring path is textually identical to HEAD. The promoted +4(2024)/+2(2023) trades cannot reappear because the branch that promoted them does not exist → they revert to the committed baseline by construction ('Failed Break' 7→3 / 2→1, 'Rubber Band' 19→18, Δtrades=+0). mt5's A==B md5+cmp on all 8 CSVs both years (reproducing anchors 2024 `04d11697…` / 2023 `b11e0767…`) is consistent with this and ACCEPTED at the level I require.
- **Decisive architectural fact re-verified:** `CSignalOrchestrator` does `if(is_engine) quality = signal.setupQuality; else quality = m_evaluator.EvaluateSetupQuality(...)` — the legacy comment-token overload is invoked ONLY on the non-`is_engine` else-branch; routed engines take their own `signal.setupQuality` and never reach it. A Factor-4 branch there feeds NOTHING to the engine/GATE path → deleting the two branches costs the GATE nothing. Confirmed, not asserted.

### Q2 — GATE work retained + inert on production; legacy un-starving correctly deferred
- **Three distinct buckets RETAINED + ordinal-safe (verified at source):** `Enums.mqh` `MODE_RUBBER_BAND`(12)+`MODE_FAILED_BREAK`(13) appended after `MODE_PANIC_MOMENTUM`(11) (existing 0..11 unchanged); `CReversalSweepEngine` Path-1 S6→`MODE_FAILED_BREAK`/'FailedBreak' (:369), Path-2 sweep→`MODE_SFP`/'LiquiditySweep' (:378), Path-3 rubber-band→`MODE_RUBBER_BAND`/'Rubber Band' (:392).
- **Correctly serves per-mode attribution when the engine turns ON:** these labels fire only when the router (`InpEnableMultiStrategy`) is ON — OFF on production, so inert there. When ON at the GATE/Phase 3.8, routed signals are scored by the engine's own `CConfluenceScorer` (not this overload) and carry their own `engine_mode` + adopted token → S6 / genuine SFP / rubber-band each get their own per-mode auto-disable bucket, and the spaced 'Rubber Band' token wires the engine rubber-band path to the orchestrator `StringFind(signal.comment,"Rubber Band")` A/A+ gate. Clean per-mode attribution, exactly as fix 3.4 intends. CONFIRMED.
- **Fix-2.1 WARN retained correctly** as a pure-LogPrint diagnostic breadcrumb (off the trade-decision path); naming the tokens does not change scoring.
- **Legacy S6/Crash comment-token un-starving correctly DEFERRED to Iter-6/6.9** with their own per-plugin kill-criteria. Promoting marginal-loser S6 (audit PF 0.78) on its comment token ahead of its own kill-gate would invert the plan's validation order; deferral is the right order and is recorded in the source comment + this log.

### Iter-6 deferral note (for downstream)
The legacy standalone-plugin comment-token (re-)scoring — `CFailedBreakReversal` ('S6: Failed Break Long …', mq5:604/`InpEnableS3S6`) and `CCrashBreakoutEntry` ('Rubber Band Short (Death Cross)', mq5:638/`InpEnableCrashDetector`) — is NOT re-scored here. It is deferred to **Iter-6/6.9**, where S6 carries its own kill-criterion (PF 0.78 marginal loser) and the Rubber-Band short carries its own. Any future un-starving of those legacy tokens on the production path must run THROUGH that gate with per-plugin validation + A/B, not via a Factor-4 branch in the legacy overload (which would front-run the gate). The engine-side 3-mode/token attribution (this phase) and the legacy-plugin edge re-validation (Iter-6/6.9) are independent objectives; both are preserved.

## Decision
- **QA-PASS → COMMITTED.** Option-b redo: compiles clean (0-err/0-new-warn), production byte-identical both 2024+2023 (A==B md5+cmp all 8 CSVs; structural — legacy scoring diff is comment-only; promoted S6/RB trades gone by construction), GATE attribution (enum modes 12/13 + Path-1/2/3 token relabel) retained + ordinal-safe + inert on production, legacy un-starving deferred to Iter-6/6.9. BOTH QA agents signed (mt5-developer + INDEPENDENT stok re-sign-off). Advance NEXT ACTION → Phase 3.3. Main session runs git.

## Commit
- **Hash:** (not committed — stok re-sign-off PENDING)
- **Message:** (pending)
