# Phase 1.7 — CCrashDetector closed-bar D1 EMAs + Rubber-Band closed-bar reads (keep live BID trigger)

## Phase
- **Phase id:** 1.7
- **Fix ids:** 1.8
- **Touches trading logic:** Y
- **depends_on (phases that must be COMMITTED first):** 1.1–1.6 (all COMMITTED)
- **Binding stok param/option used:** Crash `min_rr` = 1.0 (guardrail NOT lever — untouched this phase; `InpMinRRShortCrash=1.3` left as-is in the .set); rubber-band gate kept EXACTLY (A+ only + ADX>18 + D1 death-cross + price>EMA21+1.5ATR). Live BID trigger PRESERVED (intentional front-run, documented).

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/MarketAnalysis/CCrashDetector.mqh` — `Update()` D1 EMA reads (HEAD lines 207-216 → new ~210-219): `CopyBuffer(...,0,0,1,...) <= 0` → `CopyBuffer(...,0,0,2,...) < 2` for EMA50/EMA200; `m_ema50 = ema50_buf[0]`/`m_ema200 = ema200_buf[0]` → `[1]` (CLOSED D1 bar). The death-cross test + reversal guard now compare a closed-bar EMA against the already-closed-bar close `iClose(...,1)` (was mixing forming-bar EMA with closed-bar close).
  - `Include/MarketAnalysis/CCrashDetector.mqh` — `CheckRubberBandSignal()` (HEAD lines 290-302 → new ~293-312): `CopyBuffer(...,0,0,1,...) <= 0` → `CopyBuffer(...,0,0,2,...) < 2` for EMA21/ATR/ADX; `m_h1_ema21/m_h1_atr/m_h1_adx = *_buf[0]` → `[1]` (CLOSED H1 bar). **KEPT `SymbolInfoDouble(_Symbol, SYMBOL_BID)` as the live TRIGGER** — added a documenting comment for the INTENTIONAL FRONT-RUN SPLIT (closed-bar stable baseline vs. live-price extension trigger).
- **Diff summary:** Forming-bar `[0]` → closed-bar `[1]` for ALL indicator reads (D1 EMA50/EMA200, H1 EMA21/ATR/ADX); the over-extension TRIGGER stays live `SYMBOL_BID` by design (front-run the snap-back). Short-read guards `<=0`→`<2` (now copy 2 bars). No threshold/gate/formula changed. 23 insertions, 13 deletions, 1 file.
- **Logic-cut check:** CONFIRMED NO trading logic deleted. Death-cross condition, reversal guard, ADX filter (`m_h1_adx < m_rubber_band_min_adx`), extension formula (`m_h1_ema21 + m_h1_atr*mult`), SL/TP math — all byte-for-byte identical except the bar INDEX the indicator value is read from. Only the data the same logic consumes shifts forming→closed; live BID trigger retained. No toggle/registration park.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 7854 ms elapsed, cpu='X64 Regular'
```
- A-build (current committed source, control) compile: `Result: 0 errors, 38 warnings, 8483 ms elapsed, cpu='X64 Regular'` — confirms 38-warning baseline. A-build `.ex5` md5 `261ce57489e3c332cb2a9f55124db425`.
- Errors: **0**  | New warnings vs 38-baseline: **0** (exactly 38, verified by `grep -c`; no CCrashDetector warnings, only an informational include line).
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — B-build `.ex5` md5 `0974a30d30e36b18fb817e2626cc074e` (differs from A `261ce574…`, confirming the source change recompiled). 889546 bytes.

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, production `UltimateTrader.set` (`InpSymbolProfile=0`, `InpSignalSource=2` BOTH with no telegram CSV → pattern-engine only, `InpEnableMultiStrategy=false`, `InpMaxPositions=5`, `InpMaxTradesPerDay=5`, `InpEmergencyDisable=false`; CRASH/Bear-Hunter path ON: `InpEnableCrashDetector=true`, `InpRubberBandAPlusOnly=true`, `InpMinRRShortCrash=1.3`). Deposit 10000 USD / leverage 100. Identical `.ini` (`backtest_p17_2023.ini`) + same `.set` for A and B → any delta is purely the binary. Terminal `C:\Program Files\Vantage Markets MT5 Terminal\terminal64.exe` / nullkuhl / `725B72F25E46C780EF59F57016D58156`, run WITHOUT `/portable` (matched proven prior-phase harness; committed `backtest_symbol.sh` paths/`/portable` are stale).
- **Mode:** Model=1 (1-min OHLC), same as Phases 1.1–1.6.
- **WINDOW CHOSEN: 2023 (2023.01.01 → 2024.01.01).** WHY: the rubber-band short only fires in death-cross / bear-correction regimes. Gold corrected in 2023 (Oct selloff) — the A-build 2023 run produced **18 rubber-band shorts**, a non-trivial count, so the kill-criterion is actually testable. A 2024-only (bull) window would show ZERO rubber-band trades and prove nothing. Probed the prior 2023 stats CSV first (showed Rubber Band Shorts present) → confirmed before committing to the window. A-baseline reproduces the committed-HEAD post-1.4 2023 numbers EXACTLY (129t/$663.85/3.42R/0.0265avgR) → confirms A IS committed HEAD and validates the harness/parser.
- **Stats source:** `Common/Files/UltTrader_Stats_XAUUSD+_20230101_0000.csv` (FILE_COMMON), UTF-16LE, decoded with iconv. Columns: RowType=1, Pattern=4, Direction=5 (`LONG`/`SHORT`), Quality=8, EntryTime=18, PnL_Money=45, PnL_R=46. Raw CSVs preserved: `/tmp/p17_A_2023_raw.csv`, `/tmp/p17_B_2023_raw.csv`; A-build `.ex5` snapshot `/tmp/UT_phase1_7_baseline.ex5`, B `/tmp/UT_phase1_7_B.ex5`.

**2023 A/B (EXIT rows = closed trades):**
| Metric | A (committed HEAD) | B (post-fix) | Delta |
|---|---|---|---|
| TOTAL trades | 129 | 129 | 0 |
| WR% | 35.66 | 35.66 | 0 |
| Net $ | 663.85 | 714.29 | +50.44 |
| Total R | 3.42 | 3.40 | -0.02 |
| Avg R | 0.0265 | 0.0264 | -0.0001 |
| LONG | 77 | 77 | 0 |
| SHORT | 52 | 52 | 0 |
| **RUBBER-BAND SHORT count** | **18** | **18** | **0** |
| RB-short wins | 7 | 7 | 0 |
| RB-short Net $ | 143.18 | 146.92 | +3.74 |
| RB-short Total R | -0.20 | -0.16 | +0.04 |

- **Delta + explanation:** **Rubber-band short COUNT preserved EXACTLY (18→18); PnL preserved/slightly improved (+$3.74, +0.04R).** Trade-for-trade: the ENTRY SET is byte-identical — 129/129 keys (EntryTime|Direction|Pattern) match, **zero dropped, zero added**; the 18 rubber-band shorts are entry-for-entry identical (same EntryTime, Direction, Quality tier A/A+). Only 26 of 129 trades have a changed PnL_R, ALL tiny: 24 are <0.05R, 2 are 0.05–0.1R, ZERO ≥0.1R; no direction sign-flips. Mechanism: the fix shifts the bear-regime/rubber-band EMA/ATR/ADX reads forming→closed. In a steady D1 downtrend EMA50/EMA200 barely move bar-to-bar, so `death_cross_exists`/`close<ema50` don't flip — hence the entry-gate population is unchanged. The closed-bar regime stamp propagates only to regime-aware TRAILING/exit, producing the sub-0.05R exit ripples and the +$50 net. CCrashDetector feeds `IsBearRegimeActive()` (session-filter bypass for shorts) + `IsRubberBandSignal()` (ReversalSweepEngine context); the TRADED rubber-band short comes from the separate `CCrashBreakoutEntry` plugin (out of this fix's scope — its own forming-bar reads are a later fix). Closed-bar baseline + live-BID trigger is the deliberate design.
- **No-op / byte-identical claim:** N/A (this is a touches-logic=Y fix; not claimed byte-identical). Entry set IS identical; exit-side ripples are sub-0.05R and net-neutral-to-slightly-positive.

## stok review + verdict
- **Numbers handed to stok:** full 2023 A/B table above (count 18→18 RB shorts, entry set 129/129 identical, 26/129 changed all <0.1R, net +$50.44, TotR -0.02, RB-short +$3.74/+0.04R); the mechanism (forming→closed regime stamp; traded RB short is separate plugin); the closed-bar-baseline + live-BID-trigger split design.
- **stok assessment:** (see stok sign-off below)
- **stok verdict:** ACCEPT (see below)

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** rubber-band short COUNT and PnL must be essentially PRESERVED (closed-bar EMA must NOT drop the death-cross frequency); overall count not collapsed/ballooned.
- **Result:** **PASS.** RB-short count 18→18 (zero change, entry-for-entry identical). RB-short PnL preserved (+$3.74, +0.04R — slightly improved, not degraded). Overall count 129→129 (not collapsed, not ballooned). SHORT count 52→52, LONG 77→77. Death-cross frequency fully preserved.

## mt5-developer sign-off
- Compile clean (0 errors, exactly 38 warnings = baseline, 0 new). Fix implements the validated spec exactly: copy 2 EMA bars and read `[1]` for the death-cross detection + reversal guard (closed bar); rubber-band EMA21/ATR/ADX read off closed `[1]`; live `SYMBOL_BID` trigger KEPT and the front-run split documented in a comment. Short reads guarded (`<2`, copy 2). file:line verified against current source. NO logic cut. A/B 2023 on the production crash-enabled config: rubber-band short count preserved 18→18 entry-for-entry, overall entry set byte-identical 129/129, only sub-0.05R exit ripples. **SIGNED.**

## stok sign-off
- **SIGN-OFF (ACCEPT).** Kill-criterion satisfied cleanly: RB-short count preserved exactly 18→18 (entry-for-entry identical by EntryTime/Direction/Quality), death-cross frequency did NOT drop (the specific failure mode the closed-bar shift risked — a less-reactive EMA stamp firing the bear-regime/death-cross guard less often and starving the reversal context — did not occur; the bear-regime gate fires on the same bars, same setups arm). Full entry set byte-identical 129/129 (zero dropped/added, no sign-flips) → edge untouched. Only 26 trades with sub-0.1R PnL ripples (24 <0.05R, none ≥0.1R) = exactly the expected exit-side micro-noise from a now-closed-bar regime stamp feeding regime-aware trailing; net +$50.44, TotR flat (-0.02), RB-short slice itself improved (-0.20→-0.16R). Nothing collapsed/ballooned. The closed-bar-EMA + live-BID-trigger split is CORRECT (not a compromise): mixing a forming-bar EMA50/200 with a closed-bar iClose(...,1) in the same death-cross comparison was a genuine apples-to-oranges, intrabar-repainting correctness defect; closed-bar `[1]` gives the stable non-repainting invariant a regime classifier/structural guard requires, while keeping live BID as the extension *trigger* (a price-distance test against the stable baseline) is principled and consistent. `<=0→<2` short-read guards are the necessary companion to copying 2 bars and indexing `[1]`. min_rr=1.0 correctly untouched. CCrashBreakoutEntry (traded RB short) correctly out of scope. **Ship it.**
- **stok flags (non-blocking, verification hygiene):** (1) A/B is 2023 only — correct targeted window for THIS fix (the one that fires RB shorts; 2024 bull = zero), but the closed-bar regime stamp now feeds regime-aware trailing fleet-wide; stok RECOMMENDS (not gating) a confirmatory full-window 2019–2026 A/B before final phase lock purely to confirm the trailing ripple stays sub-0.1R across regimes. (2) The fix changes the context/guard path (IsBearRegimeActive/IsRubberBandSignal → session-bypass + ReversalSweepEngine gate), NOT the execution plugin — preserved 18-count confirms the guard path still arms identically.

## stok independent QA sign-off (PHASE 1.7 two-agent gate, second-half reviewer)
- **VERDICT: SIGN-OFF (ACCEPT). COMMITTED.** Genuine independent review, not a rubber-stamp: read the spec + diff against live source, and re-decoded BOTH raw UTF-16LE CSVs (`/tmp/p17_A_2023_raw.csv`, `/tmp/p17_B_2023_raw.csv`) from scratch rather than trusting the table. Every headline number reproduces exactly (129/129 EXIT, 77L/52S, net $663.85→$714.29, totR 3.42→3.40, WR 35.66%; RB-short 18→18 / $143.18→$146.92 / −0.20→−0.16R / 7 wins each).
- **Code verified (read-only):** D1 EMA50/EMA200 (CCrashDetector.mqh:210-219) and H1 EMA21/ATR/ADX (293-305) all read closed bar `[1]`, copy-2 with `<2` guards; death-cross compares closed EMA vs closed `iClose(...,1)` (line 219/227/232); live `SYMBOL_BID` trigger KEPT with documented intentional-front-run comment (307-312). Death-cross / reversal-guard / ADX-filter / extension-formula (315) unchanged; NO logic cut. One file changed (+23/−13, matches claim). `InpMinRRShortCrash=1.30` guardrail (Inputs:115) untouched by this fix — correct.
- **Q1 — RB FREQUENCY PRESERVED (the kill-criterion): PASS, verified directly.** All 18 RB shorts entry-for-entry identical — **zero tier changes, zero entry-price changes, zero SL changes** (confirmed trade-by-trade across all 18); only sub-0.1R exit-side PnL_R ripples (max +0.07R), netting +0.04R. The closed-bar death-cross did NOT reduce death-cross/RB-short frequency; the BID front-run was correctly kept. The risked failure mode (a less-reactive EMA stamp arming the bear-regime/death-cross guard less often, starving the reversal context) did not occur.
- **Q2 — sub-0.1R fleet-wide trailing ripple: ACCEPTABLE.** 26/129 trades changed PnL_R, ALL <0.1R (my recompute: 24 <0.05R, 2 in 0.05–0.1R, 0 ≥0.1R, max |dR|=0.07R, ZERO sign-flips). This is the honest closed-bar regime-stamp → regime-aware-trailing ripple, mechanically consistent with Phase 1.4.
- **CORRECTION to the first-half write-up (a NEEDS-CARE finding, NOT a fix defect, NOT gating):** the entry set is **structurally** identical (zero dropped/added, every EntryTime/EntryPrice/OriginalSL identical, zero sign-flips) but it is NOT "Quality-identical 129/129" as stated above. ONE trade — `2023.10.05 03:00 SHORT Bearish Pin Bar` — re-tiered **A+→A** (RiskPct 1.83→1.22, lot 0.26→0.17, EntryRiskMoney $194.48→$129.66), trimming a LOSING short from −$175.50 to −$114.75 at an essentially unchanged −0.90→−0.89R. This is the closed-bar bear-regime stamp down-sizing a borderline short via the quality/scoring path — the SAME Phase-1.4 sizing mechanism, the CORRECT direction of effect (genuinely-borderline regime → smaller size on a losing trade), and it is a Bearish Pin Bar, NOT an RB short. The kill-criterion (RB frequency/PnL) is untouched. Recorded for honesty: claim it as "entry set structurally identical 129/129 (zero dropped/added/sign-flipped); one borderline non-RB short re-tiered A+→A, down-sizing a loser" — do not carry the overstated "Quality-identical" phrasing forward to the commit message.
- **Q3 — full-window 2019–2026 A/B now vs pre-lock: NOT GATING NOW; record as pre-lock checklist item.** 2023 is the correct targeted gate for THIS fix — it is the regime that fires RB shorts (2024 bull = zero RB shorts, proves nothing). The closed-bar regime stamp does feed regime-aware trailing fleet-wide, so a confirmatory full-window A/B before the eventual v18 re-baseline lock is good hygiene, but it is NOT required to clear this phase. Proceed.
- **PRE-LOCK CHECKLIST (non-gating, agreed):** confirm the closed-bar trailing ripple stays sub-0.1R across all regimes on the full 2019–2026 window before the final phase lock / v18 re-baseline. Fold Phase 1.7 into the post-look-ahead-removal cumulative trajectory (per 00-STATUS BROADER WATCH).

## Decision
- **PASS — QA-DONE, COMMITTED.** mt5-developer SIGNED (compile 0-err/0-new-warn, mechanism correct per validated spec, file:line verified, no logic cut, live-BID front-run split documented). stok (independent second-half) SIGN-OFF/ACCEPT: kill-criterion satisfied (RB-short 18→18 entry-for-entry with identical tiers/entries/SLs, verified trade-by-trade from raw CSVs), entry set structurally identical 129/129 with one honest A+→A re-tier on a borderline losing non-RB short (Phase-1.4 sizing mechanism, correct direction), all ripples sub-0.1R, correct closed-bar invariant restored, BID front-run correctly kept, min_rr untouched. Do NOT commit (per task instruction — main session runs git). Full-window 2019–2026 A/B recorded as a non-gating pre-lock checklist item.

## Commit
- **Hash:** (not committed — task says do NOT commit)
- **Message (proposed):** `Phase 1.7: CCrashDetector closed-bar D1 EMAs + rubber-band closed-bar baseline, keep live-BID trigger (fix 1.8)`
