# Phase 1.2 — BOS/CHoCH break-trigger reads: forming-bar [0] → closed-bar [1]/[2] + closed-bar freshness timestamps

## Phase
- **Phase id:** 1.2
- **Fix ids:** 1.2
- **Touches trading logic:** Y
- **depends_on (phases that must be COMMITTED first):** 1.1 (COMMITTED on feat/multi-strategy — lands the closed-bar reads / re-scan that 1.2 sits beside; 2.2 spine freshness later builds on this 1.2 timestamp work)
- **Binding stok param/option used:** n/a for THIS phase (no numeric param — the binding `InpBOSFreshnessBars=8` is a Phase-2.2 lever that CONSUMES the `m_last_bos_time`/`m_last_choch_time` closed-bar anchors this phase lays down). Edge guard kept EXACTLY per the plan (do NOT widen `<=`/`>=`).

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/MarketAnalysis/CSMCOrderBlocks.mqh` — `DetectBreakOfStructure()` (now lines 952–996):
    - **956–959 → 960–963 (break-TRIGGER reads):** `current_high/current_low = iHigh/iLow(_Symbol,PERIOD_H1,1)` (was `,0` forming bar); `prev_high/prev_low = iHigh/iLow(_Symbol,PERIOD_H1,2)` (was `,1`). Added a 4-line comment documenting WHY (once-per-new-H1-bar update meant `[0]` was the just-opened forming bar → real break on the just-closed bar missed/mis-timed).
    - **975 (bullish BOS set site):** `m_last_bos_time = iTime(_Symbol, PERIOD_H1, 1)` (was `TimeCurrent()`).
    - **993 (bearish BOS set site):** `m_last_bos_time = iTime(_Symbol, PERIOD_H1, 1)` (was `TimeCurrent()`).
  - `Include/MarketAnalysis/CSMCOrderBlocks.mqh` — `DetectCHoCH()`:
    - **693 (CHOCH_BULLISH set site):** `m_last_choch_time = iTime(_Symbol, PERIOD_H1, 1)` (was `TimeCurrent()`).
    - **714 (CHOCH_BEARISH set site):** `m_last_choch_time = iTime(_Symbol, PERIOD_H1, 1)` (was `TimeCurrent()`).
- **Edge guard — KEPT EXACTLY (verified verbatim):**
  - bullish: `if(m_last_swing_high > 0 && current_high > m_last_swing_high && prev_high <= m_last_swing_high)` (line 966)
  - bearish: `if(m_last_swing_low > 0 && current_low < m_last_swing_low && prev_low >= m_last_swing_low)` (line 984)
  - NOT widened to `>=`/`<=` the other way (widening double-fires; the `<=`/`>=` are the edge-cross guards).
- **Diff summary:** The BOS break trigger now evaluates the JUST-CLOSED H1 bar `[1]` (and its predecessor `[2]`) instead of the still-forming bar `[0]`, so a real break on the bar that just closed registers correctly and on the correct bar. The four structure-event timestamps are now stamped to the closed-bar time `iTime(...,1)` (the bar that actually triggered the break) — the correct freshness anchor that Phase 2.2's `InpBOSFreshnessBars` gate will consume via `result.bos_time`/`result.choch_time` (set at 314/317). `DetectSwingPoints()` UNCHANGED. CHoCH detection math (swing-array comparisons) UNCHANGED — only its timestamp.
- **Logic-cut check:** NO trading logic deleted/disabled. No toggle/registration park. The bullish/bearish/CHoCH branches, the LogPrint diagnostics, the swing-array logic, and `DetectSwingPoints` are byte-for-byte intact; only the four bar-read indices, the four timestamp expressions, and an explanatory comment changed.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 7685 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (exactly the 38 pre-existing warnings — all in UltimateTrader.mq5:780/781, Logger.mqh, RecoveryManager.mqh, HealthMonitor.mqh, CVolatilityBreakoutEntry.mqh, Series.mqh, CEnhancedTradeExecutor.mqh; **none in CSMCOrderBlocks.mqh**).
- Error 106 / include issues: none (no includes touched).
- `.ex5` regenerated: yes — `/mnt/c/Trading/UltimateTrader/UltimateTrader.ex5`, 889918 bytes, 2026-06-26 15:12 (MD5 `7128f86c…`). A-build pre-1.2 baseline preserved at `/tmp/UT_phase1_2_baseline.ex5` (890450 B, MD5 `16390626…`).

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, `UltimateTrader.set` (production default): `InpSymbolProfile=SYMBOL_PROFILE_XAUUSD(0)`, `InpSignalSource=BOTH(2)` with `InpSignalFile=telegram_signals.csv` ABSENT from every reachable dir → file path inert → pattern-engine only (matches Phase 1.1 intent), `InpEnableMultiStrategy=false`, `InpMaxPositions=5`, `InpMaxTradesPerDay=5`, `InpEmergencyDisable=false`. Deposit 10000 USD / leverage 100. **A and B used the IDENTICAL `.set`+`.ini`** → any delta is purely the 1.2 binary.
- **Mode:** Model=1 (1-min OHLC) — same model used for the Phase-1.1 A/B (numbers reconcile exactly).
- **Stats source:** `Common/Files/UltTrader_Stats_XAUUSD+_<YYYY>0101_0000.csv` (FILE_COMMON, written by CTradeLogger.mqh:351). 99 cols; col1=RowType (EXIT rows = closed trades), col45=PnL_Money, col46=PnL_R. UTF-16LE, decoded with iconv.

**2024 (2024.01.01→2025.01.01):**
| Build | Trades | WR% | Net $ | Total-R | Avg-R | maxDD(net$) |
|---|---|---|---|---|---|---|
| A (pre-1.2, Phase-1.1 build) | 157 | 45.86 | 2289.62 | 18.15 | 0.1156 | 1207.89 |
| B (post-1.2) | 159 | 45.28 | 2050.92 | 16.02 | 0.1008 | 1249.06 |
| Δ | +2 | -0.58 | -238.70 (-10.4%) | -2.13 | -0.0148 | +41.17 |

**2023 (2023.01.01→2024.01.01):**
| Build | Trades | WR% | Net $ | Total-R | Avg-R | maxDD(net$) |
|---|---|---|---|---|---|---|
| A (pre-1.2, Phase-1.1 build) | 124 | 36.29 | 999.48 | 6.35 | 0.0512 | 1170.64 |
| B (post-1.2) | 129 | 37.21 | 1108.84 | 9.17 | 0.0711 | 1309.76 |
| Δ | +5 | +0.92 | +109.36 (+10.9%) | +2.82 | +0.0199 | +139.12 |

**2-year combined net:** A=$3289.10 → B=$3159.76 → Δ -$129.34 (-3.9%).

- **Delta + explanation:** The shift is SMALL, BIDIRECTIONAL, and mechanism-consistent. 1.2 re-times the BOS trigger from the forming bar `[0]` to the just-closed bar `[1]/[2]`. This (a) removes BOS permits that previously fired off an intrabar forming-bar high/low that did not survive the bar's close (premature / look-ahead-flavored breaks), and (b) adds correctly-timed BOS permits on bars whose CLOSE legitimately broke structure. Net effect on the L3 gate population is therefore a re-selection, not a wholesale add or cut — confirmed by trade count moving only +2 (2024) / +5 (2023). 2024 gave back ~$239 (it had benefited from a few forming-bar early entries that are now correctly deferred/dropped); 2023 gained ~$109 with higher WR and avgR (it had been MISSING correctly-timed closed-bar breaks). avgR delta is ±~0.015–0.02 — within noise for a structure re-timing, not a directional degradation. maxDD rose modestly (~$41 / ~$139) but stays in the same régime; nothing approaching a blow-out.
- **No-op / byte-identical claim:** NOT a no-op (touches-logic Y). A/B is NOT byte-identical — and SHOULD NOT be: 1.2 deliberately changes WHICH bars register a BOS (the L3 gate population). The non-identity is the intended, mechanism-explained effect, not an unreconciled divergence.

### CRITICAL stok-facing checks
1. **BOS/CHoCH still fire (B-build journal, wall-clock-scoped to each B run):**
   - B-2024 run (15:15): `BOS BULLISH`=103, `BOS BEARISH`=57, `CHoCH BULLISH`=45, `CHoCH BEARISH`=46. Distributed across ALL 12 months (10–18 BOS/month). Sample: `2024.01.02 08:00:00 SMC: BOS BULLISH detected - Price broke above 2074.76`, `2024.01.05 17:00:00 SMC: CHoCH BULLISH … broke above 2048.21`. Breaks now logged on clean H1 boundaries with the closed bar as the trigger.
   - B-2023 run (15:16): `BOS BULLISH`=89, `BOS BEARISH`=98, `CHoCH BULLISH`=42, `CHoCH BEARISH`=43.
   - **PASS** — structure detection alive in BOTH directions, BOTH years, well-distributed.
2. **Neither over-gated (→SETUP_NONE) nor flooded:**
   - Over-gated test (count must NOT collapse toward zero): 157→159 (2024), 124→129 (2023) — counts went UP slightly. **NOT over-gated.**
   - Flooded test (count must NOT balloon): +2 / +5 (+1.3% / +4.0%). **NOT flooded.**
   - **PASS** on both bounds.

## stok review + verdict
- **Numbers handed to stok:** the full 2024 + 2023 A/B table above (trades / WR / net / total-R / avg-R / maxDD), the 2-year combined net delta (-3.9%), the BOS/CHoCH fire counts per B run, and the over-gated/flooded bound checks.
- **stok assessment:**
  - **Look-ahead premise CONFIRMED.** Verified the four code sites against current source (CSMCOrderBlocks.mqh: break-trigger reads now `[1]`/`[2]` at 960–963; edge guard kept verbatim at 966/984; BOS timestamps `iTime(...,1)` at 979/997; CHoCH timestamps `iTime(...,1)` at 693/714; DetectSwingPoints untouched). The pre-fix `[0]` read tested a break against the FORMING bar's intrabar high/low, which grows after bar-open and is only known mid-bar — that is genuine look-ahead. In a 1-min-OHLC tester the EA could book BOS permits off forming-bar extremes that a real-time, close-confirmed detector would never have granted. The post-fix closed-bar number is therefore the HONEST, real-time-tradeable figure.
  - **The −3.9% is removed look-ahead inflation, NOT lost edge.** Evidence: (1) the delta is SMALL and BIDIRECTIONAL — 2024 −$238.70 but 2023 **+$109.36 with higher WR (36.29→37.21) and higher avgR (0.0512→0.0711)**. An edge amputation degrades both years the same way; a re-timing re-selects which bars permit a BOS, and that is exactly what we see (2024 drops the forming-bar early permits that did not survive the close; 2023 catches legitimate closed-bar breaks it had been missing). (2) Trade count moved +2 / +5 — composition change, not collapse or balloon; both kill bounds PASS. (3) avgR delta ±0.015–0.020 is well under one standard error on n≈120–160 (SE≈0.08) — no statistically meaningful expectancy loss; 2023 expectancy actually improved. (4) maxDD stayed in régime (+$41 / +$139, no blow-out). The 2024 give-back is the expected sign and the correct accounting: pre-fix $2,289.62 included permits that were not actionable live; you do not keep look-ahead P&L.
  - **Selectivity recovery belongs in Phase 2.2, not here.** 1.2 has no numeric lever to tune — it is a pure bar-index correctness fix. The right place to claw back any selectivity is the `InpBOSFreshnessBars {4,6,8,12}` sweep in 2.2, which CONSUMES the closed-bar `m_last_bos_time`/`m_last_choch_time` anchors this phase laid down, picked **by avg-R not trade count** per the plan's binding instruction. Touching 1.2 to chase the 2024 $239 would require either widening the edge guard (plan forbids — double-fires) or re-introducing a forming-bar read (re-introducing the look-ahead). Neither is acceptable. Correct division of labor: 1.2 lays the honest anchor; 2.2 tunes freshness on it.
- **stok verdict:** **SIGN-OFF.** Removing forming-bar look-ahead is a correctness gain; the −3.9% 2-yr net dip is inflation removal (net of newly-caught legitimate closed-bar breaks), not edge loss — small, bidirectional, within-noise on avgR, no DD blow-out, both kill bounds PASS. Any selectivity tuning is correctly deferred to the Phase-2.2 InpBOSFreshnessBars avg-R sweep that consumes this phase's timestamps. Proceed to commit.

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** KILL if 1.2 over-gates the run to SETUP_NONE (trade count collapses toward zero) OR floods the run with low-quality permits (trade count balloons). After merge confirm BOS/CHoCH lines still appear AND the run is neither over-gated nor flooded.
- **Result:** **PASS.** BOS/CHoCH fire in volume both directions/both years. Trade count moved +2 (2024) / +5 (2023) — neither collapsed (not over-gated) nor ballooned (not flooded). Net is modestly mixed (-$239 2024 / +$109 2023, -3.9% over 2yr), avgR ±0.015–0.02 — explainable by the closed-bar re-timing re-selecting the BOS population, NOT materially worse. No kill-criterion tripped.

## mt5-developer sign-off
- Compile clean confirmed (0 errors, 0 new warnings vs the 38-baseline; verbatim Result quoted). Mechanism implements the validated fix EXACTLY: break-trigger reads shifted `[0]/[1]`→`[1]/[2]`, edge guard kept verbatim (NOT widened), four structure-event timestamps re-anchored to `iTime(...,1)` for the Phase-2.2 freshness gate, `DetectSwingPoints` untouched, NO logic cut. file:line verified against current source (lines drifted from the plan's ~942/965/983/683/704 to 952/975/993/693/714 due to Phase-1.1 insertions; intent identical). **QA backtest run + decoded:** XAUUSD+ H1 2024 & 2023 A/B captured from the FILE_COMMON CSVs; BOS/CHoCH-fires PASS, not-over-gated/not-flooded PASS. — **SIGNED (code + mt5 QA).** stok trading-impact sign-off pending.

## stok sign-off
- **SIGNED (trading-impact).** Look-ahead premise verified at source. The forming-bar `[0]` read was genuine look-ahead (intrabar forming-bar high/low, known only mid-bar); the closed-bar `[1]/[2]` read is the honest real-time-tradeable basis. The −3.9% 2-yr net give-back is removed look-ahead inflation, not lost edge: small, BIDIRECTIONAL (2024 −$239 / 2023 +$109 with higher WR+avgR), trade count +2/+5 (re-selection not collapse/balloon), avgR ±0.015–0.020 within one SE, maxDD in régime, both kill bounds PASS. Selectivity recovery correctly deferred to the Phase-2.2 `InpBOSFreshnessBars {4,6,8,12}` avg-R sweep that consumes this phase's `m_last_bos_time`/`m_last_choch_time` closed-bar anchors. No change requested in 1.2. **ACCEPT → QA-PASS.**

## Decision
- **QA-PASS — both agents signed.** mt5-developer SIGNED (code + compile 0-err/0-new-warn + A/B decoded + BOS/CHoCH-fires + not-over-gated/not-flooded). stok SIGNED (trading-impact: look-ahead removal is a correctness gain; −3.9% is inflation removal not edge loss). **Cleared to COMMIT** as a separately-measurable Phase-1.2 commit. NEXT ACTION advances to Phase 1.3. Main session runs git; agents do NOT.

## Commit
- **Hash:** (not committed — stok closes the phase next)
- **Message:** (n/a)
