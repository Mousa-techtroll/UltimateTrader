# Phase 2.8 — CConfluenceScorer.Configure non-monotone-threshold WARNING

## Phase
- **Phase id:** 2.8
- **Fix ids:** 2.8
- **Touches trading logic:** N (diagnostic print only — no clamp, no reorder, no math change)
- **depends_on:** none for the print itself; the B-tier reachability DECISION is owned by 2.4-GATE (already COMMITTED — verdict was to NOT ship production InpPoints*, engines stay OFF)
- **Binding stok param/option used:** n/a (no-op diagnostic)

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/Validation/CConfluenceScorer.mqh` — `Configure(...)` (current source ~line 92). After the existing assignments to `m_points_aplus/a/bplus/b`, added a diagnostic block that `PrintFormat`s a WARNING when the thresholds are NOT monotonically non-increasing (`!(aplus>=a && a>=bplus && bplus>=b)`).
- **Diff summary:** WARNING print ONLY. No clamp, no reorder, no assignment change. The tier cascade in `Score()` (`>=aplus`→A+, `>=a`→A, `>=bplus`→B+, `>=b`→B, lines 259–262) is untouched. The guard explains that a non-monotone ordering (e.g. v18 default 8/7/6/7, where B=7>B+=6) makes the lower tier UNREACHABLE, and explicitly states no clamp/reorder is applied and the B-reachability decision belongs to 2.4-GATE.
- **Logic-cut check:** NO trading logic added, removed, or altered. A `PrintFormat` has no effect on scoring or tier assignment; in production (engines OFF) `Configure` is not even invoked through the engine scorer path, and where it is invoked the print does not change `out_score` or the returned tier.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 10207 ms elapsed, cpu='X64 Regular'
```
- Errors: 0 | New warnings vs 38-baseline: 0
- Error 106 / include issues: none
- `.ex5` regenerated: yes (this is the COMBINED 2.6+2.7+2.8 build used for the byte-identity B run)

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1 2024, production defaults via `[TesterInputs]` (InpEnableMultiStrategy=false, gold, BOTH). Model=1 (1-min OHLC). Vantage Markets / nullkuhl / terminal 725B72F25E46C780EF59F57016D58156; no /portable.
- **Mode:** 1-min OHLC (Model=1)
- **A (committed HEAD 35094d1, NO fixes):** `.ex5` md5 c3d19789d412d4f00fdc966aa592ccc4; 287 Stats rows; 1530 TradeEvents EXIT rows.
- **B (all three fixes 2.6+2.7+2.8 applied):** `.ex5` md5 e0c047b6bb5d5dcc8c35cfc295314e43 (binary differs — comment/string/layout only; executed production path identical).
- **A==B byte-identity result:** PASS — see verdict below.
- **No-op / byte-identical claim:** YES. This is the SHARED acceptance run for 2.6 + 2.7 + 2.8 (all three touches_logic=N). Both runs compiled `0 errors, 38 warnings`.

### Byte-identity verdict — A == B (PASS)
Protocol: A = `git stash` the 3 fix files → recompile committed HEAD → run → snapshot CSVs. B = `git stash pop` (all 3 fixes) → recompile → run → compare. Identical 395-input `[TesterInputs]` ini both runs.

| File | A md5 | B md5 | result |
|---|---|---|---|
| UltTrader_Stats          | 33317ded33bfe47d19b23ba1d4d80922 | 33317ded33bfe47d19b23ba1d4d80922 | md5 MATCH + cmp IDENTICAL |
| UltTrader_TradeEvents    | 91bb76efe1d87f9ec9a9244421f32659 | 91bb76efe1d87f9ec9a9244421f32659 | md5 MATCH + cmp IDENTICAL |
| UltTrader_Risk           | 3cfac633fe6980dc38bb780491283ba0 | 3cfac633fe6980dc38bb780491283ba0 | md5 MATCH + cmp IDENTICAL |
| UltTrader_Candidates     | 9cc430708215df0040783c055a13dcc3 | 9cc430708215df0040783c055a13dcc3 | md5 MATCH + cmp IDENTICAL |
| backtest_results         | c1642a72edec698f1cf92c61c2295490 | c1642a72edec698f1cf92c61c2295490 | md5 MATCH + cmp IDENTICAL |

**OVERALL A==B = PASS** (all 5 raw UTF-16LE CSVs md5-match + cmp clean → trade-for-trade, SL-for-SL, R-for-R identical). NO bisection needed (all three fixes applied together are byte-identical to HEAD). The byte-identity confirms the no-op claims for ALL THREE phases (2.6 comment-only, 2.7 dead-code removal, 2.8 WARN-only) on the production path (InpEnableMultiStrategy=false, XAUUSD+ H1 2024, Model=1).

## stok review + verdict
- PENDING (per task instruction).

## Kill-criteria check
- **Phase kill-criterion:** byte-identical backtest (FIX-SOUND config-diagnostic; print cannot move executed path).
- **Result:** PASS — A==B byte-identical (all 5 CSVs md5+cmp). Also covers 2.6 and 2.7 (shared run).

## mt5-developer sign-off
- Compile clean (0 err / 0 new warn); change is a WARNING print only (no clamp/reorder/math); file:line verified; no logic cut. — SIGNED

## stok sign-off
- **SIGN-OFF (2026-06-27, INDEPENDENT stok no-op gate — verified at SOURCE).** Read CConfluenceScorer:110–124: the added block is a pure `PrintFormat` inside the `if(!(m_points_aplus>=m_points_a && m_points_a>=m_points_bplus && m_points_bplus>=m_points_b))` branch — no clamp, no reorder, no assignment to the threshold members. The `Score()` tier cascade (259–262) is untouched. A diagnostic print cannot move `out_score` or the returned tier; in production (engines OFF) the engine-scorer `Configure` path is not even invoked, and where invoked the print is behaviorally inert. The B-tier reachability decision is correctly deferred to the (already COMMITTED) 2.4-GATE, not introduced here. Byte-identical on production config (this build IS the combined B; A==B). SIGNED.

## Decision
- **PASS → COMMITTED** — WARN-only print, byte-identical (A==B all 5 CSVs md5+cmp). BOTH agents signed (mt5-developer QA + INDEPENDENT stok). This shared run also confirms the no-op claims for 2.6 and 2.7.

## Commit
- **Hash:** (not committed — task instruction: do NOT commit)
- **Message:** n/a
