# Phase 6.7 + 6.8 — Risk-band doc honesty (6.7) + file-signal doc corrections (6.8)

## Phase
- **Phase id:** 6.7 + 6.8 (bundled — both DOC-ONLY; 6.7 adds one code comment)
- **Fix ids:** 6.7 (realized A+ risk hits 2.0% cap, not documented 1.5%) + 6.8 (docs/06 file-signal halt-bypass + orchestrator-registration corrections)
- **Touches trading logic:** N (6.7 = doc + one comment, byte-identical; 6.8 = doc-only)
- **depends_on:** 6.5 COMMITTED (HEAD ddd77a7); 6.8 sequenced AFTER 4.5 (the `&& g_riskMonitor.CanTrade()` file-signal gate landed in 4.5/4.4 — verified live)
- **Binding stok param/option used:** n/a (no-op; do NOT lower any multiplier — edge earned at capped sizing)

## What changed

### FIX 6.7 — effective A+ risk band (verified from CURRENT source)
The plan cited `1.5 × 1.15 × 1.25 × 1.15 ≈ 2.48% pre-cap → capped to 2.0%`. Verified against source: the third multiplier is **1.08 (A+ trend boost), not 1.15** — so the realized clean-A+-TRENDING stack is **1.5 × 1.15 × 1.25 × 1.08 = 2.33% pre-cap → clamped to 2.0%**. Either arithmetic exceeds the 2.0% cap; the load-bearing fact (realized A+ risk = 2.0% cap, NOT the 1.5% tier base) holds. Source quotes:

- **Base 1.5%** — `UltimateTrader_Inputs.mqh:45` `input double InpRiskAPlusSetup = 1.5;`
- **Pattern ×1.15** — `CTradeOrchestrator.mqh:988-998` `GetRiskForQuality`: MA-cross patterns `multiplier = 1.15` (Pin/Engulf = 1.05).
- **Regime TRENDING ×1.25** — `UltimateTrader_Inputs.mqh:423` `input double InpRegimeRiskTrending = 1.25;` (applied at `UltimateTrader.mq5:1943-1954` via `g_regimeScaler.ApplyToRisk`).
- **A+ trend boost ×1.08** — `UltimateTrader.mq5:1962-1981` `InpEnableQualityTrendBoost`: A+ in TRENDING `qt_mult = 1.08` (comment there: "1.25 * 1.08 = 1.35 effective").
- **Hard cap 2.0%** — `UltimateTrader_Inputs.mqh:49` `input double InpMaxRiskPerTrade = 2.0;`; clamp at `CTradeOrchestrator.mqh:426-433` (`cap = ... InpMaxRiskPerTrade; if(signal.riskPercent > cap) signal.riskPercent = cap;`).
- Session scalers (London 0.5×/NY 0.9×) and EC v3 are REDUCERS that can pull realized risk back below the cap on a given trade; on a clean A+ TRENDING setup the cap binds.

**Code comment (the ONLY code change)** — `Include/Core/CTradeOrchestrator.mqh`, at the cap site (immediately above `double cap = ...`, line ~425). Documents the effective band `1.5×1.15×1.25×1.08=2.33→cap 2.0` and "do NOT lower multipliers; edge earned at capped sizing"; cross-refs the 5% portfolio ceiling (Fix 4.2). git diff: `1 file changed, 10 insertions(+), 1 deletion(-)` — the single `+`/`-` on `double cap` is the SAME line (gained a trailing `.` on the preceding comment); zero executable lines changed.

### FIX 6.7 — doc edits
- `STRATEGIES.md` — after the "Quality Tier Breakdown" table (~line 38): added a note that `Risk %` is the tier BASE, plus a new **"Effective Realized Risk Band (A+ — base vs. actual)"** section with the 5-row stage table (Base 1.50% → ×1.15 1.73% → ×1.25 2.16% → ×1.08 2.33% → cap 2.00%) and the do-not-lower-multipliers + 5% portfolio-ceiling cross-ref. Also added a "Risk-% note" under "Ranking 5 — By Quality Tier Performance" takeaway (~line 454) restating realized A+ = 2.0%, not 1.5%.
  - Before: tables stated A+ "Risk % = 1.5%" with no realized-vs-base distinction.
  - After: 1.5% labelled BASE; realized 2.0% cap-bound documented with the full stage table.
- `docs/03-Risk-Model.md` — after the 6-step Risk Pipeline table (~line 22): added **"Effective Realized Risk (Base vs. Cap)"** section with the same 5-row stage table + a blockquote distinguishing **per-trade cap (2.0% `InpMaxRiskPerTrade`)** vs **portfolio cap (5.0% `InpMaxTotalExposure`, Fix 4.2)**.
  - Before: pipeline listed "1 base A+ 1.5%" and "5 Hard cap 2.0%" as separate steps; no statement that realized A+ HITS 2.0%.
  - After: explicit "realized per-trade A+ risk is the 2.0% cap, not 1.5%" + the per-trade-vs-portfolio cross-ref (4.2 sequence).

### FIX 6.8 — doc-only corrections (verified against CURRENT source)
File-signal guard verified at `UltimateTrader.mq5:2190-2194`:
```
if((InpSignalSource == SIGNAL_SOURCE_BOTH || InpSignalSource == SIGNAL_SOURCE_FILE) &&
   g_fileEntry != NULL &&
   g_posCoordinator.GetPositionCount() < InpMaxPositions &&
   !g_riskMonitor.IsTradingHalted() &&     // daily LOSS halt + error halt
   g_riskMonitor.CanTrade())               // daily TRADE-COUNT limit (Phase 4.5, per the in-code comment 2184-2189)
```
- `CRiskMonitor::IsTradingHalted()` = `m_loss_halted || m_error_halted` (`CRiskMonitor.mqh:116`) — the daily LOSS halt.
- `CRiskMonitor::CanTrade()` (`CRiskMonitor.mqh:132`) rejects on `m_trades_today >= m_max_trades_per_day` (line 151, the daily TRADE-COUNT limit) AND on `daily_pnl <= -m_daily_loss_halt_pct` (line 173, the -3% loss line).
- File signals are created/Initialize()d directly in `OnInit` (`UltimateTrader.mq5:648-653`, comment: "File signals ALWAYS run independently (never through orchestrator)") — NOT registered with the orchestrator, NOT ranked, in ANY mode.

Edits to `docs/06-CSV-File-Signals.md`:
- **~line 183** (Section 6 "Every-Tick Processing"): replaced `**Bypassed:** Daily trade limit, daily loss halt. / **Applied:** Position limit` with the full guard snippet + an "Applied to file signals" list (position limit; daily LOSS halt -3% via IsTradingHalted + re-checked in CanTrade; daily TRADE-COUNT limit via CanTrade, added Phase 4.5).
  - Before: "Bypassed: Daily trade limit, daily loss halt." (WRONG on both).
  - After: both documented as ENFORCED, with the guard quoted.
- **~lines 325-326** ("Bypassed Limits" table): removed the two WRONG rows ("Daily trade limit (5/day) — Not checked", "Daily loss halt (-3%) — Not checked"); kept the genuinely-bypassed rows (R:R min, quality-tier risk, confirmation, regime). Added a new **"Enforced Limits (NOT bypassed)"** table (position limit / loss halt / trade-count) + a blockquote noting the post-4.5 flip.
  - Before: loss halt + trade-count listed as "Not checked for file signals".
  - After: both moved to an ENFORCED table; bypass list now only the genuinely-bypassed filters.
- **~lines 359-360** ("Section 10 — SIGNAL_SOURCE_FILE"): replaced "File signals go through the orchestrator (registered as entry plugin). Subject to orchestrator ranking -- can be outranked by pattern signals" with the corrected description (own independent every-tick path; NOT registered; NOT ranked; CFileEntry created/Initialize()d directly in OnInit). Also tightened the SIGNAL_SOURCE_BOTH paragraph (file path "cannot be outranked by pattern signals"; only the shared position limit + daily trade-count/loss-halt budget couple them) and added a correction blockquote.
  - Before: SIGNAL_SOURCE_FILE claimed orchestrator-registered + ranked (WRONG).
  - After: corrected to independent non-registered non-ranked path, matching `UltimateTrader.mq5:648`.

- **Logic-cut check:** NO trading logic deleted. 6.7 = one comment (byte-identical) + doc text; 6.8 = doc text only. No toggle/registration change.

## Compile result (verbatim)
```
Result: 0 errors, 16 warnings, 9502 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 16-baseline (post-6.5): **0** (all 16 are the known pre-existing set — UltimateTrader.mq5:795/796 double→int; Logger.mqh:114/235/243/251/534; CVolatilityBreakoutEntry.mqh:194/195/205/206 static-array; Series.mqh:178 MQL5_PROGRAM_TYPE deprecation; CEnhancedTradeExecutor.mqh:154/1553/2062/2153). Comment adds nothing.
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — `UltimateTrader.ex5` 927896 bytes, 10:24:06.

## QA backtest (decoded, ACTUAL — never estimated)
- **None run — and none required.** 6.7's sole code change is a comment; 6.8 touches only doc files. No backtest for a comment-only code change + doc files.
- **Byte-identity-by-construction:** comments are stripped by the MQL5 compiler before bytecode emission → the comment cannot alter the generated `.ex5` bytecode. git diff confirms zero executable lines changed (the single `double cap` line in the diff is unchanged — it appears only because the preceding comment line gained a trailing `.`). Same basis used for the 5.11 / 6.1 inline-comment fixes. A single clean compile (0err/16warn, .ex5 regenerated) is sufficient confirmation; an A/B backtest would be vacuous.
- Doc files (`STRATEGIES.md`, `docs/03`, `docs/06`) are not compiled and cannot affect the binary.

## Doc-claim ↔ source verification (each claim TRUE to current source)
| Doc claim (post-edit) | Source verified |
|---|---|
| Realized A+ risk = 2.0% cap, not 1.5% base | `InpRiskAPlusSetup=1.5` × pattern 1.15 (`GetRiskForQuality`) × regime 1.25 (`InpRegimeRiskTrending`) × A+ boost 1.08 (`InpEnableQualityTrendBoost`, mq5:1967) = 2.33 → cap 2.0 (`InpMaxRiskPerTrade=2.0`, clamp CTradeOrchestrator:426-433) |
| Per-trade 2.0% vs portfolio 5.0% | `InpMaxTotalExposure = 5.0` (Inputs:50, Fix 4.2 aggregate stop-distance/equity ceiling) vs `InpMaxRiskPerTrade = 2.0` per-trade |
| Daily LOSS halt IS enforced for file signals | guard `!IsTradingHalted()` (mq5:2193) + CanTrade re-check (CRiskMonitor:173) |
| File signals are NOT orchestrator-registered/ranked | mq5:648-653 "ALWAYS run independently (never through orchestrator)"; no RegisterEntryPlugin for g_fileEntry |
| **Daily TRADE-COUNT limit IS now checked (post-4.5 flip)** | guard `&& g_riskMonitor.CanTrade()` (mq5:2194); CanTrade rejects `m_trades_today >= m_max_trades_per_day` (CRiskMonitor:151); in-code comment mq5:2185-2189 attributes it to Phase 4.5. The plan's stale "KEEP 'Daily trade limit: Not checked'" note is OVERRIDDEN — the doc now states it IS checked. |

## stok review + verdict
- **SIGNED — ACCEPT-WITH-FOLLOWUP** (binding; risk-modeling-honesty / doc-accuracy review). stok verified EVERY claim against source (not the writeup):
  - **6.7 band CORRECT:** spot-checked all multipliers — base 1.5% (InpRiskAPlusSetup:45) × pattern 1.15 (GetRiskForQuality CTradeOrchestrator:998-999) × regime-TRENDING 1.25 (InpRegimeRiskTrending:423, applied mq5:1947) × A+ trend-boost 1.08 (mq5:1967) = 2.33% pre-cap → clamp to InpMaxRiskPerTrade=2.0 (CTradeOrchestrator:435-442). Arithmetic right. mt5 CORRECTLY documented the ACTUAL source band (1.08→2.33) not the plan's stale 1.15→2.48, and CORRECTLY documents the REALIZED 2.0% (not the aspirational 1.5%) = the right risk-honesty fix. The load-bearing fact is ROBUST: even stripping the 1.08, 1.5×1.15×1.25 = 2.156% STILL clamps to 2.0%.
  - **No multiplier lowered / zero executable lines:** git diff = 10 ins/1 del, the single -/+ pair is a comment gaining a trailing period; 9 inserts pure comment → no executable line touched → byte-identical-by-construction sound (no A/B needed; same basis as 5.11/6.1).
  - **6.8 all 3 corrections TRUE to current code:** (a) daily-loss halt enforced (`!IsTradingHalted()` mq5:2193, IsTradingHalted=m_loss||m_error CRiskMonitor:116, re-checked in CanTrade); (b) file signals NOT orchestrator-registered (mq5:648-654 `new`+Initialize, no RegisterEntryPlugin); (c) post-4.5 daily-trade-count limit now CHECKED (`&& CanTrade()` mq5:2194, rejects on m_trades_today>=max CRiskMonitor:151) — flipping the plan's stale "KEEP 'Not checked'" is the correct CURRENT-code call (the 2 wrong rows moved from Bypassed→Enforced with Phase-4.5 attribution).
  - **per-trade 2.0% vs portfolio 5.0% (4.2) cross-ref COHERENT** (two orthogonal mechanisms; the gold-correlation aggregation point — concurrent A+ longs bounded at 5%, not 5×2%).
  - **Verdict: ACCEPT-WITH-FOLLOWUP.**

## Follow-up — RESOLVED THIS PHASE (stok-flagged precision gap)
- stok flagged: the ×1.08 row is sourced to `InpEnableQualityTrendBoost` which is **default OFF** (Inputs:63 "$0 net across 4 years tested") → in the production config the ×1.08 doesn't apply (clean-A+ TRENDING stack = 1.5×1.15×1.25 = 2.16%, STILL clamps to 2.0%); the table's conclusion holds either way but could mislead a reader into thinking the boost is live. **FIXED THIS PHASE (doc-only):** annotated the ×1.08 row "**default OFF**" + added a note in BOTH band tables (STRATEGIES.md + docs/03-Risk-Model.md) that the realized A+ risk is 2.0% with or without the toggle. Makes the honesty-fix bulletproof regardless of config.

## Kill-criteria check
- **Phase kill-criterion:** (a) 0 errors / 0 new warnings; (c) byte-identical production backtest for 6.7/6.8 (here satisfied by byte-identity-by-construction, not a run).
- **Result:** PASS — 0 errors, 16 warnings (= post-6.5 baseline, 0 new). Binary byte-identical by construction (comment stripped at compile).

## mt5-developer sign-off
- Compile clean (0err/16warn, no new warnings, .ex5 regenerated); the one code change is a comment (git diff: zero executable lines changed); every doc claim verified against current source (effective-band multipliers + cap quoted from CTradeOrchestrator/Inputs/mq5; file-signal guard + CanTrade/IsTradingHalted internals quoted from mq5:2190-2194 + CRiskMonitor); post-4.5 daily-trade-limit flip confirmed in source. No logic cut. — **SIGNED (mt5-QA-DONE)**

## stok sign-off
- **SIGNED — ACCEPT-WITH-FOLLOWUP** (binding; every claim source-verified). The flagged ×1.08-default-OFF precision gap was FIXED this phase (both band tables annotated). See stok review above.

## Decision
- **DONE — both agents signed (mt5 implementation-correct + byte-identical-by-construction; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified every claim). Committed source-only on `feat/multi-strategy`.** 6.7 documents the TRUE realized A+ risk = 2.0% cap (not the aspirational 1.5%) — effective-band table in STRATEGIES.md + docs/03 + a code comment at the cap (zero executable lines); multipliers NOT lowered (edge earned at capped sizing). 6.8 corrects docs/06 to current code (daily-loss halt ENFORCED, file signals NOT orchestrator-registered, post-4.5 daily-trade-limit now CHECKED). stok-flagged ×1.08-default-OFF gap fixed in-phase (both tables annotated). Compile 0err/16warn; byte-identical by construction (comment-only). NEXT ACTION → Phase 6.9 (the FINAL phase).

## Commit
- **Hash:** `<filled at commit>` (CTradeOrchestrator.mqh comment + STRATEGIES.md + docs/03-Risk-Model.md + docs/06-CSV-File-Signals.md + 6-7_6-8.md + 00-STATUS.md; `.ex5` intentionally uncommitted). Branch `feat/multi-strategy`.
