# Phase 1.4 — Regime classifier closed-bar ADX/ATR + realized-count ATR average

## Phase
- **Phase id:** 1.4
- **Fix ids:** bundled 1.4 + 1.5 (both edit CRegimeClassifier.mqh Update — one coherent edit/commit)
- **Touches trading logic:** Y
- **depends_on (phases that must be COMMITTED first):** 1.1, 1.2, 1.3 (all COMMITTED)
- **Binding stok param/option used:** n/a — FIX-SOUND. 1.4 = debounced-classifier closed-bar correction; 1.5 = pure robustness (realized-count divide). No tunable param introduced.

## What changed
- **File touched (verified file:line against current source):**
  - `Include/MarketAnalysis/CRegimeClassifier.mqh` `Update()` lines 132-160 (one combined edit).
- **Fix 1.4 — forming-bar [0] -> CLOSED bar [1] for the current ADX/ATR/BB/close reads, and averages built over CLOSED bars:**
  - CopyBuffer counts bumped so index `[1]` is valid: ADX `3->4` (136), BB upper/lower `3->4` (138/139), `CopyClose(...)` `start_pos 0 count 1 -> count 2` (140).
  - Current reads now closed bar: `m_regime_data.adx_value = adx[1]` (147), `m_regime_data.atr_current = atr[1]` (148), `bb_width = bb_upper[1]-bb_lower[1]`, `/ close[1]` (159-160).
  - ATR-average window now spans CLOSED bars `atr[1..]` (no forming-bar `[0]` in the window).
- **Fix 1.5 — realized-count ATR average (no fixed-50 OOB / zero-pad):**
  - `int atr_got = CopyBuffer(m_handle_atr, 0, 0, 51, atr);` (135) — request 51 so `atr[1..50]` = 50 CLOSED bars.
  - Sum `atr[1 .. MathMin(50, atr_got-1)]`, divide by the realized count `atr_count` (152-156); fallback to `atr_current` if `atr_count<=0`. CMomentumFilter idiom (`Include/MarketAnalysis/CMomentumFilter.mqh:250-262`).
- **Short-read guards:** the multi-condition `if(... )` early-returns when ANY Copy* read returns `< 2` (adx, atr_got, bb_upper, bb_lower, close), so `[1]` always exists before use.
- **Diff summary:** 1 file, +19 / -14. Only the bar the regime primitives are read from (forming->closed) and the ATR-average denominator (fixed-50 -> realized count) changed.
- **Logic-cut check:** NO trading logic deleted. `ClassifyRegimeRaw()`, the bar-confirmation hysteresis (steps 1-3), the 2-bar volatility-expansion confirmation, and the thrash-cooldown circular buffer are all UNCHANGED. This is a closed-bar re-timing + averaging-denominator robustness fix, not a path removal.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 9005 ms elapsed, cpu='X64 Regular'
```
- Errors: **0** | New warnings vs 38-baseline: **0** (A-baseline build also = `0 errors, 38 warnings`; warning count byte-equal, no new warning lines). No CRegimeClassifier warnings (only an informational `including ...CRegimeClassifier.mqh` line).
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — `UltimateTrader.ex5` 889818 bytes, 2026-06-26 15:39. A-baseline (committed-HEAD source build) saved at `/tmp/UT_phase1_4_baseline.ex5` (890834 bytes); B saved at `/tmp/UT_phase1_4_B.ex5`. A/B md5 differ as expected.

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, production `UltimateTrader.set` (`InpSymbolProfile=0` XAUUSD, `InpSignalSource=2` BOTH with telegram CSV absent -> pattern-engine only, `InpEnableMultiStrategy=false`, `InpMaxPositions=5`, `InpMaxTradesPerDay=5`, `InpEmergencyDisable=false`). Deposit 10000 USD / leverage 100. A and B used the IDENTICAL `.set`+`.ini` -> any delta is purely the 1.4 binary. Terminal: Vantage Markets MT5 Terminal / nullkuhl / 725B72F25E46C780EF59F57016D58156.
- **Mode:** Model=1 (1-min OHLC), same as Phases 1.1/1.2/1.3. A-baseline reproduces the Phase-1.3-B / committed-HEAD numbers EXACTLY (2024 140t/$1764.07/16.58R; 2023 123t/$628.18/3.95R) — confirms the baseline build IS committed HEAD and confirms the parser.
- **Stats source:** `Common/Files/UltTrader_Stats_XAUUSD+_<YYYY>0101_0000.csv` (FILE_COMMON), UTF-16LE, decoded with iconv. col1=RowType (EXIT rows = closed trades), col5=Direction, col7=Regime, col24=RiskPct, col25=LotSize, col45=PnL_Money, col46=PnL_R. CSV mtimes verified fresh after each run.

**2024 (2024.01.01 -> 2025.01.01):**
| Build | Trades | WR% | Net $ | Total-R | Avg-R | LONG | SHORT |
|---|---|---|---|---|---|---|---|
| A (committed HEAD) | 140 | 46.43 | 1764.07 | 16.58 | 0.1184 | 109 | 31 |
| B (post-1.4) | 140 | 46.43 | 1190.84 | 11.76 | 0.0840 | 109 | 31 |
| Delta | 0 | 0.00 | -573.23 (-32.5%) | -4.82 | -0.0344 | 0 | 0 |

LONG detail — A: 109t/19.31R/$2180.25 -> B: 109t/14.79R/$1604.99. SHORT — A: 31t/-2.73R/-$416.18 -> B: 31t/-3.03R/-$414.15.

**2023 (2023.01.01 -> 2024.01.01):**
| Build | Trades | WR% | Net $ | Total-R | Avg-R | LONG | SHORT |
|---|---|---|---|---|---|---|---|
| A (committed HEAD) | 123 | 34.96 | 628.18 | 3.95 | 0.0321 | 75 | 48 |
| B (post-1.4) | 129 | 35.66 | 663.85 | 3.42 | 0.0265 | 77 | 52 |
| Delta | +6 | +0.70 | +35.67 (+5.7%) | -0.53 | -0.0056 | +2 | +4 |

LONG detail — A: 75t/-0.07R/$241.94 -> B: 77t/1.53R/$548.61. SHORT — A: 48t/4.02R/$386.24 -> B: 52t/1.89R/$115.24.

**2-year combined:** net A=$2392.25 -> B=$1854.69 (Delta -$537.56, -22.5%). total-R A=20.53 -> B=15.18 (Delta -5.35R). trades A=263 -> B=269 (+6).

## Regime-distribution / trade-count assessment
- **2024 entry set is BYTE-IDENTICAL (140 trades, identical 109L/31S split, identical WR 46.43%).** The closed-bar regime stamp did NOT change which bars trade in 2024; it changed the regime CLASSIFICATION at the confirmation/execution moment, which drives (a) the Dynamic Barbell `regime_risk_multiplier` (`UltimateTrader.mq5:1582-1591`: CHOPPY x0.6 / VOLATILE x0.7 / RANGING x0.75 / TRENDING+NORMAL x1.0, applied at `CTradeOrchestrator.mqh:842-843`) and (b) regime-aware exit/trail (`CRegimeAwareExit` reads `GetCurrentRegime()`).
- **Regime stamp shift at ENTRY (col7), 2024:** A = 127 TRENDING / 12 VOLATILE / 1 RANGING -> B = 123 TRENDING / 16 VOLATILE / 1 RANGING. **+4 entries re-classified TRENDING->VOLATILE** (full risk -> 0.7x) under the closed-bar read. mean RiskPct 1.1251 -> 1.1186; mean Lot 0.1029 -> 0.1000.
- **2024 per-trade R delta (matched by EntryTime|Direction, 137 matched):** 84 trades differ in R, only **4 sign-flips**, sum dR = -5.46R (= the headline). **Magnitude distribution: 53 unchanged, 76 shifts <0.25R, 5 in [0.25,0.5)R, 0 in [0.5,1.0)R, 1 in [1.0,2.0)R, 2 >=2.0R.** The -4.82R is DOMINATED by ~81 sub-0.5R micro-adjustments — a debounced-classifier ripple, NOT a few catastrophic flips.
- **2023:** entry set shifted +6 net (122 of ~124 entries common; 1 A-only dropped, 7 B-only added). On the 122 common entries: 88 differ in R, **ZERO sign-flips, sum dR = -1.10R** (negligible). The +7 new B entries are bars where the closed-bar regime stamp opened a slightly different gate. Net 2023 essentially flat (+$35.67, totR -0.53R).
- **Count NOT collapsed (140->140, 123->129) and NOT ballooned.** No direction-class blanking. Mechanism-consistent: closed-bar H4 regime -> small per-trade risk-multiplier + exit-timing shifts (2024, identical entries) plus a handful of re-selected entries (2023). The 2024 dollar dip is honest look-ahead removal (the forming-bar H4 regime front-ran the bar close), consistent with the Iter-1 closed-bar theme and the 00-STATUS broader-watch deflation note (1.4 named there as an expected further-deflation contributor).
- **Sign vs stok's 1.5 prior:** stok noted the old fixed-50 zero-fill ATR-avg bias "errs conservative, over-classifying volatility." Net 2024 B classifies slightly MORE VOLATILE at entry (12->16). The 1.5 realized-count change removes the zero-pad bias; the residual TRENDING->VOLATILE shift is driven by 1.4's closed-bar ADX/ATR re-timing (the dominant of the two bundled effects), not by 1.5's denominator. The two are coherent and land together as designed.

## stok review + verdict
- **Numbers handed to stok:** the A/B table (2024/2023, trades/WR/net/totR/avgR/LONG/SHORT), the regime-stamp distribution shift (2024: 127/12/1 -> 123/16/1 TRENDING/VOLATILE/RANGING), the 2024 dR magnitude histogram (81 of 84 shifts <0.5R, 4 sign-flips), and the 2023 common-entry zero-sign-flip / -1.10R result.
- **stok assessment:**
  - **Code + mechanism verified at source.** `CRegimeClassifier.mqh` Update 132-160: ADX/ATR/BB/close now read CLOSED bar `[1]` (counts bumped, short-read guards `<2`), ATR-avg over `atr[1..MathMin(50,atr_got-1)]`/realized count; `ClassifyRegimeRaw()` + bar-confirmation hysteresis (steps 1-3) + 2-bar vol-expansion confirm + thrash-cooldown all UNCHANGED — no logic cut. The sizing path is verbatim as reported: `UltimateTrader.mq5:1582-1591` Dynamic Barbell maps `REGIME_VOLATILE` → `regime_risk_multiplier=0.7` (CHOPPY 0.6 / RANGING 0.75 / TRENDING+NORMAL 1.0), reading `g_marketContext.GetCurrentRegime()`; regime-aware exit reads the same. Confirmed by inspection.
  - **An edge amputation is IMPOSSIBLE BY CONSTRUCTION here.** The 2024 ENTRY SET is byte-identical (140t, same 109L/31S, same 46.43% WR). The fix did not change WHICH bars trade — it changed only the regime CLASSIFICATION STAMP at the confirmation moment, which feeds position SIZING (the Barbell multiplier) and regime-aware exit timing. No strategy path can have been removed when the population of entries is provably identical. This is a strictly cleaner case than 1.3, which had to argue re-selection-vs-amputation from year asymmetry; 1.4 cannot touch the 2024 entry set at all.
  - **The forming-bar `[0]` regime read was genuine look-ahead; the dollar deflation is its removal.** Reading ADX/ATR/BB off the still-forming H4 bar let the regime stamp front-run the bar close — the tester sized positions using regime knowledge a live EA could not have had until `[1]` closed. Closed-bar `[1]` is the only real-time-honest basis. The −32.5% / −4.82R (2024) flows through (a) +4 entries re-classified TRENDING→VOLATILE (1.0×→0.7× size) and (b) sub-0.5R regime-aware-exit ripples (81 of 84 matched shifts <0.5R, only 4 sign-flips). You do not keep P&L that was earned at a size the look-ahead illegitimately permitted.
  - **Down-sizing VOLATILE-regime trades IS the correct, intended risk behavior — the look-ahead was DEFEATING the EA's own Dynamic Barbell.** The Barbell's explicit design intent (mq5:1578-1591 comment: "protect capital in weak conditions") is to size DOWN in VOLATILE/CHOPPY/RANGING. The forming-bar read was letting trades that were genuinely entering a VOLATILE regime be classified TRENDING (the not-yet-closed bar hadn't confirmed the volatility expansion) and ride at full 1.0× TRENDING size. So the look-ahead wasn't merely inflating abstract P&L — it was specifically letting volatile-regime trades ride at full trending size, the exact posture the Barbell exists to prevent. The deflation therefore has TWO correct components pointing the same way: honest look-ahead removal AND the EA finally applying its own risk rule. The give-back is the look-ahead premium being surrendered plus the intended down-sizing taking effect. Both are correct.
  - **Direction of the VOLATILE re-class is the right sign + consistent with my 1.5 prior.** B classifies 12→16 VOLATILE at entry. I previously noted the old fixed-50 zero-pad ATR-avg "errs conservative, over-classifying volatility"; the 1.5 realized-count change removes that zero-pad bias, and the residual TRENDING→VOLATILE shift is driven by 1.4's closed-bar ADX/ATR re-timing (the dominant of the two bundled effects). More honest volatility recognition at the close = more correct down-sizing. The two bundled fixes are coherent and land together as designed.
  - **All kill bounds PASS.** Entry set byte-identical (no amputation by construction); count 140→140 / 123→129 (no collapse, no balloon); 2024 R-delta dominated by ~81 sub-0.5R micro-adjustments (debounced-classifier ripple), only 4 sign-flips; 2023 zero sign-flips on 122 common entries, net ~flat. No logic cut. No tunable param introduced (1.4 = bar-index correctness, 1.5 = denominator robustness).
  - **Caveat (recorded, not a loop):** a fraction of the 2024 −4.82R is regime-aware EXIT/trail re-timing (also reads `GetCurrentRegime()`), not pure entry sizing. This is mechanism-identified, sub-0.5R per trade, and is itself the correct look-ahead-free behavior — re-timing an exit off an honest closed-bar regime is right. Does not change the verdict.
- **stok verdict:** **SIGN-OFF.** The forming-bar regime read was genuine look-ahead (H4 ADX/ATR/BB front-ran the bar close); closed-bar `[1]` is the honest real-time basis and is NOT reverted. With the 2024 entry set byte-identical, edge amputation is impossible by construction — only SIZING changed. The −32.5% / −22.5%(2yr) deflation is acceptable honest look-ahead removal AND the correct application of the EA's intended Dynamic Barbell down-sizing: the look-ahead had been letting volatile-regime trades ride at full TRENDING size, exactly what the Barbell exists to prevent. Folds into the v18 re-baseline. No change requested.

## Kill-criteria check
- **Phase kill-criterion (plan/00-STATUS, stok context):** debounced classifier -> small intermittent effect expected; 1.5 pure robustness; steady-state (post-50-bar) near-baseline; NOT an edge amputation; count must not collapse/balloon.
- **Result:** **PASS (mt5-QA)** — 2024 entry set byte-identical (no edge amputation by construction); count 140->140 / 123->129 (neither collapsed nor ballooned); 2024 -4.82R decomposes into ~81 sub-0.5R micro-adjustments via the regime risk-multiplier + regime-aware exit (mechanism-identified), only 4 sign-flips; 2023 common entries zero sign-flips. No strategy path removed, no logic cut. CONCERN FOR STOK: the 2024 net is -32.5% in dollars (larger than "small" in $ terms though micro in per-trade R) — flagged for stok's trading-impact adjudication and the broader-watch re-baseline.

## mt5-developer sign-off
- Compile clean (0 errors, 0 new warnings vs 38-baseline); both fixes implemented per the validated spec (1.4 closed-bar [1] reads + closed-bar averages; 1.5 realized-count divide, CMomentumFilter idiom); file:line verified against current source; all Copy* short reads guarded (`< 2` early return); no logic cut. **SIGNED.**

## stok sign-off
- **SIGNED (trading-impact). VERDICT: SIGN-OFF.** Look-ahead premise verified at source (`CRegimeClassifier.mqh` Update: ADX/ATR/BB/close on closed bar `[1]`, realized-count ATR avg, guards `<2`, ClassifyRegimeRaw/hysteresis/thrash UNCHANGED, no logic cut; sizing path mq5:1582-1591 maps VOLATILE→0.7× verbatim). Forming-bar `[0]` regime read front-ran the H4 bar close = genuine look-ahead; closed-bar `[1]` is the honest real-time basis, NOT reverted. **2024 ENTRY SET byte-identical → edge amputation impossible by construction; only SIZING changed.** The −32.5%/2024 (−22.5% 2yr) dollar deflation is BOTH honest look-ahead removal AND the correct application of the EA's own Dynamic Barbell: the look-ahead had been letting genuinely-VOLATILE-regime trades classify as TRENDING and ride at full 1.0× size — exactly the posture the Barbell exists to prevent ("protect capital in weak conditions"). Closed-bar `[1]` restores the intended down-sizing (+4 entries 1.0×→0.7×) plus sub-0.5R regime-aware-exit ripples (81/84 <0.5R, 4 sign-flips). 2023 zero sign-flips on 122 common entries, net flat. All kill bounds PASS (entry set identical / count not collapsed-or-ballooned / micro-shift R distribution). Bundled 1.4+1.5 coherent (1.5 removes the conservative zero-pad bias I flagged; 1.4 closed-bar re-timing is the dominant effect). No change requested. **ACCEPT → QA-PASS. Fold into the v18 re-baseline.**

## Decision
- **QA-PASS — both agents signed.** mt5-developer SIGNED (compile 0-err/0-new-warn + both fixes per validated spec + file:line verified + Copy* short-reads guarded + no logic cut). **stok SIGNED → SIGN-OFF** (trading-impact: forming-bar regime read was genuine look-ahead; 2024 entry set byte-identical makes amputation impossible by construction; the dollar deflation is honest look-ahead removal AND the correct Dynamic-Barbell down-sizing of volatile-regime trades that were previously riding at full trending size). **Cleared to COMMIT** as a separately-measurable Phase-1.4 commit (bundled fixes 1.4+1.5). NEXT ACTION advances to Phase 1.5. Main session runs git; agents do NOT.

## stok cumulative re-baseline read (Iteration-1 boundary watch — updated through 1.4)
- **Running honest-edge trajectory (2-yr net, XAUUSD+ H1, pattern-engine production config):**
  - Post-1.1: $3,289.10 (zero-delta; 1.1 was trade-for-trade identical — establishes the pre-look-ahead-removal anchor).
  - Post-1.2 (BOS/CHoCH trigger closed-bar): $3,159.76 (−3.9% cumulative; small, bidirectional).
  - Post-1.3 (trend DIRECTION closed-bar): $2,392.25 (−27.3% cumulative; 1.3 the single biggest contributor — direction feeds ~15 plugins; year-specific look-ahead deflation, not lag).
  - **Post-1.4 (regime closed-bar + realized-count ATR): $1,854.69 (−43.6% cumulative vs the $3,289 post-1.1 anchor).** 1.4 contributes −$537.56 (−22.5% of the post-1.3 figure), driven 2024-side by SIZING only (entry set byte-identical), 2023-side ~flat.
- **Read:** Three consecutive forming-bar→closed-bar fixes have now removed ~44% of the headline backtest net — confirming my 1.3 boundary flag that the prior edge was MATERIALLY look-ahead-inflated. The deflation is honest by every test: 1.1 identical, 1.2 bidirectional within-noise, 1.3 asymmetric year-specific (not lag), 1.4 provably sizing-only (entry set byte-identical) AND the correct application of the EA's own risk rule. 1.4 is the cleanest of the four — it literally cannot have amputated edge.
- **Forward expectation:** more deflation likely from 1.7(=fix1.8 crash/rubber-band), 1.8(=fix1.9 MA200+swings), 4.7(=fix4.8 trailing), 5.2(=fix5.4 file-signal swing) as the remaining forming-bar reads close. **The honest post-Iteration-1 baseline — not the pre-fix headline, and NOT the v18 $6,140 reference — is the number against which Iter-2/3 selectivity tuning (`InpBOSFreshnessBars` sweep, 2.4-GATE tier re-derivation, per-engine fresh validation) must earn edge back.** Re-baseline all forward expectations and the v18 reference to the post-look-ahead-removal figure. This remains a WATCH item, not a stop-work item: every fix individually passed its kill-criteria and removing look-ahead is unambiguously correct. Edge recovery is owed by Iter-2/3 tuning on the honest input, not by reverting any closed-bar fix.

## Commit
- **Hash:** (not committed per instruction — main session runs git)
- **Message:** `Phase 1.4: regime classifier closed-bar [1] ADX/ATR + realized-count ATR avg (fixes 1.4+1.5)`
