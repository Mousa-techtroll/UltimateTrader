# Phase 4.4+4.5 — Split daily-loss/error halt flags + file-signal CanTrade gate

> Land TOGETHER in ONE commit (both edit the CanTrade halt-check). Iteration 4.

## Phase
- **Phase id:** 4.4+4.5
- **Fix ids:** 4.4 (halt-flag split) + 4.5 (file-signal CanTrade gate)
- **Touches trading logic:** Y (both strictly TIGHTEN the live backstop; no logic cut)
- **depends_on (phases COMMITTED first):** Iteration-4 opener 4.1@701927a, 4.2@445baa0, 4.3@edf6876 (= baseline A, current HEAD)
- **Binding stok param/option used:** `InpMaxTradesPerDay = 5` (intended SHARED pattern+file daily budget in BOTH mode — RESUME.md (c)). 4.4 strict-tightening (a post-error success can no longer lift a loss-halt) is intended.

## External-reader grep (m_trading_halted) — DONE BEFORE renaming
`grep -rn m_trading_halted` repo-wide:
- `Include/Core/CRiskMonitor.mqh` — the member + all its set/read sites (lines 33,63,80,111,132,159,166,201,215,249).
- `Include/Display/CDisplay.mqh` (34,50,64,138) — a SEPARATE private `m_trading_halted` member of CDisplay, set via `SetRiskStats(... halted ...)`; NOT a reader of CRiskMonitor's member.
- **CONCLUSION: ZERO external readers of `CRiskMonitor::m_trading_halted`.** It is private; the only public API is `IsTradingHalted()` / `CanTrade()` / `IsErrorHalted()`. Renaming the private member is safe; public API preserved (semantics = OR of both flags).
- Callers of the public API: `IsTradingHalted()` at UltimateTrader.mq5:1486,1728,2152,2233,2255; `CanTrade()` at mq5:1486,1728; `IsErrorHalted()` has no external caller. `CDailyLossHaltExit::IsTradingHalted()` is a different class/method (reads its own `m_halt_triggered`).

## What changed (file:line verified against current source)

### FIX 4.4 — `Include/Core/CRiskMonitor.mqh` (split the shared flag)
- Member: `bool m_trading_halted;` (was line 33) → `bool m_loss_halted;` (daily-loss halt; resets only on new day). `m_error_halted` (line 43) already exists.
- Constructor (was line 63): `m_trading_halted = false;` → `m_loss_halted = false;`.
- `Init()` (was line 80): `m_trading_halted = false;` → `m_loss_halted = false;`.
- `IsTradingHalted()` (was line 111): returns `m_loss_halted || m_error_halted` (OR semantics preserved).
- `CanTrade()` (was line 132): halt check `if(m_trading_halted)` → `if(m_loss_halted || m_error_halted)`.
- `CheckRiskLimits()` (was 159/166): early-return guard `if(m_trading_halted)` → `if(m_loss_halted)` (so a daily-loss check is skipped once loss-halted; error-halt does not suppress the loss check); set `m_trading_halted = true;` → `m_loss_halted = true;` on daily-loss-limit hit.
- `RecordExecutionError()` (was line 201): `m_trading_halted = true;` removed — only `m_error_halted = true;` is set (the error halt is now its own flag; CanTrade/IsTradingHalted OR it in).
- `RecordExecutionSuccess()` (was line 215): clears ONLY `m_error_halted` (removed the `m_trading_halted = false;` line) — a same-day error-recovery can no longer lift a daily-LOSS halt. THIS IS THE FIX.
- `CheckDayReset()` (was line 249): `m_trading_halted = false;` → `m_loss_halted = false;` (daily-loss halt resets on new day; `m_error_halted = false;` already reset here too).

### FIX 4.5 — `UltimateTrader.mq5` (file-signal guard)
- File-signal entry guard (was ~2108-2113 in plan; ACTUAL lines 2149-2152): added `&& g_riskMonitor.CanTrade()` to the guard so file signals respect the daily trade-count limit (and the OR'd halt flags).
- Orphan-adoption path (lines 2076-2146): NOT gated (re-tracking an existing fill is not a new entry). UNCHANGED.
- `IncrementTradesToday` at line 2212 (file path) and line 2123 (orphan path): left AS-IS (orphan double-count conservative fail-safe per plan).

- **Logic-cut check:** NO logic cut. Both halt conditions and their set-points preserved; only the single shared flag is split into two independent backstops, and the file path gains the CanTrade gate. Strictly tightens.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 10147 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (exactly 38; ZERO warnings in CRiskMonitor.mqh — confirmed by grep; the only CRiskMonitor line in the log is "information: including").
- Error 106 / include issues: none (private-member rename only; no include change).
- `.ex5` regenerated: yes — 912816 bytes, B (post-fix) md5 `9dbe9052e8b164f438026bc97e87a3c7`, ts 2026-06-27 03:12:06.

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, full 2019.01.01–2026.06.27, `[TesterInputs]` production defaults (395 params, InpEnableMultiStrategy=false, InpSignalSource=2/BOTH, InpSignalFile=telegram_signals.csv, InpMaxTradesPerDay=5, InpMaxPositions=5, InpDailyLossLimit=3.0, InpEnableDailyLossHalt=true, InpMaxConsecutiveErrors=5). Deposit 10000 USD, lev 100. A/B inis byte-identical apart from `Report=`. 2 terminal runs total (within the ≤2 bound).
- **Mode:** 1-minute OHLC (Model=1).
- **A = committed HEAD edf6876** (ex5 md5 `b083dfc4`). **B = post-fix, BOTH fixes** (ex5 md5 `9dbe9052`). Binaries DIFFER (real source change) → behavioral identity is the test, not binary identity.

### A/B overall — BYTE-IDENTICAL (both fixes combined)
| Output | A md5 (UTF-16LE raw CSV) | B md5 | cmp |
|---|---|---|---|
| Stats CSV | `a555618b7ef18c34aa502bafad854998` | `a555618b...` | IDENTICAL |
| TradeEvents CSV | `5dbe2666ce66e839e8293ddf461bc0c3` | `5dbe2666...` | IDENTICAL |
| Risk CSV | `fa4b951e56aa9fdac1d6fb64941491c2` | `fa4b951e...` | IDENTICAL |
| backtest_results CSV | `5a2b3cade25778986697702fa35ae643` | `5a2b3cad...` | IDENTICAL |

- **Report (decoded p4445_A.htm / p4445_B.htm) — A == B:** Net $25,570.97 | PF 1.31 | EP 12.72 | Total Trades 2011 | Bal-DD $3,637.66 (9.28%) | Eq-DD $4,513.87 (11.26%). These EXACTLY match the Phase-4.3 B figures (HEAD edf6876 = the 4.3 commit) → A reproduces HEAD.
- **Stats CSV (deterministic) A == B:** 961 logical closes, avg PnL_R +0.1320, sum PnL $26,205.77, 8484 EXIT rows.
- **No-op / byte-identical claim:** YES — trade-for-trade, SL-for-SL, R-for-R IDENTICAL (all 4 CSVs md5-equal + cmp-clean, report-equal). The combined effect of BOTH fixes is byte-identical in backtest.

## Per-fix byte-identity / production-affecting determination (with mechanism + evidence)

### 4.5 = BYTE-IDENTICAL IN BACKTEST → LIVE-ONLY effect. PROVEN.
- **The signal file does not exist in the tester.** `find` over the whole MetaQuotes tree for `telegram_signals*` → NONE. `MQL5/Files/` empty; FILE_COMMON `Common/Files/` holds only EA-generated logs/CSVs, no signal file.
- **Tester journal (decoded) both runs:** `CFileEntry: Could not open trade file: telegram_signals.csv` printed every check interval; `CFileEntry initialized (no trades loaded yet...)`. All 3 FileOpen attempts (FILE_COMMON, local tester Files, MQL5\Files) fail → `m_trades[]` stays empty → `CheckForEntrySignal()` returns `valid=false`.
- **File-signal trade count = 0 in BOTH A and B:** journal `[FileSignal] Executed` = 0/0; `CFileEntry: SIGNAL READY` = 0/0; TradeEvents CSV `FileSignal` rows = 0/0.
- **Mechanism:** the file-signal `if(...)` branch body is never entered with a valid signal, so the added `&& CanTrade()` clause is never the deciding factor → cannot change any backtest trade. There is no external process writing the signal file during a backtest. The fix's effect (capping file trades at the shared InpMaxTradesPerDay daily budget) is LIVE-ONLY.
- **Visible-but-benign side effect:** the tester-journal "Daily trade limit reached (5/5)" log line jumped A=74 → B=18665. This is because B now calls `CanTrade()` on the file path every qualifying tick (CanTrade LogPrints that line when m_trades_today>=5), whereas A's file guard called only `IsTradingHalted()`. It is PURE LOG NOISE — it produces ZERO trades (the branch finds no signal) and the trade CSVs are byte-identical. Confirmed: the extra lines fall on `State Updated` ticks with no `[FileSignal] Executed` following.

### 4.4 = BYTE-IDENTICAL IN BACKTEST → LIVE-ONLY tightening. PROVEN.
- **Per-run tester-journal counts (A_run vs B_run, isolated by the 03:15 / 03:16 EA-init banners):**
  | Event | A | B |
  |---|---|---|
  | `DAILY LOSS LIMIT HIT` | 20 | 20 |
  | distinct loss-halt DAYS | **10** | **10** |
  | `consecutive errors ... TRADING HALTED` (error-halt fires) | **0** | **0** |
  | `Error halt CLEARED after successful execution` | **0** | **0** |
- **Mechanism / why byte-identical:** the consecutive-error halt NEVER fires in the backtest (0 in both — `RecordExecutionError` never reaches the 5-error threshold). Therefore in the OLD shared-flag code, `m_trading_halted` was only ever set by the daily-loss path, and the `m_trading_halted=false` line inside `RecordExecutionSuccess()` (the line 4.4 removed) was UNREACHABLE as a loss-halt lifter — it only ran inside `if(m_error_halted)`, which was always false. So a loss-halt was never lifted by a same-day success in backtest in EITHER build → splitting the flag changes nothing here. Loss-halt-day set IDENTICAL (10=10, matches Phase 4.3). The tightening (a post-error success can no longer lift a loss-halt) is real but only manifests LIVE where the error halt can actually fire.

## Single-source / OR-semantics confirmation (4.4)
- **CONFIRMED at source.** `m_trading_halted` (single shared flag) → split into `m_loss_halted` (set ONLY in `CheckRiskLimits` on the −3% daily-loss hit; cleared ONLY in `CheckDayReset` on a new day) + `m_error_halted` (set in `RecordExecutionError` at the threshold; cleared in `RecordExecutionSuccess`). `IsTradingHalted()` returns `m_loss_halted || m_error_halted`; `CanTrade()` halt-check is `if(m_loss_halted || m_error_halted)`. Error-recovery (`RecordExecutionSuccess`) clears ONLY `m_error_halted`. Both halt conditions and their set-points preserved; NO logic cut. Public API (`IsTradingHalted`/`CanTrade`/`IsErrorHalted`) preserved; grep-verified ZERO external readers of the old private member (CDisplay's same-named member is a separate field set via SetRiskStats).

## File-signal-trades = 0 confirmation (4.5)
- **CONFIRMED.** A=0 / B=0 file-signal executions (journal `[FileSignal] Executed` + `CFileEntry: SIGNAL READY` + TradeEvents `FileSignal` rows, all zero in both runs). The `&& CanTrade()` guard is unreachable-as-decider in backtest because the file branch is never entered with a valid signal (file absent).

## stok review + verdict
- **PENDING — do NOT self-sign stok.** Handed to stok: (a) 4.4 strictly-tightens-the-backstop (a post-error success can no longer lift a loss-halt) is intended; (b) `InpMaxTradesPerDay=5` is the intended SHARED pattern+file daily budget in BOTH mode (the safe default 4.5 delivers).

## Kill-criteria check
- **Phase kill-criteria (Iter-4):** for the live-only/no-op claims, A/B production XAUUSD-only 2019–2026 must be trade-for-trade, R-for-R IDENTICAL.
- **Result: PASS.** Both fixes are byte-identical in backtest (4 CSV md5-equal + cmp-clean + report-equal). No regression possible. Both are live-only (4.5 caps file trades at the shared daily budget; 4.4 makes the daily-loss backstop independent of error-recovery). Neither can fire harmfully on the production backtest path.

## mt5-developer sign-off
- **SIGNED (implementation correct).** Compile 0 err / exactly 38 warn (0 new; 0 warnings in CRiskMonitor.mqh). External-reader grep done BEFORE rename (zero readers of `CRiskMonitor::m_trading_halted`). 4.4: single shared flag split into two independent backstops, OR'd in the public getters, error-recovery clears only the error flag, daily reset clears only the loss flag — both halt conditions + set-points preserved, NO logic cut. 4.5: `&& g_riskMonitor.CanTrade()` added to the file-signal guard only; orphan-adoption NOT gated; `IncrementTradesToday` at 2123/2212 left as-is. Both fixes proven byte-identical in backtest with the mechanism evidenced (error-halt never fires → split is a no-op in backtest; signal file absent → file branch never entered → CanTrade clause unreachable as decider). Live-only tightening as intended.

## stok sign-off
- **SIGNED — verdict: ACCEPT-WITH-FOLLOWUP** (binding). stok verified at source (CRiskMonitor.mqh 20–266; UltimateTrader.mq5 1486/1728/2154–2224) — implementation matches spec exactly. Ruling:
  - **(1) 4.4 split is CORRECT.** The two halts encode different risk states with different clearing conditions: `m_loss_halted` = capital-preservation ("you've bled your daily budget, stop adding risk today") → resets ONLY on a new day (nothing within the day, incl. a profitable fill, should lift it — that defeats the limit and invites revenge-trading a drawdown back); `m_error_halted` = operational ("execution malfunctioning, pause") → clears on evidence the plumbing works (a successful execution). The old shared bool let a post-error success run `m_trading_halted=false` and clear a daily-loss halt unrelated to execution health = a GENUINE risk bug (operational all-clear overriding a capital stop). OR'd in the public getters with independent clearing = correct backstop model. Verified `CheckRiskLimits` early-returns only on m_loss_halted (L168) so the loss line is still evaluated independently of error state. The strict tightening removes only "a good fill lifts your daily-loss stop" — never a wanted behavior; no downside. Live-only/structurally-unreachable in backtest (error halt 0 fires) is why there's no P&L signal — the fix matters precisely in the live failure mode the tester can't reproduce (≥5 consecutive broker rejects while a loss-halt is active). ENDORSED.
  - **(2) 4.5 shared budget is the CORRECT risk policy.** Closing the unlimited-file-trades/day hole is mandatory. InpMaxTradesPerDay is a capital-exposure ceiling in trade-count, and capital doesn't care where a signal originated — a pattern trade and a file trade risk the same equity on the same (correlated) gold move. Separate budgets would silently permit up to 10 trades/day in BOTH mode = DOUBLE the intended daily risk envelope = the same hole through a different door. Confirmed symmetric: pattern (1728), breakout-probation (1486), file (2158) all gate on the same CanTrade() and increment the same m_trades_today. Starvation (whichever source fires first consumes the allowance) is ACKNOWLEDGED + ACCEPTABLE — the ceiling binds on total risk, not fairness between sources; realized pattern flow is ~0.3 closes/day so file-starvation is rare. Orphan-adoption correctly NOT gated (re-tracking an already-open broker fill is not a new entry; blocking it would leave a live position untracked = more dangerous); IncrementTradesToday over-count is the correct fail-safe direction (tightens, never loosens).
  - **(3) Log spam (74→18665) = benign**, zero trade impact (CSVs byte-identical), non-blocking — but a real operational nuisance worth a cheap follow-up.
  - **Verdict: ACCEPT-WITH-FOLLOWUP — commit 4.4+4.5 together now.**

## Iteration-6 follow-ups (logged by stok; NEITHER gates this commit)
- **(1) Log-spam gate (cosmetic/operational):** make CanTrade()'s "Daily trade limit reached" + "Trading halted" LogPrints fire ONCE PER STATE TRANSITION (log only when m_trades_today first crosses the limit), not once per poll — or demote to a debug verbosity level. 4.5 now polls CanTrade() on the file path every tick → 18665 dead-branch log lines bloat live journals / mask genuine rejections / add VPS I/O. Small; do in the next housekeeping pass.
- **(2) Config question for the operator (policy, NOT logic — do NOT loop for it):** confirm whoever runs BOTH mode live that file (Telegram/manual) signals are INTENDED to share the 5-trade daily budget with pattern signals and can be starved by them on high-activity days. If the operator wants file signals to have a guaranteed independent allotment, that's a deliberate second-counter design change — raise as a feature request. Shared budget is the correct conservative default shipped today.

## Decision
- **DONE — both agents signed (mt5 implementation-correct; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified). Committed source-only on `feat/multi-strategy`.** Both fixes byte-identical in backtest (live-only safety tightenings, A==B all 4 CSVs md5+cmp clean); 4.4 separates capital-preservation from operational halts (correct independent clearing), 4.5 closes the unlimited-file-trades hole with the correct shared-ceiling policy. Two non-blocking Iter-6 follow-ups logged. NEXT ACTION → Phase 4.6.

## Commit
- **Hash:** `<filled at commit>` (source-only: CRiskMonitor.mqh + UltimateTrader.mq5 + 4-4_4-5.md + 00-STATUS.md; `.ex5` intentionally uncommitted). Branch `feat/multi-strategy`.
