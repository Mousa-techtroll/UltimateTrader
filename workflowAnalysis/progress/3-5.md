# Phase 3.5 — HTF-uptrend SHORT veto for ROUTED ENGINE shorts (fix 3.6, BETTER-OPTION)

## Phase
- **Phase id:** 3.5
- **Fix ids:** 3.6 (BETTER-OPTION — explicit HTF-uptrend SHORT veto for engine shorts; the central guard against negative-expectancy gold shorts re-entering through the multi-strategy engines)
- **Touches trading logic:** Y (adds a SHORT-side reject branch) — but scoped to `signal.routed_engine`, which is constant-false on the production `.set` (engines OFF by default) → BYTE-IDENTICAL on production; the veto is exercised at the engines-ON GATE / Phase 3.8.
- **depends_on (phases that must be COMMITTED first):** Iter-3 ordering (3.1–3.4 COMMITTED). The veto's short-expectancy effect is validated at the engines-ON GATE / Phase 3.7–3.8.
- **Binding stok param/option used:** **HTF short veto (3.6) = hard-ON** (no disable input for production). Reject an engine SHORT if `(h4==TREND_BULLISH || d1==TREND_BULLISH) && IsPriceAboveMA200()`, mirroring `CTrendContinuationEngine::IsUptrendActive()` exactly; audit tag `HTF_UPTREND_SHORT_VETO`.

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/Core/CSignalOrchestrator.mqh` — `CheckForNewSignals()` SHORT branch, inside `if(sig_type == SIGNAL_SHORT)` (block opens at line 630). Inserted the HTF-uptrend short veto as the FIRST check (lines 632–654), ahead of the existing ATR-minimum check (now reached via `else if`, lines 655–663):
    - `bool htf_uptrend = ((h4_trend == TREND_BULLISH) || (daily_trend == TREND_BULLISH)) && m_context.IsPriceAboveMA200();` (645–646)
    - `if(is_engine && htf_uptrend)` → set `reject_reason = "HTF_UPTREND_SHORT_VETO";`, `LogPrint`, `WriteShortDiag` (647–654). `validated` stays `false` (initialized false at line ~642 / 619), so control falls through to the existing `if(!validated)` block (line ~682) which fires `AuditCandidate(..., "VALIDATOR", "REJECT", reject_reason, ...)` and `continue`s.
    - The existing ATR-min check is now the `else if` arm (656–657) and the ATR-too-low reject is the trailing `else` (658–663) — UNCHANGED behavior for every non-vetoed short.
- **Diff summary:** A routed-engine SHORT taken INTO an HTF uptrend (H4 OR D1 bullish AND price above MA200) is rejected with audit reason `HTF_UPTREND_SHORT_VETO`. `is_engine` (== `signal.routed_engine`, set true ONLY inside an engine's `if(m_scorer != NULL)` block, and `SetScorer` is called ONLY under `InpEnableMultiStrategy`) scopes the veto to ROUTER-WIRED engine shorts; legacy/standalone shorts (and the LIVE legacy Expansion/Session/Liquidity engines, whose `m_scorer==NULL` on production) are untouched. The `htf_uptrend` test reuses the orchestrator's already-computed `h4_trend = m_context.GetH4TrendDirection()` (line 490) and `daily_trend = m_context.GetTrendDirection()` (line 489) — the SAME getters `IsUptrendActive()` reads as its `h4`/`d1`.
- **Logic-cut check:** NO trading logic deleted. The full short validator is NOT re-enabled (re-enabling it would zero ALL shorts = a cut). The ATR-min check and the trailing ATR-too-low reject still govern every short that is not routed-engine-into-uptrend. The veto only ADDS one reject branch, scoped to a config (`routed_engine`) that is off on production. Diff: `Include/Core/CSignalOrchestrator.mqh` +24/-1.

## Veto wiring confirmation (for the engines-ON GATE / Phase 3.8)
- **Mirrors `IsUptrendActive()` EXACTLY:** `CTrendContinuationEngine.mqh:59-75` defines `IsUptrendActive()` as `trend_up = (h4 == TREND_BULLISH) || (d1 == TREND_BULLISH)` (with `h4 = GetH4TrendDirection()`, `d1 = GetTrendDirection()`) AND `m_context.IsPriceAboveMA200()`. The Phase-3.5 veto is `((h4_trend == TREND_BULLISH) || (daily_trend == TREND_BULLISH)) && m_context.IsPriceAboveMA200()` — identical condition, identical getters, identical OR/AND structure. The veto FIRES when that uptrend holds for a SHORT.
- **`IsPriceAboveMA200()`** is the concrete `CMarketContext` method (CMarketContext.mqh:471-480): live BID vs cached MA200, gold-default-bullish only during warmup (Phase-1.11 `m_ma200_default_bullish=true`). Read through the `IMarketContext* m_context` the orchestrator already holds.
- **Hard-ON:** no `InpEnable*` toggle was added; the veto is unconditional given `is_engine && htf_uptrend`. It is hard-wired ON for production exactly as the binding option requires.
- **routed_engine scoping (the Phase-2.1/3.2 trap avoided):** `is_engine = signal.routed_engine` (line 566). `routed_engine` defaults false (EntrySignal::Init) and is set true ONLY inside each engine's `if(m_scorer != NULL)` scoring block; `SetScorer` is called ONLY inside `if(InpEnableMultiStrategy)` in UltimateTrader.mq5. Therefore on the production `.set` (`InpEnableMultiStrategy=false`) `routed_engine` is constant-false for every signal → the veto branch is unreachable → the LIVE legacy Expansion/Session/Liquidity engines' shorts (which stamp `major_engine` but have `m_scorer==NULL`) are NOT vetoed. Scoping on raw `major_engine != ENGINE_NONE` WOULD have vetoed those live engines' shorts on production and changed behavior — proven NOT to happen by the byte-identity below.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 10177 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (count 38=38; zero warnings in CSignalOrchestrator.mqh).
- Error 106 / include issues: none (no new includes; `m_context`, `h4_trend`, `daily_trend`, `is_engine`, `TREND_BULLISH`, `EnumToString`, `LogPrint`, `WriteShortDiag` all already in scope).
- `.ex5` regenerated: yes — in-tree `UltimateTrader.ex5` size 896694→899020, md5 `c0a40b4d…` (A baseline) → `cae2585a…` (B build). NOTE: `.ex5` md5 differs (code changed) — byte-identity is proven on the backtest CSV outputs, per protocol, NOT on the `.ex5`.

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, DEFAULT production `.set` = `MQL5/Profiles/Tester/UltimateTrader.set` (committed-phase production config: `InpEnableMultiStrategy=false`, all 4 `InpEnableEngine*=false`, `InpSignalSource=2` BOTH). Years 2024 AND 2023.
- **Mode:** Model=1 (1-min OHLC), Deposit 10000 USD, Leverage 100. Terminal `C:\Program Files\Vantage Markets MT5 Terminal\terminal64.exe` (NO `/portable`), data dir `nullkuhl` / `725B72F25E46C780EF59F57016D58156`; CSVs → `MetaQuotes/Terminal/Common/Files/`. (Committed `backtest_symbol.sh` has the STALE `Vantage International` path + wrong terminal-ID — used the correct paths directly via custom `.ini`: `backtest_p35_A_{2024,2023}.ini`, `backtest_p35_B_{2024,2023}.ini`.)
- **A (baseline = committed HEAD):** A binary = the committed-HEAD `.ex5` copied aside to `/tmp/UT_phase3_5_baseline.ex5` (md5 `c0a40b4d…`) BEFORE editing, reinstalled into the terminal Experts dir for the A run. A reproduces the committed-HEAD anchors EXACTLY:
  - 2024 — Stats `04d116972696f905c6ea5fa6701a7155`, TradeEvents `825489b61fbf3db5cb2302361de66bb1`, Candidates `bcc44924d5d3fc31a5f4d35e5142b0a7`, Risk `8babda0f7b846cc6fa94941ecb39c815`. (Headline 140t / net $1190.84 / totR 11.76 / avgR 0.0840.)
  - 2023 — Stats `b11e07675cf4d577e27663ffde0aca19`, TradeEvents `7ef40d5fcff7b79675867f8d43653ec6`, Candidates `2fba7df14c87f7abd53530d6ee67b59c`, Risk `b21a2999181a770a7893a9e61f9161cf`. (129t / net $714.29 reference.)
- **B (this phase):** B binary = `/tmp/UT_phase3_5_B.ex5` (md5 `cae2585a…`) installed into terminal Experts. ALL 8 CSV md5s IDENTICAL to A:
  - 2024 — Stats `04d11697…`, TradeEvents `825489b6…`, Candidates `bcc44924…`, Risk `8babda0f…`.
  - 2023 — Stats `b11e0767…`, TradeEvents `7ef40d5f…`, Candidates `2fba7df1…`, Risk `b21a2999…`.
- **No-op / byte-identical claim:** **YES — A == B BYTE-IDENTICAL** across all 8 CSVs (both years). `md5sum` MATCH on every file AND `cmp` byte-for-byte CLEAN on every file. Trade-for-trade, SL-for-SL, R-for-R identical.
- **Delta + explanation:** ZERO delta. The veto is gated on `is_engine = signal.routed_engine`, which is constant-false on the production `.set` (no engine has `m_scorer` set because `SetScorer` runs only under `InpEnableMultiStrategy=false`) → the `if(is_engine && htf_uptrend)` branch is never taken → no short is newly rejected → identical output by construction. This is exactly the routed_engine-scoping guarantee: had the veto been scoped on raw `major_engine != ENGINE_NONE`, the live legacy Expansion/Session/Liquidity engines' shorts WOULD have been vetoed on production and the CSVs would differ — the byte-identity PROVES the scoping is correct.
- **Iter-3 / engines-ON GATE owed (Phase 3.7–3.8):** the veto's effect on short expectancy is UNMEASURABLE with the engines off. At the engines-ON GATE (`InpEnableMultiStrategy=true` + the relevant `InpEnableEngine*=true`), confirm the veto fires for routed engine shorts (grep `HTF_UPTREND_SHORT_VETO` in the Candidates/short-diag), and measure short expectancy ONLY on trades that SURVIVE the veto over full 2019–2026 + the 2024–2026 OOS slice. The veto makes the short test honest; it does not pre-decide it.

## stok review + verdict
- **Numbers handed to stok:** the byte-identity result (A == B, all 8 CSVs md5 MATCH + cmp CLEAN, both years, A reproducing committed-HEAD anchors 2024 `04d11697…`/`825489b6…`/`bcc44924…`/`8babda0f…`, 2023 `b11e0767…`/`7ef40d5f…`/`2fba7df1…`/`b21a2999…`); the minimal source diff (+24/-1 in CSignalOrchestrator.mqh); the routed_engine-scoping reachability argument (veto unreachable on production, would-have-diverged-under-raw-major_engine, proven by byte-identity); the veto wiring (mirrors IsUptrendActive exactly, hard-ON, audit tag HTF_UPTREND_SHORT_VETO, ATR-min path + full-validator both intact = no logic cut).
- **stok assessment:** PENDING — INDEPENDENT stok review owed (do NOT self-assert on stok's behalf).
- **stok verdict:** PENDING.

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS + RESUME.md (b)4):** no-op claim on the production path → byte-identical production backtest = PASS; any divergence ⇒ the veto is leaking onto the legacy/production path (wrong scoping) ⇒ fix scoping to routed_engine and re-run until byte-identical. Forward (GATE/3.8): measure short expectancy on veto-survivors only; keep engine shorts gated in confirmed bear regimes, do NOT relax to chase count.
- **Result:** PASS — A == B byte-identical (all 8 CSVs md5 + cmp, both 2024 & 2023). No leak onto the legacy/production path → routed_engine scoping confirmed correct. No re-run needed.

## mt5-developer sign-off
- Compile clean (0 errors, 38 warnings, 0 new). Veto mirrors `CTrendContinuationEngine::IsUptrendActive()` EXACTLY (`((h4==BULLISH)||(d1==BULLISH)) && IsPriceAboveMA200()`, same getters via `m_context`). Hard-ON (no disable input). Scoped to `is_engine == signal.routed_engine` (router-wired only), NOT raw `major_engine != ENGINE_NONE` — proven by A==B byte-identity that legacy/production short behavior is untouched. Full short validator NOT re-enabled; ATR-min path intact; NO logic cut. file:line verified against current source. — **SIGNED**

## stok sign-off
- **SIGNED — INDEPENDENT, verified at SOURCE (not the write-up).** Every load-bearing claim re-checked against current source:
  - **(Q1) Byte-identity + routed_engine scoping (no leak) — CONFIRMED.** The veto branch is gated on `is_engine = signal.routed_engine` (CSignalOrchestrator.mqh:566), NOT raw `major_engine`. `routed_engine` defaults `false` (Structs.mqh:587) and is set `true` ONLY inside the four engine `m_scorer != NULL` blocks — VERIFIED at all 4 sites: CExpansionEngine.mqh:595 (inside `if(m_scorer!=NULL)` :589), CRangeReversionEngine.mqh:171 (after `if(m_scorer==NULL) return;` early-return :167 — equivalent guard), CReversalSweepEngine.mqh:223 (inside `if(m_scorer!=NULL)` :219), CTrendContinuationEngine.mqh:197 (inside `if(m_scorer!=NULL)` :193). All four `SetScorer` calls (mq5:733/739/745/755) live inside `if(InpEnableMultiStrategy)` (mq5:718). Source defaults `InpEnableMultiStrategy=false` (Inputs:498) + all four `InpEnableEngine*=false` (Inputs:499–502). Chain: engines OFF → no `SetScorer` → every `m_scorer==NULL` → no `routed_engine=true` site reached → `is_engine` constant-false → `if(is_engine && htf_uptrend)` (647) UNREACHABLE → no short newly rejected → A==B byte-identical BY CONSTRUCTION. The byte-identity (all 8 CSVs, 2024 & 2023, md5 MATCH + cmp CLEAN, A reproducing committed-HEAD anchors) is the PROOF the scoping is on `routed_engine`: a raw-`major_engine != ENGINE_NONE` scope would have vetoed the LIVE legacy Expansion/Session/Liquidity engines' shorts on production and diverged. NO leak onto the legacy/production short path.
  - **(Q2) Veto design — honest, hard-ON, not pre-deciding — CONFIRMED.** Mirrors `CTrendContinuationEngine::IsUptrendActive()` (CTrendContinuationEngine.mqh:59–75) EXACTLY: same getters (`GetH4TrendDirection`/`GetTrendDirection`, already computed at orchestrator :489–490 as `daily_trend`/`h4_trend`), same `(h4==TREND_BULLISH)||(d1==TREND_BULLISH)` OR, same `&& IsPriceAboveMA200()` AND. The veto (CSignalOrchestrator.mqh:645–647) FIRES (rejects the short) precisely when that uptrend holds. Hard-ON: no `InpEnable*` toggle; unconditional given `is_engine && htf_uptrend`. This is the central guard that must SHIP and stay ON before any engine short path is judged. It makes the short test HONEST WITHOUT pre-deciding it — it rejects shorts taken INTO a confirmed HTF uptrend only; engine shorts in confirmed bear regimes (H4 and D1 not bullish, OR price below MA200) still clear and reach the ATR-min path. NO logic cut: full short validator NOT re-enabled (which would zero ALL shorts), ATR-min check (`else if`, :656) + trailing ATR-too-low reject (:658–663) intact for every non-vetoed short; the veto only ADDS one reject branch (+24/-1).
- **stok verdict: SIGN-OFF.**

## GATE-owed: veto-survivor short expectancy (Phase 3.7/3.8, engines-ON)
- The veto's short-expectancy effect is UNMEASURABLE engine-off (the branch is unreachable on the production `.set`); this phase proves ONLY production byte-identity + correct scoping, NOT the short edge. At the engines-ON GATE (`InpEnableMultiStrategy=true` + the relevant `InpEnableEngine*=true`):
  1. **Confirm the veto fires for ROUTED engine shorts ONLY** — grep `HTF_UPTREND_SHORT_VETO` in the Candidates/short-diag; the byte-identity already proves it does not touch the legacy/production path.
  2. **Measure short expectancy ONLY on VETO-SURVIVORS** (the shorts that cleared the veto + ATR-min) — avg-R, PF, net, count, maxDD over the FULL 2019–2026 window AND the 2024–2026 OOS slice.
  3. **Keep engine shorts gated in confirmed bear regimes; do NOT relax the veto to chase trade count.** The honest outcome may still be "gold engine shorts only clear in confirmed bear regimes" — that is an acceptable answer, not a failure. Per-engine kill-criterion (PF < 1.0 OR avg-R < 0 over full window AND OOS) applies to the veto-survivor short population, judged on avg-R + net/DD, not WR/count.

## Decision
- **stok-QA-DONE — BOTH agents SIGNED (mt5-developer + INDEPENDENT stok).** Kill-criterion (production byte-identical) PASS. NO logic cut. Ready to advance (main session runs git / records the commit).

## Artifacts
- A-baseline (committed-HEAD) `.ex5`: `/tmp/UT_phase3_5_baseline.ex5` (md5 `c0a40b4d019b1f0925284dcf2cfc864e`).
- B-build (this phase) `.ex5`: `/tmp/UT_phase3_5_B.ex5` (md5 `cae2585aa6b7674be1c4416ea13b3dac`) — also the in-tree `UltimateTrader.ex5`.
- A-run CSVs: `/tmp/p35_A/{Stats,TradeEvents,Candidates,Risk}_{2024,2023}.csv`.
- B-run CSVs: `/tmp/p35_B/{Stats,TradeEvents,Candidates,Risk}_{2024,2023}.csv` (also in `Common/Files/`).
- Backtest `.ini`s (correct live paths): `725B…/backtest_p35_A_{2024,2023}.ini`, `725B…/backtest_p35_B_{2024,2023}.ini`.
- NOTE: terminal Experts `.ex5` left = B build (`cae2585a…`); restore to the committed `.ex5` if the next phase needs a clean A baseline.

## Commit
- **Hash:** <pending — NOT committed this session per task instruction; main session commits on advance>
- **Message:** <pending — e.g. "Phase 3.5: HTF-uptrend SHORT veto for routed engine shorts (fix 3.6, hard-ON)">
