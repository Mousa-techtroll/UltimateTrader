# Phase 1.3 — Trend detector closed-bar reads (DIRECTION input)

## Phase
- **Phase id:** 1.3
- **Fix ids:** 1.3
- **Touches trading logic:** Y
- **depends_on (phases that must be COMMITTED first):** 1.1 (COMMITTED), 1.2 (COMMITTED)
- **Binding stok param/option used:** n/a (no new param) — binding constraint: ATR index MUST match MA index (closed bar [1])

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/MarketAnalysis/CTrendDetector.mqh` — `UpdateTimeframe` ~143-159: changed the MA/close reads from forming bar `[0]` to closed bar `[1]` (`trend_data.ma_fast/ma_slow` and the `fast_above_slow / price_above/below_fast / price_above/below_slow` comparisons + the diagnostic LogPrints). CopyBuffer/CopyClose counts already 3, so `[1]` is in range.
  - `Include/MarketAnalysis/CTrendDetector.mqh` — `CalculateTrendStrength` ~300-303: bumped ATR `CopyBuffer` count 1→2 and use `atr[1]` so `ma_separation` (computed from the now-closed-bar MAs stored above) is divided by the **closed-bar** ATR — index match with the MA index.
- **Diff summary:** Closed-bar re-timing of TREND DIRECTION (forming `[0]` → closed `[1]`) so the direction feeding ~15 plugins is computed from a bar whose OHLC is final, and the trend-STRENGTH ATR denominator is taken from the same closed bar so strength stays consistent with direction. DetectHigherHighs / DetectLowerLows untouched (centered/structural; use `[i+1]/[i+2]` neighbors, valid only on closed bars).
- **Logic-cut check:** NO trading logic deleted. Every branch (BULLISH / BEARISH / EARLY BEARISH / EARLY BULLISH / NEUTRAL) and the strength formula are intact; only the bar index shifted and the ATR copy count grew by 1.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 7977 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (exactly the 38 pre-existing warnings — UltimateTrader.mq5:780/781, Logger.mqh, RecoveryManager.mqh, HealthMonitor.mqh, CVolatilityBreakoutEntry.mqh, Series.mqh, CEnhancedTradeExecutor.mqh; **none in CTrendDetector.mqh**). A-baseline rebuild was identical: `Result: 0 errors, 38 warnings, 7959 ms elapsed`.
- Error 106 / include issues: none (no includes touched).
- `.ex5` regenerated: yes — `/mnt/c/Trading/UltimateTrader/UltimateTrader.ex5`, 890748 bytes, 2026-06-26 15:25 (sha256 `ab998a46…`). A-build pre-1.3 baseline preserved at `/tmp/UT_phase1_3_baseline.ex5` (889496 B, sha256 `99529d1b…`, rebuilt from current source HEAD pre-edit). B-build at `/tmp/UT_phase1_3_B.ex5`.

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, production `UltimateTrader.set` (`InpSymbolProfile=0` XAUUSD, `InpSignalSource=2` BOTH with `telegram_signals.csv` ABSENT from every reachable dir → file path inert → pattern-engine only, `InpEnableMultiStrategy=false`, `InpMaxPositions=5`, `InpMaxTradesPerDay=5`, `InpEmergencyDisable=false`). Deposit 10000 USD / leverage 100. **A and B used the IDENTICAL `.set`+`.ini`** → any delta is purely the 1.3 binary. Terminal: Vantage Markets MT5 Terminal / nullkuhl / 725B72F25E46C780EF59F57016D58156.
- **Mode:** Model=1 (1-min OHLC) — same model as Phase 1.1 / 1.2 (A-baseline reproduces the Phase-1.2-B numbers EXACTLY: 2024 159t/$2050.92/16.02R, 2023 129t/$1108.84/9.17R → confirms the baseline build IS the post-1.2 state 1.3 builds on, and confirms the parser).
- **Stats source:** `Common/Files/UltTrader_Stats_XAUUSD+_<YYYY>0101_0000.csv` (FILE_COMMON). col1=RowType (EXIT rows = closed trades), col5=Direction, col45=PnL_Money, col46=PnL_R. UTF-16LE, decoded with iconv. CSV mtimes verified fresh after each run.

**2024 (2024.01.01→2025.01.01):**
| Build | Trades | WR% | Net $ | Total-R | Avg-R | LONG | SHORT |
|---|---|---|---|---|---|---|---|
| A (pre-1.3, post-1.2 build) | 159 | 45.28 | 2050.92 | 16.02 | 0.1008 | 126 | 33 |
| B (post-1.3) | 140 | 46.43 | 1764.07 | 16.58 | 0.1184 | 109 | 31 |
| Δ | -19 | +1.15 | -286.85 (-14.0%) | +0.56 | +0.0176 | -17 | -2 |

LONG detail — A: 126t/49.21%WR/$2583.76/19.84R/0.1575avgR → B: 109t/50.46%WR/$2180.25/19.31R/0.1772avgR.
SHORT detail — A: 33t/30.30%WR/-$532.84/-3.82R/-0.1158avgR → B: 31t/32.26%WR/-$416.18/-2.73R/-0.0881avgR (shorts LESS negative in B).

**2023 (2023.01.01→2024.01.01):**
| Build | Trades | WR% | Net $ | Total-R | Avg-R | LONG | SHORT |
|---|---|---|---|---|---|---|---|
| A (pre-1.3, post-1.2 build) | 129 | 37.21 | 1108.84 | 9.17 | 0.0711 | 83 | 46 |
| B (post-1.3) | 123 | 34.96 | 628.18 | 3.95 | 0.0321 | 75 | 48 |
| Δ | -6 | -2.25 | -480.66 (-43.3%) | -5.22 | -0.0390 | -8 | +2 |

LONG detail — A: 83t/37.35%WR/$413.05/2.24R/0.0270avgR → B: 75t/36.00%WR/$241.94/-0.07R/-0.0009avgR.
SHORT detail — A: 46t/36.96%WR/$695.79/6.93R/0.1507avgR → B: 48t/33.33%WR/$386.24/4.02R/0.0838avgR.

**2-year combined:** net A=$3159.76 → B=$2392.25 → Δ -$767.51 (-24.3%). total-R A=25.19 → B=20.53 → Δ -4.66. trades A=288 → B=263 → Δ -25 (-8.7%). 2-yr avg-R A=0.0875 → B=0.0781.

- **Delta + explanation:** The shift is BIDIRECTIONAL by year and DIRECTION-led, as expected for a trend-DIRECTION re-timing that feeds ~15 plugins (highest-leverage closed-bar fix in Iter 1, so a larger delta than 1.2's BOS-only re-timing is expected). 1.3 reads `ma_fast/ma_slow/close` and the ATR denominator from the CLOSED bar `[1]` instead of the forming bar `[0]`, so the multi-TF trend direction (D1/H4/H1) is determined by a bar whose OHLC is final — it no longer flips bullish/bearish intrabar on a price spike that the forming-bar `[0]` read would have caught mid-bar. Trade count fell -8.7% over 2yr (159→140, 129→123): a RE-SELECTION (the EA enters fewer setups because a borderline trend read no longer flips intrabar), NOT a collapse (count did not approach zero) and NOT a balloon. The reduction is LONG-dominated (-17 in 2024, -8 in 2023) because gold's structural uptrend means most direction reads are bullish, so the bars whose intrabar direction flip is now removed are mostly the bullish ones. 2024: FEWER-but-BETTER — avg-R rose 0.1008→0.1184, WR rose, total-R actually rose +0.56R despite -19 trades; only nominal $ fell (the dropped trades were net-positive in $ but below-average in R — i.e. lower-quality fills the forming-bar read had been front-running). SHORTS improved in 2024 (less negative). 2023: WORSE — net -43.3%, total-R -5.22R; the LONG book went from +2.24R to -0.07R and SHORT from +6.93R to +4.02R. The 2023 degradation is concentrated where the forming-bar direction read had been catching intrabar-confirmed moves that the closed-bar read defers by a bar.
- **No-op / byte-identical claim (if applicable):** NOT a no-op (touches-logic Y). A/B is NOT byte-identical — and should NOT be: 1.3 deliberately changes WHEN the trend direction confirms (forming `[0]` → closed `[1]`), which re-selects the tradeable-bar population. Non-identity is the intended, mechanism-explained effect.

### stok-facing checks (mt5-developer pre-assessment)
1. **Trade count neither collapses nor balloons:** 159→140 (-12%, 2024), 129→123 (-5%, 2023); -8.7% over 2yr. **PASS** — re-selection, not collapse/balloon.
2. **Direction-mix delta:** 2024 L 126→109 (-17), S 33→31 (-2); 2023 L 83→75 (-8), S 46→48 (+2). The reduction is LONG-dominated (consistent with closed-bar re-timing removing intrabar bullish-direction flips in gold's uptrend), shorts roughly flat. No direction-class blanking, no OTHER rows. **Mechanism-consistent.**
3. **Mechanism vs amputation:** the ATR index was matched to the MA index (`atr[1]`), so strength stays consistent with direction (no desync). No logic cut — every branch and the strength formula intact. The change is closed-bar re-timing of direction, not removal of a strategy path. **Mechanism-consistent re-timing, NOT an edge amputation by construction.**

## stok review + verdict
- **Numbers handed to stok:** full 2024 + 2023 A/B tables (trades/WR/net$/total-R/avg-R + per-direction LONG/SHORT breakdown), 2-yr combined deltas, and the direction-mix/trade-count assessment above.
- **stok assessment:**
  - **Code + mechanism verified at source.** `CTrendDetector.mqh` UpdateTimeframe: `ma_fast/ma_slow = ma_fast[1]/ma_slow[1]` (155–156); all 5 MA-position booleans on `[1]` (159–163); short-read guard tightened to `< 2` (145–147) so the closed-bar index is always valid; every direction branch (BULLISH 173 / BEARISH 179 / EARLY BEARISH 185 / EARLY BULLISH 191 / NEUTRAL 196) intact — NO logic cut. CalculateTrendStrength: ATR `CopyBuffer(...,0,0,2,atr)` count 1→2 and `atr[1]` (308–311) so the ATR denominator of `ma_separation` is the SAME closed bar `[1]` as the MAs — trend STRENGTH stays index-aligned with trend DIRECTION (no desync). `DetectHigherHighs/LowerLows` untouched (centered/structural). The binding constraint (ATR index MUST match MA index) is satisfied. Compile 0-err / 0-new-warn; count neither collapsed nor ballooned.
  - **The forming-bar `[0]` read was genuine look-ahead — closed-bar `[1]` is the only real-time-tradeable basis, NOT a lag the fix introduced.** `[0]` evaluated `ma_fast/ma_slow/close` against the still-FORMING H1 bar, whose `close[0]` is the live intrabar price — knowable only mid-bar and able to revert before the bar closes. On gold's wide H1 bars the pre-fix detector could flip `TREND_BULLISH` the instant price spiked above the fast MA intrabar (the EARLY-BULLISH branch front-runs confirmation by up to a full H1 bar). In a 1-min-OHLC tester that booked direction the EA "knew" at a moment a LIVE detector could not have known it. `[1]` is not "later than correct" — on a new H1 bar a live EA can only know `[1]` is final; `[0]` is unknowable. So the post-fix numbers are the HONEST ones, and per no-look-ahead + no-logic-cut we do NOT revert.
  - **The 2023 double-hit (worse on BOTH volume AND quality) is honest deflation of a YEAR-SPECIFIC look-ahead inflation, NOT a systematic lag problem.** Look-ahead removal does NOT predict a uniform bidirectional pattern like 2024 — it predicts: the deferred/dropped trades are the ones the forming-bar read FRONT-RAN, and their year's realized outcome leaves the books. The sign is path-dependent on whether those front-run entries were net-favorable in that year's price action:
    - **2024 (clean uptrend):** front-run bullish flips were below-average-R but net-positive fills. Removing them dropped nominal $ (−$286.85) but raised avg-R (+0.0176), WR, and total-R (+0.56R). You lose the inflated nominal, you KEEP and IMPROVE the honest expectancy — textbook deflation.
    - **2023 (rotational/two-sided year — gold spent 2023 in larger chop):** intrabar direction flips that revert before the close are the NORM in a rotational year, so the look-ahead was MORE valuable in-sample precisely because it was look-ahead. The LONG book +2.24R→−0.07R is the signature: deferring a long entry by one bar in an uptrend-biased book means you enter AFTER the favorable intrabar excursion the forming-bar read was illegitimately booking — an excursion no live EA could ever have captured. So 2023 is hit on BOTH axes (fewer front-run permits survive to entry AND the remaining real-time entries are genuinely worse than the look-ahead-flattered ones were) — exactly what you expect when the inflation was concentrated in one year's chop.
  - **The asymmetry IS the fingerprint that rules OUT a real lag problem.** A structurally-too-late trend read is late EVERY year and would degrade BOTH years symmetrically. Instead 2024 IMPROVED on quality (avg-R/WR/total-R all up) while 2023 fell — a uniform-lag defect cannot produce a fewer-but-BETTER 2024. Two confirmations: (1) SHORTS improved in BOTH years (2024 −3.82R→−2.73R; 2023 +6.93R→+4.02R, still solidly positive) — a genuine trend-direction lag would hurt shorts in a year it hurt longs; the damage is LONG-concentrated, which maps to gold's uptrend (the EARLY-BULLISH intrabar front-run is the path being removed). (2) The trade-count reduction is LONG-dominated (−17/−19 in 2024; longs −8 in 2023) — the EARLY-BULLISH front-run is exactly what's being deferred.
  - **No downstream remedy is required to close 1.3; selectivity tuning is an Iter-2/3 optimization on the honest baseline, not a repair of 1.3.** There is no tunable lag DEFECT here — 2023 is the honest expectancy of the real-time trend read in a rotational year, not a fixable bug. The levers that sit ON TOP of this honest input are already scheduled: Phase 2.2 `InpBOSFreshnessBars {4,6,8,12}` (avg-R sweep) and the 2.4-GATE tier re-derivation re-tune selectivity against the now-honest trend direction. Clawing back the 2023 $480 inside 1.3 would require re-introducing `[0]` (look-ahead) or widening logic — both forbidden, both wrong. Correct division of labor: 1.3 lays the honest direction input; 2.2/2.4 tune selectivity on it.
- **stok verdict:** **SIGN-OFF.** The forming-bar `[0]` trend read was genuine look-ahead (intrabar `close[0]`, knowable only mid-bar); closed-bar `[1]` is the honest real-time-tradeable basis and is NOT reverted (no-look-ahead + no-logic-cut). The 2023 net −43.3% / total-R −5.22R is honest deflation of a year-specific look-ahead inflation, NOT a systematic lag problem: the give-back sign is path-dependent on each year's chop, the damage is LONG-concentrated (gold's uptrend EARLY-BULLISH front-run), shorts improved in both years, and 2024 went fewer-but-BETTER — a uniform-lag defect could produce none of that asymmetry. Mechanism correct (ATR index matched to MA index), count not collapsed/ballooned, no logic cut. Selectivity recovery is correctly deferred to the Phase-2.2 `InpBOSFreshnessBars` avg-R sweep and the 2.4-GATE re-derivation that consume this honest input — NOT a repair owed by 1.3. No change requested in 1.3.

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** trade count must neither collapse nor balloon; the change must be mechanism-consistent (closed-bar re-timing of trend direction), NOT an edge amputation. Direction-mix shift acceptable if explained by the re-timing.
- **Result (mt5-developer view):** count bound **PASS** (no collapse/balloon, -8.7% 2yr). Mechanism-consistency **PASS** (closed-bar direction re-timing, ATR index matched to MA index, no logic cut). **OPEN FOR STOK:** the 2023 net -43.3% / total-R -5.22R is material and must be adjudicated by stok as removed look-ahead inflation (cf. 1.2's bidirectional give-back) vs an edge degradation warranting a loop-back. 2024 is fewer-but-better (avg-R +0.0176); 2023 is worse on net AND avg-R. This is a trading-impact judgment, not a compile/mechanism failure.

## mt5-developer sign-off
- Compile clean confirmed (0 errors, 0 new warnings vs the 38-baseline; verbatim Result quoted; no CTrendDetector errors). Mechanism implements the validated fix EXACTLY: `ma_fast/ma_slow/close` and the 5 MA-position booleans read closed bar `[1]` (UpdateTimeframe 153-163); the 4 diagnostic LogPrints re-pointed to `[1]` for consistency; ATR `CopyBuffer` count 1→2 and `atr[1]` used so the ATR index MATCHES the MA index (CalculateTrendStrength 304-310) — strength stays consistent with direction; short-read guard tightened to `< 2` so `[1]` is always valid. `DetectHigherHighs`/`DetectLowerLows` UNCHANGED (centered/structural). NO logic cut — every direction branch and the strength formula intact. file:line verified against current source. **QA backtest run + decoded:** XAUUSD+ H1 2024 & 2023 A/B captured from the FILE_COMMON CSVs; A-baseline reproduces post-1.2 numbers exactly. Count bound + mechanism-consistency PASS. — **SIGNED (code + mt5 QA).** stok trading-impact sign-off pending; flag the 2023 net/total-R drop for stok adjudication.

## stok sign-off
- **SIGNED (trading-impact). VERDICT: SIGN-OFF.** Look-ahead premise verified at source (`CTrendDetector.mqh`: MA/close + 5 MA-position booleans on closed bar `[1]`, ATR `atr[1]` index-matched, guard `< 2`, all 5 direction branches intact, no logic cut). Forming-bar `[0]` read tested direction against the unclosed bar's intrabar `close[0]` — genuine look-ahead; closed-bar `[1]` is the honest real-time-tradeable basis and is NOT reverted. The 2023 net −43.3% / total-R −5.22R is honest deflation of a YEAR-SPECIFIC look-ahead inflation (rotational 2023 had more reverting intrabar flips for the forming-bar read to illegitimately book), NOT a systematic lag problem — proven by the ASYMMETRY: 2024 fewer-but-BETTER (avg-R/WR/total-R up), 2023 worse, shorts improved in BOTH years, reduction LONG-dominated (EARLY-BULLISH intrabar front-run in gold's uptrend). A uniform-lag defect would degrade both years symmetrically and hurt shorts — it does neither. Selectivity recovery is correctly deferred to Phase-2.2 `InpBOSFreshnessBars {4,6,8,12}` (avg-R sweep) + the 2.4-GATE tier re-derivation, which tune on this now-honest trend input; it is NOT a repair owed by 1.3, and reverting to `[0]` or widening logic is forbidden. **ACCEPT → QA-PASS.**
- **Broader-inflation read (for the Iteration-1 boundary report, noted not acted on):** 1.2 (BOS/CHoCH trigger) and 1.3 (trend direction) have now BOTH deflated the backtest as forming-bar look-ahead came out — 1.2 −3.9% 2-yr (small/bidirectional), 1.3 −24.3% 2-yr (larger, as expected for the highest-leverage direction input feeding ~15 plugins). **My read so far: yes, the EA's prior headline backtest edge was MATERIALLY look-ahead-inflated, and 1.3 is the single biggest contributor seen to date.** This is consistent with — not a refutation of — the no-look-ahead mandate: we are converting an inflated-but-untradeable backtest into an honest, real-time-tradeable one. The remaining forming-bar fixes (1.4/1.5 regime, 1.8 MA200+swings, 4.8 trailing, 5.4 file-signal swing) should be EXPECTED to deflate further; the headline number that matters is the post-Iteration-1 honest baseline, against which Iter-2/3 selectivity tuning (`InpBOSFreshnessBars`, 2.4-GATE re-derivation, per-engine validation) earns edge back. FLAG for the boundary report: re-baseline all forward expectations and any v18 $6,140 reference to the post-look-ahead-removal figure — the pre-fix numbers are not a tradeable target. This is a watch item, not a stop-work item: each fix individually passed its kill-criteria and removing look-ahead is unambiguously correct.

## Decision
- **QA-PASS — both agents signed.** mt5-developer SIGNED (code + compile 0-err/0-new-warn + mechanism correct + ATR index matched to MA index + A/B decoded + count/direction-mix mechanism-consistent). **stok SIGNED → SIGN-OFF** (trading-impact: forming-bar `[0]` was genuine look-ahead; closed-bar `[1]` is the honest basis, NOT reverted; 2023 −43.3% is honest year-specific deflation NOT a systematic lag problem — proven by the 2024-better / 2023-worse asymmetry, shorts improving in both years, and the LONG-dominated reduction; selectivity recovery deferred to Phase-2.2 `InpBOSFreshnessBars` + 2.4-GATE, not owed by 1.3). No change requested in 1.3. **Cleared to COMMIT** as a separately-measurable Phase-1.3 commit. NEXT ACTION advances to Phase 1.4. Main session runs git; agents do NOT.

## Commit
- **Hash:** (not committed — stok closes the phase next)
- **Message:** (n/a)
