# Phase 1.1 — Liquidity pools: re-scan each bar, closed-bar reads, swept_time recency

## Phase
- **Phase id:** 1.1
- **Fix ids:** 1.1
- **Touches trading logic:** Y
- **depends_on (phases that must be COMMITTED first):** none (first phase of Iter 1)
- **Binding stok param/option used:** `SMC_SWEEP_RECENCY_BARS = 3` (≤5; longer re-creates stale-latch)

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/MarketAnalysis/CSMCOrderBlocks.mqh:18` — added `#define SMC_SWEEP_RECENCY_BARS 3` (named const, with comment: ≤5, longer re-creates stale-latch).
  - `…:62` — (C) added `datetime swept_time;` to `SLiquidityPool` (time of the closed bar that swept the pool).
  - `…:287` — (A) inserted `ScanForLiquidityPools();` in `Update()` AFTER `ScanForFairValueGaps()` (286) and BEFORE `DetectBreakOfStructure()` (288). Pools are now re-scanned every bar (was Init-only).
  - `…:1010-1013` — (B) `ScanForLiquidityPools()` `Copy{High,Low,Time}` start_pos `0→1` — equal-high/low scan now reads CLOSED bars only (skips the forming bar).
  - `…:1053, :1084` — initialize new `swept_time = 0` at both pool-creation sites (equal-highs + equal-lows).
  - `…:1097-1099` — (B) `UpdateLiquidityPools()` sweep-detection reads `iHigh/iLow(...,1)` (closed bar) instead of `(...,0)`; captured `closed_bar_time = iTime(...,1)`.
  - `…:1109, :1117` — (C) stamp `swept_time = closed_bar_time` at BOTH sweep sites (sell-side + buy-side).
  - `…:1126-1141` — (C) rewrote `CheckRecentLiquiditySweep()`: now requires `is_swept && swept_time>0 && (TimeCurrent()-swept_time) <= SMC_SWEEP_RECENCY_BARS*PeriodSeconds(PERIOD_H1)`.
- **Diff summary:** Repairs the DATA feeding `liquidity_swept`: pools are re-scanned each bar on closed data, sweeps are detected on the closed bar and time-stamped, and "recent sweep" is now a 3-H1-bar recency window instead of a permanent `is_swept` latch. The +15 confluence boost (GetConfluenceScore lines ~536/571) and `supports_long/short` (lines ~387/393) are UNCHANGED — only their input (`liquidity_swept`) is now correct.
- **Logic-cut check:** NO trading logic deleted/disabled. No toggle/registration park. The +15 boost and supports_long/short branches are byte-for-byte intact; only `CheckRecentLiquiditySweep` (the boolean source) and the scan/update read indices changed.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 7419 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (exactly the 38 pre-existing warnings; none in CSMCOrderBlocks.mqh).
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — `/mnt/c/Trading/UltimateTrader/UltimateTrader.ex5`, 890450 bytes, 2026-06-26 13:37:39 (pre-fix HEAD binary was 889424 bytes).

## QA backtest (decoded, ACTUAL — never estimated)
- **Config (actual):** XAUUSD+ H1, Model=1 (1-min OHLC), deposit 10000 USD / leverage 100, Optimization 0 / Visual 0 / ShutdownTerminal 1, EA-default production inputs (no .set → InpSymbolProfile=XAUUSD, InpSignalSource=BOTH w/ empty InpSignalFile so pattern-engine only, InpEnableMultiStrategy=false). A = pre-fix HEAD binary `/tmp/UltimateTrader_baseline.ex5` (889424 B); B = post-fix `UltimateTrader.ex5` (890450 B). Both run sequentially against VantageMarkets-Live XAUUSD+ history (now logged in). Tester started cleanly (prior "account not specified" abort resolved). All four runs "Test passed".
- **CSV column mapping (confirmed against header):** col 1 = RowType (rows matching /EXIT/ = closed trades), col 45 = PnL_Money ($), col 46 = PnL_R (R-multiple). EA SESSION SUMMARY reconciles with the CSV parse.

  **2024 (2024.01.01→2025.01.01):**
  | Build | Trades | WR% | Net $ | Total-R | Avg-R |
  |---|---|---|---|---|---|
  | A (pre-fix) | 157 | 45.86 | 2289.62 | 18.15 | 0.1156 |
  | B (post-fix) | 157 | 45.86 | 2289.62 | 18.15 | 0.1156 |
  | Δ | 0 | 0.00 | 0.00 | 0.00 | 0.0000 |

  **2023 (2023.01.01→2024.01.01):**
  | Build | Trades | WR% | Net $ | Total-R | Avg-R |
  |---|---|---|---|---|---|
  | A (pre-fix) | 124 | 36.29 | 999.48 | 6.35 | 0.0512 |
  | B (post-fix) | 124 | 36.29 | 999.48 | 6.35 | 0.0512 |
  | Δ | 0 | 0.00 | 0.00 | 0.00 | 0.0000 |

  (2022 and earlier skipped — only 2023/2024 needed for this A/B; both completed in ~9–11 s.)
- **Delta + explanation:** **Zero delta, trade-for-trade and to the cent, both years.** This is the touches-logic fix being **behavior-neutral on the default production config**, and the mechanism is fully explained (NOT an unreconciled divergence):
  - The +15 liquidity boost reaches the production path through `CMarketContext::GetSMCConfluenceScore` → consumed at exactly two non-engine sites: (i) `CConfluenceScorer.mqh:130` as a binary `GetSMCConfluenceScore(dir) > 0` gate, and (ii) `CSignalValidator.mqh:171/195` as `confluence_score`, rejected only if `< 40`.
  - At site (i) the score base is 50; ±15 can never flip the `>0` boolean → non-decisive.
  - At site (ii) ±15 only changes a pass/reject if a signal's score sits in the 40–55 band straddling the 40 floor. No 2023/2024 signal's decision crossed the 40 boundary because of the recency change → identical trade set.
  - The engines that weight SMC confluence heavily and tier-by-it (`CLiquidityEngine`, `CTrendContinuationEngine` — 11 + 3 call sites) are OFF by default (`InpEnableMultiStrategy=false`), so the recency correction has no production-path lever today.
  - Net result: the fix removes the stale permanent-latch DATA defect (correctness) without moving any decision that fired on this config. Conservative, expected, clean.
- **No-op / byte-identical claim (if applicable):** Not claimed as a no-op (this is touches-logic Y), but the A/B IS trade-for-trade / $-for-$ / R-for-R identical on the XAUUSD+ H1 production config — and that identity is mechanism-explained above, not coincidental.
- **SWEPT-throughout check (Build B, 2024):** **PASS — fires throughout the year.** `liquidity SWEPT` is emitted via `Print` to the tester journal (`…/Tester/logs/20260626.log`), not the FILE_COMMON run log. Build-B 2024 slice: **58,915 occurrences**, FIRST `2024.01.02 01:00:00 — Buy-side liquidity SWEPT at 2064.58`, LAST `2024.12.31 23:00:00 — Buy-side liquidity SWEPT at 2622.31`, distributed across all 12 months (~3,900–6,200/month). Confirms the per-bar re-scan works and `liquidity_swept` is alive (not a one-shot startup latch). Samples: `2024.02.01 Sell-side @2039.62`, `2024.05.01 Buy-side @2293.24`, `2024.08.01 Sell-side @2429.06`, `2024.11.01 Sell-side @2743.84`.

## stok review + verdict
- (pending — stok QA comes next per task; numbers below are ready to hand over)

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** `liquidity_swept` is a same-sign tie-breaker (+15), not a trigger — validate it still fires meaningfully after the recency gate; SMC_SWEEP_RECENCY_BARS must not exceed ~5.
- **Result:** **PASS.** SMC_SWEEP_RECENCY_BARS=3 (≤5). `liquidity_swept` fires meaningfully after the gate (58,915 hits across all of 2024, not dead) — but at recency=3 it is NON-DECISIVE on the default config (no tier/gate boundary crossed → zero A/B delta). Net P&L and trade set are not worse (identical). No kill-criterion tripped.

## mt5-developer sign-off
- Compile clean confirmed (0 errors, 0 new warnings vs 38-baseline; verbatim Result quoted above). Mechanism implements the validated fix exactly — (A) per-bar re-scan inserted after FVG/before BOS; (B) closed-bar reads in scan + UpdateLiquidityPools; (C) swept_time field + stamping + 3-bar recency gate. file:line verified against current source. NO trading logic cut; +15 boost and supports_long/short intact. **QA backtest run + decoded:** XAUUSD+ H1 2023 & 2024 A/B is trade-for-trade identical, mechanism-reconciled (boost non-decisive at current thresholds; SMC-tiering engines OFF by default), SWEPT-throughout PASS. — SIGNED (code + mt5 QA). stok trading-impact sign-off still pending.

## stok sign-off
- **VERDICT: SIGN-OFF (pass → proceed to commit).** — stok, 2026-06-26.

- **Q1 — Zero A/B delta acceptable/expected? YES.** This is a correctness repair to the DATA feeding `liquidity_swept` (removes the stale one-shot `is_swept` latch), and the +15 boost is structurally non-decisive on the default production config: it cannot flip the `GetSMCConfluenceScore(dir) > 0` flow gate (base 50), it only matters at the `<40` reject floor for a score in the 40–55 straddle band (mt5 confirmed no 2023/2024 decision crossed 40 from the recency change), and the SMC-tiering engines (CLiquidityEngine/CTrendContinuationEngine) that weight/tier BY this score are OFF by default (`InpEnableMultiStrategy=false`). Trade-for-trade / $-for-$ / R-for-R identity is therefore the EXPECTED correct outcome — behavior-neutral now, correct once Iter-3 consumers turn on. Not a concern.

- **Q2 — Is recency=3 the right BINDING value into Iter-3? SHIP 3, BUT SWEEP IT in Iter-3 (do NOT lock 3 as final).** 3 is the correct conservative SHIP value for THIS phase: ≤5, removes the latch, provably behavior-neutral (cannot regress production). But as a forward tie-breaker it is likely TOO TIGHT: a 3-H1-bar window is ~3 hours, and although the per-bar re-scan prints ~3,900–6,200 swept-bar reads/month, distinct FRESH sweeps occur only every few days — so on most engine-signal bars in Iter-3 the most recent sweep will already be stale and the +15 will be DARK, contributing nothing to A/A+ tiering at the margin. Re-deriving the value NOW is blind tuning (consumers OFF → no A/B signal to optimize against). My structural prior: the winner lands at 5–8, not 3. This must be MEASURED on the engine-on distribution, not asserted.

- **BINDING INSTRUCTION FOR PHASE 3.x (Iteration-3 engine validation):** When CLiquidityEngine / CTrendContinuationEngine are turned ON and `GetSMCConfluenceScore` becomes decisive, **sweep `SMC_SWEEP_RECENCY_BARS ∈ {3, 5, 8}` per engine and pick by avg-R (NOT by trade count or by how often the boost fires)** — same methodology the plan prescribes for `InpBOSFreshnessBars`. Promote `SMC_SWEEP_RECENCY_BARS` from a `#define` to an input (e.g. `InpSMCSweepRecencyBars`, default 3, floor 3 / cap 8) at that point so the sweep is runnable without recompiling per value. Kill-guard: do NOT exceed 8 (beyond ~5–8 the recency window re-approaches the stale-latch behavior this fix removed). The shipped 3 is a baseline anchor for that A/B, not a final tuning constant.

- **Hand-off numbers (for record):** 2024 A/B 157 trades / 45.86% WR / $2289.62 / 18.15R / 0.1156 avg-R IDENTICAL A=B; 2023 A/B 124 / 36.29% / $999.48 / 6.35R / 0.0512 IDENTICAL A=B; SWEPT 58,915 hits across all 2024 (per-bar re-scan confirmed alive, not a startup latch).

## Decision
- **mt5 QA-DONE — code complete, compiles 0-err/0-new-warn, A/B run + decoded + reconciled, SWEPT-throughout PASS. Awaiting stok trading-impact sign-off.** Do NOT commit / do NOT advance NEXT ACTION (stok QA + user commit come next, per task). On stok ACCEPT → set QA-PASS, then COMMITTED.

## Commit
- **Hash:** (not committed — per task instruction)
- **Message:** (n/a)
