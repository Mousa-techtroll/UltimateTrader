# Phase 6.3+6.4+6.6 — Logger Initialize/throttle + CFileEntry ATR-handle release + exit-plugin valid=shouldExit

> Iteration 6, three bundled BYTE-IDENTICAL hygiene fixes (regression guards; mechanics/consistency only, off the decision path). Bounded to ≤2 backtest runs (1 per A/B leg).

## Phase
- **Phase id:** 6.3+6.4+6.6
- **Fix ids:** 6.3 (Infra-6 Logger never Initialize()d), 6.4 (Entry-File-1 CFileEntry leaks ATR handle), 6.6 (exit plugins set shouldExit not valid)
- **Touches trading logic:** N (all three; logger off the decision path, ATR-handle release at Deinit, exit valid-flag consistency with live OR-guard already keying on shouldExit)
- **depends_on:** Phase 6.2 COMMITTED (HEAD 14fcb84) — yes
- **Binding stok param/option used:** n/a (no-op / mechanics hygiene)

## What changed (verified file:line against current source)

### FIX 6.3 — Logger.mqh + UltimateTrader.mq5
- `UltimateTrader.mq5` OnInit (after `g_isBacktesting` set at :475): inserted
  `Log.Initialize("", LOG_LEVEL_INFO, g_isBacktesting ? LOG_LEVEL_NONE : LOG_LEVEL_WARNING);`
  — backtest => file threshold NONE => NO file logging => no per-write close/reopen; live => WARNING-and-above to file; console stays INFO.
- `Logger.mqh` :40-41 — new members `m_rotationCheckInterval` (=256) + `m_writesSinceRotation`.
- `Logger.mqh` ctor :330-331 — init both (256 / 0).
- `Logger.mqh` Initialize() :367 — reset `m_writesSinceRotation = 0` on (re)init.
- `Logger.mqh` LogInternal() :499-510 — increment `m_writesSinceRotation` per file write; the close/reopen+size-probe rotation block now gated additionally on `m_writesSinceRotation >= m_rotationCheckInterval` (256), resetting the counter when it runs. Previously ran on EVERY file write.

#### LOG_LEVEL ordinal verification (CRITICAL — confirms live WARNING/ERROR NOT silenced)
`Include/Common/Enums.mqh` :163-171 `ENUM_LOG_LEVEL`:
```
LOG_LEVEL_NONE=0, LOG_LEVEL_DEBUG=1, LOG_LEVEL_SIGNAL=2, LOG_LEVEL_INFO=3,
LOG_LEVEL_WARNING=4, LOG_LEVEL_ERROR=5, LOG_LEVEL_CRITICAL=6
```
File-emit gate (Logger.mqh :474): `level >= m_fileLogLevel`. With file threshold = `LOG_LEVEL_WARNING`(4):
WARNING(4)>=4 ✅ emitted, ERROR(5)>=4 ✅ emitted, CRITICAL(6)>=4 ✅ emitted; INFO(3)/SIGNAL(2)/DEBUG(1) < 4 suppressed.
Ordinals are NOT inverted — passing WARNING as the live file threshold does NOT silence live WARNING/ERROR/CRITICAL. Threshold as specified is correct; no fix needed.
Logger off the decision path: confirmed — no logged-value COMPUTATION feeds a trade decision (Log.* calls are pure side-effect emission).

### FIX 6.4 — CFileEntry.mqh
- :192 — corrected the false comment "Uses persistent handle — DO NOT release (shared with other components)" -> "Lazily created by CFileEntry (NOT shared) — released in Deinitialize()."
- Deinitialize() :668+ — added guarded `if(m_atr_handle != INVALID_HANDLE) { IndicatorRelease(m_atr_handle); m_atr_handle = INVALID_HANDLE; }` before ArrayFree. Handle is lazily created in GetCurrentATR (:197-198 `iATR(symbol, PERIOD_H1, 14)`), owned by CFileEntry; was never released.

### FIX 6.6 — 4 exit plugins (9 emission sites; `signal.shouldExit = true;` -> `signal.shouldExit = signal.valid = true;`)
- `CRegimeAwareExit.mqh` :162, :173, :186, :196 (4)
- `CDailyLossHaltExit.mqh` :182, :204 (2)
- `CMaxAgeExit.mqh` :109 (1)
- `CWeekendCloseExit.mqh` :138, :157 (2)
`ExitSignal` (Structs.mqh :677) has both `bool shouldExit;` and `bool valid;` (valid = "Alias for shouldExit (AICoder V1 compatibility)"); Init() zeros both. Emission previously set only shouldExit -> a base-class scan keying on `valid` would miss them (latent trap). Live path's OR-guard already keys on shouldExit (currently safe); this sets both for consistency.

### Logic-cut check
NO trading logic deleted/disabled. All three are additive mechanics/consistency hygiene.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 8785 ms elapsed, cpu='X64 Regular'   (B leg = all 3 fixes)
```
- Errors: 0  | New warnings vs 38-baseline: 0 (all 38 pre-existing; Logger.mqh warns at :114/:235/:243/:251/:534 are pre-existing ulong/uint conversions, NOT in added code; no warning in CFileEntry.mqh or the 4 exit plugins).
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — repo `UltimateTrader.ex5` 923676 bytes, md5 `b7792c63…`.

## 3b binary-binding assertions (load-path md5 == fresh-build md5)
- **B (all 3 fixes):** FRESH(repo)=`b7792c6394eeb6467c13473501bb3fc0` == LOAD(`UltimateTrader_p63B.ex5`)=`b7792c6394eeb6467c13473501bb3fc0` -> PASS.
- **A (committed HEAD 14fcb84):** FRESH(repo)=`f74df99739e29bac97f2fcf32120c098` == LOAD(`UltimateTrader_p63A.ex5`)=`f74df99739e29bac97f2fcf32120c098` -> PASS.
- **Distinct A vs B:** A `f74df997…` != B `b7792c63…` (genuinely two binaries; B carries the 3 fixes). Both compiled `0 errors, 38 warnings` (A 8805 ms, B 8785 ms). Both != stale `9dbe9052`.
- **Method:** B compiled first from working tree; then `git stash` exposed committed HEAD 14fcb84 for A's source; A compiled+bound; `git stash pop` restored B source (all 9 6.6 sites + 6.3 + 6.4 confirmed present).

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, Model=1, full **2019.01.01–2026.06.27**, 396-param production-default `[TesterInputs]` (`InpEnableMultiStrategy=false`, `InpSignalSource=2`=BOTH, `InpSymbolProfile=0`). Deposit 10000 USD / leverage 100 / `ShutdownTerminal=1`. Inline `[TesterInputs]` cloned verbatim from the proven `backtest_p62A.ini` (the Phase-6.2 A leg that reproduced the baseline); A vs B `[TesterInputs]` diff = EMPTY (identical) — only `Expert=`/`Report=` differ. State file relocated so BOTH legs start on clean recovery state (identical start condition). 1 run each = **2 runs (bound met)**.
- **Mode:** 1-min OHLC (Model=1).
- **A (committed HEAD 14fcb84):** reproduces binding baseline — 4/4 EA-CSV md5 EXACT: Stats `3b13ea8feded6f2faec6b5a157ab05db`, TradeEvents `2222e2f955ea0eef99b50f7b4c52ebff`, Candidates `77b07fb1fc2982aaed4899b823bb0e26`, Risk `930cce92cb33e31a90a6be9a66e3946a` => Net $17,455.34 / PF 1.28 / 1742 trades / Eq-DD 12.64% (= recorded binding baseline by md5 identity).
- **B (all 3 fixes):** 4/4 EA-CSV md5 = Stats `3b13ea8f…`, TradeEvents `2222e2f9…`, Candidates `77b07fb1…`, Risk `930cce92…` — identical to A.
- **No-op / byte-identical claim:** **YES — A == B BYTE-IDENTICAL on ALL 4 EA CSVs (md5-EQUAL AND `cmp` byte-for-byte CLEAN).** No STOP condition tripped: 6.3 logger Initialize changed NOTHING in the 4 CSVs (logger confirmed off the decision path); 6.6 did NOT change exit behavior (live OR-guard already keys on shouldExit); 6.4 ATR-release at Deinit had no trade impact.

### Per-fix byte-identity mechanism (why each is byte-identical)
- **6.3 (Logger):** In the tester, `g_isBacktesting=true` => `Log.Initialize("", LOG_LEVEL_INFO, LOG_LEVEL_NONE)` => file threshold NONE => the entire `shouldLogToFile` branch (incl. the throttled rotation block) is DEAD; no file is opened/rotated. The throttle counter `m_writesSinceRotation` is only touched inside that dead branch, so it never runs. The only observable effect is the one-time console line "Logging system initialized" per pass. Logged values are pure side-effect emission — none feed a trade decision. -> CSVs unchanged.
- **6.4 (CFileEntry ATR release):** `Deinitialize()` runs once at end-of-test, AFTER all trading. `IndicatorRelease` on the lazily-created ATR handle frees a resource; it cannot alter any already-recorded trade. In the tester `telegram_signals.csv` is absent, so the handle is typically never even created (GetCurrentATR never called) — the guard `if(m_atr_handle != INVALID_HANDLE)` is false and the body is skipped. -> CSVs unchanged.
- **6.6 (exit valid=shouldExit):** The live exit dispatch's OR-guard already keys on `signal.shouldExit`; setting `signal.valid` additionally is read by NO live code path (the base-scan-on-`valid` that would have been the trap is not exercised). Adding the extra assignment is semantically inert on the dispatch. -> exit decisions and CSVs unchanged.

### LOGGER RECURSION DETECTED — diagnosed benign (not a STOP)
Journal shows one "LOGGER RECURSION DETECTED: Logging system initialized" per EA init (1:1 with "INITIALIZATION COMPLETE" — once-per-pass, NOT per-tick). Mechanism: `Initialize()` line 385 calls `Log(...)` while the init chain is conceptually "inside logging"; the recursion guard catches that one decorative self-log and `return`s cleanly. `m_isInitialized` is already true (set line 384) so no re-init loop; init completes normally ("INITIALIZATION COMPLETE" prints right after). Identical guard fires on HEAD/A via the FIRST lazy `Log.*` call (HEAD has 0 explicit `Log.Initialize`, so the first infra `Log` self-inits and trips the same guard). It is a console `Print` side-effect only — not a CSV write, not on the decision path; A==B byte-identity confirms zero trade impact. The 34GB tester journal is driven by the EA's pre-existing ~90 direct `Print()` diagnostics across a 7.5-yr H1 run, NOT by this once-per-pass line; with file logging OFF in backtest, 6.3 adds no per-tick volume (and removes the live per-write file close/reopen — the original perf defect).

## stok review + verdict
**SIGNED — ACCEPT** (binding, clean). stok verified all three at SOURCE (not from the dossier):
- **6.3 CORRECT:** ordinals sound (Enums.mqh:163-172 ascending NONE0<…<WARNING4<ERROR5<CRITICAL6; file-emit gate `level>=m_fileLogLevel` Logger:479 with WARNING threshold emits WARNING/ERROR/CRITICAL, suppresses INFO/SIGNAL/DEBUG → live WARNING/ERROR genuinely NOT silenced). Backtest=NONE → OpenLogFile skipped (:370), shouldLogToFile permanently false, the throttled rotation block (:499-547) is dead in the tester + is also the LIVE perf fix (removes ~99.6% of the per-write close/reopen+size-probe I/O while preserving rotation). Logger OFF the decision path — byte-identical A/B (md5+cmp all 4) is SUFFICIENT proof (any logged value feeding a trade would have perturbed a CSV byte; it didn't), corroborated by source mechanism.
- **"LOGGER RECURSION DETECTED" line = BENIGN, ignore.** Mechanism: LogInternal sets static inLogCall=true (:439); Initialize then calls Log(...,"Logging system initialized") which re-enters while inLogCall is still true → the guard (:431-435) prints once and returns CLEANLY (m_isInitialized already true :384 → NO re-init loop, no recursion proceeds — the guard is doing its job). Once-per-init (1:1 with INITIALIZATION COMPLETE, not per-tick), fires identically on HEAD via lazy-init (:449-451), Print-only (not a CSV write, not on the decision path) — A==B confirms zero trade impact. NOT a follow-up worth opening.
- **6.4 CORRECT:** m_atr_handle owned ONLY by CFileEntry (decl :193, seed INVALID :629, lazy iATR :197-198, guarded release :671-675 + reset to INVALID). The other GetCurrentATR hits are DIFFERENT classes with their own handles → no shared handle → the old "shared — DO NOT release" comment was genuinely WRONG. No double-release (guard + immediate reset → 2nd Deinit is a no-op); no use-after-release (release after all trading; GetCurrentATR re-checks INVALID and would re-create).
- **6.6 CORRECT — and inert by MECHANISM, not just byte-identity:** the live exit dispatch is an OR-guard `if(exit_sig.valid || exit_sig.shouldExit)` (CPositionCoordinator:2028, :3097) → already fired on the shouldExit arm before the fix, still fires after → dispatch outcome identical. All 9 sites (4/2/1/2) verified present + correctly chained (`valid` and `shouldExit` both =true). Removes a latent base-scan trap (a future base-class scan keying on `valid` alone would have silently missed these exits). ExitSignal carries both fields (Structs:677-697), both zeroed in Init().
- **Verdict: ACCEPT.**

## Residual notes (stok; neither warrants a loop)
- (cosmetic, optional) The recursion line — only if console-noise hygiene is later prioritized (make the in-Initialize confirmation a direct Print, or move inLogCall=true after the early-init block). NOT opening a follow-up.
- (consistency, not a defect) 6.6 leaves 9 sites setting BOTH flags while the dispatch OR-guards both; the eventual defensively-correct end state is to collapse to one canonical flag — a deprecation-direction note for whoever next touches the exit base, NOT in scope.
- 6.3's live behavior change (WARNING+ now actually written to a file in production, where before there was effectively no initialized file logging) is INTENDED + desirable; backtest can't exercise it → byte-identity is the right + only available regression guard (the live file-emit path is logic-inert by construction).

## Kill-criteria check
- **Phase kill-criterion:** A==B must be BYTE-IDENTICAL on all 4 CSVs (regression guards). If 6.3 changes any CSV -> STOP (logger not off decision path). If 6.6 changes exit behavior -> STOP.
- **Result:** **PASS** — A==B byte-identical (md5-equal + cmp-clean) on all 4 CSVs; A reproduces the binding baseline exactly. No STOP condition tripped.

## mt5-developer sign-off
- Compile clean: `0 errors, 38 warnings` both legs (0 new warnings). 3b enforced: A `f74df997…` / B `b7792c63…` (distinct binaries; load==fresh asserted both legs; both != stale `9dbe9052`). A reproduces binding baseline 4/4 CSV md5. All file:line verified against current source; 9 6.6 sites + 6.3 (init + throttle) + 6.4 (comment + guarded release) applied; NO logic cut. A==B byte-identical proven (md5+cmp). Mechanism per fix documented + byte-identity explained. — **SIGNED (mt5-developer)**

## stok sign-off
**SIGNED — ACCEPT** (binding, clean; source-verified all 3 + the recursion-line mechanism). No logic cut, no double-release, no exit-behavior change. See stok review above.

## Decision
- **DONE — both agents signed (mt5 implementation-correct + byte-identical proven; stok ACCEPT, binding, source-verified). Committed source-only on `feat/multi-strategy`.** 3 byte-identical hygiene fixes (6.3 logger Initialize + 256-write rotation throttle + live perf fix; 6.4 guarded ATR-handle release + corrected false comment; 6.6 valid=shouldExit at all 9 exit sites, inert by the dispatch OR-guard + removes the base-scan trap). A/B byte-identical (md5+cmp all 4 CSVs; 3b distinct binaries, A reproduced HEAD). LOGGER RECURSION line diagnosed BENIGN. NEXT ACTION → Phase 6.5.

## Commit
- **Hash:** `<filled at commit>` (source-only: UltimateTrader.mq5 + Logger.mqh + CFileEntry.mqh + CRegimeAwareExit.mqh + CDailyLossHaltExit.mqh + CMaxAgeExit.mqh + CWeekendCloseExit.mqh + 6-3_6-4_6-6.md + 00-STATUS.md; `.ex5` intentionally uncommitted). Branch `feat/multi-strategy`.
