# Phase 3.10 — Expansion Engine (eng 4): per-engine isolated base run (data-ready)

> Iteration-3b per-engine validation. Engine: `CExpansionEngine` (major engine 4).
> ONE bounded multi-year tester run, isolated via the 3.7/3.8-CONFIRMED `[TesterInputs]` method.
> This file holds the BASE-RUN DATA + single-registration proof. The stok KEEP/CUT ruling is the
> separate follow-up (00-STATUS row = "validation-data-ready (mt5) — stok to rule"). NOT committed.

## Phase
- **Phase id:** 3.10
- **Fix ids:** 3.7 (umbrella) — Expansion per-engine validation
- **Touches trading logic:** N (toggle/registration-only isolation; EA source unchanged at HEAD)
- **depends_on:** 2.4-GATE (COMMITTED), 3.5/3.6 HTF short veto ON
- **Binding stok params used (production defaults, NOT swept here):** `InpBOSFreshnessBars=8`,
  `InpSpineMinConfluence=25`, `InpDealingRangeD1Lookback=20`

## Isolation method (3.7/3.8-AUTHORITATIVE — `[TesterInputs]`, legacy fully OFF) + the double-reg resolution
- `.ini`: `/mnt/c/Users/nullkuhl/AppData/Roaming/MetaQuotes/Terminal/725B72F25E46C780EF59F57016D58156/backtest_3-10.ini`
- `[TesterInputs]` block (NOT `ExpertParameters=<file>.set`): `InpEnableMultiStrategy=true`,
  `InpEnableExpansionEngine=true` + `InpEnableEngineExpansion=true`; `InpEnableEngineTrend/Reversal/Range=false`;
  ALL legacy pattern plugins + other legacy engines OFF; `InpSignalSource=2` (=`SIGNAL_SOURCE_BOTH`, which is
  what makes `register_patterns=true` so the engine reg gate at UltimateTrader.mq5:687 passes — verified, ordinals
  PATTERN=0/FILE=1/BOTH=2 in Enums.mqh:116-118); `InpSymbolProfile=0`; `InpDisableAutoKill=true`; floors at
  production default (`InpBOSFreshnessBars=8`, `InpSpineMinConfluence=25`, `InpDealingRangeD1Lookback=20`).

### RESOLUTION of the prompt's "double-registration" caveat — REPO WINS
The prompt instructed `InpEnableEngineExpansion=true` + `InpEnableExpansionEngine=false` "so only the ROUTER
instance registers." **Source contradicts this and the repo wins:**
- Expansion has exactly ONE `RegisterEntryPlugin` path, gated SOLELY by `InpEnableExpansionEngine`
  (`UltimateTrader.mq5:687-692` — `new CExpansionEngine(...)`, `ConfigureModes(...)`, `RegisterEntryPlugin(g_expansionEngine, true)`).
- `InpEnableEngineExpansion` (Inputs:502, comment "router-driven; legacy reg unchanged") is **referenced NOWHERE
  in UltimateTrader.mq5** — it is a dead/decorative input for Expansion. (Trend/Reversal/Range each DO have an
  `InpEnableEngine*`-gated `new`+`RegisterEntryPlugin` at 731-746; Expansion does NOT — it is the EXISTING legacy
  instance, additively wired to the router.)
- The "router instance" (UltimateTrader.mq5:749-757) is NOT a second register — it only calls `SetScorer(...)` +
  `g_regimeRouter.RegisterEngine(g_expansionEngine)` on the SAME already-existing instance. `CRegimeRouter.RegisterEngine`
  (CRegimeRouter.mqh:130-136) appends to the router's internal `m_engines[]` for activation-weighting ONLY — it does
  NOT touch `g_entryPlugins[]` (the array the orchestrator polls). So there is NO double-registration in current source.
- Therefore the prompt's `InpEnableExpansionEngine=false` would yield **ZERO** Expansion plugins (engine never
  created; `g_expansionEngine` stays NULL; router block at 753 skipped). The CORRECT isolation of exactly one
  Expansion engine requires `InpEnableExpansionEngine=true`. Used that. Activation confirmed: the router gives
  ENGINE_EXPANSION weight 1.0 (volatile) / 0.5 (trending) / 0.25 (reversal) / 0 (balance) (CRegimeRouter.mqh:104-109),
  and `SetActivationWeight` sets `m_isEnabled = (w>0)` (CMajorStrategyEngine.mqh:124-127), so the engine activates.
- Paths: Vantage Markets MT5 Terminal binaries / data dir `nullkuhl` / terminal ID `725B…58156` / NO `/portable`.
- XAUUSD+, H1, **Model=1 (1-min OHLC)**, deposit 10000 USD, leverage 100, ShutdownTerminal=1.
- Window: **2019.01.01 → 2026.06.27**, single multi-year pass (NOT optimization). Wall-clock: launched 01:04:47,
  terminal exited ~01:06:18 (~90s).

### ISOLATION VERIFIED — exactly 1 plugin registered, NO double-reg, NO legacy (this run, journal 01:05:02)
From tester journal `725B…/Tester/logs/20260627.log`, MY run segment (loaded 231620 bytes of inputs at 01:05:02,
the LAST "input parameters loaded" in the 4.8 GB journal — nothing after it):
```
01:05:02  InpEnableExpansionEngine=true
01:05:02  InpEnableEngineExpansion=true
01:05:02  CExpansionEngine initialized on XAUUSD+ PERIOD_H1 | IC_mult=1.8 CompMinBars=8 MinSL=512.972 | composed: VolBO=on SessionBO=on
01:05:02  [Init] Registered entry plugin: ExpansionEngine
01:05:02  [Init] Entry Plugins: 1 registered (incl. engines)
01:05:02  CSignalOrchestrator: Registered plugin 'ExpansionEngine' (#1)
```
- **`[Init] Entry Plugins: 1 registered` = ExpansionEngine ONLY.** In my segment there is exactly ONE
  `Registered entry plugin` line (ExpansionEngine) — no second Expansion instance, no legacy plugin. Independently
  corroborated by the fresh EA log (`Common/Files/UltTrader_Log_XAUUSD+_20190101.log`): both ENTRY lines stamped
  `Engine: ExpansionEngine/MODE_LONDON_BREAKOUT`, ZERO legacy-plugin entries. Isolation is clean (no double-reg, no legacy).
- NOTE for the next agent: the 4.8 GB journal accumulates EVERY run today; earlier segments are STALE prior runs.
  Select MY run by the 01:05:02 marker (231620 bytes loaded), which is the last input-load in the file.

## Compile result (verbatim) — recompiled from clean-HEAD source before the run
```
Result: 0 errors, 38 warnings, 10408 ms elapsed, cpu='X64 Regular'
```
- Errors: 0 | New warnings vs 38-baseline: 0 (matches documented 38 pre-existing).
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — `UltimateTrader.ex5` 907398 bytes @ 01:04, then copied to the 725B `MQL5/Experts/`.
- EA source diff vs HEAD (`git diff HEAD -- '*.mqh' '*.mq5'`) = EMPTY. Only the `.ex5` shows modified (recompile artifact).

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, 2019.01.01–2026.06.27, Model=1 (1-min OHLC), isolated ExpansionEngine ONLY (1 plugin).
- **Source of numbers:** per-trade `Total_PnL`/`Total_R` from the Stats CSV
  (`Common/Files/UltTrader_Stats_XAUUSD+_20190101_0000.csv`, parsed with Python csv — naive awk mis-splits because
  `LastTrailReason` values contain commas). Cross-validated 3 ways: Stats CSV (2 EXIT rows) = EA log (2 EXIT lines) =
  `backtest_results_20260626.csv` aggregate ("Asian Breakout London" 2t / PF 0.06 / NetProfit −40.37 / AvgR −0.52).
  \* maxDD = realized closed-equity peak-to-trough (the EA MaxDD column is a 0.00 placeholder; no tester .htm emitted).
  With only 2 trades the realized DD ≈ the single −$42.85 loser. Adequate for the PF/avg-R kill-criterion.

### Isolated Expansion — FULL vs OOS
| Window | Trades | Net $ | PF | avg-R | maxDD* | WR |
|---|---|---|---|---|---|---|
| **Full 2019–2026** | 2 | **−$40.37** | **0.06** | **−0.515** | ≈$42.85 | 50.0% (W1/L1) |
| **OOS 2024–2026** | **0** | — | — | — | — | — (no fills) |

Per-year fills: 2021:1 (LONG, loss), 2022:1 (SHORT, win). **Zero fills 2019, 2020, 2023, 2024, 2025, 2026.**
The entire realized sample is 2 trades in 2021–2022; OOS (2024–2026) is EMPTY.

### PER-MODE BREAKDOWN (the key deliverable)
Mode bucket = Stats CSV `EngineMode` col (both ENTRY+EXIT rows). The two closed trades:
- tk2 LONG  2021.12.01 `MODE_LONDON_BREAKOUT` "Asian Breakout London": Total −$42.85 / −1.07R / LOSS
- tk4 SHORT 2022.01.06 `MODE_LONDON_BREAKOUT` "Asian Breakout London": Total +$2.48 / +0.04R / WIN (SAVED_BY_TP0)

| Mode | Trades | Net $ | avg-R | WR | Kill-criterion (PF<1.0 OR avg-R<0, full AND OOS) |
|---|---|---|---|---|---|
| **MODE_LONDON_BREAKOUT** (composed Session-BO / Asian Breakout London) | 2 | −$40.37 | −0.515 | 50.0% | **TRIPS** — PF 0.06 & avg-R<0 full; 0 OOS → stays OFF |
| **MODE_INSTITUTIONAL_CANDLE** (IC Breakout) | **0** | — | — | — | NO DATA (0 closed) → cannot certify; stays OFF |
| **MODE_COMPRESSION_BO** (Compression BO) | **0** | — | — | — | NO DATA (0 closed) → cannot certify; stays OFF |
| **Composed Vol-Breakout** (Donchian/Keltner, tagged MODE_COMPRESSION_BO) | **0** | — | — | — | NO DATA (0 closed) → cannot certify; stays OFF |
| **MODE_PANIC_MOMENTUM** | **0** | — | — | — | DEAD `if(false)` (removed Phase D, CExpansionEngine.mqh:462-463) — confirmed 0, as expected |
| MODE_NONE | 0 | — | — | — | none observed |

### Signal-generation vs fills (why only 2 trades in 7.5 years)
The engine is WORKING and ACTIVE, not silent. In my segment it **GENERATED 43 signals** that reached the
orchestrator (`>>> Plugin 'ExpansionEngine' generated signal`):
- 30 IC Breakout (Long 14 / Short 16, across Consol=2..5 bars) — `MODE_INSTITUTIONAL_CANDLE`
- 9 "Asian Breakout London" — composed Session-BO (`MODE_LONDON_BREAKOUT`)
- composed Vol-Breakout produced candidates (Donchian/Keltner) too
- Compression BO fired 0 IC-candidates that survived to a generated signal
**But only 2 of the 43 became closed trades.** The 41 rejects died at two gates (356 reject lines in segment):
(1) **"Signal REJECTED: Quality below minimum threshold"** — the re-derived 2.4-GATE tier thresholds; Expansion's
IC/Session signals score below the A/B floor (e.g. the 2019.03.14 IC short with R:R 0.84 was quality-rejected); and
(2) **SPREAD GATE** (XAUUSD+ spread spikes > 50pt, heavy in 2020 COVID, 100–429pt). So the IC/Compression/Vol-BO
modes are **starved at the gate, not measured** — exactly the prior-engine pattern. PANIC fired 0 (dead path).

## Kill-criteria check (engine + per-mode; PF<1.0 OR avg-R<0 over full AND OOS → stays OFF)
- **Engine aggregate:** **TRIPS** — full PF 0.06 / avg-R −0.515; OOS empty (0 trades). Clear fail.
- **MODE_LONDON_BREAKOUT (Session-BO):** **TRIPS** — PF 0.06 / avg-R −0.515 full; 0 OOS → stays OFF.
- **MODE_INSTITUTIONAL_CANDLE, MODE_COMPRESSION_BO, composed Vol-BO:** 0 closed trades → NO measurement →
  cannot certify ON; stay OFF (starved at Quality-floor + SPREAD gate, not negative-measured).
- **MODE_PANIC_MOMENTUM:** 0 fires, dead `if(false)` (removed Phase D) → confirmed dead, as expected.

GATE prior (for stok context): Expansion aggregate in the engines-ON 2.4-GATE derivation was +0.070R full /
+0.294R OOS, n57. THIS isolated single-engine run is far thinner (n2) and negative — the GATE's n57 came from
Expansion firing alongside the other engines/legacy plugins (more fills survived the shared pipeline); isolated and
legacy-OFF, only 2 Session-BO trades clear the quality floor and they net negative.

## stok review + verdict
- **Numbers handed to stok:** the full+OOS table, the per-mode breakdown, the 43-generated/2-filled gate story,
  PANIC=0 confirmation, and the single-registration proof. **RULED.**
- **stok assessment:** the data is clean and the registration proof is airtight (one Expansion plugin, no
  double-reg, no legacy, isolation confirmed at the 01:05:02 journal marker). The mt5-developer correctly resolved
  the prompt's `InpEnableEngineExpansion=false` instruction against source — `InpEnableEngineExpansion` is a dead
  decorative input, the only registration path is gated by `InpEnableExpansionEngine`, and the router touchpoint is
  `RegisterEngine` (activation-weighting) not `RegisterEntryPlugin`. The repo wins; the isolation is correct.

### stok VERDICT — Expansion engine: **CUT (stay OFF-by-default)**
**One-line reason:** isolated, legacy-off, scored through `CConfluenceScorer`, the engine collapses to **n2 / PF 0.06 /
avg-R −0.515 / OOS-empty** — it trips the Iter-3 kill-criterion outright; the only measurable mode (Session-BO) trips,
and IC / Compression / Vol-BO are starved at the scorer quality floor + spread gate (no-data, cannot certify ON).

**Per-mode ruling:**
- **MODE_LONDON_BREAKOUT (composed Session-BO / "Asian Breakout London") → CUT.** This is the *only* mode with closed
  trades and it is the one carrying the negative result: 2t / −$40.37 / PF 0.06 / avg-R −0.515 full, 0 OOS. It trips
  the kill-criterion (PF<1.0 AND avg-R<0 full; empty OOS). The +$2.48 "win" is a TP0 scratch (+0.04R), not edge.
  This is consistent with the repo's standing measured finding that **London Breakout has no edge on gold H1
  (0% WR in the legacy audit)** — Session-BO reproduces that here under isolation. Stay OFF.
- **MODE_INSTITUTIONAL_CANDLE (IC Breakout), MODE_COMPRESSION_BO, composed Vol-BO → CUT / NO-DATA, stay OFF.** Zero
  closed trades despite 43 generated signals (30 IC, 9 Session-BO, plus Vol-BO candidates). 41 of 43 died at the
  scorer quality floor (v18 `InpPoints*` on the max-9 non-monotone distribution — exactly the broken-ranking the
  2.4-GATE diagnosed) + the spread gate (COVID-2020 spikes 100–429pt). They produce **no measurement**, so I cannot
  certify them ON. **Cannot-certify = stays OFF.** I am explicit that this is "no measurable edge through the shared
  pipeline as built," not "proven negative in isolation" — but the burden of proof is on turning something ON, and
  it is unmet. Stay OFF.
- **MODE_PANIC_MOMENTUM → DEAD, confirmed.** `if(false)` removed Phase D (CExpansionEngine.mqh:462-463). 0 fires as
  expected. Nothing to rule.

**stok verdict:** **CUT — Expansion engine stays OFF-by-default. All four sub-modes stay OFF** (Session-BO trips the
kill-criterion; IC / Compression / Vol-BO are no-data starved at the scorer/spread gate and cannot be certified ON;
Panic is dead code). Production byte-identity preserved (`InpEnableExpansionEngine` default unchanged,
`InpEnableMultiStrategy=false`). No `InpPoints*` re-derived.

### GATE-vs-isolated reconciliation — the ISOLATED −0.515 is the truth for a standalone-engine decision
The 2.4-GATE recorded Expansion at **+0.070R full / +0.294R OOS, n57** — and called it the "most encouraging single
number in the GATE." This isolated run lands at **−0.515R, n2, OOS-empty.** These are not in conflict; they measure
two different things, and only one of them answers the question this phase asks:
- **The GATE +0.070/+0.294 was CROSS-CASCADE ENTANGLED.** Those 57 trades fired with all four engines + the legacy
  pattern plugins live, through the one-signal-per-bar priority cascade. Expansion's realized set there was shaped by
  which bars the *other* engines did not claim, and many of its fills survived a pipeline that the other components
  were also feeding. The GATE itself flagged this (Section 4: "entangled with the cross-engine priority cascade …
  the samples are tiny … an isolated engine may trade a different population"). The +0.294 OOS rested on a ≤24-trade
  bucket the GATE explicitly declined to certify ON — it set a positive *prior*, nothing more.
- **The ISOLATED −0.515 is the standalone truth.** With legacy off, other engines off, and signals scored through
  `CConfluenceScorer` exactly as a standalone Expansion engine would run in production, the +0.294 OOS prior does not
  survive contact with isolation: **OOS goes to ZERO trades.** The encouraging GATE number was borrowed liquidity
  from the cascade, not the engine's own edge. For a *standalone-engine KEEP/CUT decision* — which is precisely what
  Phase 3.10 is — the isolated number governs. The GATE's own rule (certify ON only on the isolated fresh numbers,
  never on the cascade-entangled tiny GATE samples) points to the same conclusion.
- **What the 41/43-rejected-at-the-scorer-floor finding says — it CONFIRMS the GATE's NO-CLEAN-EDGE verdict.** The
  2.4-GATE declared the shared confluence scorer NOT-GRADEABLE on gold H1: non-monotone meanR, empty A+ ceiling, the
  edge sitting in the raw-4/5 middle while raw-6/7 went negative. Phase 3.10 is the *downstream confirmation of that
  exact pathology*: 95% of Expansion's generated signals (41/43) are killed at the very quality floor the GATE said
  cannot rank these trades. The engine's IC/Compression/Vol-BO sub-paths are not measurable **through the shared
  scorer as built** — which is precisely "the edge (if any) is in the engine, not capturable by the scorer." The two
  trades that *did* clear the floor (both Session-BO) netted negative. So the scorer either (a) starves the modes to
  no-data or (b) lets through a slightly-losing subset — the inverse of a working tier model, consistent end-to-end
  with the GATE's Step-E HARD FAIL. The honest read: **Expansion through the current shared pipeline has no certifiable
  edge, and the one slice that gets measured loses.** CUT.

---

## ITERATION 3 — CONSOLIDATED CLOSE (stok)

All four major engines are now validated per-engine. The consolidated tally:

| Engine | Phase | GATE prior (entangled) | Isolated / per-engine result | Iter-3 verdict |
|---|---|---|---|---|
| ① Trend-Continuation | 3.7 | +0.156R, n11 | ~13 trades, marginal+ | **KEEP-tiny** (engine OFF-by-default; candidate small edge) |
| ② Reversal/Sweep | 3.8 | +0.056R full / +0.206R OOS, n125 | edge isolates to SFP-LONG only | **KEEP-for-SFP-LONG only** (engine OFF-by-default) |
| ③ Range/Mean-Reversion | 3.9 | −0.688R full / −0.530R OOS, n4 | (cut from GATE data, no isolation run) | **CUT** |
| ④ Expansion | 3.10 | +0.070R full / +0.294R OOS, n57 | n2 / PF 0.06 / −0.515R / OOS-empty | **CUT** (this phase) |

### The honest end-state — multi-strategy stays OFF-by-default; SFP-LONG and tiny Trend-Cont are candidate small edges only
Be decisive: **do NOT turn the engines on, and do NOT pursue the multi-strategy cutover as a production change at this
point.** The right end-state is **`InpEnableMultiStrategy=false` stays the production default**, exactly where v18
locked it and where the 2.4-GATE bound it. Here is the reasoning, plainly:

1. **Two of four engines are clean cuts** (Range, Expansion). They trip the kill-criterion on their own metrics.
   Turning either on would size into negative expectancy. Done.
2. **The two "keeps" are KEEP-*tiny* / KEEP-*for-one-sub-path*, not KEEP-and-ship.** Trend-Cont's entire case rests on
   **~13 trades**; Reversal's case collapses to the **SFP-LONG** slice alone (the rest of the engine — including its
   shorts — does not carry it). Neither clears a real confidence bar. The Iter-3 kill-criterion let them survive as
   *candidates*; it did not certify them as *production-ready*. A 13-trade edge is a hypothesis, not a deployable
   strategy — it is one bad month from flipping sign.
3. **The shared scorer that would gate routed engines is NOT-GRADEABLE (2.4-GATE), and Phase 3.10 just confirmed it
   downstream.** Routing any kept engine *through* the shared tier would mis-size it — the scorer ranks backwards on
   gold H1 (raw-6/7 negative, edge in raw-4/5), and it starved 41/43 Expansion signals to no-data. So even the engines
   we'd want to keep have no working production gating mechanism today. Turning the multi-strategy router on now would
   ship trades graded by a scorer we have proven cannot grade them. That is the cardinal error to avoid.
4. **The honest baseline does not need them.** The pattern-only core (Pin Bar, Engulfing, the long-biased profitable
   set) is the measured edge, ~$1,900 honest 2-yr net. The engines, at their *best entangled* GATE reading, added a
   marginal +$566 over 7.5 years that the scorer can't rank; isolated, two of four are negative and the other two are
   tiny. The multi-strategy stack is not where the edge lives. Adding complexity that the gating layer can't size is
   strictly risk-additive with no certified return.

**Therefore — the decisive Iter-3 conclusion:**
- **Production: `InpEnableMultiStrategy=false` stays the default. All four engines stay OFF. Byte-identical to v18.**
- **Range + Expansion: CUT (closed — negative on own metrics).**
- **SFP-LONG (from Reversal) and tiny Trend-Cont: the only two candidate small edges.** They are *parked as
  candidates*, NOT promoted. The correct next step for them is **to be lifted to ≥30-trade confidence later** (a
  dedicated, isolated, fresh-window validation that actually populates a certifiable sample), and to be given a
  **working gating mechanism** (the deferred scorer-axis re-weight loop-to-stok) before any of them could be routed.
  Until both of those exist, they remain OFF-by-default, no production change.
- The multi-strategy cutover is **NOT worth pursuing as a production change now.** Revisit only after (a) the scorer
  axis re-weight makes the shared tier gradeable, and (b) SFP-LONG / Trend-Cont reach ≥30-trade certified edge on
  isolated fresh runs. Neither precondition is met. Honest end-state: **stay off-by-default.**

### stok sign-off (this phase)
- **SIGNED.** Expansion = CUT (stay OFF-by-default, all four modes). GATE +0.070 was cascade-entangled; the isolated
  −0.515 / OOS-empty is the standalone truth and governs the KEEP/CUT call. The 41/43 scorer-floor rejections confirm
  the 2.4-GATE NO-CLEAN-EDGE verdict downstream. Iteration 3 closes with multi-strategy OFF-by-default; SFP-LONG and
  tiny Trend-Cont are candidate small edges to be lifted to ≥30-trade confidence later, not now. Production
  byte-identity preserved; no `InpPoints*` touched; no engine force-enabled. — **stok**

## mt5-developer sign-off
- Compile clean (0 errors / 38 warnings, 0 new); source byte-identical to HEAD; `.ex5` regenerated and copied;
  isolation verified at runtime = exactly 1 plugin (ExpansionEngine), no double-reg, no legacy; double-reg caveat
  resolved against source (InpEnableExpansionEngine is the only reg path; InpEnableEngineExpansion is a dead input;
  router touchpoint is RegisterEngine not RegisterEntryPlugin); numbers decoded from UTF-16LE Stats CSV, cross-
  validated 3 ways. — **SIGNED (mt5-developer)**

## stok sign-off
- **SIGNED.** Expansion = **CUT** (stay OFF-by-default; all four modes OFF). Session-BO trips the kill-criterion;
  IC/Compression/Vol-BO are no-data starved at the scorer/spread gate (cannot certify ON); Panic dead. The GATE
  +0.070 was cascade-entangled; the isolated −0.515 / OOS-empty governs the standalone decision. The 41/43
  scorer-floor rejections confirm the 2.4-GATE NO-CLEAN-EDGE verdict downstream. ITERATION 3 CLOSED (see the
  "ITERATION 3 — CONSOLIDATED CLOSE" section). — **stok SIGNED**

## Decision
- **RULED — Expansion CUT (stays OFF-by-default), ITERATION 3 COMPLETE.** Engine aggregate and the only measurable
  mode (Session-BO) TRIP the kill-criterion; IC/Compression/Vol-BO are NO-DATA (starved at the quality floor + spread
  gate, cannot certify ON); PANIC confirmed dead. NOT committed; production byte-identical (`InpEnableExpansionEngine`
  default unchanged; `InpEnableMultiStrategy=false`). With all four engines now validated per-engine — Trend-Cont
  KEEP-tiny, Reversal KEEP-for-SFP-LONG-only, Range CUT, Expansion CUT — the binding end-state is **multi-strategy
  stays OFF-by-default**; SFP-LONG and tiny Trend-Cont are the only candidate small edges, parked for a future
  ≥30-trade certification + scorer-axis re-weight, NOT promoted now. NEXT: deferred Phase 2.5 (finish Iteration 2),
  then Iterations 4–6.

## Commit
- **Hash:** none (NOT committed — leaving keep/cut verdict to stok, per the phase directive).
- **Message:** n/a
