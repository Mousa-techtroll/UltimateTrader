# UltimateTrader Bug-Fix — MASTER STATUS TRACKER

> Source of truth: `workflowAnalysis/bug-fix-plan.md` (6 iterations, 53 fixes). This file tracks phase-by-phase execution. Resume protocol: `workflowAnalysis/progress/RESUME.md`.

## ⏭️ NEXT ACTION
**Phase `6.2` (Iteration 6 — netting-mode fallback binding discriminator; FIX-SOUND).** **✅ PHASE 6.1 COMMITTED (2026-06-27)** — Infra-1 retcode-first trade-error/retry classifier: captured ResultRetcode(); HandleTradingError classifies on TRADE_RETCODE_* PRIMARY, legacy GetLastError switch kept as the retcode==0 fallback. BOTH agents signed: mt5 + stok **ACCEPT-WITH-FOLLOWUP** (NEITHER REJECT-LOOP trigger fired). stok: retcode-PRIMARY correct; REJECT stays RETRY (transient-by-elimination — hard reasons have dedicated retcodes already in NO-RETRY; bounded at 3); INVALID_STOPS stays NO-RETRY (Option A — never fire a non-structural stop; stand aside); m_maxRetries=3 fine (stop distance preserved across retries). **Executed-trade A/B BYTE-IDENTICAL** (13 diagnostic MARKET_CLOSED rows differ — A wastefully retried, B correctly didn't, order never opened = zero trade impact; CONFIRMS the retcode path is live + strictly more correct). Dormant INVALID_STOPS guard PARKED+documented this phase (comment-only, byte-identical). **2 deferred live-only follow-ups:** FU-1 intended-entry drift guard (InpMaxRetryDriftPoints), FU-2 explicit CONNECTION(10031)/TOO_MANY_REQUESTS(10024)→RETRY + SERVER_DISABLES_AT(10026)→NO-RETRY cases. See `6-1.md`. **Resume at Phase 6.2 — APPLY RESUME.md 3b.**

---

**ITERATION 6 (infra/hygiene 6.1–6.9) — IN PROGRESS.** **✅✅ ITERATION 5 COMPLETE (2026-06-27) — all effective phases committed (fixes 5.1, 5.3+5.4, 5.5, 5.6+5.7, 5.8+5.9, 5.10, 5.11, 5.13; 5.2=4.5 cross-ref, 5.12 already under 2.1), both-agent QA on every one.** Final phase 5.13 (dead CSignalManager + g_consecutiveErrors removal) committed — BOTH agents signed: mt5 + stok **ACCEPT** (grep-confirmed truly-dead, byte-identical, meets the 2.7 standard; the live pending path is g_signalOrchestrator's separate untouched mechanism). **⚠️ HARNESS HARDENED MID-ITERATION:** a stale-binary incident in 5.6 was caught + root-caused + RESUME.md step 3b added (mandatory `md5(load-path)==md5(fresh-build)` assertion every run); the committed chain was verified SOUND. **Binding baseline now: Net $17,455.34 / PF 1.28 / 1742t / Eq-DD 12.64% (post-5.9)** — superseded $17,018.83 (post-5.5) after 5.9's closed-bar H4 pivot.

**Before starting Iteration 6: READ workflowAnalysis/bug-fix-plan.md Iteration-6 rows (6.1–6.9) AND the accumulated deferred follow-ups** logged across Iters 4–5 that Iteration 6 should absorb: loss-scaler config re-derivation (4.1); InpMaxTotalExposure {3,4,5} sweep (4.2); CanTrade LogPrint once-per-transition + file-daily-budget operator question (4.4/4.5); spread-shock real-tick Model-4 calibration + threshold sensitivity (4.6); InpTrailChandelierMult re-derivation vs the HONEST baseline (4.8 — the stale 1.2× rejection must be re-run clean); OOS daily-halt premature-halt guard + optional InpDailyLossBaseline knob (4.3); 5.3/5.4 follow-ups (synthetic file-signal validation FU-A, PositionSelectByTicket select-side-effect FU-B, correlation-blind bucket FU-C, hardcoded PERIOD_H1 FU-D); 5.8/5.9 short/support-side empirical check; 5.10 synthetic transiently-rejecting-CSV validation + key-rollback-off-pendingKey + retry-counter; 5.11 entry_risk<=0-on-restore note; 5.13 stale "SignalManager" comment-string sweep (Structs.mqh:7/488); plus the standing pre-live items (forced-fallback macro A/B, live-calendar name-matching, CSessionEngine GMT-clock extraction) and the 6.7/6.8/6.10 doc-only items already in the plan. **Apply RESUME.md 3b on every backtest.** **Resume at Phase 6.1.**

**(prior) ✅ PHASE 5.11 COMMITTED (2026-06-27)** — XCut-7 restored-positions-lose-tp3 + STATE_FILE_VERSION v4→v5: persist+restore `tp3` AND mandatory `entry_risk_amount` (completes 5.1/5.3 for the restart path — keeps a restored position off the `_Symbol`-tick recompute that feeds the 4.2 exposure cap/EC controller); signal_id correctly EXCLUDED (audit-only, FileWriteStruct string trap). EXACT-MATCH version gate = the deterministic v4 discriminator (fires before the size check, which the harness proved insufficient alone) → broker-only fallback (safe, no data-loss). BOTH agents signed: mt5 + stok **ACCEPT-WITH-FOLLOWUP** (source-verified full chain). **A/B BYTE-IDENTICAL** (clean-state tester → restore path inactive → write-only; A reproduced HEAD byte-for-byte via 3b, distinct A/B binaries 16fc45e2/fe011dc3). stok-flagged COMMENT defect (two comments misattributed the v4-reject to the size check) FIXED this phase (comment-only, byte-identical). See `5-11.md`. **Resume at Phase 5.13 — APPLY RESUME.md 3b.**

**✅ PHASE 5.10 mt5-QA-DONE, stok PENDING (2026-06-27)** — file-signal DEFERRED-COMMIT (XCut-1). Optimistic `m_trades[i].Executed=true` kept at emit (stops same-tick re-emit) + record `m_pendingTradeIdx`/`m_pendingTradeKey`; durable `MarkExecuted` MOVED OUT of the emit path into new `ConfirmExecuted()` (fires ONLY on `ticket>0`); new `RollbackPending()` clears the optimistic flag on reject (`ticket<=0`) so a later in-window tick retries — **bounds-guards `m_pendingTradeIdx` (`>=0 && < ArraySize(m_trades)`) before any deref** (no OOB; stale-index branch logged). Wired in `UltimateTrader.mq5` file-signal result site: success→`ConfirmExecuted()`, reject→new `else`→`RollbackPending()`. Existing `InpFileMaxSlippagePct` slippage gate untouched = stale-retry self-reject (no extra retry-window logic). NO logic cut. **Compile 0err/38warn (0 new) both legs** (B `Result: 0 errors, 38 warnings, 8924 ms`; A `…9031 ms`); no warning in CFileEntry.mqh. **3b enforced:** A `a1e96bf6` / B `6b79aa6f` (distinct — B has the added code; both ≠ stale `9dbe9052`), load==fresh asserted both legs; **A reproduces committed HEAD d64965d byte-for-byte** (4/4 EA-CSV md5 == committed 5.8+5.9 run: Stats `3b13ea8f…`/TradeEvents `2222e2f9…`/Risk `c152fb4a…`/Candidates `77b07fb1…`; $17,455.34/1742t). **A/B XAUUSD+ H1 Model=1 full 2019.01.01–2026.06.27, 396-param production `[TesterInputs]` (InpEnableMultiStrategy=false, InpSignalSource=2=BOTH), 2 runs = BYTE-IDENTICAL:** all 4 EA CSVs md5-EQUAL + `cmp` byte-for-byte CLEAN; headline identical $17,455.34 / PF 1.28 / 1742t / Eq-DD 12.64%. **Byte-identity mechanism:** `telegram_signals.csv` ABSENT in the tester → `CFileEntry` loads 0 trades → `CheckForEntrySignal()` never emits → `m_pendingTradeIdx` stays −1 → `ConfirmExecuted()`/`RollbackPending()` NEVER reached → added code dead in the tester; 0 FileSignal rows in TradeEvents/Stats confirm 0 file-signal trades. **LIVE-ONLY:** the deferred-commit's recovery value is unmeasurable in the tester (path unreachable); byte-identical A/B proves NO production regression; correctness rests on reviewed code logic. **Baseline note:** the $17,018.83 figure is the PRE-5.8/5.9 anchor (HEAD ed488df); HEAD d64965d already committed 5.9's +$436.51 → A correctly = $17,455.34 (trade count 1742 unchanged). 1 follow-up flagged: synthetic transiently-rejecting-CSV validation (Iter-5 QA(b) 5.10), out of ≤2-run scope. See `5-10.md`. **Do NOT commit; awaiting stok.**

**✅ PHASE 5.8+5.9 mt5-QA-DONE, stok PENDING (2026-06-27)** — array-OOB pivot guard (5.8) + closed-bar H4 pivot (5.9), bundled across the same four pivot/obstacle loops (`CTradeOrchestrator::FindNearestObstacle` LONG+SHORT, `CAdaptiveTPManager::FindNextResistance`+`FindNextSupport`). Per loop: `CopyHigh/Low(...,PERIOD_H4, 0→1, 100,…)` + capture `n` + `for(i=2;i<n-2;i++)` + `n>=5` floor. **Compile 0err/38warn (0 new) both legs.** **3b enforced:** A `c1fb7cc5` / B `293a30de` (distinct, both ≠ stale `9dbe9052`), load==fresh both legs; **A reproduces binding baseline byte-for-byte** ($17,018.83/1742t + 3/3 EA-CSV md5 `08640614…`/`18aedaca…`/`7b3db7d1…`). **A/B:** Net $17,018.83→$17,455.34 (+2.56%), PF 1.28 flat, **1742→1742 trades identical**, Eq-DD 12.89%→12.64%. **5.8 inert in steady state; 5.9 delta = 4/909 TP-target flips with byte-identical entries+SLs. 5.9 determinism: B-twice all 4 CSV bit-identical → PASS.** No kill-criterion tripped. See `5-8_5-9.md`. **Do NOT commit; awaiting stok.**

**✅ PHASE 5.6+5.7 COMMITTED (2026-06-27)** — engine register-gating: 5.6 `&& register_patterns` on the 3 engines (restores FILE-only contract), 5.7 `&& InpEnableEngineExpansion` on engine-4 router-wiring (honors the orphan input). BOTH agents signed: mt5 + stok **ACCEPT-WITH-FOLLOWUP** (source-verified: RegisterEntryPlugin(false) early-returns so the gate genuinely removes engines from the firing array; the ungated router path is suppressor-only and can't force a fire; no double-registration; engine-1-3-vs-engine-4 gate asymmetry is structural+correct). **⚠️ HARNESS-HARDENED:** the 5.6 phase had run a 7-commit-STALE binary (`9dbe9052`, pre-4.6) — caught + root-caused (compiler writes repo path, tester loads data-dir path, no md5 assertion); **RESUME.md step 3b now MANDATES `md5(load-path)==md5(fresh-build)` every run**, distinct-md5 per A/B leg, suspect-stale-binary-first on baseline divergence. Chain confirmed SOUND: fresh HEAD `f1e251fa` = **$17,018.83/PF 1.28/1742t** byte-for-byte (= binding baseline; full forensics `REBASELINE-5-6.md`). 5.6+5.7 byte-identity proven against the CORRECT baseline. 2 non-blocking follow-ups (FU-1 doc gate-asymmetry, FU-2 verify-on-enable checklist). See `5-6_5-7.md`. **Resume at Phase 5.8+5.9 — APPLY RESUME.md 3b (assert load-path md5).**

**✅ PHASE 5.4 (= plan fixes 5.6 + 5.7) mt5-QA-DONE, stok PENDING (2026-06-27)** — engine register-gating, default byte-identical. 5.6: `&& register_patterns` on the 3 engine RegisterEntryPlugin calls (mq5:734/740/746) → restores FILE-only contract. 5.7: engine-4 router-wiring gated on `&& InpEnableEngineExpansion` (mq5:756), legacy standalone reg (mq5:687) UNCHANGED, Inputs:503 comment updated. mt5 SIGNED: compile **0 errors, 38 warnings** (0 new); engine block confirmed behind `if(InpEnableMultiStrategy)` (mq5:718); NO logic cut. **A==B BYTE-IDENTICAL proven 3 ways:** unreachable-code reasoning + identical `.ex5` md5 (`9dbe9052…`) + all 4 EA CSVs md5-equal & cmp-clean + identical tester headline. **⚠️ DATA/STATE DRIFT (equal for A&B, NOT from this fix):** clean HEAD ed488df run today = $25,570.97/PF 1.31/2011t, not the 5-5 $17,018.83/1742t — suspected `UltimateTrader_State.bin` + tester data re-sync; re-baseline on clean state next. **stok PENDING** (no-op sign-off + drift acknowledgement). See `5-6_5-7.md`. **Resume at Phase 5.5 (plan fixes 5.8+5.9).**

**(prior) ✅ PHASE 5.5 COMMITTED (2026-06-27)** — `g_session_quality_factor` per-bar reset + floor (BETTER-OPTION): per-bar reset (mq5:1454) + separate shock_factor/sq_factor locals + intensity clamp [0,1] + combine-once + FLOOR `InpMinSessionRiskFactor=0.25` (Inputs:358). BOTH agents signed: mt5 (0err/38warn) + stok **ACCEPT-WITH-FOLLOWUP** (source-verified, fix STRONGER than spec — global now telemetry-only, never read for sizing → leak impossible by construction). Fixes BOTH the cross-bar leak AND the same-bar clobber that left moderate-shock down-sizing DEAD (A: 0/1258 shock reductions reached a trade; B: 12 shock-bar trades correctly down-sized) — completes the 4.6 shock gate's MODERATE arm. **A/B Net $16,685.59→$17,018.83 (+2.0%), entries IDENTICAL 909==909 (no edge amputation), PF/DD/avg-R flat = correctness consequence not tuning.** Floor 0.25 = live two-reducer guardrail (unexercised in tester, sq≡1.0). **2 non-blocking follow-ups:** FU-1 shock-coefficient magnitude (don't tune on 12-trade sample), FU-2 block/down-size interaction confirmed clean. See `5-5.md`. **Resume at Phase 5.6+5.7.**

**(prior) ✅ PHASE 5.3+5.4 COMMITTED (2026-06-27)** — bundled: 5.3 (multi-symbol file-trail uses `pos_symbol` + symbol-from-position via PositionGetString inside CalculatePositionRiskDollars/GetCurrentMarketPrice) + 5.4 (closed-bar file-trail swing/ATR, count stays 5, 4.8 repaint class). BOTH agents signed: mt5 (0err/38warn, byte-identical) + stok **ACCEPT-WITH-FOLLOWUP** (source-verified: old code trailed a file position on symbol X with gold's ATR/swing/tick math = nonsensical SL + corrupted risk telemetry; PositionGetString is the right symbol source since SPosition has none; ONE aggregate risk bucket is the correct conservative policy for the correlated gold complex). **A/B BYTE-IDENTICAL** (file-trail blocks are SIGNAL_SOURCE_FILE-only; 0 file positions in tester → never execute). **4 deferred follow-ups:** FU-A synthetic file-signal validation of 5.4's kill-criterion (before live multi-symbol reliance), FU-B PositionSelectByTicket select-side-effect footgun, FU-C correlation-blind aggregate bucket, FU-D hardcoded PERIOD_H1 in file trail. See `5-3_5-4.md`. **Resume at Phase 5.5.**

**(prior) ✅ PHASE 5.1 COMMITTED (2026-06-27)** — symbol-match recovery guard (XCut-2): added `PositionGetString(POSITION_SYMBOL)==_Symbol` AND to all 3 startup recovery loaders (CPositionCoordinator :1325/:1423/:1501), mirroring OnTick:2083-2084. BOTH agents signed: mt5 (0err/38warn) + stok **ACCEPT-WITH-FOLLOWUP** (source-verified all 4 sites incl. De-Morgan on the 2 skip-guards). Closes a foreign-symbol-adoption risk hole (old magic-only match → manage a foreign position with gold's ATR/tick/R math); converges all adoption paths on one invariant. **A/B BYTE-IDENTICAL** (single-symbol tester → no-op; live multi-chart safety). **FU-1 logged:** document one-magic-per-instance deployment constraint (a symbol guard can't catch same-symbol/same-magic cross-instance collisions). See `5-1.md`. **Resume at Phase 5.3+5.4.**

**(prior) ✅✅ ITERATION 4 COMPLETE (2026-06-27) — all 9 fixes 4.1–4.10 COMMITTED, both-agent QA on every one.** Final piece: the TRAILING CLUSTER (4.8+4.9+4.10) committed — BOTH agents signed: mt5 (0err/38warn, all 3 source-verified, ratchet held 0/2344, clamp 273/273 toward-safety) + stok **ACCEPT-WITH-FOLLOWUP** (binding, source-verified). 4.8 closed-bar Chandelier = honest exit-side look-ahead removal (Iter-1 1.2-1.4 class); 4.9 clamp+revert = trailing internal-SL desync repaired (the runner-survival in A was UNDEFINED behavior — broker silently swallowing too-tight modifies — not an edge); 4.10 inert (latent live-safety). **⚠️ MAJOR: the honest baseline is NOW $16,685.59 / PF 1.28 / Sharpe 2.20 / Eq-DD 12.88% (2019–2026)** — supersedes the prior look-ahead-inflated $24.7k (exit-side look-ahead, on top of Iter-1's entry-side findings). **Iter-6 follow-up logged:** re-derive InpTrailChandelierMult/m_swing_lookback vs the HONEST baseline B (the stale 1.2× rejection at CPositionCoordinator:2711-2712 was measured against the inflated baseline → re-run clean; adopt wider only if net recovers WITHOUT Eq-DD>12.88% or PF<1.28). See `4-8_4-9_4-10.md`. **Resume at Phase 5.1.**

**(prior) ✅ PHASE 4.7 (fix 4.7, partial-fill) COMMITTED (2026-06-27)** — partial-fill repair: ValidateExecutedVolume fails only on executedLots<=0 (partial→WARN+accept); load-bearing `position.lot_size = executedLots` BEFORE AddPosition; g_trade NOT pinned to FOK. BOTH agents signed: mt5 + stok **ACCEPT** (source-verified complete chain). A/B BYTE-IDENTICAL (live-only). See `4-7.md`.

**(prior) ✅ PHASE 4.6 COMMITTED (2026-06-27)** — spread-unit dimensional fix: dropped `* _Point` at CEnhancedTradeExecutor.mqh:1938+2039 so the spread-shock + spread-stability ratios are points/points (samples[] are points). BOTH agents signed: mt5 (0err/38warn) + stok **ACCEPT-WITH-FOLLOWUP** (source-verified). **NOT byte-identical (plan's "live-only" hypothesis FALSIFIED)** — the fix ACTIVATES the previously-dead spread-shock gate; under Model=1 the spread leg is the SOLE shock signal (bar-range/M5 legs degenerate 0.00) → 2308 EXTREME-blocks + 1259 moderate-cuts → 2011→1905 trades, net −3.5% but PF 1.31→1.32 / Sharpe 2.51→2.63 / Eq-DD flat (RISK-REDUCING). Shipped on dimensional correctness; Model=1 = conservative lower bound on gate activity. **2 Iter-6 follow-ups logged**: real-tick (Model=4) calibration spot-check over a news-spread-stress window; threshold sensitivity (config). ⚠️ first 4.6 mt5 agent hit a SPURIOUS content-filter false-positive (persisted nothing) → orchestrator made the proven edit directly + minimal agent ran A/B. See `4-6.md`. **Resume at Phase 4.7.**

**(prior) ✅ PHASE 4.3 COMMITTED (2026-06-27)** — daily-loss halt baseline UNIFIED: CRiskMonitor is now the single source of truth, anchored to start-of-day ACCOUNT_EQUITY; CDailyLossHaltExit reads GetDailyPnL() (SetRiskMonitor injector) instead of a 2nd baseline. BOTH agents signed: mt5 (0err/38warn) + stok **ACCEPT-WITH-FOLLOWUP / SHIP-EQUITY** (source-verified). Accepted on CORRECTNESS (equity = dimensionally-correct anchor for a runner-holding book; single-source unambiguously right; halt count 10=10 falsifies over-trigger fear) — the +5.29% net is treated as LUCK not edge (path-artifact of 3 B-only halts, do NOT cite as "+5%"). **Iter-6 follow-up logged**: OOS/regime guard counting B-only halts that lock out RECOVERIES (kill-signal if OOS net/avg-R drops below A); optional InpDailyLossBaseline knob. See `4-3.md`. **Resume at Phase 4.4+4.5.**

**(prior) ✅ PHASE 4.1 COMMITTED (2026-06-27)** — consecutive-loss scaler now fed TOTAL trade PnL (not runner leg) at both close sites in `CPositionCoordinator.mqh`. BOTH agents signed: mt5 + stok **ACCEPT-WITH-FOLLOWUP** (binding). Fix is unit-correct (a banked-then-red NET WINNER must not de-risk; aligns scaler with EC v3); net −37.9% is an accepted EXPOSURE-LEVEL artifact on a FLAT per-R edge (PF 1.36→1.31, avg-R +0.143→+0.127, avg lot 0.255→0.213, maxDD 10.98%→9.17% BETTER) — buggy A's larger book was curve-fit-to-one-path, NOT kept. **Iter-6 follow-up logged** (loss-scaler config re-derivation). See `4-1.md`.

**(prior) ✅ ITERATION 2 COMPLETE (2026-06-27).** Phases 2.6/2.7/2.8 are COMMITTED — BOTH agents signed (mt5-developer QA + INDEPENDENT stok no-op sign-off). All three are no-ops (2.6 comment-only, 2.7 dead-code removal, 2.8 WARN-only print) and proven byte-identical on the production config via the shared COMBINED 2.6+2.7+2.8 A==B (XAUUSD+ H1 2024, Model=1, 395 inputs pinned; all 5 CSVs md5+cmp IDENTICAL; A Stats md5 `33317ded…` matches post-2.5 HEAD → no drift, no bisection needed). 2.7's deletions verified TRULY dead at SOURCE: repo-wide grep = exactly ONE live `EvaluateSetupQuality` call (CSignalOrchestrator:746) which post-2.1 reads `signal.setupQuality` for engines and resolves to the KEPT legacy ENUM_TREND_DIRECTION overload (CSetupEvaluator:72, intact) otherwise — zero callers of the deleted (EntrySignal&, IMarketContext*) engine overloads; CConfluenceScorer class + all other includes preserved; no error 106; no live path lost. Phase 2.5 (CSetupEvaluator CHoCH fix) ✅ COMMITTED; one non-blocking follow-up recorded (legacy bear-short A/A+ HTF-trend guard, deferred Iter-6/6.9). ITERATION 3 IS COMPLETE (all 4 engines validated per-engine; see below). **Resume at Phase 4.1.**

**✅ ITERATION 3 COMPLETE (2026-06-27).** All four major engines validated per-engine on their own avg-R/PF vs the Iter-3 kill-criterion (PF<1.0 OR avg-R<0 full AND OOS → OFF). **Tally: ① Trend-Cont = KEEP-tiny (~13t); ② Reversal/Sweep = KEEP-for-SFP-LONG-only (13t SFP-LONG carries it, SFP-SHORT dead); ③ Range/MR = CUT (GATE-decided); ④ Expansion = CUT (3.10 — isolated n2/PF 0.06/−0.515R/OOS-empty, trips outright).** **BINDING END-STATE (stok): multi-strategy stays OFF-by-default (`InpEnableMultiStrategy=false`, production byte-identical).** Two engines are clean cuts; the two "keeps" are KEEP-*tiny* / KEEP-*one-sub-path* (≤13-trade hypotheses, not deployable), and the shared scorer that would gate any routed engine is NOT-GRADEABLE (2.4-GATE, re-confirmed downstream by 3.10's 41/43 scorer-floor rejections). **SFP-LONG + tiny Trend-Cont are the ONLY candidate small edges — PARKED, not promoted**; to be lifted to ≥30-trade certified confidence on fresh isolated runs AND given a working gate (the deferred scorer-axis re-weight loop-to-stok) before any could be turned on. The multi-strategy cutover is NOT worth pursuing as a production change now. Honest 2-yr pattern-only baseline ≈ **$1,900** stands.

**Standing reminders.** (1) The 2.4-GATE is **done** (stok-signed) — do not re-open it. (2) Iteration 3 is **done** — engines stay OFF-by-default; do not force-enable, do not route through the shared tier. (3) **Iteration 2 is DONE** — 2.5 (logic, signed) + 2.6/2.7/2.8 (no-ops, signed + COMMITTED). Remaining work = **Iterations 4–6**, starting at Phase 4.1 (live-path execution/risk fixes). Full per-phase sign-off detail lives in the per-phase `NN.md` logs and the iteration tables below.

## Status vocabulary
`PENDING` → `IN-PROGRESS` → `IMPL-DONE` (code written, not yet compiled clean) → `QA-PASS` (compile 0-err + backtest decoded + BOTH agents signed off) → `COMMITTED` (separately-measurable commit recorded). Side states: `QA-FAIL-LOOP` (kill-criterion tripped / regression — looped back to stok), `PARKED` (toggle-level disable with stok sign-off, never a logic cut).

## Legend
- **Touches-logic** = `Y` if `touches_trading_logic` per the plan (uses stok's binding option + A/B + kill-criterion); `N` = no-op/dead-code/comment/defensive (acceptance = byte-identical backtest).
- **Commit** = the git hash of that phase's separately-measurable commit (filled at COMMITTED).

---

## Iteration 1 — Structure / Market-Context Foundation

| Phase | Fixes | Files | Touches-logic | Status | Commit | Notes |
|---|---|---|---|---|---|---|
| 1.1 | 1.1 | CSMCOrderBlocks.mqh (liquidity pools) | Y | COMMITTED | feat/multi-strategy | Fix applied + 0-err/0-new-warn. **A/B XAUUSD+ H1 2024 & 2023 = trade-for-trade IDENTICAL A=B** (2024: 157t/45.86%/$2289.62/18.15R; 2023: 124t/36.29%/$999.48/6.35R) — mechanism-reconciled (+15 boost non-decisive at `<40` floor / `>0` gate; SMC-tiering engines OFF by default). **SWEPT-throughout PASS** (58,915 hits Jan→Dec 2024). recency=3. **stok SIGN-OFF** (zero-delta expected/correct). BINDING: sweep `SMC_SWEEP_RECENCY_BARS ∈ {3,5,8}` per engine in Iter-3, promote to `InpSMCSweepRecencyBars` (default 3, cap 8), pick by avg-R. |
| 1.2 | 1.2 | CSMCOrderBlocks.mqh (BOS/CHoCH); consumer CConfluenceScorer | Y | COMMITTED | | NEEDS-CARE highest-leverage: BOS IS the L3 gate. **Applied + 0-err/0-new-warn** (`Result: 0 errors, 38 warnings`). Break-trigger `[0]/[1]`→`[1]/[2]` (lines 960–963), edge guard kept EXACTLY, 4 structure-event timestamps → `iTime(...,1)` (BOS 979/997, CHoCH 693/714) for 2.2 freshness; DetectSwingPoints untouched; NO logic cut. **A/B XAUUSD+ H1** — 2024: A 157t/45.86%/$2289.62/18.15R/0.1156avgR → B 159t/45.28%/$2050.92/16.02R/0.1008avgR (Δ +2t, -$238.70/-10.4%, -0.0148avgR); 2023: A 124t/36.29%/$999.48/6.35R/0.0512avgR → B 129t/37.21%/$1108.84/9.17R/0.0711avgR (Δ +5t, +$109.36/+10.9%, +0.0199avgR); 2yr net Δ -$129.34/-3.9%. **BOS/CHoCH fire PASS** (B-2024: 103/57 BOS, 45/46 CHoCH across all 12 mo; B-2023: 89/98 BOS, 42/43 CHoCH). **NOT over-gated** (count +2/+5, did not collapse) **NOT flooded** (did not balloon). No kill-criterion tripped. mt5-developer SIGNED. **stok SIGNED** — forming-bar `[0]` read was genuine look-ahead (intrabar high/low known only mid-bar); closed-bar `[1]/[2]` is the honest real-time-tradeable basis. −3.9% 2-yr dip = removed look-ahead inflation, NOT lost edge: small + BIDIRECTIONAL (2024 −$239 / 2023 +$109 w/ higher WR+avgR), avgR ±0.015–0.020 within 1 SE, DD in régime. Selectivity recovery deferred to Phase-2.2 `InpBOSFreshnessBars {4,6,8,12}` avg-R sweep that consumes this phase's closed-bar timestamps. Phase log: `progress/1-2.md`. |
| 1.3 | 1.3 | CTrendDetector.mqh | Y | COMMITTED | | NEEDS-CARE: DIRECTION input across ~15 plugins. **Applied + 0-err/0-new-warn** (`Result: 0 errors, 38 warnings`). UpdateTimeframe 153-163: `ma_fast/ma_slow/close` + 5 MA-position booleans + 4 diag LogPrints forming `[0]`→closed `[1]`; CalculateTrendStrength 304-310: ATR CopyBuffer count 1→2, use `atr[1]` so **ATR index MATCHES MA index** (no strength↔direction desync); short-read guard tightened `<=0`→`<2`. DetectHigherHighs/LowerLows UNCHANGED; NO logic cut. **A/B XAUUSD+ H1** (A=post-1.2 build, reproduces 1.2-B exactly) — 2024: A 159t/45.28%/$2050.92/16.02R/0.1008avgR (L126/S33) → B 140t/46.43%/$1764.07/16.58R/0.1184avgR (L109/S31) (Δ -19t, -$286.85/-14.0%, **+0.56R, +0.0176avgR, WR↑**); 2023: A 129t/37.21%/$1108.84/9.17R/0.0711avgR (L83/S46) → B 123t/34.96%/$628.18/3.95R/0.0321avgR (L75/S48) (Δ -6t, **-$480.66/-43.3%, -5.22R, -0.0390avgR**); 2yr net Δ -$767.51/-24.3%, totR 25.19→20.53, trades 288→263/-8.7%. **Count NOT collapsed/ballooned** (re-selection, LONG-dominated reduction consistent with closed-bar re-timing in gold's uptrend). 2024 fewer-but-better (avgR↑); 2023 worse net+avgR. mt5-developer SIGNED (compile clean + mechanism correct + ATR index matched + no logic cut). **stok SIGNED → SIGN-OFF** — forming-bar `[0]` tested direction vs the unclosed bar's intrabar `close[0]` = genuine look-ahead; closed-bar `[1]` is the honest real-time basis, NOT reverted. 2023 −43.3%/−5.22R = honest YEAR-SPECIFIC look-ahead deflation, NOT a systematic lag problem — ASYMMETRY proves it: 2024 fewer-but-BETTER (avgR/WR/totR↑), 2023 worse, SHORTS improved in BOTH years (2024 −3.82→−2.73R; 2023 +6.93→+4.02R), reduction LONG-dominated (gold-uptrend EARLY-BULLISH intrabar front-run). A uniform-lag defect would degrade both years symmetrically + hurt shorts — it does neither. Selectivity recovery deferred to Phase-2.2 `InpBOSFreshnessBars {4,6,8,12}` avg-R sweep + 2.4-GATE re-derivation (tune on the honest input; NOT owed by 1.3; reverting to `[0]`/widening logic forbidden). No change requested. Phase log: `progress/1-3.md`. |
| 1.4 | 1.4 + 1.5 | CRegimeClassifier.mqh | Y | COMMITTED | | **Combined edit (bundling).** Closed-bar ADX/ATR + realized-count avg. **Applied + 0-err/0-new-warn** (`Result: 0 errors, 38 warnings`). CRegimeClassifier.mqh Update 132-160: ADX/ATR/BB/close reads forming `[0]`->closed `[1]` (counts adx 3->4, bb 3->4, close 1->2); ATR-avg over CLOSED bars `atr[1..MathMin(50,got-1)]` / realized count (CMomentumFilter idiom, `got=CopyBuffer(...,51,...)`); all Copy* short reads guarded `<2`; ClassifyRegimeRaw/hysteresis/thrash UNCHANGED; NO logic cut. **A/B XAUUSD+ H1** (A=committed HEAD, reproduces exactly) — 2024: A 140t/46.43%/$1764.07/16.58R/0.1184avgR (L109/S31) -> B 140t/46.43%/$1190.84/11.76R/0.0840avgR (L109/S31) (Delta 0t, **ENTRY SET BYTE-IDENTICAL**, -$573.23/-32.5%, -4.82R); 2023: A 123t/34.96%/$628.18/3.95R/0.0321avgR (L75/S48) -> B 129t/35.66%/$663.85/3.42R/0.0265avgR (L77/S52) (Delta +6t, +$35.67/+5.7%, -0.53R, ZERO sign-flips on 122 common entries). 2yr net Delta -$537.56/-22.5%. **Regime-stamp shift 2024:** A 127 TRENDING/12 VOLATILE/1 RANGING -> B 123/16/1 (**+4 entries re-classified TRENDING->VOLATILE**, x1.0->x0.7 via Dynamic Barbell `regime_risk_multiplier` mq5:1582-1591 + regime-aware exit). **2024 per-trade dR (137 matched by EntryTime|Dir):** 84 differ, only 4 sign-flips, **81 of 84 shifts <0.5R** (53 unchanged / 76 <0.25R / 5 [0.25,0.5)R / 0 [0.5,1)R / 1 [1,2)R / 2 >=2R) — debounced-classifier ripple, NOT edge amputation. Count NOT collapsed/ballooned. mt5-developer SIGNED. **stok SIGNED → SIGN-OFF** — forming-bar regime read (H4 ADX/ATR/BB) front-ran the bar close = genuine look-ahead; closed-bar `[1]` is the honest real-time basis, NOT reverted. **2024 ENTRY SET byte-identical → edge amputation IMPOSSIBLE BY CONSTRUCTION; only position SIZING changed** (regime stamp re-class → Dynamic Barbell multiplier + regime-aware exit). The −32.5%/2024 (−22.5% 2yr) is acceptable honest look-ahead removal AND the correct intended risk behavior: the look-ahead had been letting genuinely-VOLATILE-regime trades classify TRENDING and ride at full 1.0× size — exactly what the Barbell exists to prevent ("protect capital in weak conditions"); closed-bar `[1]` restores the intended 1.0×→0.7× down-sizing (+4 entries) + sub-0.5R regime-aware-exit ripples (81/84 <0.5R, 4 sign-flips). 2023 zero sign-flips on 122 common, net flat. Bundled 1.4+1.5 coherent (1.5 removes the conservative zero-pad bias stok flagged; 1.4 closed-bar re-timing dominant). All kill bounds PASS. Folds into v18 re-baseline; cumulative honest-edge trajectory post-1.4 = $1,854.69 2-yr net (−43.6% vs $3,289 post-1.1 anchor). No change requested. Phase log: `progress/1-4.md`. |
| 1.5 | 1.6 | CVolatilityRegimeManager.mqh ctor | N | COMMITTED | | NOT-A-TRADING-PROBLEM: handle init. **Applied + 0-err/0-new-warn** (`Result: 0 errors, 38 warnings, 7440 ms elapsed`). 3 handle members (`m_handle_atr_h1/h4`, `m_handle_adx_h4`) set `=INVALID_HANDLE` at TOP of ctor (CVolatilityRegimeManager.mqh:91-96); handles still created in Initialize() L178-180, released in dtor L131-133; NO logic cut. **A/B XAUUSD+ H1 2024 (Model=1, production UltimateTrader.set) — A==B BYTE-IDENTICAL:** A (current source pre-fix) and B (post-fix) both 140t/46.43%/$1190.84/11.76R/0.084avgR/L109 S31; raw UTF-16LE CSVs share md5 `04d116972696f905c6ea5fa6701a7155` (cmp clean), decoded diff clean across all 281 rows, every EXIT row EntryTime/Dir/Entry/SL/PnL$/PnL_R identical → trade-for-trade, SL-for-SL, R-for-R IDENTICAL. mt5-developer SIGNED. Awaiting stok quick no-op sign-off → COMMITTED. Phase log: `progress/1-5.md`. |
| 1.6 | 1.7 | CVolatilityRegimeManager.mqh ATR-history | Y | COMMITTED | | FIX-SOUND risk lane; steady-state identical. **Applied + 0-err/0-new-warn** (`Result: 0 errors, 38 warnings, 7422 ms elapsed`). UpdateATRHistory body 386-396: `int got=CopyBuffer(m_handle_atr_h1,0,0,m_history_size,atr_buffer)`; loop `i<got`; divide `sum/got` (CMomentumFilter realized-count idiom, same as 1.5). NO `[0]`->`[1]` shift (out of fix-1.7 scope, per-plan). Destination `m_atr_history[]` sized 120>=got -> in-bounds; SOURCE zero-pad + fixed divisor was the bug. NO logic cut. **A/B XAUUSD+ H1 2024 (Model=1, production UltimateTrader.set) — A==B BYTE-IDENTICAL:** A (committed HEAD baseline) and B (post-fix) both 140t/46.43%/$1190.84/11.76R/0.084avgR/L109 S31; raw UTF-16LE CSVs share md5 `04d116972696f905c6ea5fa6701a7155` (cmp clean), decoded diff clean across all 281 rows. A reproduces the exact post-1.5 baseline md5 -> confirms A IS committed HEAD. Steady-state identity PROVEN; the warmup-bounded divisor delta (only when CopyBuffer<120 in first <120 H1 bars) did not surface at all on the full-year run. mt5-developer SIGNED. Awaiting stok no-op/risk-lane sign-off -> QA-PASS. Phase log: `progress/1-6.md`. |
| 1.7 | 1.8 | CCrashDetector.mqh | Y | COMMITTED | | NEEDS-CARE: gates the ONLY profitable short (Rubber Band). **Applied + 0-err/0-new-warn** (`Result: 0 errors, 38 warnings, 7854 ms elapsed`). Update D1 EMA reads forming `[0]`→closed `[1]` (copy 2, guard `<2`) so death-cross + reversal guard compare closed-bar EMA vs closed-bar `iClose(...,1)`; CheckRubberBandSignal EMA21/ATR/ADX forming `[0]`→closed `[1]` (copy 2, guard `<2`) for a STABLE baseline, **live `SYMBOL_BID` trigger KEPT (intentional front-run, documented in comment)**; death-cross/reversal-guard/ADX-filter/extension-formula/SL-TP math UNCHANGED; NO logic cut. **WINDOW = 2023** (gold correction; A-build fires 18 RB shorts — 2024 bull = zero, proves nothing). **A/B XAUUSD+ H1 Model=1, production .set (CrashDetector ON, RubberBandAPlusOnly)** — A (committed HEAD, reproduces post-1.4 2023 baseline exactly) 129t/35.66%/$663.85/3.42R/0.0265avgR (L77/S52) → B 129t/35.66%/$714.29/3.40R/0.0264avgR (L77/S52) (Δ 0t, +$50.44, -0.02R). **RUBBER-BAND SHORT count PRESERVED 18→18 entry-for-entry identical** (same EntryTime/Dir/Quality A/A+), RB-short PnL +$3.74/-0.20→-0.16R (preserved/improved). **ENTRY SET BYTE-IDENTICAL 129/129** (zero dropped/added, no sign-flips); only 26/129 trades changed PnL_R, ALL sub-0.1R (24 <0.05R, 2 0.05-0.1R, 0 ≥0.1R) = exit-side regime-aware-trailing ripple from closed-bar regime stamp. Count NOT collapsed/ballooned, death-cross frequency NOT dropped → KILL-CRITERION PASS. mt5-developer SIGNED. **stok SIGNED → ACCEPT** — correct invariant restored (forming-bar EMA mixed with closed-bar close was genuine intrabar-repainting defect; closed-bar `[1]` = stable non-repainting regime/structure basis, live-BID trigger = principled price-distance test against stable baseline). Non-blocking stok flags: confirmatory full-window 2019-2026 A/B recommended before final lock (trailing ripple is fleet-wide); traded RB short (CCrashBreakoutEntry) correctly out of scope. **stok independent second-half QA (re-decoded BOTH raw CSVs from scratch):** SIGN-OFF confirmed; all 18 RB shorts verified entry-for-entry with ZERO tier/entry/SL changes. CORRECTION to first-half write-up: entry set is STRUCTURALLY identical 129/129 (zero dropped/added/sign-flipped, all EntryTime/EntryPrice/SL identical) but NOT "Quality-identical" — ONE borderline non-RB short (`2023.10.05 03:00 Bearish Pin Bar`) re-tiered A+→A (RiskPct 1.83→1.22, down-sized a LOSER −$175.50→−$114.75 at −0.90→−0.89R) = the Phase-1.4 closed-bar regime-stamp SIZING mechanism, correct direction, NOT a fix defect, NOT gating. NOT committed (task instruction — main session runs git). Phase log: `progress/1-7.md`. |
| 1.8 | 1.9 | CMarketContext.mqh (MA200 + swings) | Y | COMMITTED | | NEEDS-CARE: L1 prem/disc feeds scorer +2. **Applied + 0-err/0-new-warn** (`Result: 0 errors, 38 warnings, 7980 ms elapsed`). UpdateMA200 784-793: CopyBuffer count 1→2, use `[1]` (closed), guard `>=2`. UpdateSwingPoints 798-831: CopyHigh/CopyLow start_pos `0→1` (closed bars, aligns GetDrawOnLiquidity shift-1), loops bound on realized count `got_high`/`got_low` (short-read guard), loop structure intact. NO logic cut, no new input. **A/B XAUUSD+ H1 Model=1 production .set — A==B BYTE-IDENTICAL all 3 windows:** 2024 (A reproduces post-1.7 HEAD: 140t/46.43%/$1190.84/11.76R/0.0840avgR/L109 S31, md5 `04d116972696f905c6ea5fa6701a7155`), 2023 (129t/35.66%/$714.29/3.40R/0.0264avgR/L77 S52, md5 `b11e07675cf4d577e27663ffde0aca19`), 2020 (122t/38.52%/-$115.46/-1.28R/-0.0105avgR/L96 S26, md5 `c287747b54b2afca810f40a6c1b46c3a`); each A/B `cmp` clean, paired diff 0 dropped/0 added/0 tier-changes/0 SL-dist-changes (max\|dSL\|=0.000 pts)/0 R-deltas/0 sign-flips. **TIER + SL-distance distributions UNCHANGED every year.** Mechanism: `CMarketContext::Update()` is once-per-H1-bar (271-274) on the new-bar tick → forming bar `[0]` is degenerate (high==low==open) → never the 20-bar swing extreme → cached swings/MA200 identical at consumption (CConfluenceScorer +2 prem/disc, CSignalValidator, CTradeOrchestrator, CMarketStateManager — all verified live on pattern-only config; engines OFF by default). Same structural reason as 1.5/1.6 (byte-identical), NOT 1.2-1.4 deflation (those read forming-bar indicator values). Count NOT collapsed/ballooned → KILL-CRITERION PASS. mt5-developer SIGNED. **stok SIGN-OFF (independent agent, verified against source not the A/B write-up):** Q1 byte-identity is the correct/expected outcome — verified once-per-H1-bar Update() (271-274) consumes the cache at the new-bar tick when forming bar `[0]` is degenerate (high≈low≈open); over a 20-bar MathMax/MathMin (InpSwingLookback=20, Inputs:99, unchanged at mq5:499) a fresh open is never the 20-bar extreme, so `[0..19]` vs `[1..20]` are identical at the decision point → the swing shift genuinely cannot move the cached extremes (1.5/1.6 settled-value class, NOT 1.2-1.4 indicator deflation). Q2 the +2 prem/disc re-tier was never possible here (GetDealingRangeHigh/Low + GetEquilibrium at 696-704 are computed directly from m_swing_high/low, so a re-tier needs equilibrium to move — forbidden by Q1); the genuine prem/disc sensitivity surfaces in Phase 2.4, which re-sources the dealing range from a D1 20-bar IPDA range (different/slower timeframe) AND re-derives tier thresholds (2.4-GATE) — so absence here is expected by construction, not a missed test. Non-blocking caveat: byte-identity proven on pattern-only config (engines OFF); CReversalSweepEngine's location filter reads the same axis and could surface the shift in Iter 3 per-engine validation — correctly out of this phase's production-path scope. KILL-CRITERION PASS. **BOTH agents signed → COMMITTED.** Concern resolved: plan expected a possible borderline-tier re-shift via the +2 axis; zero delta is real+explained (degenerate forming bar) and the axis sensitivity correctly defers to Phase 2.4. Phase log: `progress/1-8.md`. |
| 1.9 | 1.10 | CMacroBias.mqh; Enums.mqh (MACRO_MODE_PRICE_FALLBACK); CDisplay.mqh | Y | COMMITTED | | **BOTH agents signed (mt5 + INDEPENDENT stok). stok SIGN-OFF — every number verified at source.** ±2 bound (CMacroBias 303–311) cannot reach any `<=-3` long-veto gate (CSignalValidator `m_validation_macro_strong=3` literal mq5:539 / PBC 381 / CRegimeAwareExit thresh=3); Factor-3 (188–194) +1 for abs∈{0,1,2} identical → no tier moves. Fallback dormant in tester (DXY/VIX available) → A==B byte-identical 2024+2023. stok extra finding (mt5 missed): bear-context MR-long tightening at CSignalValidator 458/466 is the only live delta — directionally correct, un-measured → forced-fallback A/B recorded as mandatory PRE-LIVE checklist (NOT a blocker). PBC framing correction: ENABLED not disabled (5 trades/2024) — logged for Iter-3/6.9. KILL-CRITERION PASS (veto structurally impossible). Phase log: `progress/1-9.md`.** WIRED AnalyzePriceFallback into the both-symbols-unavailable branch; added `MACRO_MODE_PRICE_FALLBACK` (Enums 226-228); fallback EMA reads shifted to closed bar `[1]/[2]` (D1 EMA200 copy 1→2 use `[1]`; H4 fast/slow copy 2→3 slope `[1]` vs `[2]`); price-vs-200 uses live BID (documented); bias via existing ±2 thresholds; CDisplay renders PRICE_FALLBACK. NO logic cut, no new input. 0-err/0-new-warn (`Result: 0 errors, 38 warnings, 7621 ms`). **CRITICAL: the fallback does NOT activate on this config.** Tester journal proves DXY=`USDX` AND VIX are BOTH available (`MacroBias initialized (DXY: true, VIX: true)` every run; full −3..+3 live macro distribution; zero `NEUTRAL_FALLBACK`/`PRICE_FALLBACK`/`score forced to 0` lines). macro is LIVE not pinned-0 — the "pinned 0" audit refers to a LIVE account lacking USDX/VIX subscription, NOT the tester (which auto-downloads both). **A/B XAUUSD+ H1 Model=1 production .set = A==B BYTE-IDENTICAL 2024 & 2023** (2024: 140t/46.43%/$1190.84/11.76R/0.0840avgR/L109 S31, md5 `04d116972696f905c6ea5fa6701a7155`; 2023: 129t/35.66%/$714.29/3.40R/0.0264avgR/L77 S52, md5 `b11e07675cf4d577e27663ffde0aca19`; cmp clean, 0 paired-row diffs; identical live macro-score distribution A vs B both years). **stok-facing:** (1) fallback does NOT activate → macro 0→live delta = NONE here; (2) cannot veto healthy longs — ±2 fallback range cannot reach ANY −3 long-veto gate (CSignalValidator macro_strong=3, PBC `<=-3`, CRegimeAwareExit thresh=3) and CSetupEvaluator abs∈{0,1,2} all award same +1; bullish branch skews long with the uptrend by design; (3) does NOT re-empower PBC (PBC actually ENABLED in this .set [5 trades 2024], byte-identical A==B, gates `<=-3`/`>=3` beyond ±2); (4) count NOT collapsed/ballooned (140/129 identical). KILL-CRITERION PASS. mt5-developer SIGNED. **stok sign-off PENDING** (independent agent). Concern handed to stok: the behavioral change is only realizable on a live account without USDX/VIX in Market Watch (un-backtestable here) — stok to decide if a forced-fallback A/B is needed. Phase log: `progress/1-9.md`. |
| 1.10 | 1.11 | CRangeBoxDetector.mqh | N | COMMITTED | | NOT-A-TRADING-PROBLEM: tautology→real guard. **BOTH agents signed (mt5-developer + INDEPENDENT stok).** stok verified guard at source (172 = `Bars(H1)>=m_box_lookback+11`, matches shift-11/`m_box_lookback`-span reads at 177–178; body unchanged, no logic cut); A==B byte-identical correct+expected (tautology already always-true on gold + full-year `Bars(H1)>>lookback+11` + RangeBox engines OFF) → zero production impact. **Implemented + validated JOINTLY with Phase 1.11 in ONE combined byte-identity A/B** (both no-ops on gold). Constant-true guard `m_box_lookback+10<=100` → real `Bars(_Symbol,PERIOD_H1)>=m_box_lookback+11` (168–172); width-stability body unchanged; NO logic cut. 0-err/0-new-warn (`Result: 0 errors, 38 warnings, 7807 ms`). **A/B XAUUSD+ H1 2024 production .set = A==B BYTE-IDENTICAL** (A reproduces committed-HEAD Phase-1.9 md5; B = both fixes; all 5 CSVs cmp-clean + md5-match: Stats `04d116972696f905c6ea5fa6701a7155`, TradeEvents `825489b61fbf3db5cb2302361de66bb1`, Risk/Candidates/backtest_results identical; 140t/46.43%/$1190.84/11.76R/0.0840avgR/L109 S31 A==B). NO bisection needed (A==B with both applied). Mechanism: tautology was already always-true on gold + full-year `Bars(H1)>>lookback+11` + RangeBox engines OFF by default. KILL-CRITERION PASS (byte-identical). mt5-developer SIGNED. **stok sign-off PENDING** (independent agent — do NOT self-assert). Phase log: `progress/1-10.md`. |
| 1.11 | 1.12 | CMarketContext.mqh (IsPriceAboveMA200) | Y | COMMITTED | | NEEDS-CARE: directional gate. Keep gold default TRUE. **BOTH agents signed (mt5-developer + INDEPENDENT stok).** stok verified at source: `m_ma200_default_bullish` (73) defaults TRUE (ctor 150) → no-data branch (465) = exact prior `return true` on gold; live-BID compare unchanged; `IsMA200Available()` additive on concrete class only (NOT interface 34). Independent caller-grep = 5 refs all no-arg bool, none break; only directional consumer (CTrendContinuationEngine:71) OFF by default. A==B byte-identical correct by construction; FALSE-default delta lives only on non-gold/history-gapped feeds off the gold trade path. KILL-CRITERION PASS. **Implemented + validated JOINTLY with Phase 1.10 in ONE combined byte-identity A/B** (both no-ops on gold). Added `IsMA200Available() const` helper (454–457); no-data branch `if(m_ma200_value<=0) return true` → `if(!IsMA200Available()) return m_ma200_default_bullish` (459–467); new member `m_ma200_default_bullish` (73) DEFAULT TRUE (ctor 150 — gold profile, preserves warmup long bias); live-BID `bid>m_ma200_value` compare UNCHANGED; NO new input; NO logic cut. **Caller-grep BEFORE contract change:** all 5 callers (CMarketStateManager:117 pass-through, CDisplay:88 display, CTrendContinuationEngine:71 directional gate [engine OFF by default], CMarketContext def, IMarketContext:34 interface default) are no-arg bool → signature preserved, none break; `IsMA200Available()` added on concrete class only (not interface). 0-err/0-new-warn (`Result: 0 errors, 38 warnings, 7807 ms`). **A/B XAUUSD+ H1 2024 production .set = A==B BYTE-IDENTICAL** (5/5 CSVs cmp-clean + md5-match; 140t/46.43%/$1190.84/11.76R/0.0840avgR/L109 S31 A==B). NO bisection needed. Mechanism: gold default TRUE = prior hardcoded `return true`; MA200 available after warmup so data-available branch dominates; the FALSE-default behavioral delta is only realizable on non-gold/history-gapped symbols (off this config's scope). KILL-CRITERION PASS (gold default TRUE; XAUUSD trade-for-trade identical). mt5-developer SIGNED. **stok sign-off PENDING** (independent agent — do NOT self-assert). Phase log: `progress/1-11.md`. |

**Iter 1 kill/loop:** if 1.2 over-gates/floods, or 1.7(=fix1.8) reduces rubber-band frequency, or 1.9(=fix1.10) vetoes healthy longs → loop to stok before Iter 2.

---

## Iteration 2 — Scoring / Validation Pipeline

| Phase | Fixes | Files | Touches-logic | Status | Commit | Notes |
|---|---|---|---|---|---|---|
| 2.1 | 2.1 | CSignalOrchestrator.mqh; Structs.mqh (ADD `routed_engine` bool); CSetupEvaluator.mqh (WARN); + CExpansionEngine/CTrendContinuationEngine/CReversalSweepEngine/CRangeReversionEngine (set routed_engine in non-NULL-scorer block) | Y | ✅ COMMITTED | | **OPTION-2 REDO — BOTH agents signed (mt5-developer + INDEPENDENT stok re-sign-off). stok verified at SOURCE: `routed_engine` provably constant-false on production `.set` (`m_scorer` NULL by default; `SetScorer` only under `InpEnableMultiStrategy`; flag set inside all 4 engines' `if(m_scorer!=NULL)` blocks) → byte-identity STRUCTURAL not coincidental; semantic correct (true⟺scorer-ran⟺router-wired); Iter-3 migration coherent. ITER-3 VERIFICATION ITEM RECORDED: 4 separate per-engine flag sites (no shared base TagAndScore) → re-check the "every routed engine sets routed_engine in its non-NULL-scorer block" invariant with the router ON in Iter 3 and whenever engines change. KILL-CRITERION PASS.** **OPTION-2 REDO DONE + re-QA PASS.** Added `bool routed_engine` to EntrySignal (Structs.mqh:549, Init false :587); set `routed_engine=true` INSIDE each engine's `if(m_scorer!=NULL)` scoring block (CExpansionEngine:595 / CTrendContinuationEngine:176 / CReversalSweepEngine:217 / CRangeReversionEngine:164 — note repo has NO shared base TagAndScore, so all 4 per-engine sites covered); orchestrator `is_engine = signal.routed_engine` (CSignalOrchestrator.mqh:566, NOT raw major_engine); `best_signal.routed_engine` copied (:810); WARN + major_engine copy + regime_risk_multiplier gating retained; NO logic cut. **0-err/0-new-warn (`Result: 0 errors, 38 warnings, 7600 ms`).** **A==B BYTE-IDENTICAL BOTH 2024 AND 2023** — all 5 CSVs cmp-clean + md5-match committed-HEAD: 2024 Stats `04d116972696f905c6ea5fa6701a7155`/TradeEvents `825489b6…`/Risk `8babda0f…`/Candidates `bcc44924…`/backtest_results `177ca6a9…`; 2023 Stats `b11e07675cf4d577e27663ffde0aca19`/TradeEvents `7ef40d5f…`/Risk `b21a2999…`/Candidates `2fba7df1…`/backtest_results `07d903de…`. **Expansion trades RETURNED** (2024 ExpansionEngine rows 12/12, 2023 2/2); ZERO divergence incl. the prior sub-0.25R ripple → trade-for-trade/SL/R IDENTICAL = committed HEAD (2024 140t/$1190.84/11.76R; 2023 129t/$714.29/3.40R). Mechanism: `routed_engine` true ONLY via non-NULL `m_scorer` (SetScorer called only under `InpEnableMultiStrategy`) → on default path every signal `routed_engine=false` → legacy evaluator path unchanged → byte-identical; engine-scorer consumption deferred to Iter 3 (router ON, 2.4-GATE thresholds). **mt5-developer SIGNED (Option 2); stok re-sign-off PENDING (independent).** Phase log `progress/2-1.md` OPTION-2 REDO section. Do NOT commit; do NOT advance to 2.2 until stok re-signs. ----- (stok decision record:) **stok BINDING DECISION: OPTION 2 (scope-to-router).** Option 1 (current impl, `is_engine = major_engine != ENGINE_NONE`) is REJECTED — it honors a never-computed default `SETUP_NONE` on the 3 legacy-registered engines (`m_scorer` is NULL unless `InpEnableMultiStrategy` ran `SetScorer`; verified `CMajorStrategyEngine.mqh:42/53`, `CExpansionEngine.mqh:589`, `Structs.mqh:570`), so honoring it = a logic cut by side-effect + prejudges engine scoring before the 2.4-GATE. **REDO predicate:** add `bool routed_engine` to EntrySignal (default false), set `true` ONLY inside `TagAndScore`'s `if(m_scorer!=NULL)` block (true iff router-wired), and replace `bool is_engine = (major_engine!=ENGINE_NONE)` with `bool is_engine = signal.routed_engine` (acceptable equiv: `InpEnableMultiStrategy && major_engine!=ENGINE_NONE`); copy `routed_engine` in the best_signal block alongside `major_engine`. **Acceptance:** production `.set` (multi-strategy OFF) must be A==B trade/SL/R-identical BOTH 2024+2023 (md5-match committed-HEAD: 2024 Stats `04d116972696f905c6ea5fa6701a7155`, 2023 `b11e07675cf4d577e27663ffde0aca19`); the 6/2024+1/2023 Expansion trades must return; ZERO divergence (not even sub-0.25R ripple). **Iter-3 migration:** when `InpEnableMultiStrategy` turns ON, the same engine objects get `SetScorer`+`RegisterEngine` → `routed_engine=true` → tier honored on 2.4-GATE-RE-DERIVED `InpPoints*`. Full redo spec in `progress/2-1.md` (BINDING stok DECISION section). mt5: re-implement, re-compile 0-err/0-new-warn, re-run A/B, PROVE byte-identity, then BOTH agents sign. Do NOT advance to 2.2. | **IMPLEMENTED + 0-err/0-new-warn** (`Result: 0 errors, 38 warnings, 7812 ms`). is_engine=(major_engine!=ENGINE_NONE); engine signals SKIP legacy EvaluateSetupQuality, use engine scorer setupQuality/qualityScore, keep router regime_risk_multiplier; risk via GetRiskForQuality(quality,comment); best_signal.major_engine copied (also 5.12); CSetupEvaluator engine-token WARN added; NO logic cut. **A/B XAUUSD+ H1 production .set — NOT byte-identical** (A reproduces committed-HEAD: 2024 Stats md5 `04d116972696f905c6ea5fa6701a7155`, 2023 `b11e07675cf4d577e27663ffde0aca19`). 2024: A 140t/$1190.84/11.76R → B 134t/$946.50/10.30R (−6t); 2023: A 129t/$714.29/3.40R → B 128t/$825.20/4.47R (−1t, net UP). **The TRULY-legacy (`major_engine==ENGINE_NONE`) ENTRY set IS byte-identical both years** (2024 134/134, 2023 128/128 — 0 entry/SL/quality diffs); the ONLY drops are `ExpansionEngine` signals (`major_engine=ENGINE_EXPANSION`) + sub-0.25R exit ripple (0 ≥0.25R both years). **ROOT CAUSE:** the task's "engines off by default → byte-identical" premise is FALSE for this `.set` — `CExpansionEngine`/`CSessionEngine`/`CLiquidityEngine` are major engines registered as LEGACY plugins (`InpEnableExpansionEngine`/`SessionEngine`/`LiquidityEngine=true`, mq5:686/674/665, NOT gated on `InpEnableMultiStrategy`) and DO stamp major_engine. Candidates audit: A scored these A/A+ via the legacy comment-token evaluator; B's honored engine CConfluenceScorer returns SETUP_NONE (stricter, un-re-derived thresholds) → rejected. **Fix 2.1 is working EXACTLY as the literal spec says** — the conflict is `is_engine` SCOPE: spec-literal (any major_engine, current impl, rescopes legacy-registered engines NOW) vs scope-to-router (only InpEnableMultiStrategy engines, keeps production byte-identical, defers consumption to Iter 3). **mt5 SIGNED on compile+mechanism; NOT signing byte-identity. LOOPED to stok for the binding is_engine-scope decision** (mt5 recommends scope-to-router: honoring on a NON-re-derived scorer silently disables 3 live engines — the 2.4-GATE exists to prevent exactly that). Phase log: `progress/2-1.md`. Do NOT advance to 2.2 until reconciled. |
| 2.2 | 2.2 | CSMCOrderBlocks.mqh; CMarketContext.mqh; CConfluenceScorer.mqh; IMarketContext | Y | ✅ COMMITTED | | BETTER-OPTION: spine freshness+direction. `InpBOSFreshnessBars=8` (floor 6/cap 18; `{4,6,8,12}` per-engine avg-R sweep DEFERRED to Phase 3.8 — blind while dormant). **BOTH agents signed (mt5-developer + INDEPENDENT stok). stok SIGN-OFF verified at source:** byte-identity STRUCTURAL (scorer `new`'d at the single mq5:719 site inside `if(InpEnableMultiStrategy)` → constant-NULL + spine/input unreachable on production `.set`); spine `(bos!=BOS_NONE)&&dir-match&&fresh(≤8 H1 bars)` correctly decays the LATCHED-BOS staleness (never reset pre-fix) via Phase-1.2 closed-bar timestamps; plumbing additive+correct; wiring to all 4 engines confirmed. **BINDING axis-4/5 ruling: KEEP the gated `structure_shift` flowing into axis 4 (+1) AND axis 5 (+1) — one consistent definition, NOT a looser BOS for the points. Small/self-limiting: all engines set `requiresConfirmation=false` → axis 5 UNAFFECTED; axis 4 de-rates ≤1pt and never kills (`engine_confluence>0` is the independent spine).** Phase 3.8 sweep must use the SAME gated `structure_shift` for the gate AND axes 4/5. Two Iter-3 verification items recorded (GetRecentBOS/Time cross-field alignment; requiresConfirmation invariant). CSetupEvaluator:383 dead 4-arg Configure harmless (default param; deleted 2.7). **mt5 SIGNED; stok SIGNED.** 1.2 closed-bar anchors VERIFIED present (`m_last_bos_time`:153/`m_last_choch_time`:164, stamped `iTime(...,1)`). IMPLEMENTED: `GetLastBOSTime()`/`GetLastCHoCHTime()` getters (CSMCOrderBlocks:651-656); `GetRecentBOSTime()` on IMarketContext (:73-74) + CMarketContext override (max of BOS/CHoCH closed-bar time, :708-718); CConfluenceScorer spine `structure_shift=(bos!=BOS_NONE)` → `(bos!=BOS_NONE) && dir-match(LONG:BULLISH/SHORT:BEARISH) && fresh(TimeCurrent()-bos_time <= bars*PeriodSeconds(H1))`, `m_bos_freshness_bars`+`Configure()` 5th defaulted param; `InpBOSFreshnessBars=8` input; mq5:720 passes it. NO logic cut. **0-err/0-new-warn (`Result: 0 errors, 38 warnings, 9290 ms`).** **A/B XAUUSD+ H1 production .set = A==B BYTE-IDENTICAL 2024 & 2023** — A=clean-HEAD recompile (cmp-IDENTICAL to committed anchors), B=fix; all 10 CSVs (5×2yr) md5-match + cmp-clean (2024 Stats `04d11697…`/140t/$1190.84/0.0840avgR; 2023 Stats `b11e0767…`/129t/$714.29/0.0264avgR). Mechanism: scorer never constructed when `InpEnableMultiStrategy=false` (`new` only inside that gate at mq5:719) → edited spine + new input unreachable on default path → byte-identity is STRUCTURAL. Iter-3 wiring verified: 4 engines call `m_scorer.Score(signal,m_context,…)`; `ctx.GetRecentBOSTime()` reads SMC closed-bar 1.2 anchors. KILL-CRITERION PASS (byte-identical). Phase log `progress/2-2.md`. Do NOT commit; do NOT advance until stok re-signs. |
| 2.3 | 2.3 | CConfluenceScorer.mqh; CReversalSweepEngine; CRangeReversionEngine; +Inputs +mq5 Configure | Y | ✅ COMMITTED | | BETTER-OPTION: objective spine `InpSpineMinConfluence=25` (default; 0-100, below SMC hard-reject 40). Killzone axis ONLY for real expansion/breakout `engine_mode` (DISPLACEMENT/LONDON_BREAKOUT/COMPRESSION_BO/INSTITUTIONAL_CANDLE/PANIC_MOMENTUM), NOT a clock window (Silver Bullet/NY-Cont/London-Close excluded). **BOTH agents signed (mt5-developer + INDEPENDENT stok). stok SIGN-OFF (verified at SOURCE, not the write-up):** objective spine `CConfluenceScorer:118` reads `ctx.GetSMCConfluenceScore(dir) >= m_spine_min_confluence` (NOT `signal.engine_confluence`) → no engine self-certifies past the L3 gate; both named engines (CReversalSweepEngine:204-205, CRangeReversionEngine:139-140 TagAndRetarget) set REAL `GetSMCConfluenceScore(dir)` (`grep` confirms ZERO fabricated `engine_confluence=<int>` remain, 0 possible); `GetSMCConfluenceScore` is a real `IMarketContext:45`/`CMarketContext:510` method; killzone axis 7 (:180-186) keys off `engine_mode` (all 11 ENUM_ENGINE_MODE exist Enums.mqh:282-292, awarded set = correct expansion subset, 3 clock modes excluded); byte-identity STRUCTURAL — scorer `=NULL` (mq5:155), `new`'d only inside `if(InpEnableMultiStrategy)` (717/719), all 4 SetScorer in that block → constant-NULL on production `.set` → `Score()` never invoked → all edits unreachable → A==B forced. **SCOPE RULING (binding): ACCEPT leaving the other engines (CExpansionEngine 65/70, CSessionEngine 50, CLiquidityEngine computed) self-certified for now — the objective spine already makes their fabricated `engine_confluence` decorative w.r.t. both the L3 gate and the killzone point; migrating them is out of 2.3 scope, blind/un-validatable while the router is OFF, and recorded as an Iter-3 item (NOT done now).** `InpSpineMinConfluence=25` CONFIRMED as the right un-tuned floor (below the validator's 40 hard-reject → necessary-but-weaker; sweep by avg-R in Iter 3, do not lock). Expansion-only killzone CONFIRMED + the clock-mode exclusion is FINAL (honors measured no-killzone-edge: Silver Bullet −2.1R/6yr, London BO & NY Cont 0% WR). Four Iter-3 items recorded in `progress/2-3.md` (migrate other engines' confluence; sweep spine floor + BOS freshness by avg-R; confirm expansion killzone point earns avg-R; re-check no double-count / `routed_engine` invariant with router ON). KILL-CRITERION PASS. **mt5 SIGNED; stok SIGNED.** IMPLEMENTED: scorer L3 engine spine `(engine_confluence>0)` → `ctx.GetSMCConfluenceScore(dir) >= m_spine_min_confluence`; added `m_spine_min_confluence` (ctor+Configure 6th defaulted param =25); killzone axis 7 `engine_confluence>=70` → expansion-mode-gated; CReversalSweepEngine:204 + CRangeReversionEngine:139 (TagAndRetarget chokepoint, all 4 range sub-paths) set REAL `m_context.GetSMCConfluenceScore(dir)` (0 possible), removed fabricated 55/60/75 + `<=0→60` floor; `InpSpineMinConfluence=25` input (MULTI-STRATEGY ENGINES group); mq5:720-723 passes it. NO logic cut. **0-err/0-new-warn (`Result: 0 errors, 38 warnings, 8177 ms`).** **A/B XAUUSD+ H1 production .set = A==B BYTE-IDENTICAL 2024 & 2023** — A=baseline `.ex5` (md5 `9efea856…`) copied aside before editing, reproduces committed Phase-2.2 anchors EXACTLY (2024 Stats `04d11697…`/140t/$1190.84/0.0840avgR; 2023 Stats `b11e0767…`/129t/$714.29/0.0264avgR); B=fix (`.ex5` `13fa4527…`); ALL 8 CSVs (4×2yr) raw-md5 MATCH + `cmp` byte-for-byte clean. Mechanism: `InpEnableMultiStrategy=false` → scorer `new`'d only at mq5:719 inside that gate → constant-NULL → edited spine + new input + killzone + engines' scorer-consumed confluence all UNREACHABLE on default path → byte-identity STRUCTURAL. ExpansionEngine signals still present via LEGACY evaluator (`routed_engine=false`, Phase 2.1) — unaffected. Beneficial side effect (Iter-3 note): the objective spine also neutralizes the OTHER engines' fabricated `engine_confluence` (Expansion/Session/Liquidity self-certify 50/65/70 — out of 2.3 scope) for the L3 gate + killzone. KILL-CRITERION PASS (byte-identical). Phase log `progress/2-3.md`. Do NOT commit; do NOT advance until stok signs. |
| 2.4 | 2.4 | CMarketContext.mqh; CConfluenceScorer.mqh; UltimateTrader_Inputs.mqh; UltimateTrader.mq5 | Y | ✅ COMMITTED | | BETTER-OPTION: D1 dealing range `InpDealingRangeD1Lookback=20`. **Triggers the tier-threshold re-derivation (see 2.4-GATE).** **BOTH agents signed (mt5-developer + INDEPENDENT stok). stok SIGN-OFF (verified at SOURCE, not the write-up):** (1) byte-identity correct — production `.set` (decoded) `InpEnableMultiStrategy=false` + all 4 `InpEnableEngine*=false`; grep confirms `GetDealingRangeHigh/Low`/`GetEquilibrium`/`IsInDiscount/IsInPremium` read ONLY by CConfluenceScorer + the 3 router engines (CReversalSweepEngine:110/114-115, CTrendContinuationEngine:351-352, CRangeReversionEngine:89/93-94) → scorer constant-NULL + engines OFF → all 2.4 edits unreachable → A==B STRUCTURAL (8 CSVs md5+cmp both years). (2) D1-IPDA(20)-for-LOCATION vs H1-closed-swing-for-SL is the INTENDED + ICT-correct de-correlation — `GetSwingHigh/Low` (:691/696) UNCHANGED=H1 SL anchor; dealing range (:771-772)=`iHigh/iLow(PERIOD_D1,1..20)` closed D1; eq (:775)=D1 midpoint; the two axes now move independently (H1 swing→stop, D1 range→location bias). Dropped axis-1 `eq>0` sub-point (:128-138, now 0-1) = correct de-double-count (same equilibrium axis 2 consumes); +2 prem/disc (:148-149) kept as SOLE D1-backed location axis. (3) raw ceiling 10→9 (1+2+2+1+1+1+1; clamp@10 :195 retained-unreachable) is a DIRECT MANDATORY input to the 2.4-GATE — must re-derive on the new max-9 distribution 2019–2026, do NOT carry v18 `InpPoints*` forward (Risk-Register 2 & 6). Non-blocking forward note: D1-IPDA-20 sits in DISCOUNT for much of gold's uptrend pullbacks → +2 long-discount axis fires more often → density shift too. This phase correctly did NOT touch `InpPoints*`. KILL-CRITERION PASS. **mt5 SIGNED; stok SIGNED.** IMPLEMENTED: re-sourced GetDealingRangeHigh/Low (+GetEquilibrium) from an HTF D1 IPDA window — `D1DealingRangeHigh/Low()` scan `iHigh/iLow(_Symbol,PERIOD_D1,1..InpDealingRangeD1Lookback=20)` CLOSED D1 bars (CMarketContext.mqh:744/757, getters :771-772, eq :775); **KEPT GetSwingHigh/Low = the H1 closed swing (:691/696, UNCHANGED) → SL anchoring stays H1 = the de-correlation**; new ctor param `dealing_range_d1_lookback=20` (:147) wired from `InpDealingRangeD1Lookback` (Inputs:505, mq5:511). Scorer axis 1: DROPPED the `eq>0` sub-point (CConfluenceScorer.mqh:138, axis-1 now 0-1, was derived from the same swing), KEPT the +2 prem/disc (axis 2 :148-149, now the sole independent location axis backed by the D1 range; raw ceiling 9, clamp@10 retained-unreachable). NO logic cut. **0-err/0-new-warn (`Result: 0 errors, 38 warnings, 9088 ms`).** **A/B XAUUSD+ H1 production .set = A==B BYTE-IDENTICAL 2024 & 2023** — A=pre-edit baseline `.ex5` (md5 `13fa4527…`, reproduces committed Phase-2.3 anchors EXACTLY: 2024 Stats `04d11697…`/140t/$1190.84/0.0840avgR, 2023 `b11e0767…`/129t/$714.29/0.0264avgR); B=fix (`.ex5` `8913894…`); ALL 8 CSVs (Stats/TradeEvents/Risk/Candidates × 2yr) raw-md5 MATCH + `cmp` byte-for-byte clean (8 PASS/0 FAIL). Mechanism: `InpEnableMultiStrategy=false` + `InpEnableEngine*=false` → scorer never `new`'d + the 3 router engines OFF; GetDealingRange/GetEquilibrium/IsInDiscount/IsInPremium + the scorer axes are consumed ONLY by the scorer + those 3 engines (full repo grep) → all 2.4 edits UNREACHABLE on the default path → byte-identity STRUCTURAL. ExpansionEngine still present via LEGACY evaluator (12 rows B-2024, `routed_engine=false`, Phase 2.1) — unaffected. The de-correlated score distribution is what the 2.4-GATE (next phase) re-derives `InpPoints*` against; behavior exercised in Iter 3. KILL-CRITERION PASS (byte-identical). Phase log `progress/2-4.md`. stok concern handed over: confirm (a) the D1-IPDA(20) vs H1-swing-SL split is the right de-correlation for the 2.4-GATE / Iter 3; (b) dropping the axis-1 eq>0 sub-point + the new 0-9 raw ceiling is acceptable input to the tier re-derivation (do NOT carry v18 `InpPoints*` forward). |
| 2.4-GATE | (re-derive InpPoints*) | CConfluenceScorer.mqh (7-axis out-param) + CTradeLogger.mqh (LogGateScore) + Inputs (thresholds) | Y | ✅ GATE STOK-SIGNED — verdict (c) HYBRID | | **BINDING GATE VERDICT (stok-signed) → see `progress/2-4-GATE-verdict.md`.** **OUTCOME = (c) HYBRID, weighted to (b):** the SHARED-CONFLUENCE-SCORER TIER PATH is declared **NO-CLEAN-EDGE / NOT-GRADEABLE** → **`InpEnableMultiStrategy=false` STAYS in production**, **NO production `InpPoints*` shipped** (v18 `8/7/6/7` explicitly NOT carried forward; production thresholds untouched/never-consumed while engines OFF; derivation `9/8/4/3` stays a derivation artifact) — the GATE answering the Iter-3 edge question IN THE NEGATIVE for the shared-scorer path per design Section 5.2. **BUT the engines are NOT the pure losers the old buggy backtests showed:** the corrected-engine aggregate is a small REAL OOS-positive edge (PF 1.146, avgR +0.051 full / +0.21 OOS) that the SCORER FAILS TO RANK — so the engine edge is pushed to **per-engine 3.7–3.10 judged on each engine's OWN avg-R/PF, NOT the shared tier.** **Monotone thresholds derivable? NO** — Step-E HARD FAIL (scorer's raw6/7 NEGATIVE −0.099/−0.093, edge in raw4/5 +0.214/+0.112; cumulative-from-top "≥7" loses −0.081; only the WIDEST cuts positive — inverse of a working scorer), empty A+ ceiling (raw9=0, raw8=1 → cannot certify a top decile ≥50), density below the 200 hard floor (197) with 1/7 buckets ≥30. A SCORER-VALIDITY FAILURE, NOT a cut-point problem — explicitly NOT gerrymandered (design Step E / 5.2 forbids it). OOS is positive but ≤13-trade buckets and ALSO non-monotone-in-direction (peak at raw4 +0.500) → does not rescue the ranking. **Per-engine keep/cut READ:** **CUT RangeReversion** (avgR −0.688 full / −0.530 OOS, n4 — trips kill-criterion outright, decidable from GATE data NOW, no isolation run) → **Phase 3.9 ANSWERED = stays OFF**; **KEEP-but-small** TrendCont (+0.156/+0.156), Expansion (+0.070/**+0.294 OOS**), ReversalSweep (+0.056/+0.206) — direction decidable now (all marginally+ / OOS+) but **certify ON only on their OWN isolated 3.7/3.8/3.10 fresh numbers** (GATE samples are cross-cascade-entangled + tiny; positive prior, fresh-numbers certification). **Scorer-axis re-weight = recorded loop-to-stok Iter-3 item, NON-blocking** for the per-engine path (only load-bearing if a future iter wants the SHARED tier to gate engines). **NEXT ACTION → Iter-3b per-engine validation: Phase 3.9 (Range) records the CUT from GATE data (no run); run 3.7 (TrendCont) / 3.8 (Reversal — incl. deferred `InpBOSFreshnessBars {4,6,8,12}` + `InpSpineMinConfluence` avg-R sweeps + HTF-veto-survivor short measurement) / 3.10 (Expansion) ISOLATED per-engine on own avg-R/PF vs the Iter-3 kill-criterion (PF<1.0 OR avgR<0 full AND OOS → OFF); kept engines trade on own entry/SL/TP + per-engine risk, NOT a shared-tier multiplier; production stays byte-identical.** Floors NOT relaxed, no engine force-enabled, production byte-identity preserved. Do NOT run git. — (mt5 DERIVATION DATA below) **DERIVATION DATA READY → see `progress/2-4-GATE-data.md` (the per-bucket expectancy table + density + veto/news + per-engine breakdown).** mt5 EXECUTED the engines-ON derivation per design Section 3/6: compile `Result: 0 errors, 38 warnings` (0 new; EA `db7b1998…`); ADDITIVE-LOGGING PROOF — production `.set` (engines OFF) 2024 A==B BYTE-IDENTICAL (committed-HEAD `229950b9…` vs logging-build `db7b1998…`; all 4 CSVs md5+cmp, anchors `04d11697…`/`825489b6…`/`bcc44924…`/`8babda0f…`; NO GateScores file on production → logging inert, no leak into production scoring). Ran `UltimateTrader_GATE_derive.set` (engines+all-subpaths ON, auto-disable OFF, floors HELD 8/25/20/10, thresholds 9/8/4/3, CompressionBO ON) XAUUSD+ H1 Model=1 2019–2025 + 2026 YTD. **RESULTS:** 560 scored signals; **HTF_SHORT_VETO assertion PASS (159 scored-vetoed shorts, 0 TRADED)**; IsNewsDay=0 (static-blackout removed at runtime). Expectancy pop (traded, veto+news excluded) = **197 FULL / 78 OOS**. **DENSITY INADEQUATE:** 197 < 200 hard floor; only **1 of 7** raw buckets ≥30 (raw=5, n=123); A+ band (raw8-9)=1 trade (<50). **MONOTONICITY FAIL (full):** meanR raw5(+0.112)>raw6(−0.099)≈raw7(−0.093), raw3(−0.153)<raw4(+0.214) — non-monotone (scorer-validity signal per Step E). raw9 EMPTY. Tier-as-scored degenerate at B+ (500/560). **4 GATE-routed engines aggregate:** 197t, net +$566.79, PF 1.146, avgR +0.051 (OOS +$632/avgR +0.21); ExpansionEngine +0.070, ReversalSweep +0.056, TrendCont +0.156, **RangeReversion −0.688 (loser, full+OOS)**; routed net-neg 2019–21 (−$857) / net-pos 2022–26 (+$1,424). **VERDICT (mt5 read):** points toward no-clean-grade / low-edge for the shared-scorer path (density inadequate + non-monotone + empty A+) — design Section 5 territory. mt5 did NOT set `InpPoints*`. stok derives thresholds (PROVISIONAL-conservative vs scorer-axis re-weight loop vs keep `InpEnableMultiStrategy=false`) + signs off before 3.7. Artifacts: `claude/gate_axis_expectancy.csv`, `claude/gate/`, `UltimateTrader_GATE_derive.set`. — (DESIGN BELOW, BINDING) **HARD GATE before Iter 3 — stok DESIGN COMPLETE, ready for mt5 execution. See `progress/2-4-GATE-design.md` (BINDING).** **ORDERING RESOLVED = Option (b) REORDER:** land+commit **3.1–3.6 FIRST** (HTF short veto live, 3-mode split, S6 box, swing-lookback=10, news-flat functional-or-parked), THEN the GATE derivation, THEN 3.7–3.10. Justification: 3.1–3.6 each move the score distribution (mode→killzone axis 7; veto→removes neg-EV shorts from the population; swing-lookback→axes 2/3; news-flat→population; S6 box→population) and none reads `InpPoints*`, so the GATE must fit the corrected-engine distribution Iter-3 will be judged on. New sequence: `2.4 → 3.1..3.6 → [2.4-GATE] → 3.7..3.10`. **DENSITY PLAN:** derive with `InpEnableMultiStrategy=true` + ALL 4 `InpEnableEngine*=true` + ALL sub-paths ON + per-mode auto-disable OFF (one SHARED threshold set → need the full cross-engine distribution); spine/SMC floors HELD at production default (NEVER relaxed — must rank only trades production can take); the ONE relaxation = derivation logging-width thresholds `9/8/4/3` so raw≥3 is logged+traded; min 30 trades/bucket, A+ at-or-above ≥50, total ≥400 (hard floor 200); inadequate density is itself a partial "no-edge" finding → conservative PROVISIONAL thresholds, never relax floors to manufacture density. **SWEEPS:** `InpBOSFreshnessBars`/`InpSpineMinConfluence` HELD at 8/25 for the GATE; swept per-engine by avg-R in 3.7–3.10 (per 2.2/2.3). **EXEC:** new `UltimateTrader_GATE_derive.set` (production `.set` untouched); add 7-axis out-param to `Score()` (additive→byte-identity preserved) + `LogGateScore` fired INSIDE each engine's scoring block (the Candidates path is truncated by `valid=(tier!=SETUP_NONE)` + orchestrator `continue` — cannot see SETUP_NONE/raw-0–2); join `GateScores.SignalID ⋈ TradeEvents.SignalID`, realized outcome = `Total_R`; run 2019–2025 `backtest_all.sh 1` + 2026 YTD Model=1 XAUUSD+ H1; derive 4 thresholds by cumulative-from-top top-decile A+, monotone (HARD: non-monotone expectancy = scorer-validity FAIL→loop to stok, NOT gerrymander cuts), B-reachability decided consciously. Do NOT carry v18 8/7/6/7 forward. **ACCEPTANCE:** ordering honored + zero vetoed shorts in population + density adequate + monotone + A+=genuine top decile + OOS sign-coherent + B decided. **KILL:** A+ realized EV≤0 even at raw-9 → engines have no gradeable edge on this shared-scorer path → keep `InpEnableMultiStrategy=false`, report to stok. stok sign-off required before 3.7. |
| 2.5 | 2.5 | CSetupEvaluator.mqh (CHoCH) | Y | ✅ COMMITTED | | NEEDS-CARE: un-masks bear-SHORT +2 boost. KILL if short avg-R degrades or A/A+ short count inflates w/o PnL → REJECT. **BOTH agents signed (mt5-developer + INDEPENDENT stok). stok SIGN-OFF (2026-06-27, verified at SOURCE):** fix matches spec exactly — `trend_alignment` isolated 0–3 (127–140), bear +2 boost (110–114) + pattern/H4 bonus (143–146) independent in `points`, `points += MathMax(trend_alignment, choch_points)` (179); cap-at-10 (320–321) AFTER full cascade + BEFORE tiers (324–327), intact, no super-tier; NO logic cut. **KILL-CRITERION PASS (neither limb tripped):** (a) short avg-R IMPROVED every cut (full +0.0072, OOS +0.0076), no degradation; (b) A/A+ short count +18/+5.8% came WITH net +$157/+3.7% AND per-trade avg-R +0.0045 → inflation WITH proportional+ PnL. **Q2 OOS A/A+ pocket −$1,252 = ACCEPTABLE residual** (fix made a pre-existing −$1,314 pocket LESS negative + improved its avg-R; kill-criterion governs degradation/PnL-less-inflation, neither occurred; gold structural-uptrend penalty on counter-trend shorts, not a defect). **Q3 overall −1.07% = ACCEPTABLE collateral** (LONG −$392 loss-scaler ripple from re-sequenced fills, NOT the short side this fix targets; overall avg-R still +0.0019; the CHoCH branch SHOULD be reachable). **NON-BLOCKING FOLLOW-UP FLAG (stok):** LEGACY pattern path → engine HTF-short-veto (3.6) does NOT apply → legacy bear-regime-short A/A+ promotions have no HTF-trend guard (only the bear-regime gate + tier/risk math) → recommend an HTF-trend guard on the legacy bear-short A/A+ path (mirror 3.6 scoped to legacy bear shorts) in a later iter (Iter-6/6.9 candidate) — NOT a 2.5 blocker. FIX (CSetupEvaluator.mqh ~116–177): isolated true `trend_alignment` (0–3) into its OWN local (was added straight into `points`); `points += MathMax(trend_alignment, choch_points)` at end of Factor-1A block → preserves bear +2 SHORT boost + pattern/H4 bonus AND makes the CHoCH branch reachable. Cap-at-10 (307–308) confirmed intact after block (B tiers = only 4 defined, no super-tier). NO logic cut. **0-err/0-new-warn** — B `Result: 0 errors, 38 warnings, 10251 ms`; A (clean-HEAD recompile) `Result: 0 errors, 38 warnings, 10380 ms`. A `.ex5` `398e1cdc…` ≠ B `.ex5` `0c90169f…` (real scoring change, declared NEEDS-CARE, NOT byte-identical). **A/B XAUUSD+ H1 full 2019–2026, production `[TesterInputs]` (InpEnableMultiStrategy=false, gold, BOTH; 395 inputs pinned; Model=1) — 2 runs.** Journal proof (B): bear-SHORT +2 boost fired 493×; NEW reachable `CHoCH … replacing trend-alignment points 0/1 with CHoCH points 2` fired 183× (correctly competes vs ISOLATED 0/1, was masked before). **OVERALL:** A 953t/$25121.13/+0.1256avgR → B 966t/$24851.22/+0.1275avgR (Δ +13t, −$269.91/−1.07%, +0.0019avgR); OOS(≥2024) A 341t/$17449.68/+0.1840 → B 344t/$17123.99/+0.1830 (Δ +3t, −$325.69/−1.87%, −0.0010avgR). **SHORT-SIDE (decision data):** SHORT full A 362t/$5336.36/**+0.1240avgR** → B 371t/$5458.58/**+0.1312avgR** (Δ +9t, +$122.22, **+0.0072 IMPROVED**); SHORT OOS A 76t/$333.12/**+0.0726** → B 76t/$308.47/**+0.0803** (Δ +0t, −$24.65, **+0.0076 IMPROVED**); SHORT tiers full A A+196/A112/B+54 → B A+209/A117/B+45; **SHORT A/A+ full A 308t/$4272.88/+0.1243avgR → B 326t/$4430.11/+0.1288avgR (count +18/+5.8%, net +$157.23/+3.7%, avgR +0.0045)**; SHORT A/A+ OOS A 50t/−$1313.59/−0.1186 → B 58t/−$1252.33/−0.0966 (count +8, net +$61.26 less-neg, avgR +0.0220). LONG full A 591t/$19784.77/+0.1266 → B 595t/$19392.64/+0.1252 (Δ +4t, −$392.13 — the overall dip is LONG-side loss-scaler ripple, NOT the short side this fix touches). **KILL-CRITERION: PASS (NOT tripped)** — (a) short avg-R IMPROVED on every cut (full +0.0072, OOS +0.0076), did NOT degrade; (b) A/A+ short count inflated +18 but net rose proportionally+ (+$157 full, OOS less-neg +$61, per-trade avg-R up) → inflation WITH PnL, not without. **stok-facing:** un-masks the bear-SHORT +2 boost → ~18 more bear-regime shorts promoted A/A+ (raises risk on those); on 2019–2026 they are net-positive; OOS A/A+ short pocket stays net-negative (−$1,252, less so than A's −$1,314) — confirm gated/acceptable given gold's structural uptrend, do not relax. Phase log `progress/2-5.md`. |
| 2.6 | 2.6 | CMarketFilters.mqh | N | ✅ COMMITTED | | NOT-A-TRADING-PROBLEM: comment/rename only. Byte-identical. **Comment-only edit applied** to `CalculatePatternConfidence` (CMarketFilters:136): doc-comment + in-body comment documenting that `pattern`/`entry_price`/`ma_fast_period`/`ma_slow_period` are intentionally ignored (this is an ADX/ATR ENVIRONMENT gate, not pattern confidence; locked baseline) + the quiet-regime cull note (ATR<6 AND ADX outside 20–50). NO math/signature/control-flow change — `git diff` is entirely inside comments. Compile `Result: 0 errors, 38 warnings`. Byte-identical proven via COMBINED 2.6+2.7+2.8 A==B (raw CSV md5 + cmp; see 2-8.md). **BOTH agents signed (mt5-developer QA + INDEPENDENT stok).** stok SIGN-OFF (2026-06-27): comments emit no code (compiler discards them); byte-identity confirmed on production config (A==B, A Stats md5 `33317ded…` matches post-2.5 HEAD). Phase log `progress/2-6.md`. |
| 2.7 | 2.7 | CSetupEvaluator.mqh (delete dead overloads + orphan include) | N | ✅ COMMITTED | | FIX-SOUND dead code. Watch error 106 from include removal. Byte-identical. **GREP-VERIFIED FIRST then applied.** DELETED: the two dead engine overloads `EvaluateSetupQuality(const EntrySignal&, IMarketContext*, int&)` + `(const EntrySignal&, IMarketContext*)` (were CSetupEvaluator 402/410) AND the orphan `#include "CConfluenceScorer.mqh"` (was line 16, used ONLY by the deleted overload at 404). KEPT: the legacy `EvaluateSetupQuality(ENUM_TREND_DIRECTION daily,…)` overload (line 72 — the live non-engine call from CSignalOrchestrator:746) + the CConfluenceScorer class & all other includes of it (UltimateTrader.mq5:101, 4 engines, CMajorStrategyEngine). Repo-wide grep = ONLY caller is CSignalOrchestrator:746, which post-2.1 reads `signal.setupQuality` for engines (no call) and resolves to the LEGACY overload otherwise → engine overloads provably dead. Tests/TestQualityScoring.mq5 is a standalone replica (no include, not a caller). Compile `Result: 0 errors, 38 warnings` — **no error 106 / no undeclared-identifier** from the include removal. Byte-identical via COMBINED 2.6+2.7+2.8 A==B (see 2-8.md). **BOTH agents signed (mt5-developer QA + INDEPENDENT stok).** stok SIGN-OFF (2026-06-27, verified at SOURCE not just write-up): re-ran the repo-wide grep — exactly ONE live `EvaluateSetupQuality` call (CSignalOrchestrator:746); read 735–748 confirming the 7-arg call resolves to the KEPT legacy overload (CSetupEvaluator:72, intact 72–76), engine branch reads `signal.setupQuality` (no call); zero callers of the deleted (EntrySignal&, IMarketContext*) overloads (built a LOCAL non-shared scorer); CConfluenceScorer class + UltimateTrader.mq5:101 / 4 engines / CMajorStrategyEngine includes all preserved; no live path lost. Phase log `progress/2-7.md`. |
| 2.8 | 2.8 | CConfluenceScorer.mqh (Configure WARN) | N | ✅ COMMITTED | | FIX-SOUND config-diagnostic. Decide B-tier reachability with 2.4-GATE. **WARN-only edit applied** in `Configure` (CConfluenceScorer:~92): `PrintFormat` when `!(aplus>=a && a>=bplus && bplus>=b)` documenting that a non-monotone ordering makes a lower tier UNREACHABLE (e.g. v18 8/7/6/7 → SETUP_B dead). NO clamp, NO reorder, NO math change — tier cascade (Score 259–262) untouched; B-reachability decision left to 2.4-GATE (already COMMITTED). Compile `Result: 0 errors, 38 warnings`. Byte-identical via COMBINED 2.6+2.7+2.8 A==B (see 2-8.md). **BOTH agents signed (mt5-developer QA + INDEPENDENT stok).** stok SIGN-OFF (2026-06-27, verified at SOURCE): the guard (CConfluenceScorer:114–124) is a pure `PrintFormat` inside the `!(aplus>=a && a>=bplus && bplus>=b)` branch — no clamp, no reorder, no assignment; the Score() tier cascade is untouched; a diagnostic print cannot move `out_score` or the returned tier; byte-identity confirmed (A==B). B-reachability correctly deferred to 2.4-GATE. Phase log `progress/2-8.md`. |

**Iter 2 kill/loop:** 2.5 short-side avg-R degradation → reject/loop. **MANDATORY: 2.4-GATE complete + signed off before Iter 3.**

---

## Iteration 3 — Multi-Strategy Engine Revision + Fresh Validation

### 3a — Per-engine correctness (land before validation)

| Phase | Fixes | Files | Touches-logic | Status | Commit | Notes |
|---|---|---|---|---|---|---|
| 3.1 | 3.1 | CReversalSweepEngine.mqh | Y | COMMITTED | | **BOTH agents signed (mt5-developer + INDEPENDENT stok). stok SIGN-OFF (verified at SOURCE, not the write-up):** (1) byte-identity STRUCTURAL — engine `new`'d only inside `if(InpEnableMultiStrategy)` (false on prod `.set`, same anchors `04d11697…`/`b11e0767…` reproduced) → S6-box lifecycle unreachable → A==B forced; journal "S6: Failed Break Long" lines = legacy standalone telemetry, NOT the engine S6. (2) box lifecycle is a faithful CRangeReversionEngine mirror (ctor NULL :253 / `new`+Init BEFORE S6 :289-298 / Update once-per-bar :362 / Deinit+delete AFTER S6 :328→:332, correct teardown order) and wired into S6 (`new CFailedBreakReversal(m_box_detector)` :298 → CFailedBreakReversal:113-117 adds GetBoxHigh/Low when IsBoxValid → box-edge sweeps restored over PDH/PDL). **S6-long-only on gold triple-guarded:** `g_profileEnableS6Short=false` (Inputs:72) short-circuits the SHORT loop (CFailedBreakReversal:191) BEFORE any box-high level is consumed; XAUUSD profile branch (mq5:273) is a bare `break` — NO override (only JPY:289/GBPJPY:305 set it true); + AdoptSignal prem/disc gate + Phase-3.5 HTF short veto. Box edges CANNOT resurrect short S6 on gold. (3) **fatal box-Init ACCEPTED** — box is core shared infra the engine owns (not an optional alpha path); fail closed (`return false`, matches CRangeReversionEngine:255) is the safer/debuggable contract; asymmetry vs the non-fatal S6/Liq/RB sub-detectors is principled (shared dependency vs independent edge). **Engines-ON validation OWED at the GATE / Phase 3.8:** box must reach IsBoxValid often enough on 2019–2026 to add measurably over PDH/PDL, and S6's edge judged FRESH per Iter-3 kill-criterion (PF<1.0 OR avg-R<0 → stays OFF). NO logic cut. KILL-CRITERION PASS (byte-identical). See `progress/3-1.md`. — (mt5 detail below) FIX-SOUND: give S6 its range-box. S6 LONG-only; keep HTF short veto + S6-short disable. **mt5 side complete — gave S6 its real box detector** (only `CReversalSweepEngine.mqh` touched; `CFailedBreakReversal.mqh` needed NO code change — it already accepts the box pointer and gates short via `g_profileEnableS6Short`). Added `CRangeBoxDetector *m_box_detector` (ctor NULL; Initialize `new`+`Init()` BEFORE S6; `Update()` in CheckForEntrySignal after the per-bar guard; Deinit+delete AFTER S6 freed); `m_s6 = new CFailedBreakReversal(m_box_detector)` (real box, was NULL). Lifecycle mirrors CRangeReversionEngine (ctor :199 / Init :254-260 / Update :348 / Deinit :316). NO logic cut. **Box-wiring + S6-long-only inspection (for engines-ON at GATE/3.8):** box correctly lifecycle-managed + wired into S6 (S6 now adds `GetBoxHigh/Low` to its sweep levels when `IsBoxValid()`, restoring box-edge sweeps over PDH/PDL-only). S6 short loop short-circuited by `if(!g_profileEnableS6Short) return signal;` (CFailedBreakReversal:191) BEFORE `levels_above` is consumed; on gold `InpEnableS6Short=false` (Inputs:72) + the gold profile inherits it (mq5:268, XAUUSD branch does NOT override) → box edges CANNOT resurrect short S6. HTF short veto (Phase 3.5, hard-ON) is the orchestrator-level 2nd guard. **0-err/0-new-warn (`Result: 0 errors, 38 warnings, 7628 ms elapsed, cpu='X64 Regular'`).** **A/B XAUUSD+ H1 Model=1 production .set = A==B BYTE-IDENTICAL 2024 & 2023** — A (backed-up committed-HEAD `.ex5`) and B (fix-3.1 build) reproduce the committed anchors (2024 Stats `04d11697…`/TradeEvents `825489b6…`; 2023 Stats `b11e0767…`/TradeEvents `7ef40d5f…`); all 4 CSVs/yr md5 MATCH + `cmp` IDENTICAL. Mechanism: engine only `new`'d inside `if(InpEnableMultiStrategy)` (OFF on prod `.set`) → S6-box change unreachable → no-op on production path. KILL-CRITERION PASS (byte-identical). mt5-developer SIGNED; **stok sign-off PENDING** (independent agent — do NOT self-assert). See `progress/3-1.md`. |
| 3.2 | 3.4 | CReversalSweepEngine.mqh; Enums.mqh; CSetupEvaluator.mqh | Y | mt5-QA-DONE | | **OPTION-(b) REDO DONE + re-QA PASS — mt5-developer SIGNED; INDEPENDENT stok re-sign-off PENDING. Do NOT commit / do NOT advance to Phase 3.3 until stok re-signs.** Per stok's binding Option-(b) (Option-A impl), DELETED the two Factor-4 else-if branches at `CSetupEvaluator.mqh:243-246` ('FailedBreak'/'Failed Break'→+2, 'Rubber Band'→+1); the legacy comment-token overload is invoked ONLY on the non-engine else-branch (orch:723; routed engines use `signal.setupQuality` at :721) so the engine/GATE path loses NOTHING — the branches had only un-starved the LIVE legacy S6/Crash plugins (deferred to Iter-6/6.9). **KEPT** (unchanged): `MODE_RUBBER_BAND`(12)+`MODE_FAILED_BREAK`(13) enum (appended/ordinal-safe), CReversalSweepEngine Path-1 S6→`MODE_FAILED_BREAK`/'FailedBreak' / Path-2 sweep→`MODE_SFP`/'LiquiditySweep' / Path-3 rubber-band→`MODE_RUBBER_BAND`/'Rubber Band', the Fix-2.1 WARN list. NO logic cut. **0-err/0-new-warn (`Result: 0 errors, 38 warnings, 7801 ms`).** **A/B XAUUSD+ H1 production .set = A==B BYTE-IDENTICAL 2024 & 2023** — A=committed-HEAD `445ae15b…` (reproduces anchors), B'=Option-b redo `3943b42b…`; ALL 8 CSVs (Stats/TradeEvents/Candidates/Risk×2yr) md5 MATCH + `cmp` clean (2024 Stats `04d11697…`/140t/$1190.84/11.76R; 2023 `b11e0767…`/129t/$714.29/3.40R). **Promoted-trade reversal CONFIRMED:** the first-B +4(2024)/+2(2023) S6/Rubber-Band promotions are GONE — 'Failed Break' 7→3 (2024) / 2→1 (2023), 'Rubber Band' 19→18 (2023), Δtrades=+0 both years. Mechanism: removing an additive legacy Factor-4 branch restores prior byte-identical production behavior; the GATE attribution (enum modes + token relabel) is fully retained (engine OFF on prod → inert there, active when the engine turns ON at the GATE/Phase 3.8). KILL-CRITERION (byte-identical on production) PASS. Phase log `progress/3-2.md` Option-(b) REDO section. Do NOT commit; do NOT advance until stok re-signs. ----- (stok decision record:) **stok BINDING DECISION = OPTION (b): SCOPE the 'FailedBreak'/'Rubber Band' Factor-4 branches OFF the legacy production path; keep production BYTE-IDENTICAL now (mirrors Phase-2.1 Option-2). Recommended impl = Option A: DELETE the two branches at `CSetupEvaluator.mqh:243-246`** — the engine/GATE path reads `signal.setupQuality` and does NOT route through this legacy comment-token overload (`EvaluateSetupQuality(...)` :67, invoked at orch:723 ONLY on the non-`is_engine` else-branch), so the GATE loses NOTHING; the branches AS PLACED fire ONLY for the live legacy S6/Crash plugins on production. Alt Option B: guard `&& StringFind(pattern,"S6:")<0` / `&& StringFind(pattern,"Death Cross")<0` AND drop the spaced `"Failed Break"` OR-clause — more fragile, no production gain, A preferred. **KEEP the `MODE_RUBBER_BAND`/`MODE_FAILED_BREAK` enum values + Path-1/2/3 token relabel (inert on production, serves the GATE) + the Fix-2.1 WARN list + enum-ordinal-safety PASS.** Rationale: the plan defers legacy-plugin comment/edge re-validation (incl. S6/PBC kill-criteria) to Iter-6/6.9; promoting a known marginal-LOSER (S6, audit PF 0.78) on its comment token NOW — ahead of its own kill-criterion run — inverts the validation order, and the −$35(2024)/−$82(2023) drag is mechanism-consistent (the +trades are 6/7 S6-long loser vs 1 Rubber-Band winner +0.130R). REDO ACCEPTANCE: production `.set` A==B md5-match committed-HEAD BOTH years (2024 Stats `04d11697…`, 2023 `b11e0767…`); the +4/+2 S6/RB trades disappear; ZERO divergence (not even sub-0.25R ripple). mt5 re-implement → re-compile 0-err/0-new-warn → re-run A/B → PROVE byte-identity → BOTH agents sign. **stok NOT-SIGNED on current impl; do NOT commit; do NOT advance past 3.2.** Full predicate in `progress/3-2.md` (BINDING stok DECISION + SCOPING PREDICATE). — (prior mt5-QA-DONE write-up:) BETTER-OPTION: add MODE_RUBBER_BAND + MODE_FAILED_BREAK (3 buckets). **Must land before any engine A/B** (clean attribution). **mt5 side complete — 3 distinct modes/tokens wired + Factor-4 branches added. 0-err/0-new-warn (`Result: 0 errors, 38 warnings, 8962 ms`).** IMPLEMENTED: Enums.mqh APPENDED `MODE_RUBBER_BAND` (ord 12) + `MODE_FAILED_BREAK` (ord 13) at END (existing 0..11 unchanged); CReversalSweepEngine Path1 S6→`MODE_FAILED_BREAK`/'FailedBreak', Path2 sweep→`MODE_SFP`/'LiquiditySweep' (unchanged), Path3 rubber-band→`MODE_RUBBER_BAND`/'Rubber Band' (spaced, now matches the orchestrator A/A+ gate StringFind 738-739); CSetupEvaluator Factor-4 added 'FailedBreak'/'Failed Break'→+2 and 'Rubber Band'→+1 (both <=2 guardrail). NO logic cut (entry-set diff: 0 trades dropped either year). **ENUM-ORDINAL-SAFETY PASS** — appended at end; `PersistedModePerformance.mode_id` serialized only by Expansion/Liquidity/Session engines (value-keyed read `==`, not ordinal index), none track the 2 new modes; CReversalSweepEngine has no mode-perf persistence; no `switch` on ENUM_ENGINE_MODE; CTradeLogger EnumToString is value-mapped. **A/B XAUUSD+ H1 production .set — A != B (NOT byte-identical) both 2024 & 2023.** A reproduces committed anchors EXACTLY (2024 Stats `04d11697…`, 2023 `b11e0767…`). 2024: A 140t/$1190.84/0.0840avgR → B 144t/$1155.77/0.0794avgR (Δ +4t, −$35.07/−0.32R); 2023: A 129t/$714.29/0.0264avgR → B 131t/$632.59/0.0168avgR (Δ +2t, −$81.70/−1.20R). **ROOT CAUSE (proven by entry-set diff, 0 dropped):** the task's "engine OFF → byte-identical" premise is INCOMPLETE — the new Factor-4 branches are reachable on the production path via the LIVE LEGACY standalone plugins `CFailedBreakReversal` (g_failedBreakRev, mq5:604, gated `InpEnableS3S6=true`, emits 'S6: Failed Break …') and `CCrashBreakoutEntry` (g_crashEntry, mq5:638, `InpEnableCrashDetector=true`, emits 'Rubber Band Short (Death Cross)') — both `routed_engine=false` → scored by the legacy CSetupEvaluator (orch:723). Previously starved at 0 → now +2/+1 → promotes previously-sub-tradeable setups: 2024 +4 NEW all 'S6: Failed Break Long'; 2023 +2 NEW = one 'S6: Failed Break' + one 'Rubber Band Short'. Common-entry Q/risk shifts = sequential loss-scaler ripple. **mt5 SIGNED on compile+mechanism+ordinal-safety+wiring; NOT signing byte-identity (A != B). LOOPED to stok** for the binding decision: ACCEPT the deliberate starvation fix re-scoring the live legacy S6/Crash plugins (small honest negative; their edge validated in Iter-6/6.9 anyway), or SCOPE the Factor-4 branches to engine/`routed_engine` signals only so production stays byte-identical. Phase log `progress/3-2.md`. Do NOT commit; do NOT advance until stok signs. |
| 3.3 | 3.3 | CRangeReversionEngine.mqh | N | COMMITTED | | **byte-identical, telemetry-only. BOTH QA agents signed (mt5-developer + INDEPENDENT stok no-op sign-off). stok confirmed at SOURCE: one-line diff at `CRangeReversionEngine.mqh:127` (`TagAndRetarget()`) + router gating `g_engineRange new` (mq5:742) inside `if(InpEnableMultiStrategy)` (mq5:718, default false) → unreachable on production → structural no-op; engine's own method, no legacy side channel. A==B byte-identical PASS.** FIX-SOUND telemetry fidelity (day_type stamp). **What changed:** `CRangeReversionEngine.mqh:127` `TagAndRetarget()` — `signal.day_type = DAY_RANGE;` (hardcoded) → `signal.day_type = (m_context != NULL) ? m_context.GetDayType() : DAY_RANGE;` (real classified day type w/ NULL guard, matching CReversalSweepEngine.mqh:208 + the local `engine_confluence` NULL-guard convention at :139-140). NO trade-decision change; NO logic cut. **0-err/0-new-warn (`Result: 0 errors, 38 warnings, 9157 ms elapsed, cpu='X64 Regular'`).** **A/B XAUUSD+ H1 2024 Model=1 production `.set` = A==B BYTE-IDENTICAL** — A (pre-edit recompile of current source; `CRangeReversionEngine.mqh` clean vs HEAD) reproduces the committed-HEAD anchors EXACTLY (Stats `04d11697…`, TradeEvents `825489b6…`, Candidates `bcc44924…`, Risk `8babda0f…`); B (fix) = same 4 md5s; all 4 CSVs md5 MATCH + `cmp` byte-for-byte clean (sizes 348670/1409638/278030/66060). Mechanism: `g_engineRange = new CRangeReversionEngine(...)` (mq5:742) + RegisterEngine ALL inside `if(InpEnableMultiStrategy)` (mq5:718; false on prod `.set`, `InpEnableEngineRange=false` too) → the edited `TagAndRetarget()` (the engine's OWN method; NO legacy standalone plugin routes through it, unlike fix 3.4) is unreachable on production → byte-identity STRUCTURAL by construction. KILL-CRITERION (byte-identical on production) PASS. See `progress/3-3.md`. Main session runs git. |
| 3.4 | 3.5 | CTrendContinuationEngine.mqh; UltimateTrader.mq5; Inputs | Y | mt5-QA-DONE | | **OPTION A IMPLEMENTED + QA PASS — mt5-developer SIGNED; INDEPENDENT stok sign-off PENDING. Do NOT commit / do NOT advance to Phase 3.5 until stok signs.** NEEDS-CARE: `InpTrendSwingLookback=10` (NOT 20 — 20 reaches structurally-irrelevant lows → oversized stop). Made the previously-unused `m_swing_lookback` LOAD-BEARING per Option A. **What changed:** (1) `CTrendContinuationEngine.mqh` member `double m_swing_lookback` (was "(unused placeholder)") → `int m_swing_lookback` ("CLOSED H1 bars [1..N] for the conservative zone_low SL anchor"); (2) `BuildZoneLong()` zone_low computation — `zone_low` starts as `GetSwingLow()`, then is lowered to the lowest CLOSED H1 low over `[1..m_swing_lookback]` (read via `CopyLow(_Symbol,m_timeframe,1,m_swing_lookback,lows)`, **start_pos 1 → never the forming bar [0]**, `got>0` + `lookback_low>0` guards) iff that is MORE CONSERVATIVE (lower); (3) ctor gained `int swing_lookback=10` param, seeds `m_swing_lookback=(swing_lookback>0)?swing_lookback:10` (was hardcoded `=20`); (4) `UltimateTrader.mq5:730` `new CTrendContinuationEngine()` → `new CTrendContinuationEngine(14,0.5,100.0,2.0,InpTrendSwingLookback,PERIOD_H1)` (inside `if(InpEnableMultiStrategy)`); (5) `UltimateTrader_Inputs.mqh` added `input int InpTrendSwingLookback=10;` to the MULTI-STRATEGY ENGINES group. **m_min_sl_points floor PRESERVED + never newly violated:** a lower/wider zone_low only INCREASES `risk=entry-sl`, so the existing `if(risk < m_min_sl_points*_Point) return signal;` SL-too-tight reject still fires and is never bypassed; the conservative-min anchor is never tighter than GetSwingLow-only, so it cannot introduce a min-SL violation. **NO logic cut** (all 4 cascade paths + every gate intact; only the SL anchor widened + a dead member revived; Option B = delete the member is the documented fallback ONLY if the GATE/3.7 shows worse avg-R than GetSwingLow-only). **0-err/0-new-warn (`Result: 0 errors, 38 warnings, 9642 ms elapsed, cpu='X64 Regular'`).** **A/B XAUUSD+ H1 2024 Model=1 production `.set` = A==B BYTE-IDENTICAL** — A (committed-HEAD recompile, saved `/tmp/UT_phase3_4_baseline.ex5` BEFORE editing) reproduces the committed-HEAD 2024 anchors EXACTLY (Stats `04d11697…`, TradeEvents `825489b6…`, Candidates `bcc44924…`, Risk `8babda0f…`; 140t/$1190.84/11.76R/0.0840avgR); B (Option A) = same 4 md5s; all 4 CSVs raw md5 MATCH + `cmp` byte-for-byte clean (sizes 348670/1409638/278030/66060). Mechanism: the engine is `new`'d ONLY inside `if(InpEnableMultiStrategy)` (mq5:718; false on prod `.set`, `InpEnableEngineTrend=false`) → never instantiated → `BuildZoneLong()`'s new conservative-min `zone_low` is UNREACHABLE on production → byte-identity STRUCTURAL by construction; the SL-anchor effect is exercised only at the engines-ON GATE/Phase 3.7 (where stok compares avg-R conservative-min vs GetSwingLow-only; Option-B fallback if worse). KILL-CRITERION (byte-identical on production) PASS. mt5-developer SIGNED; **stok sign-off PENDING** (independent agent — do NOT self-assert). See `progress/3-4.md`. |
| 3.5 | 3.6 | CSignalOrchestrator.mqh | Y | mt5-QA-DONE | | **HTF-UPTREND SHORT VETO for routed engine shorts IMPLEMENTED + QA PASS — mt5-developer SIGNED; INDEPENDENT stok sign-off PENDING. Do NOT commit / do NOT advance to the 2.4-GATE until stok signs.** BETTER-OPTION: the central guard against negative-expectancy gold shorts re-entering through the multi-strategy engines. Hard-ON, NO disable input. **What changed:** `CSignalOrchestrator.mqh` `CheckForNewSignals()` SHORT branch (`if(sig_type==SIGNAL_SHORT)`, line 630) — inserted the veto as the FIRST check (632-654) ahead of the existing ATR-min check (now `else if`, 656-657): `bool htf_uptrend = ((h4_trend==TREND_BULLISH)||(daily_trend==TREND_BULLISH)) && m_context.IsPriceAboveMA200();` then `if(is_engine && htf_uptrend){ reject_reason="HTF_UPTREND_SHORT_VETO"; ...}` → `validated` stays false → existing `if(!validated)` block fires `AuditCandidate(...,"VALIDATOR","REJECT","HTF_UPTREND_SHORT_VETO",...)` + `continue`. **Mirrors `CTrendContinuationEngine::IsUptrendActive()` EXACTLY** (same `((h4||d1)BULLISH) && IsPriceAboveMA200()`, same getters via `m_context` — orchestrator already computes `h4_trend=GetH4TrendDirection()` :490, `daily_trend=GetTrendDirection()` :489). **CRITICAL SCOPING (Phase-2.1/3.2 trap avoided):** gated on `is_engine == signal.routed_engine` (line 566; true ONLY when `InpEnableMultiStrategy` wired the scorer — `routed_engine` set inside each engine's `if(m_scorer!=NULL)` block, `SetScorer` only under `InpEnableMultiStrategy`), NOT raw `major_engine!=ENGINE_NONE` (which would also veto the LIVE legacy Expansion/Session/Liquidity engines' shorts on production and change behavior). **NO logic cut** (full short validator NOT re-enabled = would zero ALL shorts; ATR-min path + trailing ATR-too-low reject intact for every non-vetoed short; veto only ADDS one branch, off on production). Diff +24/-1. **0-err/0-new-warn (`Result: 0 errors, 38 warnings, 10177 ms elapsed, cpu='X64 Regular'`).** **A/B XAUUSD+ H1 Model=1 production `.set` = A==B BYTE-IDENTICAL 2024 & 2023** — A (committed-HEAD `.ex5` saved `/tmp/UT_phase3_5_baseline.ex5` md5 `c0a40b4d…` BEFORE editing) reproduces the committed-HEAD anchors EXACTLY (2024 Stats `04d11697…`/TradeEvents `825489b6…`/Candidates `bcc44924…`/Risk `8babda0f…`; 2023 Stats `b11e0767…`/TradeEvents `7ef40d5f…`/Candidates `2fba7df1…`/Risk `b21a2999…`); B (md5 `cae2585a…`) = same 8 md5s; ALL 8 CSVs raw md5 MATCH + `cmp` byte-for-byte CLEAN. Mechanism: `routed_engine` constant-false on production (engines OFF, no `SetScorer` since `InpEnableMultiStrategy=false`) → `if(is_engine && htf_uptrend)` unreachable → no short newly rejected → byte-identity STRUCTURAL; had it been scoped on raw `major_engine!=ENGINE_NONE` the live legacy engines' shorts WOULD have diverged — the byte-identity PROVES the routed_engine scoping is correct (no leak onto the legacy/production path). KILL-CRITERION (byte-identical on production) PASS. **Engines-ON GATE/Phase 3.8 OWED:** veto's short-expectancy effect unmeasurable engine-off; at engines-ON confirm `HTF_UPTREND_SHORT_VETO` fires for routed engine shorts only and measure short expectancy on veto-SURVIVORS over full 2019–2026 + 2024–26 OOS (the veto makes the short test honest, it does not pre-decide it — keep engine shorts gated in confirmed bear regimes, do NOT relax to chase count). mt5-developer SIGNED; **stok sign-off PENDING** (independent agent — do NOT self-assert). See `progress/3-5.md`. |
| 3.6 | 3.2 | CRegimeRouter.mqh; CMarketContext.mqh; Inputs (news group) | Y | QA-PASS | | **DAY_DATA NEWS-FLAT WIRED + QA PASS — BOTH QA agents SIGNED (mt5-developer + INDEPENDENT stok). 2.4-GATE Section-1 binding exception SATISFIED (functional via static fallback). NEXT ACTION → Phase 2.4-GATE derivation (main session commits 3.6 on advance).** **stok INDEPENDENT SIGN-OFF (verified at SOURCE):** (d) byte-identity + `InpEnableMultiStrategy` gating CONFIRMED no-leak — single chokepoint `IsDataDay()`:1031 `if(!InpEnableMultiStrategy||!InpEnableNewsFlat) return false;`; prod `.set` decoded `InpEnableMultiStrategy=false` → constant-false → `GetDayType()`:858 DAY_DATA branch unreachable → all callers keep pre-3.6 value, STRUCTURAL. Traced ALL 6 GetDayType/DAY_DATA consumers: CExpansionEngine:581 telemetry stamp = ONLY production-reachable caller (A==B proves gate neutralizes it); CRegimeRouter:73 = ONLY DAY_DATA decision branch, router-only; CLiquidityEngine:365 reads decision-path `m_day_type` from `CDayTypeRouter::ClassifyDay()` (read at source :39-98) which returns ONLY TREND/VOLATILE/RANGE — never DAY_DATA → dead branch, not a leak; CDayTypeRouter:110 = string label. (a) static schedule encoded VERBATIM (`IsStaticNewsBlackout`:965-992), 11/11 probe PASS, offset=3. (b) FOMC dom-approx + over-flag-safe ACCEPTABLE — fails toward MORE flat, never exposure into a release; GATE derives on the static population cleanly. (c) FUNCTIONAL static-carried (calendar dead err 4014) satisfies the 2.4-GATE Section-1 binding exception sub-rule 1 without the parked-toggle escape. **Live-calendar proxy:** ACCEPT as code-reviewed (`IsCalendarNewsWindow`:995-1021, HIGH US+XAU proxy, no name-match) — LIVE-ONLY/untestable, ZERO GATE impact, bounded/guarded/never-throws, errs over-flat. **PRE-LIVE checklist (deferred, non-blocking):** narrow live path to the 5 named events via `StringFind(ev.name,...)` + re-confirm calendar availability on the live terminal. KILL-CRITERION PASS. See `progress/3-6.md` stok sign-off. — NEEDS-CARE: revives the dead router DAY_DATA gate (GetDayType never returned DAY_DATA). `InpNewsWindowMinutes=15`, `InpEnableNewsFlat=true`. **CALENDAR-AVAILABILITY (hard-gate prereq, probed FIRST):** `CalendarValueHistory` returns **-1 / err 4014 (ERR_FUNCTION_NOT_ALLOWED)** for ALL country scopes (US/EU/XAU/ALL) in the tester on XAUUSD+ → MQL5 calendar **DEAD in the tester** → the **static-blackout fallback carries the fix** (calendar path retained for live, with init-count print). **3.6 lands FUNCTIONAL (static-carried), NOT toggle-parked** — `NewsFlatProbeEA` proved 11/11 known 2024 dates flag correctly (NFP 1st-Fri 02-02 @ GMT 13:00 & 12:00 DST-pair; CPI 02-13; PPI 02-16; PCE 02-29; FOMC 03-20 @ 18:00 & 19:00; FOMC 01-31 edge-date correctly MISSED per the stok-approved dom-16-22 approximation; quiet bars not flagged). **What changed:** `CMarketContext.mqh` — new `IsDataDay()` (master predicate, gated `InpEnableMultiStrategy && InpEnableNewsFlat`) + `IsStaticNewsBlackout()` (binding stok schedule: NFP 1st-Fri / CPI dom10-15 / PPI dom11-16 / PCE dom26-31 / FOMC Wed dom16-22 meeting-months {1,3,5,6,7,9,11,12}; DST via flagging BOTH GMT hours 12|13 data / 18|19 FOMC) + `IsCalendarNewsWindow()` (live calendar path, HIGH-impact USD/XAU) + `ResolveGMTOffset()` (same derivation as CSessionEngine; offset=3 in tester confirmed) + `ProbeNewsCalendar()` (mandatory calendar-count print on init); `GetDayType()` gets `if(IsDataDay()) return DAY_DATA;` FIRST (before the vol branch). `CRegimeRouter.mqh:68-73` comment-only (existing `if(data_day) return 0.0;` gate now LIVE). `Inputs` new NEWS FLAT group. **0-err/0-new-warn (`Result: 0 errors, 38 warnings, 9618 ms elapsed, cpu='X64 Regular'`).** **A/B XAUUSD+ H1 Model=1 production `.set` = A==B BYTE-IDENTICAL 2024 & 2023** — A (committed-HEAD `.ex5` `/tmp/UT_phase3_6_baseline.ex5` md5 `cae2585a…` saved BEFORE editing) reproduces the anchors EXACTLY (2024 Stats `04d11697…`/TradeEvents `825489b6…`/Candidates `bcc44924…`/Risk `8babda0f…`; 2023 Stats `b11e0767…`/TradeEvents `7ef40d5f…`/Candidates `2fba7df1…`/Risk `b21a2999…`); B (md5 `229950b9…`) = same 8 md5s; ALL 8 CSVs raw md5 MATCH + `cmp` byte-for-byte CLEAN. **BYTE-IDENTITY INVESTIGATION (QA(a)-mandated — GetDayType IS reachable on production):** CExpansionEngine (LIVE legacy engine) stamps `signal.day_type = GetDayType()` (`CExpansionEngine.mqh:581`, unconditional) → `pos.day_type` → CSV DayType column → a DAY_DATA return WOULD diverge the telemetry CSV. (Decision-path `m_day_type` is from CDayTypeRouter, unaffected.) **Resolved by gating `IsDataDay()` behind `InpEnableMultiStrategy`** (false on production → constant-false → DAY_DATA branch unreachable → telemetry stamp unchanged → byte-identical), matching the plan's "DAY_DATA consumed only when router ON" invariant. **NO logic cut** (advertised-but-dead DAY_DATA gate WIRED, not removed; never throws — calendar failure → static fallback). KILL-CRITERION (production byte-identical + functional landing) PASS. **GATE-owed:** at the engines-ON 2.4-GATE confirm DAY_DATA both zeros engine weight (CRegimeRouter:73) AND that news-day signals are removed/flagged so the expectancy ranking is not news-poisoned (2-4-GATE-design Section 1 exception). mt5-developer SIGNED; **stok sign-off PENDING** (independent agent — do NOT self-assert). See `progress/3-6.md`. |

### 3b — Per-engine FRESH 2019–2026 validation (one phase per engine; engines stay OFF until earned)

| Phase | Fixes | Files | Touches-logic | Status | Commit | Notes |
|---|---|---|---|---|---|---|
| 3.7 | 3.7 (umbrella) Trend-Cont | (validation; toggles only) | Y | PENDING | | FRESH full-window + 2024–26 OOS. FVG re-entry + Pullback specs. KILL: PF<1.0 OR avg-R<0 (full AND OOS) → sub-path stays OFF. |
| 3.8 | 3.7 (umbrella) Reversal/Sweep | validation | Y | ✅ stok-RULED — KEEP-FOR-SFP-LONG, engine OFF-by-default; NO sweep ordered | | **stok VERDICT (2026-06-27):** KEEP-FOR-SFP-LONG, `InpEnableEngineReversal=false` stays production default (byte-identical). **PER-MODE:** SFP-LONG **KEEP** (13t/+$302.87/PF 3.24/+0.278R full; PF 16.22/+0.570R/87.5% WR OOS — PASSES full+OOS, carries the engine); SFP-SHORT **CUT** (0/8 full, 0/3 OOS, PF 0.00, −0.286R — TRIPS, the historically-dead short, re-confirmed on HTF-veto survivors); Rubber-Band **CUT-unmeasured**; S6 **CUT-unmeasured**. **SWEEP DECISION → SKIP (not warranted).** The GateScores diagnostic (`UltTrader_GateScores_XAUUSD+_20190109_0400.csv`, 293 scored candidates incl. SETUP_NONE) FALSIFIES the freshness-starves-Rubber-Band hypothesis: **MODE_RUBBER_BAND produced 0 scored candidates** — gated UPSTREAM of the scorer by its own `(IsBearRegimeActive()||IsRubberBandSignal())` bear gate (CReversalSweepEngine.mqh:392), so `InpBOSFreshnessBars`/`InpSpineMinConfluence` are never reached for it; the death-cross short simply did not trigger on gold's 2019–2026 uptrend (regime-correct, not broken). The single discriminating point on ALL rejects is **Axis-4 (fresh+aligned structure-shift, 1pt)**: passers score 6=B+ with ax4=1, rejects score 5=NONE with ax4=0; `InpSpineMinConfluence` is **not binding** (every reject was SCORED, i.e. cleared the L3 spine — they died on Axis-4, not the spine). The ONLY freshness-reachable population is **~9 S6-LONG candidates on a PF-0.78 marginal-loser** → recovering frequency at unproven/likely-negative expectancy is NOT a pass; overfit risk > EV → no sweep ordered. **FLOORS RE-AFFIRMED (binding 2.2/2.3):** `InpSpineMinConfluence` ≥ 25, never relaxed below validator hard-reject 40 discipline; `InpBOSFreshnessBars` within floor 6 / cap 18 (the plan's proposed `4` is BELOW the floor — rejected on that ground). If EVER swept (Iter-6, not now): `InpBOSFreshnessBars ∈ {8,12}` ONLY (≤2 runs, isolated Reversal/legacy-OFF, spine HELD at 25), kill-criterion on the S6-LONG trades surfaced — but judged LOW-VALUE on this evidence, deferred. **ENGINE-LEVEL:** keep-for-SFP-LONG, OFF-by-default, low-freq, NOT force-enabled, NOT a shared-tier multiplier (per 2.4-GATE NO-CLEAN-EDGE — aggregate PF 1.41 is carried by SFP-LONG, dragged by the dead short); if ON, SFP-LONG ONLY + HTF short veto live (stripped 17 losers, the 8 survivors still 0/8). Confidence-raiser (non-blocking): SFP-LONG n=13 is 2024-concentrated → lift toward ≥30 across ≥3 regimes via 2026+ walk-forward, judged on SFP-LONG's own avg-R full+OOS. NOT committed; production byte-identical. **NEXT → Phase 3.9 (Range/MR).** | **ISOLATED base run DONE (see `progress/3-8.md`); 1 plugin = ReversalSweepEngine (`[Init] Entry Plugins: 1 registered`, verified MY-run journal 00:43 + EA log all-ReversalSweep + ShortDiag Plugins=1). XAUUSD+ H1 Model=1, 2019.01.01–2026.06.27. Compile `0 errors, 38 warnings` (0 new), source byte-clean at HEAD.** **FULL 2019–26:** 21t / $126.66 / PF 1.41 / avg-R +0.063 / WR 47.6% / closed-eq maxDD $199.46. **OOS 2024–26:** 11t / $262.54 / PF 3.17 / avg-R +0.289 / WR 63.6% / maxDD $97.09 (all OOS fills are 2024). **PER-MODE:** only `MODE_SFP` fired (all 21t) — SFP-LONG 13t/+$302.87/PF 3.24/+0.278R/76.9% (PASS full+OOS) carries the engine; SFP-SHORT 8t/−$176.21/PF 0.00/−0.286R/**0% WR** TRIPS kill (the expected-dead path, confirmed). **MODE_RUBBER_BAND (expected profitable short) and MODE_FAILED_BREAK (S6) fired 0× — starved, NOT measured** → cannot certify; stay OFF pending the freshness sweep. MODE_NONE: 0. **HTF short veto: 17 vetoed / 8 traded;** the 8 veto-SURVIVOR shorts are still 0/8 (−$176.21, −0.286R) → residual short edge negative even after the veto. **KILL-CRITERION:** engine aggregate PASSES but is carried entirely by SFP-LONG; SFP-SHORT TRIPS; Rubber-Band/Failed-Break have NO DATA. **OWED (separate follow-ups, NOT done):** `InpBOSFreshnessBars {4,6,8,12}` (+ `InpSpineMinConfluence`) avg-R sweep — prime suspect for the starved Rubber-Band/Failed-Break — then stok's KEEP/CUT-per-mode ruling. NOT committed; `InpEnableEngineReversal=false` production default unchanged (byte-identical). S6 LONG / Rubber-Band SHORT (A+ only) / Liq-Sweep. |
| 3.9 | 3.7 (umbrella) Range/MR | validation | Y | ✅ CUT (GATE-decided; see 3-9.md) | | RangeReversion avg-R −0.688 full / −0.530 OOS — trips the kill-criterion, decided at the 2.4-GATE, no separate run. Stays CUT / OFF-by-default (`InpEnableEngineRange=false`); production byte-identical. BB-MR LONG-dominant (RSI 25/75), gated InBalance + news-flat. |
| 3.10 | 3.7 (umbrella) Expansion (eng 4) | validation | Y | ✅ stok-RULED — CUT, engine OFF-by-default (all 4 modes); ITERATION 3 COMPLETE | | **stok VERDICT (2026-06-27):** **CUT** — `InpEnableExpansionEngine` production default unchanged (byte-identical when `InpEnableMultiStrategy=false`). Isolated, legacy-off, scored through `CConfluenceScorer`, the engine collapses to n2 / PF 0.06 / avg-R −0.515 / OOS-empty → trips the Iter-3 kill-criterion outright. **PER-MODE:** MODE_LONDON_BREAKOUT (Session-BO) **CUT** (the only measurable mode, 2t/PF 0.06/−0.515R full, 0 OOS — TRIPS; matches the repo's standing London-Breakout-0%-WR finding); MODE_INSTITUTIONAL_CANDLE / MODE_COMPRESSION_BO / composed Vol-BO **CUT-unmeasured** (43 generated, 41 rejected at the scorer quality floor + spread gate → no data → cannot certify ON → stays OFF); MODE_PANIC_MOMENTUM dead `if(false)` confirmed 0. **GATE-vs-ISOLATED RECONCILIATION:** the GATE +0.070R full / +0.294R OOS (n57) was CROSS-CASCADE ENTANGLED (fired alongside the other engines + legacy plugins, borrowed liquidity from the cascade); ISOLATED + legacy-off + scored, it is −0.515R / OOS-EMPTY. For a standalone-engine KEEP/CUT decision the **isolated number governs** (per the GATE's own rule: certify ON only on isolated fresh numbers). The **41/43-rejected-at-the-scorer-floor finding CONFIRMS the 2.4-GATE NO-CLEAN-EDGE verdict downstream** — 95% of Expansion signals die at the very quality floor the GATE proved cannot rank gold-H1 trades; the edge (if any) is in the engine, not capturable by the shared scorer, and the one slice that gets measured loses. **NOT force-enabled, NOT a shared-tier multiplier. NOT committed; production byte-identical.** ITERATION 3 COMPLETE — all 4 engines validated: Trend-Cont KEEP-tiny, Reversal KEEP-for-SFP-LONG-only, Range CUT, Expansion CUT → binding end-state **multi-strategy stays OFF-by-default**; SFP-LONG + tiny Trend-Cont are candidate small edges to be lifted to ≥30-trade confidence + given a working gate (scorer-axis re-weight) before any promotion. See `progress/3-10.md` for the full ruling + Iter-3 close. | **ISOLATED base run DONE (see `progress/3-10.md`); 1 plugin = ExpansionEngine (`[Init] Entry Plugins: 1 registered`, verified MY-run journal 01:05 + EA log all-ExpansionEngine; NO double-reg, NO legacy).** Resolved the prompt's double-reg caveat against source: Expansion's ONLY registration is `InpEnableExpansionEngine` (UltimateTrader.mq5:687); `InpEnableEngineExpansion` is referenced nowhere for Expansion (dead input) and the router touchpoint (749–757) is `RegisterEngine` not `RegisterEntryPlugin` (does NOT add to `g_entryPlugins[]`) — so `InpEnableExpansionEngine=true` (NOT false) is the correct isolation; false → 0 plugins. XAUUSD+ H1 Model=1, 2019.01.01–2026.06.27. Compile `0 errors, 38 warnings` (0 new), source byte-clean at HEAD. **FULL 2019–26: 2t / −$40.37 / PF 0.06 / avg-R −0.515 / WR 50% / closed-eq maxDD ≈$42.85. OOS 2024–26: 0 trades.** **PER-MODE:** only `MODE_LONDON_BREAKOUT` (composed Session-BO) fired (both trades): 2t/−$40.37/PF 0.06/−0.515R full, **0 OOS** → TRIPS kill (PF<1.0 AND avg-R<0 full; OOS empty). **MODE_INSTITUTIONAL_CANDLE, MODE_COMPRESSION_BO, composed Vol-BO: 0 closed trades** (43 signals GENERATED — 30 IC + 9 Session-BO + Vol-BO candidates — but rejected by Quality-floor (2.4-GATE tiers) + SPREAD GATE; only 2 Session-BO survived) → NO DATA, cannot certify, stay OFF. **MODE_PANIC_MOMENTUM = 0** (dead `if(false)`, removed Phase D) — confirmed. **KILL-CRITERION:** engine aggregate TRIPS (PF 0.06 / −0.515R full, 0 OOS); every measurable sub-mode TRIPS or NO-DATA. NOT committed; `InpEnableExpansionEngine` production default unchanged (byte-identical when `InpEnableMultiStrategy=false`). stok to rule KEEP/CUT. |

**Iter 3 kill/loop:** any sub-path PF<1.0 or avg-R<0 (full AND OOS) → stays OFF, handed to stok with fresh decoded numbers, NOT re-judged on old findings, NOT force-enabled to chase count. Default `InpEnableMultiStrategy=false` stays byte-identical.

---

## Iteration 4 — Execution / Risk Enforcement

| Phase | Fixes | Files | Touches-logic | Status | Commit | Notes |
|---|---|---|---|---|---|---|
| 4.1 | 4.1 | CPositionCoordinator.mqh | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct + stok ACCEPT-WITH-FOLLOWUP, binding). COMMITTED source-only on feat/multi-strategy.** stok ruling: fix is UNIT-CORRECT (banked-then-red net winner must not de-risk; aligns scaler with EC v3); the −37.9% net is an accepted EXPOSURE-LEVEL artifact on FLAT per-R edge (PF/avg-R flat, avg lot 0.255→0.213, maxDD BETTER 10.98%→9.17%) — buggy A's larger book = curve-fit-to-one-path streak-ordering of the one-sided scaler, NOT preserved (keeping it would violate the no-curve-fit mandate). **Iter-6 follow-up logged** (loss-scaler config re-derive: ① banked-then-red streak-reset policy, ② InpLossLevel1/2Threshold (calibrated vs buggy 647 count, re-derive upward vs correct 580), ③ reductions — OOS split fit-2019-22/confirm-2023-26, A/B vs B NOT buggy A, accept only if avg-R≥+0.12 & PF flat-or-better & maxDD≤9.17%). FIX-SOUND mechanism applied: loss-scaler fed TOTAL trade PnL not runner-leg at BOTH sites. **What changed (CPositionCoordinator.mqh):** site1 — runner-only `RecordTradeResult(profit)` deleted; `total_trade_pnl=profit+partial_realized_pnl` hoisted to :2602; `RecordTradeResult(total_trade_pnl)` :2604; logger-block redeclaration removed (reuses hoisted total :2627/2629). site2 — runner-only call deleted; `RecordTradeResult(total_trade_pnl)` moved below `total_trade_pnl` :3025 → fires :3035; `runner_r_multiple` (:3027) PRESERVED for per-mode RecordModeResult. RemovePosition after both → m_positions valid. NO logic cut. **Compile `Result: 0 errors, 38 warnings, 10153 ms elapsed, cpu='X64 Regular'` (0 new).** **A/B [TesterInputs] prod defaults (395 params, InpEnableMultiStrategy=false) XAUUSD+ H1 Model=1 full 2019.01.01–2026.06.27, 2 runs:** A(HEAD `e0cfbb3a`)=2200 trades / net **$39,005.22** / PF 1.36 / Bal-maxDD 10.98% / avg Total_R +0.143; B(post-fix `6d77c09e`)=2021 trades / net **$24,234.27** / PF 1.31 / Bal-maxDD **9.17%** / avg Total_R +0.127. **Delta: net −$14,771 (−37.9%) WORSE; maxDD −1.81pp BETTER; PF/avg-R ≈ flat.** Mechanism clean (one-sided `ApplyLossScaling` streak-reorder cascades through sizing→exposure→divergent population; avg LotSize 0.255→0.213). **Net-outcome streak validation PASS:** A misclassified 69 banked-then-red NET WINNERS as LOSS (by Runner_PnL 647L/403W vs by Total_PnL 580L/470W); B counts by Total_PnL (540L/426W). Ticket 18 (Runner −$5.12, Partial +$7.52, **Total +$2.40**) logged `LOSS recorded` in A, `WIN recorded. Streak:3W` in B. **NEUTRAL-OR-BETTER: NO on net (material −38% drop), YES on maxDD/avg-R/PF → REJECT-LOOP candidate, stok rules.** See progress/4-1.md. — FIX-SOUND: feed loss-scaler TOTAL trade PnL not runner-leg. Net-winner must not de-risk. |
| 4.2 | 4.2 | UltimateTrader.mq5; CTradeOrchestrator.mqh; CPositionCoordinator.mqh | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct + stok ACCEPT-WITH-FOLLOWUP, binding, source-verified). COMMITTED source-only on feat/multi-strategy.** stok verified all 3 sub-questions in source: LOT-down-not-SL (preserves structural 1R/EC v3), hand-floor-to-lot_step (NormalizeLots would round-up & re-breach), stop-distance-risk-$ measure + equity denom (dimensionally correct for an R-based book; conservative since it sums ORIGINAL risk). maxDD +$42/+0.09pp = single 2021-01-04-rescale path artifact, not a regression; fix value = fat-tail breach elimination (5.84%/audit~10% → ~5.0%). **Iter-6 follow-up logged:** InpMaxTotalExposure {3,4,5} sensitivity-sweep vs InpMaxPositions=5 (all concurrent gold longs = ONE correlated bet; pick by OOS maxDD-at-flat-net; commit enforcement first for a clean baseline). NEEDS-CARE most consequential risk fix: `InpMaxTotalExposure=5.0` now ENFORCED (was read only by CDisplay). **3 edit sites:** (1) CPositionCoordinator.mqh ~981 — new public `double GetTotalOpenRiskPct(double equity)` after GetPositionCount (guards equity>0; sums EXISTING CalculatePositionRiskDollars over open positions / equity ·100 — reused risk model, no new one). (2) CTradeOrchestrator.mqh — `class CPositionCoordinator;` forward-decl (~24, coordinator included AFTER orchestrator → forward-decl avoids circular include), `m_pos_coordinator` member (~37, init NULL ~78), `SetPositionCoordinator` setter (~122), and the GATE in ExecuteSignal inserted AFTER `final_risk_pct=risk_pct;` (line 539, the candidate's resolved risk%) BEFORE the executor: if `open_risk+final_risk_pct>InpMaxTotalExposure` → `headroom=cap-open_risk`; `headroom<=0`→hard-reject; else rescale LOT only (`scale=headroom/final_risk_pct`, floor to lot_step BY HAND not NormalizeLots so it can't round up & breach), `floored<min_lot`→hard-reject (`REJECT_EXPOSURE_CAP`, mirrors lot<=0 path exactly: LogRiskAudit lot=0, return; NO daily-counter increment), else apply floored lot + rescale audited risk%, **SL untouched**. (3) UltimateTrader.mq5 ~1077 — `g_tradeOrchestrator.SetPositionCoordinator(g_posCoordinator);` in the setter cluster (both objects constructed at 950/970). NO logic cut. **Compile `Result: 0 errors, 38 warnings, 10160 ms elapsed, cpu='X64 Regular'` (0 new; none in edited files).** **A/B [TesterInputs] prod defaults (395 params, InpEnableMultiStrategy=false) XAUUSD+ H1 Model=1 full 2019.01.01–2026.06.27, 2 runs:** A(HEAD 701927a `6d77c09e`)=2021 trades / net **$24,234.27** / PF 1.31 / Bal-maxDD 9.17% / Eq-maxDD 11.15% / avg Total_R +0.1275; B(post-fix `6ad0268f`)=2021 trades / net **$24,286.09** / PF 1.30 / Bal-maxDD 9.26% / Eq-maxDD 11.23% / avg Total_R +0.1266. **Δ net +$51.82 (+0.21%) NEAR-FLAT; PF −0.01, avg-R −0.0009, maxDD +0.09pp (all flat/noise).** **(a) Cap BINDS:** B agent-log = **3 rescale events / 0 hard-rejects** (2021-01-04 lot 0.27→0.14 risk 1.46→0.76%; 2023-04-13 0.21→0.20 risk 1.38→1.32%; 2025-02-10 0.24→0.23 risk 0.98→0.94% — all "SL unchanged"); peak aggregate open-risk A **5.84%** (2021-01-04, 4 concurrent) EXCEEDS 5% → B caps each candidate to ~5.0% by the EA's own equity-denominated metric (CSV-proxy 5.84%→5.14%, residual = one-trade granularity + denominator diff per plan). **(b) LOT-not-SL CONFIRMED:** cross-match of 964 common trades = **110 LOT-changed with SL-distance IDENTICAL, 0 SL-distance changes, 0 dropped/added** (the 3 cap rescales SLdist byte-identical 6.32/9.92/8.99). **Kill-criterion (material net drop) NOT tripped** → no config loop owed by logic. **mt5 read = PASS.** stok to rule: near-flat net + marginally-worse maxDD is the plan-expected outcome (cap binds only 3× over 7.5yr because prod book rarely stacks past 5% under InpMaxPositions=5); flag if cap/InpMaxPositions interaction warrants a CONFIG review (NOT a logic defect). See progress/4-2.md. |
| 4.3 | 4.3 | CRiskMonitor.mqh; CDailyLossHaltExit.mqh | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct + stok ACCEPT-WITH-FOLLOWUP / SHIP-EQUITY, binding, source-verified). COMMITTED source-only on feat/multi-strategy.** stok ruling: equity = dimensionally-CORRECT anchor for a runner-holding book (balance is blind to floating runner P&L = the bug); single-source-of-truth unambiguously right; halt count 10=10 falsifies the over-trigger kill-criterion → accepted on CORRECTNESS. **The +5.29% net is LUCK not edge** (path-artifact of 3 B-only halts preceding losers on this one history) — do NOT cite as "equity baseline made +5%"; defensible claim = "unified two disagreeing daily-stop baselines onto one dimensionally-correct equity anchor; net-neutral-to-protective in-sample; over-trigger fear falsified; OOS premature-halt risk pending." **Iter-6 follow-up logged:** OOS/regime guard (count B-only halts that lock out RECOVERIES, kill-signal if OOS net/avg-R < A); optional InpDailyLossBaseline {equity/balance} knob (one-line swap via the single-source plumbing). **Fix:** CRiskMonitor = SINGLE SOURCE OF TRUTH — `m_daily_start_balance = ACCOUNT_EQUITY` at Init + day-reset (start-of-day EQUITY baseline, the plan's PRIMARY); `GetDailyPnL()` math unchanged (now equity-vs-equity). CDailyLossHaltExit STOPS computing its own balance-reconstructed baseline → reads `m_risk_monitor.GetDailyPnL()` via injected `SetRiskMonitor` (forward-decl `class CRiskMonitor;`, Phase-4.2 pattern; wired in UltimateTrader.mq5 after both objects exist); legacy baseline kept only as NULL-fallback. NO logic cut (3% threshold `InpDailyLossLimit=3.0`, reset-on-new-day, halt/exit actions all preserved). **Compile `Result: 0 errors, 38 warnings` (0 new; 0 warn in either edited file).** A `.ex5` `c75a310b` (HEAD recompile, reproduces 4.2 anchor) ≠ B `1a6e5404` (real change, NEEDS-CARE). **A/B XAUUSD+ H1 full 2019–2026, 395 prod params (InpEnableMultiStrategy=false, BOTH, Model=1), 2 runs:** A(HEAD 445baa0) 2021 deals/966 closes/net $24,286.09/PF 1.30/Bal-DD 9.26%/Eq-DD 11.23%/avg-R +0.1266 → B 2011 deals/961 closes/net **$25,570.97**/PF 1.31/Bal-DD 9.28%/Eq-DD 11.26%/avg-R **+0.1320**. **Δ: −10 deals/−5 closes, net +$1,284.88/+5.29%, avg-R +0.0054, maxDD +0.02–0.03pp (within noise).** Yearly: 2019/2020 byte-identical; 2021 +$322, 2022 +$319, 2025 +$510, 2026 +$114 — ALL divergent years net-POSITIVE. **DAILY-HALT EVENT COUNT: A 10 days / B 10 days — SAME (NOT more trigger-happy).** 7 common; 3 A-only (2020.10.13, 2023.01.10/11); 3 B-only (2021.01.05, 2021.11.10, 2022.11.10). Renumber-proof cross-match: 960 common, 6 A-only losers B avoided (−$389.98), 1 B-only (+$28.96). **OVERNIGHT-RUNNER GIVE-BACK EXAMPLE (2021.01.05, B-only):** overnight LONG runners 663(+2.50R)/667(+2.11R) floating-profit RAISED B's start-of-day-equity baseline; early give-back hit −3.07% at 03:47 → B halt → blocked ticket-676 (A realized −$170.10); A's balance baseline never crossed −3%. Exactly the plan-flagged mechanism — but PROTECTIVE here. **SINGLE-SOURCE CONFIRMED** (code: CDailyLossHaltExit reads GetDailyPnL, no 2nd baseline; runtime: 0 independent CDailyLossHaltExit closes in BOTH A and B — protection via CRiskMonitor CanTrade block). **mt5 read: equity baseline BENIGN/protective on this strategy (same halt count, net UP, losers avoided) — fallback NOT indicated by numbers; stok rules.** **KILL-CRITERION (over-trigger → net drop): NOT tripped (net +5.29%).** See `progress/4-3.md`. — NEEDS-CARE: single daily-loss source, equity baseline. |
| 4.4 | 4.4 + 4.5 | CRiskMonitor.mqh; UltimateTrader.mq5 | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct + stok ACCEPT-WITH-FOLLOWUP, binding, source-verified). COMMITTED source-only on feat/multi-strategy.** stok ruling: 4.4 correctly separates a capital-preservation stop (loss → resets only on new day) from an operational stop (error → clears on execution recovery) with the right independent clearing — old shared bool let an operational all-clear override a capital stop = genuine risk bug; strict tightening has no downside; matters in the live ≥5-consecutive-reject failure mode the tester can't reproduce. 4.5 shared-ceiling is the correct risk policy (capital-exposure ceiling doesn't care about signal origin; separate budgets would allow 2× daily risk in BOTH mode); symmetric across pattern/breakout/file paths; starvation acceptable (ceiling binds on total risk); orphan-adoption correctly NOT gated. **2 non-blocking Iter-6 follow-ups logged:** log-spam gate (CanTrade LogPrint once-per-transition) + operator config question (shared vs independent file daily budget = feature request). **Bundled (both edit CanTrade halt-check).** Split `m_trading_halted`→`m_loss_halted`+`m_error_halted` (OR'd in getters; error-recovery clears only error flag; daily reset clears only loss flag); added `&& CanTrade()` to file-signal guard (orphan-adoption NOT gated; IncrementTradesToday left as-is). Compile 0err/38warn (0 new). **External-reader grep: ZERO readers of `CRiskMonitor::m_trading_halted`** (CDisplay same-name field is separate). **A/B XAUUSD+ H1 full 2019–2026 Model=1 prod-defaults = BYTE-IDENTICAL** (4 CSVs md5-equal+cmp-clean; report A==B: Net $25,570.97/PF 1.31/2011t/Bal-DD 9.28%; 961 closes/avgR +0.1320). **BOTH fixes live-only:** 4.5 byte-identical — signal file `telegram_signals.csv` absent in tester (`Could not open trade file` every run), 0 file-signal trades A&B, `&&CanTrade()` unreachable-as-decider; 4.4 byte-identical — consecutive-error halt NEVER fires in backtest (0 A&B), so the removed `m_trading_halted=false` in RecordExecutionSuccess was unreachable-as-loss-halt-lifter; loss-halt days IDENTICAL 10=10. Benign B log-noise: "Daily trade limit reached" 74→18665 (extra CanTrade LogPrints, 0 trades). mt5-developer SIGNED. **stok PENDING** for (a) 4.4 strict-tightening intended, (b) InpMaxTradesPerDay=5 shared budget. See `4-4_4-5.md`. NOT committed. |
| 4.5 | 4.6 | CEnhancedTradeExecutor.mqh | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct + stok ACCEPT-WITH-FOLLOWUP, binding, source-verified). COMMITTED source-only.** stok: a permanently-~0.01 ratio is DEAD CODE masquerading as a safety gate (worse than no gate); blocking entries into 2–3× spread is sound XAUUSD execution-risk control; −3.5% net is the right price (Sharpe +0.12 on FLAT Eq-DD, same shape as 4.2/4.3); Model=1 = CONSERVATIVE LOWER BOUND on gate activity (live fires all 3 legs) → dimensional correctness suffices to ship; ~5%-of-bars EXTREME rate is config/tuning surface not a logic defect. **2 Iter-6 follow-ups logged:** real-tick (Model=4) calibration spot-check over a news-spread-stress window; threshold sensitivity (config). ⚠️ first 4.6 mt5 agent hit a SPURIOUS content-filter false-positive (persisted nothing) → orchestrator made the proven dimensional edit directly + minimal agent ran A/B. FIX-SOUND dimensional spread fix (drop `* _Point` at 1938/2039; samples[] are points). **A/B DIVERGED — NOT live-only; the earlier flat-spread hypothesis is FALSIFIED.** Compile B 0err/38warn. A (buggy HEAD c858e6c, `63d6c306`) reproduces baseline EXACTLY (4 CSV md5 == 4.4/4.5 A); B (`2882bbde`) DIFFERS on all 4 CSVs (md5+cmp). **Mechanism:** Site-1 shock-spread leg 0→3567 `[ShockDetector]` events (Spread/Avg now 2–3.2× crossing 2.0/3.0 thresholds; under Model=1 it's the sole live shock signal) → 2308 EXTREME-blocked + 1259 moderate-risk-cut bars → trades 2011→1905; Net $25,570.97→$24,667.66 (−3.5%), PF 1.31→1.32, Sharpe 2.51→2.63, Eq-DD 11.26%→11.28%, risk-reducing. Site-2 spread-stability gate dimensionally correct but NON-binding in tester (0 quality-block/halve in both A&B). mt5-developer SIGNED; stok PENDING (independent). See `4-6.md`. |
| 4.6 | 4.7 | CEnhancedTradeExecutor.mqh; CTradeOrchestrator.mqh | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct + byte-identical proven; stok ACCEPT, binding, source-verified COMPLETE chain). COMMITTED source-only.** stok: the OLD behavior was the dangerous one (rejecting a real partial → a LIVE untracked naked position with no dynamic SL/BE/trailing/partial-TP/daily-loss management = worst EA risk state on fast XAUUSD); accept+track is risk-correct. Complete scaled set {lot_size, original_lots, remaining_lots, entry_risk_amount} — traced all consumers (partial-TP sizing, R-milestones, CalculatePositionRiskDollars), nothing re-reads requested volume post-chokepoint; full-fill guard makes scaling an exact no-op (byte-identical). NOT pinning FOK is right (FOK rejection rate climbs in exactly the fast/thin conditions edge setups fire). No dangling remainder (market orders); exposure-cap interaction fail-safe (short fill consumes less headroom). **1 non-blocking backlog item:** tier-faithful top-up on partials (product enhancement, not a defect). NEEDS-CARE partial-fill repair, NO logic cut. (1) `ValidateExecutedVolume` (CEnhancedTradeExecutor.mqh:1616) fails ONLY on `executedLots<=0`; partial fill → WARN+accept. (2) **LOAD-BEARING:** `position.lot_size = exec_result.executedLots` at CTradeOrchestrator.mqh:705 (was `= lot_size`) so downstream original/remaining seeding (EA 1492/1644/2004/2202) uses ACTUAL filled volume. (3) `entry_risk_amount` scaled by `filled_lots/lot_size` at :724-728 (CalculatePositionRiskDollars returns it directly). (4) `g_trade` NOT pinned to FOK. Compile **0err/38warn (0 new)**. **A/B XAUUSD+ H1 full 2019–2026 Model=1 prod-defaults (395 inputs, InpEnableMultiStrategy=false) = BYTE-IDENTICAL** — all 4 CSVs md5-equal + cmp-clean (Stats `edeb9c96…`, TradeEvents `ebee9ad3…`, Risk `2847e38a…`, backtest_results `44981cde…`; report A==B Net $24,667.66/PF 1.32/1905t/Sharpe 2.63). A reproduces Phase-4.6 B baseline byte-for-byte → harness sound. **Short-fill branch NEVER fired in B** (`Partial fill accepted`=0 journal+EA log; `Volume mismatch`=0; `Zero volume executed`=0 → every tester fill full, executedLots==requested → new branches inert). **LIVE-ONLY** (MT5 tester never partial-fills; FOK-only XAUUSD). 2 runs (≤2 bound). NOT committed. See `4-7.md`. |
| 4.7 | 4.8 + 4.9 + 4.10 | CATRTrailing, CChandelierTrailing, CHybridTrailing, CSteppedTrailing, CParabolicSARTrailing, CPositionCoordinator.mqh | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct, all 3 source-verified; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified). COMMITTED source-only. CLOSES ITERATION 4.** stok: 4.8 = honest exit-side look-ahead removal (Iter-1 1.2-1.4 class — forming-bar saw hindsight-only intrabar extremes); 4.9 = the runner-survival in A was UNDEFINED behavior (broker silently swallowing too-tight modifies while internal SL desynced), NOT an edge → fixing the desync correctly tightens; combined −32.4% = honest deflation with textbook de-repaint signature (WR↑ fewer false stops + avg-R↓ no longer catching the exact top). **The honest baseline is NOW B: Net $16,685.59 / PF 1.28 / Sharpe 2.20 / Eq-DD 12.88%** — supersedes the inflated $24.7k. **Iter-6 follow-up:** re-derive InpTrailChandelierMult/m_swing_lookback vs HONEST B (stale 1.2× rejection at CPositionCoordinator:2711-2712 was vs the inflated baseline → re-run clean; adopt wider only if net recovers WITHOUT Eq-DD>12.88% or PF<1.28; optional 4.8-only run to split attribution). **Trailing cluster — landed IN ORDER 4.8→4.9→4.10 (ONE compile / ONE A/B). mt5-developer SIGNED; stok SIGNED.** **Active trailing plugin on the 395 prod defaults = CChandelierTrailing ONLY** (`InpTrailStrategy=4`; mq5:814-830 enables exactly one; every EXIT's LastTrailReason="Chandelier", A=647/B=614; CATR/Swing/SAR/Stepped/Hybrid all DISABLED). **4.8** (forming-bar→closed-bar): `CopyBuffer(...,0,1,1,buf)`+`buf[0]` in all 5 plugins (CATR 141/144, CChandelier 149/152, CHybrid 232/235, CStepped 135/138, CParabolicSAR 145/148 — SAR flip now on completed bar); CopyHigh/CopyLow swing scans LEFT (already shift 1); is_better ratchet PRESERVED. **4.10** (`if(atr<=0) return update;`) added to CATR:144 + CHybrid:235 (Chandelier+Stepped already had it). **4.9** (CPositionCoordinator trailing-commit ~2727-2945): clamp normalized_sl to `MathMax(STOPS_LEVEL,FREEZE)*point` from BID(long)/ASK(short), ONLY ever moving SL AWAY from market; re-verify is_better vs old_sl AFTER clamp (skip if backward); commit pos.stop_loss only to the clamped value; `+STOPS_LEVEL_CLAMP` gate_reason; on INVALID_STOPS modify-reject revert pos.stop_loss=old_sl + `INVALID_STOPS_REVERT` gate_reason. NO logic cut. Compile **0err/38warn (0 new)** (A ex5 `f29ac0af` ≠ B `1873a63b` neg-control). **A/B XAUUSD+ H1 full 2019–2026 Model=1 prod-defaults (395, InpEnableMultiStrategy=false), 2 runs.** A == committed HEAD `a95f53e` (3 CSV md5 reproduce Phase-4.7 B baseline → harness sound). **NOT byte-identical — real exit-side delta. Overall A→B: Net $24,667.66→$16,685.59 (−32.4%) / PF 1.32→1.28 / Trades(deals) 1905→1742 / EP 12.95→9.58 / Sharpe 2.63→2.20 / Bal-DD $3,612.77(9.44%)→$3,416.90(11.35%) / Eq-DD $4,405.55(11.28%)→$3,941.64(12.88%).** Whole-POSITION basis (Stats EXIT rows) trades A=908/B=909 (deal-count drop = different partial/exit deals, NOT fewer entries). Yearly (pos-basis net/avgR): 19 $1254/0.135→$569/0.078; 20 −$47→−$72; 21 $2344/0.213→$1814/0.104; 22 $3630→$1851; 23 $924→−$152; 24 $2193→$1500; 25 $15572/0.358→$12677/0.377; 26 −$600→−$999. **ATTRIBUTION (task hypothesis CORRECTED): delta = 4.8 (dominant, closed-bar Chandelier ATR) + 4.9 (NOT inert — Vantage XAUUSD+ tester has NONZERO stops/freeze ~0.20px; clamp MOVED SLs on 135 trades / 273 trail-update events / 409 tagged rows). 4.10 INERT (0 — Chandelier active, already had guard; CATR/CHybrid disabled).** 4.9 corroboration: A had 345 TRAIL_BROKER_FAIL (252 "Invalid stops"+93 "Market closed"); B only 75 — clamp lets ~250 previously-rejected tightenings land. `INVALID_STOPS_REVERT`=0 (clamp prevents the reject upstream; revert is the live backstop). **Entry set 908/908 identical (0 in A-not-B; +1 B = freed-slot Rubber Band Short 2022.11.10), all 907 common entries byte-identical Entry+OriginalSL → trailing changed EXITS not entries.** VALIDATION: is_better ratchet HELD (0 backward moves / 2344 TRAIL_INTERNAL); clamp toward-safety 273/273 (0 toward-market) → runner never under-protected. Exit-change signature: WR 44.4%→50.6% UP, avg-R 0.136→0.096 DOWN = honest exit-side look-ahead removal (forming-bar `[0]` used intrabar high/low/ATR; closed-bar `[1]` is the real-time basis — same class as 1.2/1.3/1.4). **KILL READ:** −32% net is a MATERIAL drop, broad-based, NOT a no-op — but mechanism-correct honest-repaint-removal (NOT a defect, NOT edge loss); handed to stok to rule acceptable-deflation vs config-revisit (e.g. closed-bar-aware InpTrailChandelierMult re-tune). See `4-8_4-9_4-10.md`. |

**Iter 4 kill/loop:** 4.2 net P&L material drop, 4.3 over-trigger on overnight runners → loop to stok. No-op claims (4.5,4.6 common path,4.4 partial of 4.9) require R-for-R identical 2019–2026.

---

## Iteration 5 — Core / Orchestration + Persistence

| Phase | Fixes | Files | Touches-logic | Status | Commit | Notes |
|---|---|---|---|---|---|---|
| 5.1 | 5.1 | CPositionCoordinator.mqh; UltimateTrader.mq5 | N | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct + byte-identical proven; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified all 4 sites incl. De-Morgan on the 2 skip-guards). COMMITTED source-only.** stok: old magic-only match was a genuine risk defect (adopt a foreign-symbol position → manage with gold's ATR/tick/R/lot math = nonsensical SL + false R state, a silent uncapped hole active during restart/crash recovery); mirroring the mandatory OnTick guard converges all 4 adoption paths on ONE invariant; correct fail-safe boundary (reject foreign now, leave multi-symbol mgmt to 5.3). **FU-1 logged:** document one-magic-per-instance deployment constraint (symbol guard can't catch same-symbol/same-magic cross-instance collision). FIX-SOUND: symbol-match recovery loaders. Added `PositionGetString(POSITION_SYMBOL)==_Symbol` AND-condition to all THREE startup recovery loaders in `CPositionCoordinator.mqh` — **verified file:line vs current source (HEAD 253f0e7): ReconcileWithBroker :1322 (skip-guard, De-Morgan `!=magic\|\|!=_Symbol`), broker-only fallback in LoadOpenPositions :1416 (positive `==magic && ==_Symbol`), LoadOrphanBrokerPositions :1490 (skip-guard De-Morgan)** — mirroring the live OnTick adoption guard at **`UltimateTrader.mq5:2083-2084`** (prompt cited 2043-2044; drifted) EXACTLY (same `PositionGetString(POSITION_SYMBOL)` compare, same `_Symbol`). Single-symbol-per-chart guard; NOT broadened to multi-symbol mgmt (5.3's lane); NO logic cut. **0-err/0-new-warn (`Result: 0 errors, 38 warnings, 10283 ms elapsed, cpu='X64 Regular'`; 0 warnings ref CPositionCoordinator).** **A/B XAUUSD+ H1 Model=1 full 2019.01.01–2026.06.27, 395 prod defaults (`InpEnableMultiStrategy=false`) = A==B BYTE-IDENTICAL on ALL 4 output CSVs (md5-equal + cmp-clean):** Stats `fc9f934ea78025d30c079bdd8fd67a80`, TradeEvents `40b897c19fbfaa9dec08e962f2b569a2`, Risk `49876b51edd0540a4f08f16eb88f5c3b`, backtest_results `0424aa9ff3c89a0867e25be78527e4de`. A reproduces committed HEAD 253f0e7 exactly (3 EA-CSV md5 == recorded p48B baseline; headline **Net $16,685.59 / PF 1.28 / 1742t / Sharpe 2.20 / Eq-DD 12.88%** == binding baseline). **Mechanism:** single-symbol tester → `PositionGetString(POSITION_SYMBOL)` always == `_Symbol` → new AND always true → no recovery decision changes → byte-identical (expected no-op). The guard's value is LIVE multi-chart safety (stops adopting a foreign-symbol position and managing it with gold's tick math). KILL-CRITERION (production byte-identical) PASS. See `progress/5-1.md`. |
| 5.2 | 5.3 + 5.4 | CPositionCoordinator.mqh | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct + byte-identical proven; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified). COMMITTED source-only.** stok: 5.3 correct+complete (old code trails file-position symbol-X with gold's ATR/swing/tick math = nonsensical SL + corrupted risk telemetry poisoning the aggregate gate; PositionGetString is the right+only symbol source since SPosition has none; ONE aggregate bucket = correct conservative policy for the correlated gold complex, per-symbol would be LESS safe); 5.4 correct repaint class (4.8), count stays 5, ratchet preserved; byte-identical proves no regression, live-only improvement accepted on the 4.8 argument. **4 deferred follow-ups:** FU-A synthetic file-signal validation of 5.4 kill-criterion (before live multi-symbol reliance), FU-B PositionSelectByTicket select-side-effect footgun, FU-C correlation-blind bucket, FU-D hardcoded PERIOD_H1. **Bundled (same trail-block lines), ONE compile / 2 runs.** **STEP-0:** both trail blocks are FILE-SIGNAL-position-only (inside `signal_source==SIGNAL_SOURCE_FILE` @1694; pos_symbol derived @1697-1698); `telegram_signals.csv` ABSENT in tester → 0 file positions → blocks never execute → 5.3+5.4 BYTE-IDENTICAL in backtest (stated before implementing). **5.3:** threaded in-scope `pos_symbol` into every iATR/iHighest/iHigh/iLowest/iLow in BOTH trail blocks (block1 TP3/runner @InpFileTrailAfterTP2, block2 PART-A @InpFileSignalTrailing); derived symbol-from-position INSIDE CalculatePositionRiskDollars (~290) + GetCurrentMarketPrice (~318) via PositionGetString(POSITION_SYMBOL) fallback _Symbol (SPosition has NO symbol field — line-517 `symbol` is EntrySignal; first compile errored 256, fixed) → ~15 call sites untouched; aggregate risk bucket UNCHANGED. **5.4:** iHighest/iLowest last arg 0→1 (closed bars), ATR `CopyBuffer(...,0,1,1,buf)`+`buf[0]`, **count STAYS 5**. NO logic cut; is_better ratchet preserved. **Lines drifted post-5.1** (plan cited ~1799/1812/.../1882; verified actual block1 @1828/1841/1843/1847/1849, block2 @1893/1905/1906/1910/1911). Compile **`0 errors, 38 warnings, 9236 ms`** (0 new; neg-control A.ex5 `3f8d9831…`≠B.ex5 `880897…`). **A/B XAUUSD+ H1 Model=1 full 2019.01.01–2026.06.27, 395 prod defaults (`InpEnableMultiStrategy=false`), 2 runs = A==B BYTE-IDENTICAL on ALL 4 CSVs (md5-equal + cmp-clean):** Stats `fc9f934e…`, TradeEvents `40b897c1…`, Risk `49876b51…`, backtest_results `0424aa9f…` — same md5s as the Phase-5.1 committed-HEAD baseline → A AND B reproduce HEAD `90e4d82` byte-for-byte (Net **$16,685.59** / PF **1.28** / **1742t** / Sharpe **2.20** / Eq-DD **12.88%** A==B). **0 file positions CONFIRMED:** 0 FileTP/FileSignal/FILE_INDEPENDENT markers in B Stats+TradeEvents, 0 FileSignal rows in backtest_results, 0 `[FileTP*]/[FileTrail]/[FileExit]/[FileSignal]` lines in tester journal (journal shows `SIGNAL_SOURCE_BOTH` + 27,613× `telegram_signals` read-attempts → CSV absent → branch never entered). **MECHANISM:** file-only branch never entered + single-symbol tester → PositionGetString==`_Symbol` → helpers resolve to `_Symbol` → byte-identical by construction. **5.4 KILL (TP3-hit must NOT drop with premature-stop): NOT EXERCISABLE in tester** (file path unexercised); 5.4 is LIVE-ONLY (4.8 repaint-correctness class). No production regression possible (byte-identical). **stok-facing flag:** a synthetic file-signal validation backtest (inject telegram_signals.csv) would be needed to measure the 5.4 KILL-criterion — OUT of ≤2-run scope, follow-up for stok. See `5-3_5-4.md`. |
| 5.3 | 5.5 | UltimateTrader.mq5; UltimateTrader_Inputs.mqh | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified — fix STRONGER than spec: global now telemetry-only, never read for sizing → leak impossible by construction). COMMITTED source-only.** stok: fixes BOTH the cross-bar leak AND the same-bar clobber that left the moderate-shock down-sizing DEAD (A 0/1258 reached a trade → B 12 correctly down-sized) = the intended completion of the 4.6 shock gate's MODERATE arm; entries identical 909==909 → edge amputation impossible; +2.0% = correctness consequence not tuning (flat PF/DD/avg-R, InpMinSessionRiskFactor never bound); floor 0.25 = right live two-reducer guardrail; block/down-size mutually exclusive (verified). **2 non-blocking follow-ups:** FU-1 shock-coefficient magnitude (don't tune on 12-trade sample), FU-2 confirmed-clean interaction. BETTER-OPTION implemented exactly: PER-BAR RESET `g_session_quality_factor=1.0` at top of `if(isNewBar)` (mq5:1454); SEPARATE locals `shock_factor`/`sq_factor` (mq5:1743-1744); shock path CLAMP `MathMax(0,MathMin(1,intensity))` → `shock_factor=1-int*0.5` (mq5:1759-1766); sq path writes `sq_factor` (mq5:1784); COMBINE-ONCE + FLOOR at apply `combined=shock*sq; MathMax(combined,InpMinSessionRiskFactor)` (mq5:1904-1906); NEW input `InpMinSessionRiskFactor=0.25` in EXECUTION INTELLIGENCE group (Inputs:358). NO logic cut (1258 moderate + 2310 EXTREME shock events identical A/B). **Compile `0 errors, 38 warnings` (exactly baseline, 0 new).** **A/B XAUUSD+ H1 Model=1 full 2019.01.01–2026.06.27, prod defaults (A=395 / B=396 params, `InpEnableMultiStrategy=false`), 2 runs.** A reproduces committed-HEAD byte-for-byte (EA CSV md5 Stats `fc9f934e…`/TradeEvents `40b897c1…`/Risk `49876b51…`; tester headline **Net $16,685.59 / PF 1.28 / Sharpe 2.20 / 1742t / Eq-DD 12.88%** == binding baseline — harness sound). **NOT byte-identical (expected, production-affecting):** B = **Net $17,018.83 (+$333.24/+2.0%) / PF 1.28 / Sharpe 2.23 / 1742t / Bal-DD 11.37% / Eq-DD 12.89%**. Trade count + entry set IDENTICAL (909==909 EA entries, 0 added/dropped → edge amputation impossible; only SIZING changed). **LEAK PROOF:** A had NO per-bar reset but the sq-gate `else`(=1.0) over-reset wiped the shock `*=` before apply → **0 of 1258 shock reductions ever reached a trade in A** (`[SessionQuality] Risk:` apply-lines = 0). B freshly recomputes every bar → **12 shock-bar signals correctly down-sized** (apply-lines = 12). B shock_factor ∈ [50%,67%], **0 below 50%** (clamp+separation; no compound/monotonic-shrink). **FLOOR BINDS: 0 of 12** executed (min combined 0.51 > 0.25; floor is the live two-reducer guardrail, not exercised in tester where sq≡1.0). **SIZE: avg LotSize A 0.1847 → B 0.1865 (+0.97%)** (B down-sizes 12 shock trades then path-dependent balance ripple lifts later-year lots). avgR flat (0.0956→0.0952). Net moves both ways yearly — path ripple, not directional edge. CORRECTNESS fix (honest per-bar risk scaling), not P&L tuning; `InpMinSessionRiskFactor=0.25` is plan default, NOT tuned. KILL-CRITERION (factor returns to 1.0 each bar / no monotonic shrink) PASS. See `progress/5-5.md`. |
| 5.4 | 5.6 + 5.7 | UltimateTrader.mq5; Inputs | N | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified). COMMITTED source-only.** 5.6: `&& register_patterns` on the 3 engine RegisterEntryPlugin calls (mq5:734/740/746) → restores FILE-only contract; 5.7: engine-4 router-wiring `&& InpEnableEngineExpansion` (mq5:756, legacy standalone reg :687 UNCHANGED). stok verified at source: RegisterEntryPlugin(false) early-returns (:366) so the gate genuinely removes engines from g_entryPlugins[] (orchestrator fires only from that array :917); the ungated regime-router path is SUPPRESSOR-ONLY (SetActivationWeight→m_isEnabled, CMajorStrategyEngine:124-128) and CANNOT force a fire; no double-registration; engine-1-3 (array-gated) vs engine-4 (router-gated) asymmetry is the only coherent mapping for the reused instance. NO logic cut; gates behind `if(InpEnableMultiStrategy)` (:718). **⚠️ STALE-BINARY INCIDENT — RESOLVED:** the in-phase A/B accidentally ran a 7-commit-stale pre-4.6 binary (`9dbe9052`), so its "A==B byte-identical / $25,570.97 / data-drift" was a STALE ARTIFACT (NOT drift). **AUTHORITATIVE validation (REBASELINE-5-6.md):** fresh working-tree build `f1e251fa` (≠`9dbe9052`) = committed-HEAD ed488df byte-for-byte (**$17,018.83/PF 1.28/1742t**; 3/3 EA CSV md5: Stats `08640614…`/TradeEvents `18aedaca…`/Risk `7b3db7d1…`) → since HEAD lacks the gates and working-tree-WITH-gates reproduces it exactly, **5.6+5.7 PROVEN byte-identical no-ops on the prod path. CHAIN SOUND** (no history drift, 44,267 bars/10.59M ticks unchanged). Root cause = compiler-writes-repo vs tester-loads-data-dir + no md5 assertion → **RESUME.md step 3b** now mandates `md5(load-path)==md5(fresh-build)` every run. 2 non-blocking follow-ups (FU-1 doc gate-asymmetry, FU-2 verify-on-enable checklist). See `5-6_5-7.md` (incl. ⚠️ CORRECTION) + `REBASELINE-5-6.md`. |
| 5.5 | 5.8 + 5.9 | CTradeOrchestrator.mqh; CAdaptiveTPManager.mqh | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified all 4 loops). COMMITTED source-only.** stok: 5.9 closed-bar pivot is correct (start_pos=0 put the FORMING H4 bar as a pivot right-neighbor → repaintable target/level; start_pos=1 = closed-bar = standard HTF-pivot practice); target/level-side fix → entries byte-identical is the RIGHT signature (entry-move would be a red flag); determinism (B bit-identical twice) = correct+sufficient proof. 5.8 = textbook OOB guard, inert in steady state. +2.56%/better-Eq-DD = CORRECTNESS outcome NOT tuning — but stok HONESTY CAVEAT: treat "better Eq-DD/Sharpe" as PATH NOISE on 4 trades, not a banked risk improvement (accept on correctness+determinism). **1 tracked follow-up:** short/support low[] path correct-by-symmetry (verified in source) but never empirically exercised on this long-biased run — confirm on a short-exercising backtest. **3b worked** (distinct binaries, A reproduced baseline byte-for-byte → stale-binary risk closed). **Bundled (same four loops):** CTradeOrchestrator `FindNearestObstacle` LONG high[] (:1014-1032) + SHORT low[] (:1058-1076); CAdaptiveTPManager `FindNextResistance` (:483-501) + `FindNextSupport` (:506-528). Per loop: `CopyHigh/Low(...,PERIOD_H4, 0→1, 100, arr)` (5.9 closed-bar) + capture `n` + `for(i=2;i<n-2;i++)` + `n>=5`/`n<5 return 0` floor (5.8 OOB guard). NO logic cut. **Compile 0err/38warn (0 new) both legs; none in edited files.** **3b: A `c1fb7cc5` / B `293a30de` distinct, both ≠ stale `9dbe9052`; load==fresh asserted both legs.** **A REPRODUCES BINDING BASELINE byte-for-byte** — $17,018.83/PF1.28/1742t/Eq-DD12.89% + EA-CSV md5 Stats`08640614…`/TradeEvents`18aedaca…`/Risk`7b3db7d1…` (= recorded baseline EXACT → load-path binary is fresh HEAD, stale risk closed). **A/B (XAUUSD+ H1 Model=1 full 2019–2026, 396 prod defaults):** A→B Net $17,018.83→**$17,455.34 (+$436.51/+2.56%)**, PF 1.28→1.28 flat, **trades 1742→1742 IDENTICAL**, ENTRY_OPENED 909→909 identical, Sharpe 2.23→2.26, Eq-DD 12.89%→12.64% (better), Recovery 4.26→4.40. **5.8 INERT in steady state** (start_pos=1,count=100 on H4 ⇒ n always 100 post-warmup ⇒ guard equals old `i<98`; never trips mid-run; first entry 2019.01.03 far past warmup). **5.9 delta fully attributed:** entry set BYTE-IDENTICAL (Time\|Dir\|Entry\|SL\|RiskDist diff=0; Candidates.csv md5-equal A==B) → NOT entry look-ahead; only **4/909 entries** had shifted TP1/TP2 (2022.12.14, 2023.12.04, 2025.02.13, 2025.09.30, all LONG = high[] resistance loops) = forming→closed H4 pivot flips on the structure-target/obstacle level; SLs unchanged; lifecycle unchanged (EXIT_FILL 909=909, TP0/TP1/TP2 partials 445/258/126 identical); +4 event rows = +2 TRAIL_INTERNAL/+2 TRAIL_BROKER_OK on the 4 TP-shifted runners. **5.9 DETERMINISM (kill-criterion): B run TWICE = ALL 4 CSV BIT-IDENTICAL** (md5-equal+cmp-clean: Stats`3b13ea8f…`/TradeEvents`2222e2f9…`/Risk`c152fb4a…`/Candidates`77b07fb1…`) → PASS, forming-bar non-determinism eliminated. **NO kill-criterion tripped** (P&L divergence explained, not unexplained). mt5 SIGNED; stok PENDING (do NOT self-sign). 3 terminal runs (2 A/B + 1 determinism re-run, allowed THIS phase). See `5-8_5-9.md`. |
| 5.6 | 5.10 | CFileEntry.mqh; UltimateTrader.mq5 | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct + byte-identical proven; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified every site). COMMITTED source-only.** stok: deferred-commit correct (optimistic flag = re-derivable in-memory state; durable key only on ticket>0; rollback never commits key → retryable; bounds-guard sufficient); old behavior = high-sev defect (valid never-traded signal burned on transient reject); NO runaway — bounded on TWO axes (600s InpSignalTimeTolerance window + slippage gate anchored to STATIC CSV entry → late/degraded "fills hours late at bad price" structurally IMPOSSIBLE); no race that loops. **3 follow-ups** (FU-1 synthetic transiently-rejecting-CSV validation = only test that measures recovery value; FU-2 key rollback off m_pendingTradeKey not idx; FU-3 optional retry counter for log hygiene). FIX-SOUND deferred-commit (XCut-1). **What changed:** `CFileEntry.mqh` — new members `m_pendingTradeIdx`(=-1)/`m_pendingTradeKey` (80-81, ctor 631-632); emit site (~836-845) KEEPS optimistic `m_trades[i].Executed=true` (stops same-tick re-emit) + records pending idx/key but DEFERS `MarkExecuted`; new `ConfirmExecuted()` (681-694) fires durable `MarkExecuted` ONLY on `ticket>0` (idempotent); new `RollbackPending()` (706-725) clears the optimistic flag on reject so a later in-window tick retries — **bounds-guards `m_pendingTradeIdx` (`>=0 && < ArraySize(m_trades)`)** before deref, stale-index branch logged. `UltimateTrader.mq5` file-signal result site (~2255/2261-2266): success→`ConfirmExecuted()`, new `else` (ticket<=0)→`RollbackPending()`. `InpFileMaxSlippagePct` slippage gate untouched (stale-retry self-reject; no extra retry-window logic). NO logic cut. **Compile 0err/38warn (0 new) both legs** (B `Result: 0 errors, 38 warnings, 8924 ms`; A `…9031 ms`); no warning in CFileEntry.mqh; no error 106. **3b:** A `a1e96bf6` / B `6b79aa6f` distinct (B has the added code), both ≠ stale `9dbe9052`, load==fresh both legs; **A reproduces committed HEAD d64965d byte-for-byte** (4/4 EA-CSV md5 == committed 5.8+5.9 run; $17,455.34/1742t). **A/B (XAUUSD+ H1 Model=1 full 2019.01.01–2026.06.27, 396 prod defaults, InpEnableMultiStrategy=false, InpSignalSource=2=BOTH, 2 runs) = BYTE-IDENTICAL:** all 4 EA CSVs md5-EQUAL + `cmp` CLEAN (Stats `3b13ea8f…`/TradeEvents `2222e2f9…`/Risk `c152fb4a…`/Candidates `77b07fb1…`); headline identical $17,455.34/PF 1.28/1742t/Eq-DD 12.64%. **Byte-identity mechanism:** `telegram_signals.csv` ABSENT in tester → CFileEntry loads 0 trades → emit path never entered → pending stays -1 → Confirm/Rollback never reached → added code dead in tester; 0 FileSignal rows in TradeEvents/Stats = 0 file-signal trades. **LIVE-ONLY:** recovery value (retry transiently-rejected file/Telegram signals) unmeasurable in tester; byte-identical A/B proves no production regression; correctness rests on reviewed logic (optimistic flag stops re-emit; ConfirmExecuted only on ticket>0; bounds-guarded RollbackPending re-enables retry; slippage gate self-rejects stale retries). **Baseline note:** $17,018.83 is the PRE-5.8/5.9 anchor (HEAD ed488df); HEAD d64965d already committed 5.9's +$436.51 → A correctly = $17,455.34, 1742t unchanged. **KILL-CRITERION (default byte-identical) PASS.** 1 follow-up: synthetic transiently-rejecting-CSV validation (Iter-5 QA(b) 5.10), out of ≤2-run scope. mt5 SIGNED; stok PENDING (do NOT self-sign). See `5-10.md`. |
| 5.7 | 5.11 | Structs.mqh; CPositionCoordinator.mqh (STATE_FILE_VERSION 4→5) | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct + byte-identical proven; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified full chain). COMMITTED source-only.** stok: entry_risk_amount mandatory-persist is LOAD-BEARING (short-circuits CalculatePositionRiskDollars at :314-315 before the tick recompute → feeds the 4.2 exposure cap/EC controller; completes 5.1/5.3 for the restart path); signal_id correctly EXCLUDED (audit-only, never a trade/risk decision); EXACT-MATCH version gate fires FIRST = deterministic v4 discriminator (size-alone insufficient, harness-proven); broker-only fallback SAFE (broker is authoritative, state file is a cache, no data-loss); version-bump cost acceptable/self-announcing. **stok-flagged COMMENT defect FIXED this phase** (2 comments misattributed v4-reject to the size check → corrected to credit the version gate; comment-only, byte-identical). 1 residual note (no symmetric entry_risk<=0-on-restore check — lower-stakes, not added). **BETTER-OPTION implemented exactly (XCut-7, live-only persistence/restore).** **What changed:** `Structs.mqh` `PersistedPosition` (ends ~303): appended TWO POD doubles `tp3` (314) + `entry_risk_amount` (315) — NO string member (signal_id intentionally EXCLUDED; FileWriteStruct can't serialize a dynamic string → pointer/garbage; verified PersistedPosition has 0 string-typed members). `CPositionCoordinator.mqh:44` STATE_FILE_VERSION 4→5; `PositionToPersisted` (~228) persists both; `RestoreFromPersisted` (~276) restores both (authoritative — only v5 reaches it) + WARN when restored SIGNAL_SOURCE_FILE pos has InpFileUseTP3 but tp3<=0; `LoadPositionState` version gate (~1242) STRENGTHENED to EXACT match (`version != STATE_FILE_VERSION` → graceful broker-only fallback = the DETERMINISTIC v4-vs-v5 discriminator) + a `FileSize < header + N*sizeof(PersistedPosition)` size check (defense-in-depth, `ulong` to avoid warn); per-record short-read guard + CRC32 remain. NO logic cut. **WHY:** restored positions lost tp3 (`has_tp3` false → 3-way 33/33/34 ladder silently degraded to 2-way 50/50) AND entry_risk_amount=0 forced `CalculatePositionRiskDollars` into the `_Symbol`-tick recompute → re-broke the wrong-symbol R error 5.1/5.3 closed for a restored foreign-symbol/file position. **Compile `Result: 0 errors, 38 warnings, 8808 ms` (0 new; none in edited files; 1 interim ulong→long warn fixed).** **3b enforced both legs:** A (HEAD 58b7ae5) `16fc45e2…` / B (final shipping) `fe011dc3…` (DISTINCT — B carries schema+version-gate); load==fresh asserted both; **A reproduces binding baseline byte-for-byte** ($17,455.34/PF1.28/1742t/Eq-DD12.64% + 4/4 EA-CSV md5 Stats `3b13ea8f…`/TradeEvents `2222e2f9…`/Risk `c152fb4a…`/Candidates `77b07fb1…`). **A/B XAUUSD+ H1 Model=1 full 2019.01.01–2026.06.27, 396 prod defaults (InpEnableMultiStrategy=false, InpSignalSource=2) = BYTE-IDENTICAL:** all 4 CSVs md5-EQUAL + cmp-CLEAN. **Restore-path NOT exercised (tester journal OnInit):** `LoadPositionState: Loaded 0 record(s)…CRC32 verified` → `LoadOpenPositions: …falling back to broker-only recovery` → `[Init] Loaded 0 existing positions`; with 0 records ReconcileWithBroker early-returns → `RestoreFromPersisted` NEVER called → tp3/entry_risk_amount restore reads never run; schema change affects ONLY the WRITTEN format. **Fix IS live in B:** leg A wrote a v4 state file (version byte 04), leg B wrote v5 (05) — schema bump active, output byte-identical. **v4 GRACEFUL-REJECT proven by standalone harness** (`MQL5/Scripts/P511_StateCompatCheck.mq5`, compiles 0err/0warn): sizeof v4=192 / v5=208 (+16, v5 LARGER); a worst-case v4 file (2 records + big mode-perf trailer, 1116B) has total ≥ min_v5_size 440 so `size-check-alone-rejects=NO` (proves size-check alone insufficient) → STRENGTHENED version gate REJECTS it deterministically (`file v4 != current v5` → broker-only fallback, NOT mis-parsed). Genuine in-tree v4 artifact preserved at `Logs/p511_state_backup/`. KILL-CRITERION (default byte-identical + v4 graceful reject) PASS. mt5 SIGNED; stok PENDING. See `5-11.md`. |
| 5.8 | 5.12 + 5.13 | CSignalOrchestrator.mqh; UltimateTrader.mq5; CSignalManager.mqh | N | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct + byte-identical proven; stok ACCEPT, binding, source-verified truly-dead, meets 2.7 standard). COMMITTED source-only. CLOSES ITERATION 5.** stok independently re-grepped to exit-1 (zero hits); the dead class was a never-wired duplicate with a divergent 2-bar-expiry trap (latent footgun removed); live pending path is g_signalOrchestrator's SEPARATE untouched mechanism; SPendingSignal correctly preserved; A/B binaries correctly DISTINCT but every executed instruction identical; shipped .ex5 == tested B. 5.12 already under 2.1 (no action). 1 cosmetic Iter-6 follow-up (stale "SignalManager" comments Structs.mqh:7/488). **LAST Iteration-5 phase — ITERATION 5 COMPLETE.** **5.12 VERIFY = already covered under 2.1** (NO edit): `best_signal.major_engine = signal.major_engine;` already present at `CSignalOrchestrator.mqh:830` with comment `// Fix 2.1 (also fix 5.12)…` (+ `routed_engine` :833). Telemetry-only, inert engines-off. **5.13 dead-code removal (GREP-CONFIRMED-DEAD before removing — 2.7 discipline):** `g_signalManager` allocated(`:884`)+deleted(`:1277`) but **ZERO method invocations repo-wide** (`g_signalManager->`/`.` = 0 hits) — the LIVE pending/confirmation path is `g_signalOrchestrator` (its own `SPendingSignal m_pending_signal`+methods, mq5:1585-1721), CSignalManager is a never-invoked duplicate carrying a divergent 2-bar-expiry trap. `g_consecutiveErrors` = declaration(`:356`) ONLY, ZERO reads/writes/branches. Both provably dead. **`SPendingSignal` STRUCT (Structs.mqh:490) PRESERVED** (used live mq5:1352/1598/1721, CSignalOrchestrator:108/225, CTradeOrchestrator:786). **Removed:** include(:96), global decl(:343), g_consecutiveErrors+comment(:355-356), allocation(:884), teardown(:1277) — **reverse-order teardown of all LIVE globals preserved untouched**; `git rm Include/Core/CSignalManager.mqh`. Post-removal grep all 3 identifiers = ZERO hits. NO logic cut. **Compile `Result: 0 errors, 38 warnings, 8469 ms` (== 38 baseline, 0 new, 0 dropped; no error 106 — nothing else included the file).** **3b enforced both legs:** A (committed HEAD 21957b4, built in `/mnt/c` worktree, CSignalManager present) `8364f77b…` / B (post-fix) `d4aaabcc…` (DISTINCT — B removed the class; neither = stale `9dbe9052`); load==fresh asserted both. **A reproduces binding baseline byte-for-byte** ($17,455.34/PF1.28/1742t/Eq-DD12.64% + 4/4 EA-CSV md5 Stats `3b13ea8f…`/TradeEvents `2222e2f9…`/Risk `c152fb4a…`/Candidates `77b07fb1…`). **A/B XAUUSD+ H1 Model=1 full 2019.01.01–2026.06.27, 397-line prod `[TesterInputs]` (InpEnableMultiStrategy=false, InpSignalSource=2), 2 runs = BYTE-IDENTICAL:** all 4 CSVs md5-EQUAL + `cmp` byte-for-byte CLEAN; identical headline $17,455.34/1.28/1742t/12.64%. **Mechanism:** CSignalManager methods never called → removing the class changes no executed instruction; g_consecutiveErrors never read → removing it changes nothing; major_engine inert engines-off. KILL-CRITERION (default byte-identical) PASS. mt5 SIGNED; **stok PENDING** (do NOT self-sign). See `5-13.md`. **Do NOT commit; awaiting stok.** |

**Iter 5 kill/loop:** 5.2(=5.4) TP3-hit drops with premature-stop → reconsider; 5.5(=5.9) unexplained P&L divergence → loop. No-op claims byte-identical.

---

## Iteration 6 — Infrastructure + Hygiene

| Phase | Fixes | Files | Touches-logic | Status | Commit | Notes |
|---|---|---|---|---|---|---|
| 6.1 | 6.1 | CEnhancedTradeExecutor.mqh; ErrorHandlingUtils.mqh | Y | DONE ✅ COMMITTED | | **DONE — BOTH agents signed (mt5 implementation-correct + byte-identical executed trades; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified — NEITHER REJECT-LOOP trigger fired). COMMITTED source-only.** stok: retcode-PRIMARY correct (GetLastError stale/generic on server-reject; legacy switch kept as retcode==0 fallback); REJECT stays RETRY (transient-by-elimination, hard reasons have own retcodes already in NO-RETRY, bounded at 3); INVALID_STOPS stays NO-RETRY (Option A — never fire a non-structural stop, stand aside) → dormant structural-SL guard PARKED+documented this phase (comment-only byte-identical); m_maxRetries=3 fine (stop distance preserved across retries). 2 deferred live-only follow-ups (FU-1 intended-entry drift guard InpMaxRetryDriftPoints; FU-2 CONNECTION/TOO_MANY_REQUESTS→RETRY + SERVER_DISABLES_AT→NO-RETRY). NEEDS-CARE: retcode-first classifier DONE. A/B byte-identical on ALL executed trades (Stats/TradeEvents/Candidates md5+cmp + headline $17,455.34/1.28/1742t/12.64%); 13-row Risk-CSV delta = MARKET_CLOSED(10018) failed-exec diagnostics ONLY (never-executing weekend-gap orders) → B correctly stops after 1 attempt vs A's 2 → no prod regression, confirms retcode path live+correct. 3b: A `f1f45d51` / B `58c77639` distinct, A==binding baseline. Cap=loop bound+attempt gate (3). INVALID_STOPS structural-SL guard added (dormant given NO-RETRY — flagged stok-d). stok call-outs (a)–(d) PENDING. See `6-1.md`. |
| 6.2 | 6.2 | CEnhancedTradeExecutor.mqh | N | PENDING | | FIX-SOUND: netting fallback discriminator. **Land after 4.6** (both edit ValidateExecutedVolume). |
| 6.3 | 6.3 | Logger.mqh; UltimateTrader.mq5 | N | PENDING | | FIX-SOUND mechanics/perf. Trade count/P&L MUST be byte-identical. Verify LOG_LEVEL ordinals. |
| 6.4 | 6.4 | CFileEntry.mqh | N | PENDING | | NOT-A-TRADING-PROBLEM: release ATR handle. Trade set unchanged. |
| 6.5 | 6.5 | CEnhancedPositionManager, CSmartTrailingStrategy, CStandardExitStrategy, infra surface; mq5 includes 29–30 | N | PENDING | | **Repair-and-deprecate, do NOT delete.** #ifdef-fence; symbol-derived filling; guard Sleep. Remove dead includes ONLY after grep. Do NOT re-activate health stack. |
| 6.6 | 6.6 | CRegimeAwareExit, CDailyLossHaltExit, CMaxAgeExit, CWeekendCloseExit | N | PENDING | | NOT-A-TRADING-PROBLEM: set valid=shouldExit=true. Identical exit behavior. |
| 6.7 | 6.7 | CTradeOrchestrator.mqh comment; STRATEGIES.md; docs/03 | N | PENDING | | **Documentation-only.** Tabulate effective 2.0% cap. Do NOT lower multipliers. |
| 6.8 | 6.8 | docs/06-CSV-File-Signals.md | N | PENDING | | **Doc-only. Sequence AFTER 4.4(=4.5)** (trade-count line changes once CanTrade gates file path). |
| 6.9 | 6.9 (B4 sweep) | CFalseBreakoutFadeEntry, CRangeBoxEntry, CPullbackContinuationEngine, CCrashBreakoutEntry, CSessionBreakoutEntry, CRangeEdgeFade, candles, etc. | Mixed (Y for PBC/Crash/RangeBox min-RR) | PENDING | | Grouped sweep. **PBC kill-criterion PF<1.0/avg-R<0** (live, re-test). Crash min_rr=1.0 guardrail (≥1.33 amputates the only profitable short). RangeBox min_rr=1.0 validate before exceeding. Validate Crash+1.8 together vs same bear window. |

**Iter 6 kill/loop:** PBC PF<1.0 or avg-R<0 → disable PBC + reconcile STRATEGIES.md; CrashBreakout/rubber-band frequency drop → loop to stok. No-op claims byte-identical.

---

## Phase count summary
- Iter 1: 11 phases (1.4 bundles fixes 1.4+1.5)
- Iter 2: 9 phases (incl. the 2.4-GATE re-derivation phase)
- Iter 3: 10 phases (6 correctness 3a + 4 per-engine validation 3b)
- Iter 4: 7 phases (4.4 bundles 4.4+4.5; 4.7 bundles 4.8→4.9→4.10 in order)
- Iter 5: 8 phases (5.2 bundles 5.3+5.4; 5.4 bundles 5.6+5.7; 5.5 bundles 5.8+5.9; 5.8 bundles 5.12+5.13)
- Iter 6: 9 phases
- **Total: 54 phases** covering the 53 fixes (one extra phase = the standalone 2.4-GATE tier re-derivation).
