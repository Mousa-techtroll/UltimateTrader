# Phase 1.9 — CMacroBias price-based macro fallback (wire dead AnalyzePriceFallback)

## Phase
- **Phase id:** 1.9
- **Fix ids:** 1.10
- **Touches trading logic:** Y (ADDS a live scoring input *in principle*; but see "fallback activation" — the branch never executes on this production config, so trade-path effect is nil here)
- **depends_on (phases that must be COMMITTED first):** 1.8 (COMMITTED)
- **Binding stok param/option used:** existing ±2 thresholds (no new input); default DXY=`USDX` / VIX=`VIX` from production `.set`

## Does the fallback ACTIVATE on this config? — NO (decisive, evidence-based)
**The IMPORTANT FIRST STEP answer: the price-based fallback is WIRED but never EXECUTES on the production Vantage tester config, because BOTH DXY (`USDX`) and VIX ARE available to the EA in the Strategy Tester.**

- Production `.set`: `InpDXYSymbol=USDX`, `InpVIXSymbol=VIX` (decoded from `MQL5/Profiles/Tester/UltimateTrader.set`).
- Tester journal (`Tester/logs/20260626.log`, UTF-16LE decoded) shows, for **every** run including all 4 of this phase's runs:
  - `USDX: history synchronized from 2022.01.03 to 2026.06.25` and `VIX: history ... synchronized` (2022–2026) — the tester auto-downloads both secondary symbols.
  - `MacroBias initialized (DXY: true, VIX: true)` — i.e. `m_dxy_available && m_vix_available` are BOTH `true`.
  - `score forced to 0` / `NEUTRAL_FALLBACK` / `PRICE_FALLBACK` / `Price-based macro fallback` appear **ZERO times** in the entire journal.
- macro_score is therefore **fully LIVE** (DXY/VIX-driven), NOT pinned to 0. Sampled distributions confirm a full −3..+3 spread (e.g. 2024 run: `-1:2338 -2:2699 -3:771 0:62 1:1024 2:818 3:112`).

**Why the audit said "macro_score pinned to 0 on Vantage today":** that observation is about a LIVE account (where `USDX`/`VIX` may not be subscribed in Market Watch → `SymbolSelect` returns false → the old early-return forced 0). In the **Strategy Tester**, MT5 auto-downloads any referenced symbol's history, so `SymbolSelect("USDX"/"VIX")` succeeds and macro is live. The two environments differ. On the production *backtest* config, wiring the fallback changes nothing because the gating condition (`!m_dxy_available && !m_vix_available`) is never satisfied.

**Net:** This is the same byte-identical class as Phases 1.5/1.6/1.8 — a genuine wiring of a dead path whose gate is never satisfied on this config. On a LIVE account that lacks USDX/VIX, the fix WOULD activate and replace the pinned-0 with a −2..+2 price proxy; that scenario is not reachable in the tester and is correctly out of this phase's measurable scope (flagged to stok below).

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/Common/Enums.mqh` — `ENUM_MACRO_MODE` (226–228): added `MACRO_MODE_PRICE_FALLBACK` (3rd value; `MACRO_MODE_NEUTRAL_FALLBACK` retained as legacy, comment updated). Verified no `switch` requires the new case (only CDisplay renders it — updated below).
  - `Include/MarketAnalysis/CMacroBias.mqh` — `Update()` (~119–142): replaced the "force neutral" early-return body with a call to `AnalyzePriceFallback()`; sets `m_mode = MACRO_MODE_PRICE_FALLBACK`; derives bias via the **existing ±2 thresholds** (`>=2`→BULLISH, `<=-2`→BEARISH, else NEUTRAL). `AnalyzePriceFallback()` (~281–315): removed the stale "currently unreachable" banner; shifted EMA reads to the **CLOSED bar [1]** (D1 EMA200 copy `1→2`, read `[1]`; H4 fast/slow copy `2→3`, slope compares closed `[1]` vs prior closed `[2]`); short reads guarded (`<2`/`<3`); **price-vs-200 intentionally uses live `SYMBOL_BID`** (documented front-run, mirroring CCrashDetector's closed-baseline + live-trigger split from fix 1.8). Scoring math (±2/±1) unchanged.
  - `Include/Display/CDisplay.mqh` (104–113): macro-mode string now renders `PRICE_FALLBACK` distinctly (was a 2-way ternary that would have mislabeled it "REAL"). Display-only, off the trade path.
- **Diff summary:** Wire the previously-dead `AnalyzePriceFallback` into the both-symbols-unavailable branch; add `MACRO_MODE_PRICE_FALLBACK`; fallback EMAs now read closed bar `[1]/[2]` (BID for the price-vs-200 test). 3 files, +41/−18.
- **Logic-cut check:** NO logic cut. The legacy `MACRO_MODE_NEUTRAL_FALLBACK` enum value is retained; the only "removal" is the forced-`bias_score=0`/`BIAS_NEUTRAL` assignment, which is *replaced* by a real scoring path (a wiring-up, exactly what Governing Principle 1 mandates for advertised-but-dead config). No trading logic deleted.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 7621 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (exactly 38; none in Enums.mqh / CMacroBias.mqh / CDisplay.mqh — edited files are warning-clean).
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — `UltimateTrader.ex5` 891,534 bytes, 16:45, md5 `1d78fa96ff70bd203059a58fb345ff38` (≠ baseline `2303c69e8127e49a24fdbf805b8663ff` → binary changed, fix compiled in). A-build backed up to `/tmp/UT_phase1_9_baseline.ex5` before editing.

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, production `UltimateTrader.set` (`InpSymbolProfile=0`, `InpSignalSource=2` BOTH/no telegram CSV → pattern-engine only, `InpEnableMultiStrategy=false`, `InpEnablePullbackCont=true`, `InpEnableCrashDetector=true`, `InpRubberBandAPlusOnly=true`, `InpMaxPositions=5`, `InpMaxTradesPerDay=5`). Deposit 10000 / leverage 100. Same `.set` + `.ini` for A and B per year → any delta is purely the binary. Terminal `C:\Program Files\Vantage Markets MT5 Terminal\terminal64.exe` / nullkuhl / `725B72F25E46C780EF59F57016D58156`, WITHOUT `/portable` (proven prior-phase harness; committed `backtest_symbol.sh` path/ID are stale).
- **Mode:** Model=1 (1-min OHLC), same as Phases 1.1–1.8.
- **Windows:** 2024 (strong uptrend) and 2023 (correction/mixed regime).

| Build | Year | trades | WR | net $ | totR | avgR | L/S |
|---|---|---|---|---|---|---|---|
| A | 2024 | 140 | 46.43% | 1190.84 | 11.76 | 0.0840 | 109/31 |
| B | 2024 | 140 | 46.43% | 1190.84 | 11.76 | 0.0840 | 109/31 |
| A | 2023 | 129 | 35.66% | 714.29 | 3.40 | 0.0264 | 77/52 |
| B | 2023 | 129 | 35.66% | 714.29 | 3.40 | 0.0264 | 77/52 |

- **Delta + explanation:** ZERO delta. A==B **byte-identical** per year (raw UTF-16LE md5: 2024 `04d116972696f905c6ea5fa6701a7155`, 2023 `b11e07675cf4d577e27663ffde0aca19`; `cmp` clean each — and both md5s match the committed-HEAD baselines from Phases 1.6/1.8, confirming A reproduces HEAD). Paired EXIT-row diff (key EntryTime|Direction|Total_R|OriginalSL|Quality) = **0 differences** in both years. Mechanism: the wired fallback branch never executes (DXY+VIX both available), so the macro scoring input is unchanged.
- **Macro-score distribution delta (A vs B, from the tester journal):** IDENTICAL.
  - 2024: A and B both `−1:2338  −2:2699  −3:771  0:62  1:1024  2:818  3:112`.
  - 2023: A and B both `−1:2825  −2:1625  −3:214  0:347  1:422  2:1074  3:631`.
  - macro is fully live (−3..+3), not pinned to 0; A==B because the fallback never fires.
- **No-op / byte-identical claim:** YES — trade-for-trade, SL-for-SL, R-for-R IDENTICAL on XAUUSD+ H1 production across 2024 and 2023. The `.ex5` changed; the production trade path did not.

## stok review + verdict
- **Numbers handed to stok:** A==B byte-identical (md5-matching raw CSVs, cmp clean, 0 paired-row differences) across 2024+2023; identical live macro-score distribution A vs B; the journal proof that DXY+VIX are available in the tester and the fallback (old or new) never executes; the four stok-facing assessments below.
- **stok assessment:** COMPLETE — independent source-verification (NOT a re-read of the mt5 write-up). Verified at file:line:
  - **Fallback bound is genuinely ±2.** `CMacroBias::AnalyzePriceFallback` (303–311) returns exactly one of `{+2,+1,−1,−2}` (or `0` on handle failure). No path to ±3. CONFIRMED.
  - **All three hard LONG-vetoes require `<= -3`, unreachable by ±2 — verified at source, not trusted:** `CSignalValidator` 588/632 `macro_score <= -m_validation_macro_strong` with `m_validation_macro_strong=3` (LITERAL 7th positional arg at mq5:539, confirmed); `CPullbackContinuationEngine` 381 `macro_score <= -3`; `CRegimeAwareExit` 184 `macro_score <= -InpMacroOppositionThreshold` with `InpMacroOppositionThreshold=3` (Enums/input line 19). CONFIRMED — a −2 floor reaches none.
  - **CSetupEvaluator Factor-3 (188–194) cannot move a tier:** `abs>=3`→+3, but `abs∈{1,2}`→+1 AND `abs==0`→+1 (neutral fallback). So wiring the fallback awards the SAME +1 the pinned-0 already awards → net Factor-3 tier delta = 0. CONFIRMED (write-up correct).
  - **Macro source chain confirmed live on the trade path:** orchestrator 491 `m_context.GetMacroBiasScore()` → CMarketContext 469–472 → `CMacroBias.GetBiasScore()`; fed into `ValidateEntryConditions` at 633/693 (validator IS invoked on production trade path). So a live fallback WOULD flow into these branches — the dormancy is purely the tester's DXY/VIX availability, not a dead consumer.
  - **Enum addition clean:** `MACRO_MODE_PRICE_FALLBACK` appended LAST (no ordinal renumber of existing values), legacy `MACRO_MODE_NEUTRAL_FALLBACK` retained, only consumer is CDisplay 108–110 (if/else-if, all three handled). No exhaustive switch needs the case. NO logic cut.
- **stok ADDITIONAL FINDING (not in the mt5 write-up — the write-up enumerated only the three `<=-3` gates):** there is a FOURTH macro-conditioned LONG branch reachable by a ±2 fallback — `CSignalValidator` 441–474, the **bear-context** (price < 200 EMA) long path. A long is rejected at 472 unless an exception holds; two exceptions are macro-gated: 458 `IsMeanReversionPattern && macro_score >= 0` and 466 `macro_score >= 2`. A NEGATIVE fallback (−1/−2) would make both false, withholding an MR-long allow-exception. **Why this does NOT break "cannot veto healthy longs":** (a) the fallback returns −1/−2 ONLY when price is below the D1 200 EMA — exactly the bear context this branch governs; withholding a counter-trend MR-long exception when the price-macro itself reads bearish is CORRECT directional behavior, not a spurious veto. (b) A "healthy H1 pullback long" in gold's structural uptrend occurs with price ABOVE the 200 EMA, where the fallback returns +1/+2 and this entire bear-block is skipped (353 takes the bull branch — which only ever ADDS longs, e.g. 466 `>=2` ALLOWS, and PBC scoring 598 `macro>=1`→+1 reinforces longs). (c) Not reachable in the tester (DXY+VIX available). So this is a NARROW, DIRECTIONALLY-CORRECT, weak-side (counter-trend bear-context long) tightening on a live account — consistent with the repo's long-with-trend philosophy — NOT a healthy-long veto. It is, however, a genuine live-only behavioral delta vs. the pinned-0 baseline, which is why the forced-fallback experiment is recorded as a pre-live checklist item rather than ignored.
- **stok verdict:** **SIGN-OFF (accept as code-reviewed).** Byte-identity on the production tester config is correct and mechanism-explained (gate `!m_dxy_available && !m_vix_available` never satisfied — DXY/VIX auto-downloaded). The structural argument (bound ±2, cannot reach any `<=-3` veto, cannot move a Factor-3 tier) is SUFFICIENT to accept the code as reviewed, and I verified every load-bearing number independently at source. The fallback's bullish branch skews long with gold's uptrend by design and structurally cannot veto healthy (above-200-EMA) longs. The single live-only delta I found is the bear-context counter-trend MR-long tightening, which is directionally correct. **I do NOT require the forced-fallback A/B as a blocker to advance** (the live path is un-backtestable in the normal tester, is off the critical path of every downstream phase, and the bounded-±2 proof holds regardless) — but per the user's thorough/validate mandate I RECORD it as a mandatory PRE-LIVE checklist item (below), because there is a real un-measured live behavioral delta vs the pinned-0 baseline.

### stok PRE-LIVE checklist item (mandatory before any live deployment that lacks USDX/VIX subscription)
**Forced-fallback validation A/B (for mt5 to run, NOT now / NOT a Phase-1.9 blocker):**
- **Method:** force `m_dxy_available=false && m_vix_available=false` (rename `USDX`/`VIX` symbols in the tester, OR temporary debug override), so the gate at CMacroBias 125 is satisfied and `AnalyzePriceFallback` actually executes.
- **Symbol/window:** XAUUSD+ H1, run a bull year (2024) AND a bear/correction year (2023) so both fallback polarities fire.
- **Confirm (kill checks):** (1) tester journal shows `Price-based macro fallback ... => score {+2,+1,-1,-2}` lines and `MACRO_MODE_PRICE_FALLBACK`; (2) the fallback macro distribution skews LONG-positive in the uptrend (more +2/+1 than −1/−2 in 2024); (3) trade COUNT does not collapse vs the pinned-0/DXY-live baseline — specifically that above-200-EMA pullback longs are NOT suppressed; (4) the ONLY long deltas vs baseline are in the bear-context (price<200EMA) MR-long branch (CSignalValidator 458/466), and they REMOVE counter-trend longs (correct), do not remove trend-aligned longs. **REJECT-to-stok if:** trend-aligned (above-200) long count drops, OR any short count balloons, OR avg-R degrades materially vs the DXY-live baseline.

### stok-facing assessments (mt5-developer, evidence-based — for stok to verify)
1. **Does the fallback activate (macro 0 → live)?** NO on this config. Both USDX and VIX are available in the tester (`MacroBias initialized (DXY: true, VIX: true)` every run; full −3..+3 macro distribution; zero fallback log lines). macro_score is already live and is UNCHANGED by the fix (identical A/B distribution). The fix matters only on a live account lacking USDX/VIX subscription (not testable here).
2. **If it activated, would the new score skew LONG with gold's edge and not veto healthy H1 pullbacks?** It cannot veto pullback longs even if it did fire: the fallback is bounded to **−2..+2**, and every macro-consuming veto on the long path requires a STRONGER score — `CSignalValidator` blocks LONG only at `macro_score <= -m_validation_macro_strong` with `m_validation_macro_strong=3` (constructor arg, mq5:539; lines 588/632), `CPullbackContinuationEngine` blocks LONG only at `macro_score <= -3` (391/381), and `CRegimeAwareExit` force-closes a BUY only at `macro_score <= -InpMacroOppositionThreshold` with `InpMacroOppositionThreshold=3`. A −2 floor can reach NONE of these −3 gates. In `CSetupEvaluator` Factor-3 (189–194), `abs(score)∈{0,1,2}` all award the **same +1** point, so a ±2 fallback cannot move a tier (only `abs>=3` gives +3, unreachable). The fallback's bullish branch (`above_200 && H4-up → +2`) does skew long with the structural uptrend by design. Conclusion: structurally cannot veto healthy longs; can only mildly favor longs.
3. **Does it re-empower the (allegedly disabled) CPullbackContinuationEngine?** NO. (Note: PBC is actually ENABLED in this config — `InpEnablePullbackCont=true` — and trades 5 times in 2024 [3 LONG / 2 SHORT]; the "disabled" framing in the task is inaccurate for this `.set`.) Either way: the fallback never fires, so PBC sees identical macro inputs A vs B and produces the identical 5 trades (byte-identical CSV). And PBC's macro gates (`<=-3` LONG / `>=3` SHORT) are beyond the ±2 fallback range, so even if the fallback fired it could not change PBC's gating.
4. **Count not collapsed/ballooned?** PASS — 140/129 identical A==B in both years; tier/SL/R distributions identical (0 paired-row differences).

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** 1.9(=fix1.10) must not veto healthy longs → loop to stok before Iter 2. Also: count not collapsed/ballooned; new live scoring input must skew long with edge, not veto pullbacks; must not re-empower CPullbackContinuationEngine.
- **Result:** PASS — A==B byte-identical (no veto, no count change, no PBC re-empowerment). The veto-risk is structurally impossible on this config because (a) the fallback never executes here and (b) the ±2 fallback range cannot reach any −3 long-veto gate even if it did.

## mt5-developer sign-off
- Compile clean (0 errors, exactly 38 warnings = baseline, 0 new). Fix implements the validated spec exactly: AnalyzePriceFallback is WIRED into the both-symbols-unavailable branch, sets `MACRO_MODE_PRICE_FALLBACK` (added to Enums), derives bias via the existing ±2 thresholds; fallback EMA reads shifted to closed bar `[1]/[2]`; price-vs-200 uses live BID (documented). NO logic cut, no new input. The fallback does NOT activate on this production tester config (DXY `USDX` + VIX both available — journal-proven), so A/B is byte-identical (md5-matching raw CSVs, cmp clean, 0 paired-row differences, identical live macro-score distribution) across 2024 and 2023. file:line verified against current source. **SIGNED.**

## stok sign-off
- **SIGNED — SIGN-OFF (accept as code-reviewed).** Independent agent; every load-bearing number verified at source (file:line), not re-read from the mt5 write-up. See the stok review + verdict section above for the full source-trace.
  - **Q1 byte-identity:** CONFIRMED correct + mechanism-explained (fallback gate never satisfied in tester; DXY `USDX` + VIX auto-downloaded → `MacroBias initialized (DXY: true, VIX: true)`; A==B byte-identical 2024+2023 is the expected dormant-path outcome, same class as 1.5/1.6/1.8).
  - **Q2 structural argument SUFFICIENT to accept as code-reviewed:** YES. Bound is provably ±2 (CMacroBias 303–311); all three hard LONG-vetoes require `<=-3` (CSignalValidator `m_validation_macro_strong=3` literal mq5:539; PBC 381; CRegimeAwareExit `InpMacroOppositionThreshold=3` line 19) — none reachable; CSetupEvaluator Factor-3 (188–194) awards +1 for abs∈{0,1,2} identically so no tier can move. I do NOT require the forced-fallback A/B as a blocker (un-backtestable in normal tester; off the critical path of all downstream phases; bounded-±2 proof holds regardless). RECORDED as a mandatory PRE-LIVE checklist item (above) because I independently found one live-only behavioral delta vs pinned-0 (the bear-context counter-trend MR-long tightening at CSignalValidator 458/466) that this backtest does not measure — directionally correct, but un-measured.
  - **PBC framing correction (Q3) — LOGGED for downstream:** `CPullbackContinuationEngine` is ENABLED in this `.set` (`InpEnablePullbackCont=true`, trades 5× in 2024 [3L/2S]), NOT "disabled" as the plan/task framing stated. The re-empowerment check passes regardless (byte-identical A==B; and PBC's own gates are `<=-3`/`>=3` at lines 381/388, beyond the ±2 fallback range, so even a live fallback could not change PBC gating). **Downstream impact:** Iter-6 Phase 6.9 (PBC kill-criterion PF<1.0/avg-R<0, "live, re-test") and any Iter-3 PBC handling must treat PBC as an ENABLED, ALREADY-TRADING path on this config — the re-test is of a live engine, not a re-enable from disabled. Flagged so the PBC baseline is not mis-anchored.

## Decision
- **PASS (mt5 side)** — compile clean, A==B byte-identical, kill-criterion not tripped. Status → **mt5-QA-DONE**; stok PENDING. Do NOT commit, do NOT advance NEXT ACTION until the independent stok agent signs off.
- **PASS (stok side) — SIGN-OFF.** Both QA agents signed. Status → **QA-PASS → COMMITTED** (main session runs git). KILL-CRITERION (1.9 must not veto healthy longs) PASS — structurally impossible: ±2 fallback cannot reach any `<=-3` long-veto gate, the only live delta is a directionally-correct counter-trend bear-context MR-long tightening, and the path is dormant in the tester (A==B). Advance NEXT ACTION → Phase 1.10 (fix 1.11 — CRangeBoxDetector tautology, NOT-A-TRADING-PROBLEM, expect byte-identical). Forced-fallback A/B recorded as a mandatory PRE-LIVE checklist item (NOT a blocker). PBC enabled-not-disabled framing correction logged for downstream Iter-3/Iter-6 PBC handling.

## Commit
- **Hash:** (not committed — main session runs git; awaiting stok sign-off)
- **Message:** (pending)
