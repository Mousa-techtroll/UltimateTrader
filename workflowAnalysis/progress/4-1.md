# Phase 4.1 — Consecutive-loss scaler fed TOTAL trade PnL, not runner leg

## Phase
- **Phase id:** 4.1
- **Fix ids:** 4.1
- **Touches trading logic:** Y (loss-scaler streak → position sizing where trades bank partials then run red)
- **depends_on (phases COMMITTED first):** none (Iter 4 opener; independent of engine outcomes)
- **Binding stok param/option used:** n/a (FIX-SOUND mechanism alignment; no tunable param)

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/Core/CPositionCoordinator.mqh` — **Site 1** (single-close path; was 2598-2599, total at 2622): deleted the runner-only `m_quality_risk_strategy.RecordTradeResult(profit)`; hoisted `double total_trade_pnl = profit + m_positions[index].partial_realized_pnl;` to **:2602** (above the logger block); new `m_quality_risk_strategy.RecordTradeResult(total_trade_pnl)` at **:2604**; removed the now-duplicate `total_trade_pnl` declaration inside the `if(m_trade_logger != NULL)` block (it reuses the hoisted variable at :2627/:2629).
  - `Include/Core/CPositionCoordinator.mqh` — **Site 2** (CloseAllPositions loop; was 3020-3021, total at 3024): deleted the runner-only `RecordTradeResult(profit)`; moved the loss-scaler call **below** `double total_trade_pnl = profit + m_positions[i].partial_realized_pnl;` (:3025) so it fires `RecordTradeResult(total_trade_pnl)` at **:3035**. `runner_r_multiple` (:3027) PRESERVED unchanged — it feeds the per-mode `RecordModeResult(...)` calls (:3047/:3052/:3057), an intentionally separate (runner-leg) telemetry concern.
- **Diff summary:** The consecutive-loss scaler (`CQualityTierRiskStrategy::RecordTradeResult` → `AddWin`/`AddLoss`, `Include/RiskPlugins/CQualityTierRiskStrategy.mqh:267`) now receives `total_trade_pnl = runner profit + realized partials` at BOTH close sites instead of the runner-leg `profit`. A banked-then-red trade (`profit<0` but `total_trade_pnl>0`) now records a WIN (resets loss streak) instead of a LOSS (which incremented `m_consecutive_losses` → `ApplyLossScaling` de-risk). `RemovePosition` is after both sites, so `m_positions[...]` is still valid.
- **Logic-cut check:** NO logic cut. `RecordTradeResult` preserved at both sites (re-fed, not removed); `runner_r_multiple`→`RecordModeResult` untouched; `RecordStrategyTrade`/`RecordClosedTradeR` (already total-based) unchanged. Only the loss-scaler argument changed runner→total.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 10153 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (exactly 38; none in CPositionCoordinator.mqh)
- Error 106 / include issues: none
- `.ex5` regenerated: yes — 908312 bytes, 2026-06-27T01:54:44 (B build md5 `6d77c09e…`; A=committed HEAD `.ex5` md5 `e0cfbb3a…`)

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, full 2019.01.01–2026.06.27, `[TesterInputs]` production defaults (395 params; `InpEnableMultiStrategy=false`, `InpSignalSource=2`/BOTH, `InpMaxTradesPerDay=5`, `InpDisableAutoKill=true`, `InpEnableLossScaling=true` L1@2→x0.75 / L2@4→x0.50). Deposit 10000 USD, leverage 100. A and B inis byte-identical apart from `Report=` name; only the `.ex5` differs.
- **Mode:** Model=1 (1-minute OHLC). 2 terminal runs total (A then B), within the ≤2 bound.
- **A (committed HEAD `.ex5`):** Net **$39,005.22** | PF **1.36** | Total Trades **2200** | Balance maxDD **$6,042.52 (10.98%)** | Equity maxDD 13.74% | Expected Payoff $17.73 | per-trade avg Total_R **+0.143** (from Stats CSV, 1050 closes).
- **B (post-fix `.ex5`):** Net **$24,234.27** | PF **1.31** | Total Trades **2021** | Balance maxDD **$3,455.85 (9.17%)** | Equity maxDD 11.15% | Expected Payoff $11.99 | per-trade avg Total_R **+0.127** (966 closes).
- **Delta:** Net **−$14,770.95 (−37.9%)**; PF −0.05; trades −179; Balance maxDD **−1.81pp (better)**; avg-R −0.016. Divergence concentrates in 2025 (A $22,410 vs B $15,301 = ~half the gap) and 2024 (A $5,658 vs B $2,255); 2020 flips slightly negative in B.
- **Mechanism (attribution is clean — only the scaler input changed):** `ApplyLossScaling` (CQualityTierRiskStrategy:71-91) is **one-sided** (only de-risks on loss streaks ≥2/≥4; win streak does NOT up-size). Re-feeding it the TOTAL PnL changes the WIN/LOSS classification of ~69 trades/run, which changes the loss-streak STATE at every subsequent trade → changes the lot multiplier on every later trade → changes equity → changes which later signals clear the exposure/equity gates → a path-dependent cascade that diverges the whole trade population (1050 vs 966 closes) and net by 38%. Avg LotSize fell A 0.255 → B 0.213, i.e. B sizes SMALLER on average despite fewer "loss" classifications — confirming the effect is path-dependent streak-reordering, not a simple size-up. The change is the correct, intended mechanism but it is NOT net-neutral on this gold dataset.
- **No-op / byte-identical claim:** N/A — this is a deliberate trading-logic change; not expected byte-identical.

## Net-outcome streak validation (stok kill-criterion) — PASS (validation), but see net regression
- **Per-run, from the deterministic Stats CSV (`Total_PnL` = `Runner_PnL` + `PartialRealized_PnL`):**
  - **Run A:** 1050 closes. Classified by `Runner_PnL` (A's actual scaler input): **647 LOSS / 403 WIN**. Classified by `Total_PnL` (correct): 580 LOSS / 470 WIN. **Gap = 69 banked-then-red NET WINNERS** that A wrongly recorded as losses (de-risking the book after a net win).
  - **Run B:** 966 closes. Scaler now classifies by `Total_PnL`: **540 LOSS / 426 WIN** (correct). 55 banked-then-red trades in B's (different) set now correctly count as wins.
- **Concrete banked-then-red example — Ticket 18 (exit 2019.01.31 18:43):** Runner_PnL **−$5.12**, PartialRealized **+$7.52**, **Total_PnL +$2.40 (NET WINNER)**. The EA Stats `Result` column already reads WIN (it is total-based), but the SCALER differed:
  - **Run A log:** `Position closed: Ticket 18 | PnL: $-5.12 | Exit: TP0_HIT` immediately followed by `CQualityTierRisk: LOSS recorded. Streak: 1L` (scaler saw −5.12 → AddLoss → de-risk).
  - **Run B log:** same close, scaler now sees +2.40 → `CQualityTierRisk: WIN recorded. Streak: 3W` (AddWin, loss streak reset).
- **Verdict on validation:** the streak logs now reflect NET trade outcomes — a banked-then-red NET WINNER records as a WIN in B and did NOT de-risk; it was a LOSS in A. **Validation criterion PASS.**

## Kill-criteria check
- **Phase kill-criterion (Iter-4):** "net P&L material drop without a mechanism → loop to stok." Net dropped −37.9% ($39,005 → $24,234). The drop HAS a clear mechanism (one-sided loss-scaler streak reordering cascading through sizing/exposure into a divergent trade population), so it is "explained," but it is a **material adverse net regression** and maxDD is the only metric that improved.
- **Result:** **net = WORSE (−37.9%), maxDD = BETTER (−1.81pp Balance / −2.59pp Equity), avg-R ≈ flat (−0.016), PF ≈ flat (−0.05).** NEUTRAL-OR-BETTER = NO on net; YES on maxDD/risk. This is a REJECT-LOOP / stok-ruling situation per RESUME.md (d), NOT a clean PASS.

## mt5-developer sign-off
- Compile clean (0 err / 0 new warn); mechanism implements the validated fix exactly (total fed at both sites, runner-only calls removed, per-mode runner telemetry preserved, RemovePosition after); file:line verified against current source; NO logic cut. **mt5-developer: SIGNED (implementation correct).** Note for stok: the implementation is correct AND the net effect is materially negative on the gold dataset — this is a trading-impact decision, not an implementation defect.

## stok sign-off
- **SIGNED — verdict: ACCEPT-WITH-FOLLOWUP** (binding). stok independently verified both close sites in source, confirmed `ApplyLossScaling` is one-sided, and confirmed `RecordTradeResult` records WIN on `total>0` / LOSS on `total<0` / no-touch on exact zero (benign).
  - **(1) Fix is unit-correct, unambiguously.** A consecutive-loss scaler must count *trade* outcomes (net P&L), not a *leg*. Feeding the runner's −$5.12 to a loss counter while the trade netted +$2.40 (Ticket 18) is a unit error. The fix aligns the scaler with EC v3 (which already records on total/R via `RecordClosedTradeR`) — before the fix, two risk controllers in the same book disagreed about what a "win" is. That was the actual defect; now resolved. No logic cut; `runner_r_multiple`→`RecordModeResult` correctly preserved.
  - **(2) The −37.9% is an EXPOSURE-LEVEL effect on a FLAT per-R edge** (PF 1.36→1.31, avg-R +0.143→+0.127 ≈ flat; avg lot 0.255→0.213 = smaller book; maxDD 10.98%→9.17% = better). Buggy A de-risked *more often* (647 vs 580 loss-classifications) yet ran a *larger* book — non-monotone, the tell that A's bigger book is a path-dependent streak-ordering of a one-sided scaler fed mis-classified losses, concentrated in the 2024–2025 trend. That extra $14.7k is **curve-fit to one historical path**, not a reproducible sizing policy.
  - **(3) Mandate alignment:** keeping the bug to preserve P&L is exactly the no-curve-fit violation the user forbade — and B is the better book on risk-adjusted terms anyway (better maxDD on flat per-R edge). Reject-to-keep-P&L would optimize the one number (net on fixed history) explicitly disallowed.
  - **Verdict: ACCEPT-WITH-FOLLOWUP** — commit 4.1 as-is now; the exposure recovery is a separate, named Iteration-6 optimization (NOT a reason to loop).

## Iteration-6 follow-up (logged by stok; flagged optimization / curve-fit risk)
- **Loss-scaler config re-derivation.** The fix corrected *what the scaler sees* but exposed that the one-sided scaler now leaves the book under-deployed vs its measured edge. Re-derive, priority order:
  1. **Banked-then-red streak policy** (logic-shaped, evaluate first): currently `total>0`→`AddWin`→full reset of `m_consecutive_losses`. Consider an intermediate "don't-increment-but-don't-fully-reset" rule (runner thesis did fail). Current full-reset is the defensible default; question is whether data prefers the softer rule.
  2. **`InpLossLevel1Threshold`/`InpLossLevel2Threshold`** (2/4): implicitly calibrated against the buggy inflated loss count (647); against the correct count (580) de-risk may now trigger too early → re-derive upward.
  3. **`InpLossLevel1Reduction`/`InpLossLevel2Reduction`** (x0.75/x0.50): depth of de-risk; touch only if 1–2 don't close the gap.
- **Mandatory guardrail:** re-derive on an OUT-OF-SAMPLE split (fit 2019–2022, confirm 2023–2026); require PF and avg-R flat-or-better; A/B against **B** (shipped baseline), NEVER buggy A. Kill-criterion: if re-derived config can't raise net while holding avg-R ≥ +0.12 and maxDD ≤ B's 9.17% OOS, keep B's config as shipped.

## Decision
- **DONE — both agents signed (mt5 implementation-correct; stok ACCEPT-WITH-FOLLOWUP, binding). Committed source-only on `feat/multi-strategy`.** Fix mechanism correct; net-outcome streak validation PASS; the material net regression is an accepted exposure-level/curve-fit-to-bug effect, with the exposure recovery deferred to the Iteration-6 follow-up above. NEXT ACTION → Phase 4.2.

## Artifacts
- A report: `…/725B…/p41_A.htm` | B report: `…/725B…/p41_B.htm`
- A/B inis: `…/725B…/backtest_p41_A.ini`, `backtest_p41_B.ini` (identical apart from Report=)
- A Stats snapshot: `/tmp/p41/A_Stats.csv` (+utf8) | B Stats: `/tmp/p41/B_Stats.csv` (current Common/Files)
- A binary: `/tmp/p41/UT_A_HEAD.ex5` (md5 e0cfbb3a) | B binary: `/tmp/p41/UT_B_postfix.ex5` (md5 6d77c09e)
- Banked-then-red set: `/tmp/p41/banked_red.json` (69 trades, run A)

## Commit
- **Hash:** (filled at commit — source-only, `.ex5` intentionally uncommitted)
