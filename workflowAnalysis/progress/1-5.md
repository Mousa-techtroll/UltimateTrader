# Phase 1.5 — CVolatilityRegimeManager ctor handle defensive-init (fix 1.6)

## Phase
- **Phase id:** 1.5
- **Fix ids:** 1.6
- **Touches trading logic:** N (NOT-A-TRADING-PROBLEM — pure defensive init; acceptance = byte-identical backtest)
- **depends_on (phases that must be COMMITTED first):** 1.4 (COMMITTED)
- **Binding stok param/option used:** n/a (no-op)

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/MarketAnalysis/CVolatilityRegimeManager.mqh` — constructor `CVolatilityRegimeManager()` (begins line 91). Added three defensive-init lines at the TOP of the ctor body (immediately after the opening `{`, before `m_enabled = true;`): `m_handle_atr_h1 = INVALID_HANDLE; m_handle_atr_h4 = INVALID_HANDLE; m_handle_adx_h4 = INVALID_HANDLE;`
- **Diff summary:** The 3 indicator-handle members (declared lines 71-73) were uninitialized in the ctor. They are still created in `Initialize()` (lines 178-180: `iATR` H1, `iATR` H4, `iADX` H4) and released in the destructor (lines 131-133). The defensive init guarantees that if the destructor runs before `Initialize()` (object constructed then destroyed without init), the `!= INVALID_HANDLE` guards in the destructor see a sentinel value instead of uninitialized garbage, preventing a spurious `IndicatorRelease()` of a random handle id. On the production path `Initialize()` always runs and overwrites all three, so behavior is unchanged.
- **Logic-cut check:** NO trading logic deleted or short-circuited. Handles still created in `Initialize()` exactly as before. No toggle/registration change. Pure additive defensive init.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 7440 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (38 == 38; same pre-existing warnings, all in CEnhancedTradeExecutor.mqh)
- Error 106 / include issues: none
- `.ex5` regenerated: yes — `/mnt/c/Trading/UltimateTrader/UltimateTrader.ex5` 889682 bytes, built 15:54:30 (md5 7ac80a9f6f121f01036b296f8c1220ea = `/tmp/UT_B_postfix.ex5`). A-build (pre-fix, current source) `/tmp/UT_A_prefix.ex5` md5 ec2f96ff..., also `Result: 0 errors, 38 warnings`.

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, 2024 (one year sufficient for no-op identity check), deposit 10000 USD, leverage 100, ShutdownTerminal=1, data dir user nullkuhl / terminal 725B72F25E46C780EF59F57016D58156
- **Mode:** Model=1 (1-min OHLC), same as Phases 1.1/1.2/1.3/1.4. Identical `UltimateTrader.set` + identical `backtest_p15.ini` for both runs — only the binary differs. Terminal: Vantage Markets MT5 Terminal / nullkuhl / 725B72F25E46C780EF59F57016D58156.
- **A (current source, pre-fix — freshly rebuilt):** `/tmp/UT_A_prefix.ex5` → CSV `/tmp/A_2024.csv` — Trades=140, WR=46.43%, Net=$1190.84, TotalR=11.76, AvgR=0.084000, LONG=109 SHORT=31. (Reproduces committed-HEAD post-1.4 numbers exactly — validates the A reference + parser.)
- **B (this phase, post-fix):** `/tmp/UT_B_postfix.ex5` → CSV `/tmp/B_2024.csv` — Trades=140, WR=46.43%, Net=$1190.84, TotalR=11.76, AvgR=0.084000, LONG=109 SHORT=31.
- **Delta + explanation:** ZERO delta. A defensive ctor init that `Initialize()` overwrites on the production path cannot change any trade — exactly as predicted by the mechanism.
- **No-op / byte-identical claim:** **A == B byte-identical.** Raw UTF-16LE CSVs share md5 `04d116972696f905c6ea5fa6701a7155` (`cmp -s` clean); decoded `diff` clean across all 281 rows (140 ENTRY + 140 EXIT); every EXIT row's EntryTime/Direction/EntryPrice/SL/PnL$/PnL_R identical → trade-for-trade, SL-for-SL, R-for-R IDENTICAL. YES.

## stok review + verdict
- **Numbers handed to stok:** A==B byte-identical (md5 `04d116972696f905c6ea5fa6701a7155`); 2024 XAUUSD+ H1 140t/$1190.84/11.76R unchanged pre/post-fix. Awaiting stok quick no-op sign-off (REPORT handed to main session for stok).
- **stok assessment:** No-op confirmed. A==B byte-identical (raw md5 04d116972696f905c6ea5fa6701a7155, decoded diff clean 281/281 rows, trade-for-trade/SL-for-SL/R-for-R identical, 140t/$1190.84/0.084avgR both builds). Defensive ctor init to INVALID_HANDLE is inert on the production path because Initialize() (L178-180) always overwrites all three handles before use. Zero trading impact — nothing to adjudicate.
- **stok verdict:** SIGN-OFF.

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** no-op claim → byte-identical production backtest = PASS; any divergence ⇒ no-op claim wrong, STOP and reconcile.
- **Result:** PASS — A==B byte-identical (raw-byte + decoded + trade-for-trade/R-for-R). No divergence; no-op claim holds.

## mt5-developer sign-off
- Compile clean (0 errors, 0 new warnings); mechanism implements fix 1.6 (3 handle members defensive-init to INVALID_HANDLE at top of ctor; handles still created in Initialize() L178-180, released in dtor L131-133); file:line verified against current source; NO logic cut; A==B byte-identical confirms no-op on production path. — SIGNED

## stok sign-off
- No-op (defensive ctor handle-init, inert on production path — Initialize() overwrites all 3); A==B byte-identical (md5 04d116972696f905c6ea5fa6701a7155, 281/281 rows, R-for-R identical); zero trading impact. — SIGNED

## Decision
- **PASS — BOTH AGENTS SIGNED (mt5 + stok), byte-identical no-op → COMMITTED.** Do NOT commit (per task instruction — main session runs git).

## Commit
- **Hash:** n/a (Do NOT commit — per task instruction)
- **Message:** n/a
