# Phase 6.5 — Dead orphan exec/exit/trailing classes: REPAIR-AND-DEPRECATE (do NOT delete)

> Iteration 6, fix 6.5 (Infra-8 subset). REPAIR-AND-DEPRECATE only — nothing LIVE changed; all repairs are to compiled-out / dead code. Expected BYTE-IDENTICAL. Bounded to ≤2 backtest runs (1 per A/B leg = 2 runs).

## Phase
- **Phase id:** 6.5
- **Fix id:** 6.5 (Infra-8 subset — dead orphan exec/exit/trailing classes carry latent FOK/magic0/unguarded-Sleep bugs; large dead infra surface)
- **Touches trading logic:** N (fence is compiled out; the 3 dead classes are not instantiated/invoked on the live path; the removed includes were transitively dead)
- **depends_on:** Phase 6.3+6.4+6.6 COMMITTED (HEAD e465fd6) — yes
- **Binding stok param/option used:** n/a (repair-and-deprecate; no trading-logic change)
- **Branch / HEAD:** feat/multi-strategy, A=committed HEAD `e465fd6`

## Grep evidence (BEFORE touching anything)

### (1) ExecuteTrade(TradeData&) is UNINVOKED (dead orphan)
`grep -rn "ExecuteTrade(" Include/ UltimateTrader.mq5` → exactly ONE hit: the definition itself at `CEnhancedTradeExecutor.mqh:2247 bool ExecuteTrade(TradeData &data)`. No caller anywhere. The LIVE execution path is `CTradeOrchestrator::ExecuteSignal()` (`CTradeOrchestrator.mqh:198`) → `m_executor.ExecuteTradeWithRetries(...)` (`:688`); `ExecuteTradeWithRetries`/`ExecuteTradeAttempt` are SEPARATE live methods (not `ExecuteTrade(TradeData&)`). The concurrent-execution cluster `m_executing` / `m_executionStartTime` / `CheckExecutionFlag()` is referenced ONLY from inside `ExecuteTrade(TradeData&)` (the method body, its ctor inits at 1968-1969, and `CheckExecutionFlag` which `ExecuteTrade` calls at 2291) → the whole cluster is reachable only from the dead method.

### (2) The two includes at mq5:29-30 are TRANSITIVELY DEAD → removed
The includes are `HealthMonitor.mqh` + `CHealthBasedRiskAdjuster.mqh` (NOT the exec/exit/trailing classes — those are never included at all; see below).
- `grep -rn "HealthMonitor.mqh\|CHealthBasedRiskAdjuster.mqh"` (whole repo): only `UltimateTrader.mq5:29-30` and the dead `CComponentManager.mqh:15-16` (+ each file's own header comment + the internal `CHealthBasedRiskAdjuster→HealthMonitor` include).
- `grep -rn "CHealthMonitor\b\|CHealthBasedRiskAdjuster\b"` excluding the 2 infra files + the 2 ComponentManagement files → **EMPTY** (no live symbol consumer).
- `grep -rn "CComponentManager.mqh\|IComponentManager.mqh"` excluding `Include/ComponentManagement/` → **EMPTY** ⇒ the entire ComponentManagement subtree (the only consumer of the two includes) is itself never `#include`d anywhere live.
- The only near-miss hits are coincidental name matches, NOT consumers: `ENUM_HEALTH_STATUS` is a DIFFERENT enum defined in `Include/Common/Enums.mqh:138` (consumed live via `CMarketContext.GetSystemHealth()` at `CMarketStateManager.mqh:127`); `RecordTradeResult` at `CPositionCoordinator.mqh:2706` is `m_quality_risk_strategy.RecordTradeResult(...)` (the RISK STRATEGY's method), NOT `CHealthBasedRiskAdjuster`'s. Removing the two includes leaves `ENUM_HEALTH_STATUS` (separate include) and all live paths intact.
- **Conclusion:** removable. Compile after removal = `0 errors` (no error 106) → confirmed nothing transitively needed them.

### (3) The three "dead orphan" classes are never #included live
- `grep -rn "CSmartTrailingStrategy.mqh"` → only the file's own header comment. Not `#include`d anywhere.
- `grep -rn "CStandardExitStrategy.mqh"` → only the file's own header comment. Not `#include`d anywhere.
- `grep -rn "CEnhancedPositionManager.mqh"` → only `CComponentManager.mqh:19` (which is itself dead per (2)). Not reachable live.
So all three compile OUT of the live build entirely (never included) — editing them cannot affect the binary's behavior; repairs are latent-only.

## What changed (verified file:line against current source)

### A. FENCE — CEnhancedTradeExecutor.mqh (4 #ifdef ULTIMATETRADER_ENABLE_LEGACY_EXECUTE … #endif regions)
Macro `ULTIMATETRADER_ENABLE_LEGACY_EXECUTE` is **not defined anywhere** → all 4 regions compiled OUT by default. The cluster is self-contained (grep (1)), so fencing it is purely structural:
- **:85-93** — member decls `m_executing` + `m_executionStartTime` fenced (with a why-comment). `m_lastSendTime` (Fix 6.2) left OUTSIDE the fence (it is live).
- **:101-118** — `CheckExecutionFlag()` fenced (reads the fenced members; only called by the dead ExecuteTrade).
- **:1978-1982** — the two ctor inits `m_executing=false; m_executionStartTime=0;` fenced (must move with the members or they'd reference undeclared members when the macro is undefined). `m_lastSendTime=0;` left outside.
- **:2257-2528** — the entire `bool ExecuteTrade(TradeData &data)` method fenced, with a `DEPRECATED / INACTIVE` banner explaining the orphan status + the live path. `#endif` is labelled `// ULTIMATETRADER_ENABLE_LEGACY_EXECUTE` and sits immediately before the class-closing `};`.
- Fence balance verified: 4 `#ifdef` ↔ 4 matching `#endif`. No FOK / Sleep / magic-0 inside this method (the latent bugs live in the 3 orphan classes, not here) — fencing is structural only; `data.MagicNumber` (the passed-in magic) is already validated `<= 0` inside.

### B. CSmartTrailingStrategy.mqh (DEAD — TrailingPlugins) — DEPRECATED banner + latent repairs
- **:6** (header) — `DEPRECATED / INACTIVE — not in the live build (Fix 6.5)` banner (live trailing = CPositionCoordinator + registered TrailingPlugins; not wired anywhere; do not re-activate without A/B + stok).
- **Initialize() :283** — `SetExpertMagicNumber(0)` → `SetExpertMagicNumber(InpMagicNumber)` (EA magic param, in scope after UltimateTrader_Inputs.mqh).
- **Initialize() :285** — `SetTypeFilling(ORDER_FILLING_FOK)` → `SetTypeFillingBySymbol(m_symbolName)` (`m_symbolName` inherited from CTradeStrategy base; `SetTypeFillingBySymbol` exists in this terminal's Trade.mqh:94/1432; derives FOK/IOC/RETURN from SYMBOL_FILLING_MODE — matches the live convention of NOT hardcoding FOK). No Sleep in this class.

### C. CStandardExitStrategy.mqh (DEAD — ExitPlugins) — DEPRECATED banner + latent repairs
- **:6** (header) — `DEPRECATED / INACTIVE` banner (live exits = CRegimeAwareExit/CDailyLossHaltExit/CMaxAgeExit/CWeekendCloseExit via CPositionCoordinator).
- **:151, :204** — `Sleep(200 * (attempt + 1));` → `if(!MQLInfoInteger(MQL_TESTER)) Sleep(...);` (both close-retry loops).
- **Initialize() :286** — `SetExpertMagicNumber(0)` → `SetExpertMagicNumber(InpMagicNumber)`.
- **Initialize() :288** — `SetTypeFilling(ORDER_FILLING_FOK)` → `SetTypeFillingBySymbol(m_symbolName)`.

### D. CEnhancedPositionManager.mqh (DEAD — Execution; only consumer is dead CComponentManager) — DEPRECATED banner + Sleep guards
- **:5** (header, matches the file's existing `Enhanced EA Team` legacy header — EDIT-matched, not rewritten) — `DEPRECATED / INACTIVE` banner + explicit "do NOT re-activate the health/concurrency stack into the trade gate."
- **:395, :1362, :1769** — `Sleep(10);` → `if(!MQLInfoInteger(MQL_TESTER)) Sleep(10);` (the three P2-07 single-retry lock-backoff sites).
- **:897** — `Sleep(100 * (attempt + 1));` → guarded.
- **:1950** — `Sleep(delay);` → guarded.
- **:580-581** — pre-existing `if(!MQLInfoInteger(MQL_TESTER)) Sleep(delay);` was ALREADY guarded; left as-is. No FOK/magic-0 in this class. (6/6 Sleep sites now guarded.)

### E. UltimateTrader.mq5 :29-30 — removed the two transitively-dead includes
Replaced the two `#include` lines with a Fix-6.5 comment recording the removal + the grep rationale + the "do NOT re-activate the health stack" note. `ENUM_HEALTH_STATUS` (live) is unaffected (defined in Enums.mqh, included separately).

### Health stack NOT re-activated
Per the plan + RESUME: HealthMonitor/Recovery/Timeout/Concurrency left dead+deprecated, NOT wired into the trade gate (latent Infra-2..5 bugs could silently scale/halt trading). The only actions on that surface are: removing the two dead includes from mq5, and adding Sleep guards + a DEPRECATED banner to the dead CEnhancedPositionManager.

### Logic-cut check
NO trading logic deleted/disabled/re-routed. The fence compiles out a never-called method; the 3 orphan classes are never included live; the removed includes were dead. All repairs are latent-only (correct IF ever re-enabled).

## Compile result (verbatim)
- **B (post-fix, working tree):** `Result: 0 errors, 16 warnings, 8814 ms elapsed, cpu='X64 Regular'`
- **A (committed HEAD e465fd6, via git stash):** `Result: 0 errors, 38 warnings, 9010 ms elapsed, cpu='X64 Regular'`
- **Errors:** 0 both legs. No error 106 (the include removal broke no transitive dependency).
- **Warnings:** 38 (A, baseline) → **16 (B)** — a DROP of 22 NEW-ZERO. Removing the two dead includes (HealthMonitor.mqh + CHealthBasedRiskAdjuster.mqh) eliminated the 22 pre-existing type-conversion/etc warnings those two files emitted. **0 NEW warnings introduced** — all 16 remaining are pre-existing in surviving files (UltimateTrader.mq5 ×2, Logger.mqh ×5, CVolatilityBreakoutEntry.mqh ×4, Series.mqh ×1, CEnhancedTradeExecutor.mqh ×4). The deprecated/fenced dead code added no warnings; the fenced region is compiled out and the orphan classes are not included live so their bodies aren't compiled into the EA. (As the plan anticipated: deprecating/fencing dead code DROPS warnings.)

## 3b binary-binding assertions (load-path md5 == fresh-build md5)
- **B (post-fix):** FRESH(repo `UltimateTrader.ex5`)=`c2c36a6d65ebc4b5b5141bc864bf388e` == LOAD(`Experts/UltimateTrader_p65B.ex5`)=`c2c36a6d65ebc4b5b5141bc864bf388e` → **PASS**.
- **A (committed HEAD e465fd6):** FRESH(repo)=`da095bc3f5a6bb3000536f5faffc0985` == LOAD(`Experts/UltimateTrader_p65A.ex5`)=`da095bc3f5a6bb3000536f5faffc0985` → **PASS**.
- **Distinct A vs B:** A `da095bc3…` ≠ B `c2c36a6d…` (genuinely two different binaries — B is missing the 2 dead includes + carries the fence/repairs; confirmed also by 38 vs 16 warnings). Not a vacuous "compiled once, ran twice" A/B.
- **Method:** B compiled first from the working tree + bound; `git stash push` exposed committed HEAD e465fd6 for A's source (verified: includes 29-30 PRESENT, fence count 0); A compiled + bound; `git stash pop` restored B source (verified: includes removed, fence count 4, SetTypeFillingBySymbol present in both repaired classes). Per RESUME 3b: data-dir load-path `.ex5` deleted before each compile; each leg has its own uniquely-named `_p65X.ex5`.

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, Model=1 (1-min OHLC), full **2019.01.01–2026.06.27**. `.ini` cloned VERBATIM from the proven `backtest_p63A.ini` (the prior-phase A leg that reproduced the baseline); ONLY `Expert=` and `Report=` changed. A vs B `[TesterInputs]` diff = EMPTY (identical 396-param production defaults: `InpEnableMultiStrategy=false`, `InpSignalSource=2`, `InpSymbolProfile=0`). Deposit 10000 USD / leverage 100 / ShutdownTerminal=1. State file relocated so BOTH legs start on clean identical recovery state. 1 run each = **2 runs (bound met)**.
- **A (committed HEAD e465fd6):** REPRODUCES the binding baseline EXACTLY by md5 — Stats `3b13ea8feded6f2faec6b5a157ab05db`, TradeEvents `2222e2f955ea0eef99b50f7b4c52ebff`, Candidates `77b07fb1fc2982aaed4899b823bb0e26`, Risk `930cce92cb33e31a90a6be9a66e3946a` ⇒ **Net $17,455.34 / PF 1.28 / 1742 trades / Eq-DD 12.64%** (= recorded post-6.1 binding baseline by md5 identity; stale-binary risk closed).
- **B (post-fix):** 4/4 EA-CSV md5 = Stats `3b13ea8f…`, TradeEvents `2222e2f9…`, Candidates `77b07fb1…`, Risk `930cce92…` — identical to A.

### Byte-identity proof (4 CSV md5 + cmp)
| CSV | A md5 | B md5 | md5 | cmp |
|---|---|---|---|---|
| Stats | 3b13ea8f… | 3b13ea8f… | EQUAL | CLEAN |
| TradeEvents | 2222e2f9… | 2222e2f9… | EQUAL | CLEAN |
| Candidates | 77b07fb1… | 77b07fb1… | EQUAL | CLEAN |
| Risk | 930cce92… | 930cce92… | EQUAL | CLEAN |

**A == B BYTE-IDENTICAL on ALL 4 EA CSVs (md5-EQUAL AND `cmp` byte-for-byte CLEAN), despite genuinely distinct binaries (`da095bc3…` vs `c2c36a6d…`).** No STOP condition tripped.

### Byte-identity MECHANISM (WHY)
1. **Fenced ExecuteTrade(TradeData&) + m_executing cluster:** `ULTIMATETRADER_ENABLE_LEGACY_EXECUTE` undefined → the entire method + members + CheckExecutionFlag + ctor inits are compiled OUT of B. The live path (ExecuteSignal → ExecuteTradeWithRetries) never referenced them, so removing them from the compilation changes no live behavior.
2. **CSmartTrailingStrategy / CStandardExitStrategy:** never `#include`d by any live header (grep (3)) → their bodies are not compiled into the EA at all → the FOK/magic-0/Sleep repairs are invisible to the build. (They are latent-only: correct IF re-enabled.)
3. **CEnhancedPositionManager:** only consumer is the dead CComponentManager, which is itself never included (grep (2)) → not compiled into the EA → Sleep guards + banner invisible to the build.
4. **Removed includes (HealthMonitor.mqh + CHealthBasedRiskAdjuster.mqh):** transitively dead (grep (2)) — no live symbol used them; removing them only drops the 22 warnings their files emitted. The binary shrinks/loses those compilation units but no live code path is touched.

Net: the binaries differ (different warnings, different md5) but every line that actually executes at runtime is identical → CSVs byte-identical. This is the expected REPAIR-AND-DEPRECATE outcome ("something LIVE was touched" would have perturbed a CSV byte — it didn't).

## Kill-criteria check
- **Phase kill-criterion:** A == B must be BYTE-IDENTICAL on all 4 CSVs (the fix touches only fenced/dead code + dead includes). If NOT byte-identical → STOP (something touched WAS live), report what diverged for stok, do NOT force.
- **Result:** **PASS** — A == B byte-identical (md5-equal + cmp-clean) on all 4 CSVs; A reproduced the binding baseline exactly. No STOP condition tripped; nothing live diverged.

## mt5-developer sign-off
- Compile clean: A `0 errors, 38 warnings`; B `0 errors, 16 warnings` (0 NEW warnings; 22 dropped via the dead-include removal). No error 106 → include removal broke no transitive dependency. Fence balanced (4 ↔ 4). 3b enforced: A `da095bc3…` / B `c2c36a6d…` (distinct; load==fresh asserted both legs). A reproduces the binding baseline 4/4 CSV md5. All file:line verified against current source (lines drifted from the plan: ExecuteTrade was at 2247 not 2103; CSmartTrailing FOK/magic at the now-283/285; CStandardExit at now-151/204/286/288). Grep proves ExecuteTrade(TradeData&) uninvoked + the 2 includes transitively dead + the 3 orphan classes never included. NO logic cut; all repairs latent-only. A==B byte-identity proven (md5+cmp all 4) with the mechanism documented. — **SIGNED (mt5-developer)**

## stok sign-off
- **SIGNED — verdict: ACCEPT-WITH-FOLLOWUP** (binding). stok independently re-verified the load-bearing greps (not on trust):
  - **(1) Truly non-live:** ExecuteTrade(TradeData&) = def @2270 + 3 Fix-6.5 comment lines, ZERO callers (live path = ExecuteSignal:198→ExecuteTradeWithRetries:688, distinct); m_executing cluster reachable only from inside the dead method. The 2 removed includes' ONLY consumer is the dead CComponentManager (never included outside its own subtree → grep empty); the 3 orphan classes never compiled into the EA. ENUM_HEALTH_STATUS/RecordTradeResult hits confirmed coincidental name-matches. Byte-identical A/B + distinct binaries + 22-warning drop = the EXACT expected signature of "removed dead weight, touched nothing live."
  - **(2) Include removal right** (only consumer itself never included → nothing to deprecate-in-place; 0-err/no-106 proves no transitive dep broke). **22-warning drop BENIGN** (those warnings originated IN the 2 removed dead headers — type-conversion noise; 16 remaining all pre-existing in surviving files). **→ baseline-warning gate is now 16** for subsequent phases (holding 38 would mask 22 warnings' worth of regressions).
  - **(3) Fence correct + SURGICAL:** verified it catches ONLY the 2 dead members (85-93) + their 2 ctor inits (1978-1982), leaving the LIVE Fix-6.2 m_lastSendTime OUTSIDE the fence (:94/:1983); fence balance 4 #ifdef ↔ 4 #endif. Latent repairs sound IF re-enabled: SetTypeFillingBySymbol EXISTS in this terminal's Trade.mqh:94/1432 (would compile; SYMBOL_FILLING_MODE-derived = right for XAUUSD+ where hardcoded FOK can be rejected); magic 0→InpMagicNumber matches the live SetExpertMagicNumber(InpMagicNumber) (mq5:862); all 6 Sleep sites guarded (the :581 "unguarded" was a same-line-grep false positive, guarded by :580).
  - **(4) Health stack correctly LEFT DEAD+DEPRECATED, NOT re-wired** — re-activating an unaudited health→risk path into the live gate is a silent-killswitch/silent-position-multiplier risk; do NOT revive without a dedicated phase + full A/B + a stok review of every scaling/halt branch.
  - **Verdict: ACCEPT-WITH-FOLLOWUP.**

## Follow-ups (logged by stok; non-blocking, NOT a loop)
- **FU-1 (process):** the baseline-warning gate is now **16** (was 38; the 22 dropped were dead-header noise) — subsequent phases gate on 0 errors / no new warnings above 16. (RESUME.md updated this phase.)
- **FU-2 (cosmetic, deferred to the 6.9 comment sweep):** label the other three `#endif`s with `// ULTIMATETRADER_ENABLE_LEGACY_EXECUTE` for future-reader clarity. Not required.
- **Carry-forward (NOT this phase):** if the health stack (HealthMonitor/Recovery/Timeout/Concurrency) is EVER revived, Infra-2..5 must be repaired + A/B'd + stok-reviewed BEFORE any wiring into the trade gate.

## Decision
- **DONE — both agents signed (mt5 implementation-correct + byte-identical proven; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified greps + fence surgicality + latent-repair soundness). Committed source-only on `feat/multi-strategy`.** Dead orphan exec/exit/trailing repaired-and-deprecated (ExecuteTrade fenced in #ifdef; magic/FOK/Sleep latent repairs; DEPRECATED banners; 2 transitively-dead health includes removed → 38→16 warnings, 0 new). Health stack left dead, NOT re-wired. A/B byte-identical (3b distinct binaries, A reproduced HEAD). Baseline-warning gate now 16. NEXT ACTION → Phase 6.7+6.8 (doc-only).

## Commit
- **Hash:** `<filled at commit>` — source-only (UltimateTrader.mq5 + CEnhancedTradeExecutor.mqh + CSmartTrailingStrategy.mqh + CStandardExitStrategy.mqh + CEnhancedPositionManager.mqh + RESUME.md (warn-gate→16) + 6-5.md + 00-STATUS.md; `.ex5` intentionally uncommitted). Branch `feat/multi-strategy`.
