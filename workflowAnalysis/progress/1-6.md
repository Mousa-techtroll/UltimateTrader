# Phase 1.6 — CVolatilityRegimeManager ATR-history realized-count average

## Phase
- **Phase id:** 1.6
- **Fix ids:** 1.7 (CVolatilityRegimeManager.mqh UpdateATRHistory — realized-count divide)
- **Touches trading logic:** Y (classified FIX-SOUND — risk lane: SL/size during warmup only)
- **depends_on (phases that must be COMMITTED first):** 1.1, 1.2, 1.3, 1.4, 1.5 (all COMMITTED)
- **Binding stok param/option used:** n/a — FIX-SOUND. No tunable param introduced. Same realized-count idiom as Phase 1.4's fix 1.5 (CMomentumFilter `Include/MarketAnalysis/CMomentumFilter.mqh:245-262`).

## What changed
- **File touched (verified file:line against current source):**
  - `Include/MarketAnalysis/CVolatilityRegimeManager.mqh` `UpdateATRHistory()` — was lines 381-396 (plan cited ~377-392; verified actual at 381-396 pre-edit). Edit landed at the body lines ~386-396.
- **Fix 1.7 — divide the ATR-history average by the REALIZED CopyBuffer count, not the fixed `m_history_size`:**
  - Captured `int got = CopyBuffer(m_handle_atr_h1, 0, 0, m_history_size, atr_buffer);` (was an inline `CopyBuffer(...) > 0` test in the `if`).
  - Loop bound `i < m_history_size` -> `i < got` (no longer touches zero-padded / un-realized `atr_buffer` cells on a short read).
  - Divisor `sum / m_history_size` -> `sum / got` (CMomentumFilter realized-count idiom).
  - Guard `if(got > 0)` preserved (was `if(CopyBuffer(...) > 0)`); no behavior on a zero read.
- **OOB note:** `m_atr_history[]` is `ArrayResize`d to `m_history_size` (120) in `Initialize()` (line 126); `got <= m_history_size` always, so writing `m_atr_history[i]` for `i < got` is in-bounds — the destination was never the OOB risk, the SOURCE (`atr_buffer`) zero-padded cells + the fixed divisor were the bug.
- **Diff summary:** 1 file, +9 / -5 (incl. a 4-line explanatory comment). The ONLY behavioral change is the divisor: fixed `m_history_size` (120) -> realized `got`. They differ ONLY when `got < 120` (warmup, <120 H1 bars of ATR available); steady-state `got == 120` is identical to the old code.
- **No `[0]`->`[1]` shift applied:** the plan for fix 1.7 specifies ONLY the realized-count robustness change; it does NOT call for a forming-bar->closed-bar shift here (distinct from fixes 1.4/1.8/1.9). The `start_pos=0` read is kept exactly as-is. Strictly per-plan, no scope creep.
- **Logic-cut check:** NO trading logic deleted. `ClassifyRegime()`, `GetSLMultiplier()`/`AdjustSLForVolatility()`, the regime thresholds, and the consumers are all UNCHANGED. This is an averaging-denominator robustness fix only.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 7422 ms elapsed, cpu='X64 Regular'
```
- Errors: **0** | New warnings vs 38-baseline: **0** (count byte-equal to the established 38-warning baseline; the 38 are the known pre-existing warnings in UltimateTrader.mq5(780/781), Logger.mqh, RecoveryManager.mqh, HealthMonitor.mqh, CVolatilityBreakoutEntry.mqh, Series.mqh, CEnhancedTradeExecutor.mqh). No warning emitted from CVolatilityRegimeManager.mqh (only the informational `including ...CVolatilityRegimeManager.mqh` line).
- Error 106 / include issues: none.
- `.ex5` regenerated: yes — `UltimateTrader.ex5` 890538 bytes, 2026-06-26 16:01. A-baseline build saved at `/tmp/UT_phase1_6_baseline.ex5` (889682 bytes, md5 `7ac80a9f6f121f01036b296f8c1220ea`); B build saved at `/tmp/UT_phase1_6_B.ex5` (890538 bytes, md5 `a097f054a76dd9a4f5fa6b483ceb5b9a`). A/B md5 differ as expected (binary changed).

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, production `UltimateTrader.set` (`InpSymbolProfile=0` XAUUSD, `InpSignalSource=2` BOTH with telegram CSV absent -> pattern-engine only, `InpEnableMultiStrategy=false`, `InpMaxPositions=5`, `InpMaxTradesPerDay=5`, `InpEmergencyDisable=false`). Deposit 10000 USD / leverage 100. A and B used the IDENTICAL ini (`backtest_p16.ini`) + the same production `.set` -> any delta is purely the 1.7 binary. Terminal: `C:\Program Files\Vantage Markets MT5 Terminal\terminal64.exe` / nullkuhl / `725B72F25E46C780EF59F57016D58156`. (NOTE: ran WITHOUT `/portable` — the committed `backtest_symbol.sh`/`backtest_runner.sh` paths AND the `/portable` flag are stale/wrong for this data dir; matched the proven prior-phase harness.)
- **Mode:** Model=1 (1-min OHLC), same as Phases 1.1-1.5. A-baseline reproduces the committed-HEAD / post-1.5 numbers EXACTLY (140t / $1190.84 / 11.76R) AND its raw CSV md5 = `04d116972696f905c6ea5fa6701a7155` = the exact post-1.5 baseline md5 recorded in 00-STATUS Phase-1.5 -> confirms A IS committed HEAD and confirms the parser/harness.
- **Stats source:** `Common/Files/UltTrader_Stats_XAUUSD+_20240101_0000.csv` (FILE_COMMON), UTF-16LE, decoded with iconv. col1=RowType (EXIT rows = closed trades), col5=Direction, col7=Regime, col24=RiskPct, col25=LotSize, col45=PnL_Money, col46=PnL_R. CSVs preserved: `/tmp/p16_A_2024_raw.csv`, `/tmp/p16_B_2024_raw.csv`.

**2024 (2024.01.01 -> 2025.01.01):**
| Build | Trades | WR% | Net $ | Total-R | Avg-R | LONG | SHORT |
|---|---|---|---|---|---|---|---|
| A (committed HEAD, baseline) | 140 | 46.43 | 1190.84 | 11.76 | 0.0840 | 109 | 31 |
| B (post-1.7) | 140 | 46.43 | 1190.84 | 11.76 | 0.0840 | 109 | 31 |
| Delta | 0 | 0.00 | 0.00 | 0.00 | 0.0000 | 0 | 0 |

- **Delta + explanation:** **ZERO. A == B BYTE-IDENTICAL.** Raw UTF-16LE CSVs share md5 `04d116972696f905c6ea5fa6701a7155` (`cmp` clean), decoded files `diff`-identical across all 281 rows. This is exactly the EXPECTED steady-state-identical result: the fix changes the ATR-history average divisor from fixed 120 to the realized `got` count, which differs ONLY during the first <120 H1 bars of warmup (where `CopyBuffer` returns <120 ATR bars). In a full-year 2024 run the warmup window is exhausted long before any consumed `UpdateATRHistory()`, so `got == 120` everywhere it matters and the average is bit-for-bit identical. No warmup-bounded delta even surfaced (the 2024 window's pre-trade warmup absorbed it entirely).
- **No-op / byte-identical claim:** YES — trade-for-trade, SL-for-SL, R-for-R IDENTICAL (raw CSV md5 match + cmp clean + decoded diff clean). The classified-Y "risk-lane" change is steady-state-identical AND, on this 2024 full-year production config, surfaced ZERO observable warmup delta. Any conceivable delta is strictly warmup-bounded (first <120 H1 bars) and mechanism-consistent (removes the old zero-fill low-bias of `m_atr_average`, which the old code only ever exhibited during warmup); it is NOT a steady-state edge change — proven by the byte-identical full-year run.

## stok review + verdict
- **Numbers handed to stok:** A==B byte-identical (raw md5 `04d116972696f905c6ea5fa6701a7155`, cmp clean, decoded diff clean, 140t/$1190.84/11.76R/0.0840avgR identical on both); the mechanism (divisor fixed-120 -> realized `got`, differs only during <120-bar warmup); the steady-state-identical classification; the no-`[0]`->`[1]`-shift confirmation (per-plan, no scope creep).
- **stok assessment:** (pending stok sign-off — handed for the trading-impact confirmation that a byte-identical full-year A/B is acceptable for this FIX-SOUND risk-lane item, and that the warmup-bounded conservative-bias removal is correct).
- **stok verdict:** (pending)

## Kill-criteria check
- **Phase kill-criterion (plan/00-STATUS):** FIX-SOUND risk lane (SL/size during warmup); old zero-fill bias was conservative; steady-state (post-warmup) must be identical. No edge amputation; count must not collapse/balloon.
- **Result:** **PASS (mt5-QA)** — A==B byte-identical full-year 2024 (raw md5 match, cmp clean, decoded diff clean): steady-state IS provably identical, count not collapsed/ballooned (140->140, identical 109L/31S split, identical WR), no edge change. The only behavioral difference (warmup divisor) did not surface at all on the full-year run. No logic cut. No tunable param introduced.

## mt5-developer sign-off
- Compile clean (0 errors, 0 new warnings vs 38-baseline). Fix implements the validated spec exactly: capture `got=CopyBuffer(...)`, sum/divide by realized `got` (CMomentumFilter idiom, same as 1.5). file:line verified against current source (UpdateATRHistory body 386-396). Short-read no longer touches zero-padded cells; destination `m_atr_history[]` in-bounds (sized 120 >= got). No `[0]`->`[1]` shift (correctly out of fix-1.7 scope). No logic cut. A/B XAUUSD+ H1 2024 BYTE-IDENTICAL (raw CSV md5 `04d116972696f905c6ea5fa6701a7155`, cmp clean) — proves steady-state identity; any residual delta is strictly warmup-bounded and mechanism-consistent. **SIGNED.**

## stok sign-off
- **SIGN-OFF.** FIX-SOUND risk-lane item, accepted. A==B byte-identical full-year 2024 (raw md5 `04d116972696f905c6ea5fa6701a7155`, cmp clean, decoded diff clean across all 281 rows; 140t/$1190.84/0.0840avgR both builds) proves zero steady-state trading impact — no edge change, count not collapsed/ballooned (140→140, 109L/31S split + WR identical). The only behavioral change (ATR-history average divisor fixed-120 → realized `got`) is mathematically warmup-bounded (first <120 H1 bars where CopyBuffer<120) and did not surface on the full-year run; it removes the old conservative zero-fill low-bias on `m_atr_average`, which is strictly a robustness improvement and the correct risk behavior. No tunable param, no logic cut, no `[0]`→`[1]` scope creep (correctly out of fix-1.7 scope). stok ACCEPT → QA-PASS.

## Decision
- **mt5-QA-DONE — mt5-developer SIGNED.** Awaiting stok sign-off to reach QA-PASS. mt5 side: compile 0-err/0-new-warn + realized-count fix per validated spec + file:line verified + short-read safe + no logic cut + A==B byte-identical full-year (steady-state identity proven; warmup-bounded-only). Do NOT commit (main session runs git; per instruction this phase is NOT committed). On stok ACCEPT -> QA-PASS -> COMMITTED -> advance NEXT ACTION to Phase 1.7 (fix 1.8, CCrashDetector).

## Commit
- **Hash:** (not committed per instruction — main session runs git)
- **Message (proposed):** `Phase 1.6: CVolatilityRegimeManager ATR-history realized-count average (fix 1.7)`
