# Phase 5.6 + 5.7 — Multi-strategy engine register-gating (BUNDLE)

> Iteration 5. Bundled because both edit the engine `RegisterEntryPlugin` / router-registration block in `UltimateTrader.mq5` (lines ~734–757) and both are DEFAULT BYTE-IDENTICAL (production `InpEnableMultiStrategy=false` → the whole engine block is unreachable → the appended gates change nothing on the prod path).

## Phase
- **Phase id:** 5.6 + 5.7 (bundle)
- **Fix ids:** 5.6 (engines not gated on `register_patterns`) + 5.7 (orphan input `InpEnableEngineExpansion` never referenced)
- **Touches trading logic:** N on the production path (default byte-identical). The gates only become live when `InpEnableMultiStrategy=true` — there they correctly restore the FILE-only contract (5.6) and honor the per-engine enable input (5.7).
- **depends_on (COMMITTED first):** 5.5 (HEAD `ed488df`). No cross-phase ordering needed.
- **Binding stok param/option used:** n/a (no-op on prod; latent toggle-gating only).

## What changed
- **Files touched (verified file:line against current source ed488df):**
  - `UltimateTrader.mq5:734` — FIX 5.6: `RegisterEntryPlugin(g_trendContEngine, InpEnableEngineTrend)` → `... InpEnableEngineTrend && register_patterns`.
  - `UltimateTrader.mq5:740` — FIX 5.6: `RegisterEntryPlugin(g_reversalSweepEngine, InpEnableEngineReversal)` → `... InpEnableEngineReversal && register_patterns`.
  - `UltimateTrader.mq5:746` — FIX 5.6: `RegisterEntryPlugin(g_engineRange, InpEnableEngineRange)` → `... InpEnableEngineRange && register_patterns`.
  - `UltimateTrader.mq5:~753` — FIX 5.7: engine-4 router-wiring guard `if(g_expansionEngine != NULL)` → `if(g_expansionEngine != NULL && InpEnableEngineExpansion)`. Comment updated to record the 5.7 gate and that the legacy standalone registration (`~687`) is left UNCHANGED (the reuse-as-component path).
  - `UltimateTrader_Inputs.mqh:503` — FIX 5.7: comment for `InpEnableEngineExpansion` updated to "gates router registration when InpEnableMultiStrategy (Phase 5.7); legacy standalone reg unchanged".
- **Diff summary:**
  - 5.6 mirrors how the legacy pattern plugins are gated (`UltimateTrader.mq5:582–655`, all `&& register_patterns`). Engines are pattern-signal strategies; in a FILE-only run (`InpSignalSource=SIGNAL_SOURCE_FILE`, `register_patterns=false`) they must NOT register/fire. The three `g_regimeRouter.RegisterEngine(...)` calls are deliberately left unconditional (mirrors engines 1–3's existing structure — only the plugin enabled-flag is gated, exactly as the legacy plugins gate only their `RegisterEntryPlugin` enabled-arg).
  - 5.7 gives engine-4 (the reused `g_expansionEngine`) the same per-engine enable gate engines 1–3 have. Engine 4 has no `RegisterEntryPlugin` line in this block (it is the legacy plugin reused as a router component), so the orphan input is wired onto the `RegisterEngine` guard. The LEGACY standalone registration at `~687` (`if(InpEnableExpansionEngine && register_patterns) ... RegisterEntryPlugin(g_expansionEngine, true)`) is untouched.
- **Logic-cut check:** NO trading logic deleted. Both changes are registration-level gates appended to existing conditions. No engine logic, no SL/TP math, no scorer path altered.

## Engine block is behind `if(InpEnableMultiStrategy)` — CONFIRMED
`UltimateTrader.mq5:718` opens `if(InpEnableMultiStrategy)`; the scorer, regime router, all three gated `RegisterEntryPlugin` calls (734/740/746), and the engine-4 router-wiring (~753) all sit inside that block (verified by reading 718–758). Production default `InpEnableMultiStrategy=false` (Inputs:499) ⇒ the block never executes ⇒ neither appended gate is ever evaluated on the production path ⇒ default byte-identical by construction.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 8596 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (exactly the 38-warning baseline; none reference the edited lines mq5:734/740/746/753 or Inputs:503).
- Error 106 / include issues: none (no new includes added).
- `.ex5` regenerated: yes — 912386 bytes, 2026-06-27 06:52:59 (build B = both fixes).

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, Model=1, full 2019.01.01–2026.06.27, `[TesterInputs]` production defaults (`InpEnableMultiStrategy=false`; `InpSignalSource=2`=BOTH ⇒ `register_patterns=true`; `InpEnableExpansionEngine=true` legacy; `InpEnableEngineExpansion=false`). Same 396-param set for A and B (5.6/5.7 add NO new inputs).
- **Mode:** 1-min OHLC (Model=1).
- **Binaries:** A = clean HEAD ed488df (stashed the 2 source edits, recompiled → `UltimateTrader_p56A.ex5`); B = both fixes (`UltimateTrader_p56B.ex5`). **Both .ex5 share md5 `9dbe9052e8b164f438026bc97e87a3c7` — the compiler produced byte-IDENTICAL bytecode** (the source text differs only in gating conditions on unreachable code inside `if(InpEnableMultiStrategy=false)`). This alone guarantees A==B before any run.
- **A (HEAD ed488df, p56A — run 06:55):** Net **$25,570.97** / PF **1.31** / **2011** trades / Sharpe 2.51 / Eq-DD Max 11.26% ($4,513.87) / Bal-DD Max 9.28% ($3,637.66) / Recovery 5.66. EA Stats: 961 ENTRY / 961 EXIT rows; all 961 entries `SIGNAL_SOURCE_PATTERN` (no telegram_signals.csv present → 0 file signals).
- **B (both fixes, p56B — run 07:01):** Net **$25,570.97** / PF **1.31** / **2011** trades / Sharpe 2.51 / Eq-DD Max 11.26% ($4,513.87) — **identical to A in every headline field.**
- **Byte-identity proof (4 EA output CSVs):** ALL FOUR md5-EQUAL **and** cmp-CLEAN —
  - `Stats.csv` A=B `a555618b7ef18c34aa502bafad854998` (cmp clean)
  - `TradeEvents.csv` A=B `5dbe2666ce66e839e8293ddf461bc0c3` (cmp clean)
  - `Candidates.csv` A=B `9e6c17d1772fe5e9b0f321189c920c8d` (cmp clean)
  - `Risk.csv` A=B `fa4b951e56aa9fdac1d6fb64941491c2` (cmp clean)
  - Snapshots: `/tmp/p56/A/*.csv`, `/tmp/p56/B/*.csv`. Tester reports: `<datadir>/p56_A.htm`, `p56_B.htm`. Inis: `<datadir>/backtest_p56A.ini`, `backtest_p56B.ini` (diff = Expert/Report lines ONLY; both 396-param HEAD prod-default set, `InpEnableMultiStrategy=false`).
- **Byte-identity mechanism (PROVEN 3 ways):** (1) Source: `InpEnableMultiStrategy=false` ⇒ the engine block at mq5:718 is never entered ⇒ engines are never `new`'d/registered/router-wired ⇒ the appended `&& register_patterns` (5.6) and `&& InpEnableEngineExpansion` (5.7) gates sit on UNREACHABLE code on the prod path. (Secondary: even if reached, prod `register_patterns=true` (InpSignalSource=2=BOTH) would make the 5.6 gate itself a no-op too.) (2) Bytecode: the two `.ex5` share md5 — the change is behavior-neutral at the bytecode level. (3) Runtime: all 4 CSVs + tester headline byte-identical A==B.

### ⚠️ DATA/STATE DRIFT NOTE (NOT caused by 5.6/5.7 — affects A and B equally; flagged for stok + next session)
- The directive's sub-expectation "A reproduces committed HEAD ($17,018.83 / PF 1.28 / 1742t)" did **NOT** hold. Clean HEAD ed488df run TODAY at 06:55 produced **$25,570.97 / PF 1.31 / 2011t**, whereas the SAME source recorded in `5-5.md` at 06:38/06:40 produced **$16,685.59 (pre-5.5) / $17,018.83 (5.5) at 1742t**. p55_A.htm/p55_B.htm/p56_A.htm tester reports (all on disk) confirm the three headlines.
- This is **environment/data drift between sessions, NOT a code change**: (a) the working tree has NO tracked-source edits beyond the two fix files (`git diff --stat HEAD` = only UltimateTrader.mq5 + UltimateTrader_Inputs.mqh); (b) A and B (same drifted environment, adjacent runs) are byte-identical → the fix is provably a no-op regardless of the absolute level; (c) the divergence is large (+269 trades, +$8.5k) and uniform-ish across years (A entries/yr: 2019:93 2020:125 2021:119 2022:150 2023:130 2024:146 2025:144 2026:54), consistent with a different tester data/state baseline rather than a logic edit.
- **Most-likely cause (UNCONFIRMED — would need a run to bisect, out of the ≤2 budget):** the EA reads/writes `UltimateTrader_State.bin` (+ yearly `.bak`s) in `Common\Files` (persisted-position / EC-v3 recovery state) and the tester re-generated ticks today (XAUUSD+ 10,588,926 ticks / 44,267 H1 bars). A stale/differently-populated state file reloaded on init, and/or a tester history re-sync, would shift the headline identically for A and B. **The binding baseline number should be RE-ESTABLISHED on a clean state file at the start of the next phase** (delete/relocate `UltimateTrader_State*.bin/.bak` from Common\Files, re-run HEAD) before any phase makes a baseline-relative P&L claim. This does NOT affect the 5.6/5.7 byte-identity verdict.

## Gate correctness (by reasoning — NOT a run, within ≤2-run budget)
- 5.6: with `InpEnableMultiStrategy=true` + `register_patterns=false` (FILE-only, `InpSignalSource=SIGNAL_SOURCE_FILE`), the three engines would now NOT register — restoring the documented contract that engines are pattern-signal strategies.
- 5.7: with `InpEnableMultiStrategy=true` + `InpEnableEngineExpansion=false`, engine-4 would now NOT be router-wired (previously it always was when multi-strategy was on, regardless of the input) — honoring the orphan input. The legacy standalone expansion path (gated on `InpEnableExpansionEngine`) is independent and unchanged.

## stok review + verdict
- **Numbers handed to stok:** A==B byte-identity (4 EA CSVs md5-equal + cmp-clean; identical tester headline $25,570.97 / PF 1.31 / 2011t); identical `.ex5` md5; the 3-way byte-identity mechanism; AND the DATA/STATE DRIFT NOTE above (HEAD's absolute headline drifted from the 5-5-recorded $17,018.83/1742t to $25,570.97/2011t between sessions — affects A and B equally, NOT caused by 5.6/5.7).
- **stok assessment:** PENDING (independent agent).
- **stok verdict:** PENDING.

## Kill-criteria check
- **Phase kill-criterion (plan Iter-5 QA (e) + 5.6/5.7 rows):** default byte-identical (A==B on the production XAUUSD-only 2019–2026 path).
- **Result:** **PASS** — A==B is byte-identical (all 4 CSVs md5-equal + cmp-clean + identical tester headline; identical `.ex5` bytecode). The fix changes nothing on the production path. (The separate HEAD-vs-recorded-baseline drift is an environment/state-file artifact, equal for A and B, and does not bear on the byte-identity kill criterion.)

## Gate-correctness verdict (by reasoning, no run spent)
- 5.6 (`&& register_patterns` on the 3 engines): mirrors the legacy pattern-plugin gating (mq5:582–655). With `InpEnableMultiStrategy=true` + `InpSignalSource=SIGNAL_SOURCE_FILE` (FILE-only, `register_patterns=false`), the three engines would now NOT register — restoring the documented contract that engines are pattern-signal strategies. SOUND.
- 5.7 (`&& InpEnableEngineExpansion` on the engine-4 router-wiring): mirrors engines 1–3's per-engine enable gate. With `InpEnableMultiStrategy=true` + `InpEnableEngineExpansion=false`, engine-4 would now NOT be router-wired (was previously always wired when multi-strategy was on). Honors the previously-orphan input. The LEGACY standalone expansion registration (mq5:687, gated on `InpEnableExpansionEngine`) is independent and UNCHANGED. SOUND.

## ⚠️ CORRECTION (post-run, by orchestrator) — THIS PHASE'S A/B RAN A STALE BINARY; in-phase byte-identity was VACUOUS
- The in-phase A/B (above) BOTH ran `9dbe9052` — a **7-commit-stale PRE-4.6 binary** (c858e6c / 4.4+4.5 era), NOT a fresh ed488df build. So "A==B byte-identical" was **vacuous** (the same stale binary run twice), and the "$25,570.97/2011t + data/state drift" interpretation was **WRONG** — that figure is the pre-4.6 chain headline, the signature of a stale binary, not environment drift. Root cause: the compiler writes the repo `.ex5` but the tester loads the data-dir `Experts/` copy, and the fresh build never reached the load path + no md5 assertion gated the run. Full forensics: `progress/REBASELINE-5-6.md`. RESUME.md step **3b** now mandates a `md5(load-path)==md5(fresh-build)` assertion every run to prevent recurrence.
- **AUTHORITATIVE VALIDATION (REBASELINE-5-6.md):** a FRESH compile of the working tree (which CONTAINS the 5.6+5.7 gates) → `.ex5` md5 `f1e251fa` (≠ stale `9dbe9052`) → clean run = **Net $17,018.83 / PF 1.28 / 1742t / Eq-DD 12.89%**, reproducing committed-HEAD ed488df **byte-for-byte (3/3 EA CSV md5: Stats `08640614…` / TradeEvents `18aedaca…` / Risk `7b3db7d1…`)**. Since HEAD ed488df does NOT contain the 5.6+5.7 edits and the working-tree (WITH them) reproduces it exactly, **5.6+5.7 are PROVEN byte-identical no-ops on the production path** — now against the CORRECT baseline. The chain is sound; no history/state drift (44,267 bars / 10.59M ticks, unchanged).

## mt5-developer sign-off
- Compile clean (`0 errors, 38 warnings`, 0 new); both fixes implemented exactly per spec at verified file:line (mq5:734/740/746/756, Inputs:503); engine block confirmed behind `if(InpEnableMultiStrategy)` (mq5:718); NO logic cut. **Byte-identity: the in-phase A/B was VACUOUS (stale `9dbe9052`); the AUTHORITATIVE proof is the REBASELINE-5-6 fresh-build run (`f1e251fa` = $17,018.83/1742t = committed-HEAD byte-for-byte, working-tree-WITH-gates == HEAD-without).** Gate correctness verified by reasoning (gates on code unreachable when InpEnableMultiStrategy=false). — **SIGNED (on the corrected validation).**

## stok sign-off
- **SIGNED — verdict: ACCEPT-WITH-FOLLOWUP** (binding). stok verified the gating LOGIC against live source (not prose):
  - **(1) 5.6 CORRECT at the mechanism level.** `RegisterEntryPlugin(plugin,false)` early-returns (mq5:366) → never appends to g_entryPlugins[]; the orchestrator only fires plugins from that array (:917). So in a FILE-only run (register_patterns=false) the gate genuinely removes the 3 engines from the firing set, mirroring every legacy pattern plugin (:582-585). The engines are PATTERN-signal generators that must NOT self-generate in a FILE-only run (contract violation) — fix restores it. **No risk of wrong suppression:** the ungated regime-router path (RegisterEngine) only calls SetActivationWeight→m_isEnabled (CMajorStrategyEngine:124-128), which is a SUPPRESSOR-ONLY flag (makes an already-array-registered engine early-return at weight 0) — it CANNOT add an engine to the array or force a fire. The two paths AND together; the router can't override the 5.6 gate.
  - **(2) 5.7 CORRECT.** Engine-4 is the REUSED g_expansionEngine (no RegisterEntryPlugin in the multi-strategy block — its array-registration lives in the legacy path :687-692, gated on InpEnableExpansionEngine && register_patterns). The orphan input InpEnableEngineExpansion correctly attaches to the only engine-4-specific wiring in the block: the RegisterEngine guard (:756). Leaving the legacy standalone registration ungated is right (orthogonal reuse-as-component path). NO double-registration (block calls only SetScorer+RegisterEngine). The engine-1-3 (array-gated) vs engine-4 (router-gated) asymmetry is STRUCTURAL and correct (the only coherent mapping for a reused instance), not a defect.
  - **(3) SHIP NOW:** zero cost on the prod path (byte-identical), benefit = gates are correct WHEN multi-strategy is first switched on (vs a latent FILE-only pattern-leak / orphan-input tripwire discovered live).
  - **Verdict: ACCEPT-WITH-FOLLOWUP.**

## Follow-ups (logged by stok; non-blocking, NOT a loop)
- **FU-1 (doc, low):** document the deliberate engine-1-3 (array-gated) vs engine-4 (router-gated) asymmetry where the multi-strategy engine set is described (STRATEGIES.md / architecture guide) so the reuse-as-component design isn't later "fixed" into a double-registration bug. The mq5:749-755 comment already captures intent; mirror one line when multi-strategy is documented for real.
- **FU-2 (verify-on-enable, low):** when multi-strategy is eventually turned on for real validation, add to that phase's checklist: (a) a FILE-only run with InpEnableMultiStrategy=true confirms 0 engine-sourced signals (5.6 live); (b) InpEnableEngineExpansion=false confirms engine-4 absent from the router roster while still present via legacy (5.7 live). Latent/unexercised until then — acceptable for a default-off toggle.

## Decision
- **mt5-QA-DONE (on corrected validation); stok PENDING.** Byte-identity proven authoritatively via REBASELINE-5-6 (fresh `f1e251fa` working-tree run = committed-HEAD $17,018.83/1742t byte-for-byte). The in-phase A/B was a stale-binary artifact (now corrected; RESUME.md 3b prevents recurrence). Awaiting stok's GATING-LOGIC sign-off, then commit (5.6+5.7 source + RESUME.md 3b procedure-fix + REBASELINE-5-6.md). Do NOT self-sign stok.

## Commit
- **Hash:** `<filled at commit>` (source-only: UltimateTrader.mq5 + UltimateTrader_Inputs.mqh + RESUME.md (3b) + REBASELINE-5-6.md + 5-6_5-7.md + 00-STATUS.md; `.ex5` intentionally uncommitted). Branch `feat/multi-strategy`.

## Artifacts
- Source edits: `UltimateTrader.mq5` (734/740/746/756), `UltimateTrader_Inputs.mqh` (503).
- Binaries: `<datadir>/MQL5/Experts/UltimateTrader_p56A.ex5` == `UltimateTrader_p56B.ex5` (md5 `9dbe9052e8b164f438026bc97e87a3c7`).
- Inis: `<datadir>/backtest_p56A.ini`, `backtest_p56B.ini` (396-param HEAD prod default; diff = Expert/Report only).
- CSV snapshots: `/tmp/p56/A/{Stats,TradeEvents,Candidates,Risk}.csv` == `/tmp/p56/B/...` (md5-equal, cmp-clean).
- Tester reports: `<datadir>/p56_A.htm` ($25,570.97/1.31/2011t), `p56_B.htm` (identical).
