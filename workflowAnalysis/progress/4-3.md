# Phase 4.3 — Daily-loss halt baseline unification (single source of truth, equity baseline)

## Phase
- **Phase id:** 4.3
- **Fix ids:** 4.3
- **Touches trading logic:** Y (changes the daily-loss baseline from balance to start-of-day equity; unifies the two halts onto one PnL)
- **depends_on (phases COMMITTED first):** Iteration-4 opener (4.1 @701927a, 4.2 @445baa0 = HEAD baseline A)
- **Binding stok param/option used:** PRIMARY = start-of-day EQUITY baseline (the plan's primary). Threshold unchanged = `InpDailyLossLimit = 3.0`% (UltimateTrader_Inputs.mqh:51) — already shared by both halts; only the BASELINE source is unified. Documented FALLBACK (stok to rule, not me): keep BALANCE baseline but share from one place.

## What changed (3 edit sites, file:line verified against current source)

### Site 1 — `Include/Core/CRiskMonitor.mqh` (SINGLE SOURCE OF TRUTH, equity baseline)
- **Member comment** (~22–27): documented that `m_daily_start_balance` now holds the start-of-day EQUITY snapshot (name kept for getter/ABI compat — `GetDailyStartBalance()` and any external readers unchanged).
- **`Init()` baseline (was line 75):** `m_daily_start_balance = AccountInfoDouble(ACCOUNT_BALANCE)` → `AccountInfoDouble(ACCOUNT_EQUITY)`.
- **`CheckDayReset()` baseline (was line 241):** same `ACCOUNT_BALANCE` → `ACCOUNT_EQUITY` swap; day-reset log line relabelled "Start equity:".
- **`GetDailyPnL()` (was 91–99):** UNCHANGED math `((current_equity - m_daily_start_balance)/m_daily_start_balance)*100` — now equity-vs-start-of-day-equity (the equity-baseline daily-loss line). Comment marks it the single source of truth.
- **`CheckRiskLimits()` halt (was 149–174):** UNCHANGED — still halts when `daily_pnl <= -m_daily_loss_halt_pct` (3%). Reset-on-new-day, halt action, notifications all preserved. NO logic cut.

### Site 2 — `Include/ExitPlugins/CDailyLossHaltExit.mqh` (consumer reads the shared PnL)
- **Forward decl** `class CRiskMonitor;` after the includes (CRiskMonitor.mqh is included AFTER this file in the main order; inline call resolves in the single-TU build — mirrors the Phase 4.2 CPositionCoordinator forward-decl pattern).
- **Member** `CRiskMonitor *m_risk_monitor;` (private), `= NULL` in the constructor.
- **Setter** `void SetRiskMonitor(CRiskMonitor *monitor)` next to `SetContext`.
- **`UpdateDailyStats()` (was 62–89):** day-reset bookkeeping PRESERVED; when `m_risk_monitor != NULL` (the production path) `m_daily_pnl_pct = m_risk_monitor.GetDailyPnL()` and return — STOPS computing its own (disagreeing) balance-reconstructed baseline. Legacy self-computed baseline kept only as a NULL-fallback (standalone plugin use) so the plugin is never broken. `GetDailyRealizedPnL()` still referenced in the fallback (no dead-method warning).
- **Halt/exit actions (140–187):** UNCHANGED — still compares `m_daily_pnl_pct <= -InpDailyLossLimit` and closes positions. Same 3% threshold as CRiskMonitor → both halts now agree.

### Site 3 — `UltimateTrader.mq5` (wire the injector after both objects exist)
`g_dailyLossExit.SetRiskMonitor(g_riskMonitor);` added immediately after `g_riskMonitor.Init()` (was ~994). `g_dailyLossExit` is constructed at ~768; `g_riskMonitor` at ~989–994 → both exist at the wire point.

- **Logic-cut check:** NO logic cut. The 3% threshold, reset-on-new-day, and halt/exit actions all stay; only the BASELINE source is unified (to start-of-day equity) and the second consumer reads the shared PnL.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 10318 ms elapsed, cpu='X64 Regular'
```
- Errors: 0 | New warnings vs 38-baseline: 0 (exactly 38; ZERO warnings in CRiskMonitor.mqh or CDailyLossHaltExit.mqh — confirmed by grep). The only two UltimateTrader.mq5 warnings (lines 785/786, `double→int` conversion) are pre-existing exit-plugin-block warnings, BEFORE my insertion at ~999; part of the 38-baseline.
- Error 106 / include issues: none (forward-decl `class CRiskMonitor;` resolved cleanly; CRiskMonitor.mqh is included AFTER CDailyLossHaltExit.mqh, inline GetDailyPnL() call resolves in the single-TU build — same pattern as Phase 4.2's CPositionCoordinator forward-decl)
- `.ex5` regenerated: yes — B (post-fix) md5 `1a6e5404`, 912418 bytes. A (HEAD 445baa0 source recompile) md5 `c75a310b`, 0err/38warn (`Result: 0 errors, 38 warnings, 10063 ms elapsed`). A `.ex5` ≠ B `.ex5` (real behavior change — equity baseline + shared PnL — declared NEEDS-CARE, NOT byte-identical).

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, full 2019.01.01–2026.06.27, `[TesterInputs]` production defaults (395 params; `InpEnableMultiStrategy=false`, `InpSignalSource=2`/BOTH, `InpDailyLossLimit=3.0`, `InpEnableDailyLossHalt=true`, `InpMaxPositions=5`, `InpMaxTradesPerDay=5`, `InpDisableAutoKill=true`). Deposit 10000 USD, leverage 100. A/B inis byte-identical apart from `Report=`. **Mode=1 (1-minute OHLC).** 2 terminal runs total (A then B) — within the ≤2 bound.
- **A = committed HEAD 445baa0** (binary md5 `c75a310b`; reproduces the Phase-4.2 committed-HEAD figures exactly — net $24,286.09 / PF 1.30 / 2021 trades / Bal-DD 9.26% / Eq-DD 11.23%, identical to 4-2.md's B row, confirming HEAD = the 4.2 anchor).
- **B = post-fix** (binary md5 `1a6e5404`; in-tree `UltimateTrader.ex5`).

### A/B overall (report = authoritative for net/PF/maxDD; avg-R from deterministic Stats CSV)
| Metric | A (HEAD 445baa0) | B (post-fix, equity baseline) | Δ |
|---|---|---|---|
| Total Trades (report deals) | 2021 | 2011 | **−10** |
| Logical trades (CSV closes) | 966 | 961 | **−5** |
| Net Profit (report) | $24,286.09 | $25,570.97 | **+$1,284.88 (+5.29%)** |
| Net (CSV sum Total_PnL) | $24,905.80 | $26,205.77 | +$1,299.97 (+5.22%) |
| Profit Factor | 1.30 | 1.31 | +0.01 |
| Expected Payoff | $12.02 | $12.72 | +$0.70 |
| avg Total_R (CSV) | +0.1266 (966) | +0.1320 (961) | **+0.0054** |
| Balance maxDD | $3,498.00 (9.26%) | $3,637.66 (9.28%) | +$139.66 / +0.02pp |
| Equity maxDD | $4,336.78 (11.23%) | $4,513.87 (11.26%) | +$177.09 / +0.03pp |

### Yearly net split (CSV Total_PnL by exit-year)
| Year | A net | B net | Δ |
|---|---|---|---|
| 2019 | +1,253.81 | +1,253.81 | 0 (byte-identical) |
| 2020 | −116.88 | −116.88 | 0 (byte-identical) |
| 2021 | +2,187.46 | +2,509.34 | **+321.88** |
| 2022 | +3,642.60 | +3,961.62 | +319.02 |
| 2023 | +863.28 | +883.44 | +20.16 |
| 2024 | +2,250.14 | +2,264.94 | +14.80 |
| 2025 | +15,325.28 | +15,834.99 | +509.71 |
| 2026 | −499.89 | −385.49 | +114.40 |

- B is **better on net (+5.29%) AND avg-R (+0.0054) with FEWER trades (−5 logical / −10 deals)**. 2019/2020 byte-identical (the first behavioral divergence is the 2021.01.05 halt). All divergent years are net-positive for B. This is the opposite of the plan's trigger-happy kill-concern: the equity baseline cut net-NEGATIVE trading days.

## Daily-loss-halt event count (A vs B) + overnight-runner example
- **Halt source measured:** CRiskMonitor's `CheckRiskLimits()` halt (journal line `DAILY LOSS LIMIT HIT: -X%` + paired `Alert:`), the single-source halt. A = balance baseline, B = equity baseline.
- **Halt-day count: A = 10 distinct days, B = 10 distinct days — SAME COUNT.** B is NOT more trigger-happy in aggregate. The SETS differ:
  - **7 common halt days** (both): 2020.06.01, 2020.06.02, 2021.03.31, 2021.10.19, 2022.06.06, 2022.10.04, 2026.02.10.
  - **3 A-only** (A's balance baseline halted, B's equity baseline did not): 2020.10.13, 2023.01.10, 2023.01.11.
  - **3 B-only** (B's equity baseline halted, A's balance baseline did not): **2021.01.05, 2021.11.10, 2022.11.10.**
- **Renumber-proof trade cross-match** (key = EntryTime+Direction+OriginalSL+EntryPrice; ticket IDs re-sequence after the first divergence so a by-ticket diff overstates — this is the TRUE diff): **960 common trades**, **6 A-only** (A took, B blocked by its earlier halt) net **−$389.98**, **1 B-only** (+$28.96). The 6 A-only losers B avoided all fall on the 3 B-only halt days; the 1 B-only trade is on an A-only halt day. Net trade-level: B avoided −$389.98 of losers, took +$28.96 more.
- **CONCRETE OVERNIGHT-RUNNER GIVE-BACK EXAMPLE — 2021.01.05 (B-only halt):** Both A and B held multiple overnight LONG runners entered 2021.01.04 into 2021.01.05 — notably ticket 663 (R=+2.50, MFE_R=3.88) and ticket 667 (R=+2.11, MFE_R=3.09), large floating-profit runners. B's start-of-day-EQUITY baseline on 2021.01.05 was set INCLUDING that overnight floating profit; as the early session gave back (ticket 671 closed −$110.95 at 03:00 and the runners' floating profit retraced), B's equity dropped to **−3.07% of the elevated baseline at 03:47:40 → halt.** A's start-of-day-BALANCE baseline excludes overnight floating profit, saw a smaller % drop, and **never crossed −3%**. The B halt then blocked **ticket 676 (entry 2021.01.05 14:00 LONG, A realized −$170.10)** — a loser A took and B avoided. This is exactly the plan-flagged mechanism: overnight floating profit raises the equity baseline → a give-back crosses the 3% line balance-baseline A never sees → B halts SOONER and (here) avoids a subsequent loser.

## Single-source-of-truth confirmation
- **CONFIRMED at code + runtime.** Source: `CDailyLossHaltExit.UpdateDailyStats()` now does `m_daily_pnl_pct = m_risk_monitor.GetDailyPnL()` and returns when the monitor is wired (production path); it no longer reconstructs its own `start_balance = current_balance − realized_pnl`. CRiskMonitor anchors `m_daily_start_balance = ACCOUNT_EQUITY` at Init + day-reset and owns `CheckDayReset()`. One PnL, one baseline, one threshold (`InpDailyLossLimit=3.0`, already shared).
- **Runtime cross-check:** B journal shows **0 CDailyLossHaltExit "Closing all positions and halting trading"** prints — and so does A (0). In BOTH builds the daily-loss protection is realized via CRiskMonitor's `CanTrade()/IsTradingHalted()` new-trade block (10 halts), not via CDailyLossHaltExit force-closing; the fix unified the BASELINE without changing which mechanism engages. No second disagreeing baseline remains (the only behavioral change is A's balance line → B's equity line; CDailyLossHaltExit no longer computes an independent number).

## Equity-vs-balance trigger-happiness read (for stok)
- **mt5 read: the equity baseline is BENIGN on this strategy, NOT trigger-happy.** Evidence: (1) halt-day count is IDENTICAL (10 vs 10) — the equity baseline does not halt MORE often; (2) net is +5.29% / avg-R +0.0054 / PF +0.01 — BETTER, not worse; (3) the 3 newly-halted (B-only) days each PREVENTED net-negative trading (6 losers totaling −$389.98 avoided, incl. the 2021.01.05 ticket-676 give-back); (4) maxDD is a hair worse (+$140 Bal / +$177 Eq, +0.02–0.03pp) — a deterministic-equity path ripple, within noise, not a regression. The plan's kill-criterion ("over-trigger on overnight runners → net could DROP") did NOT trip: net rose. The plan's documented FALLBACK (keep BALANCE baseline, share from one place) is therefore NOT indicated by these numbers — but the baseline ruling is stok's, not mine.
- **Caveat handed to stok:** this is a single historical path; the equity baseline's protective effect here came from halting on overnight-runner give-backs that happened to precede losers. On a different path an equity baseline could halt before a recovery. The numbers favor SHIP-EQUITY; the fallback exists if stok weighs the overnight-runner-give-back risk differently for the live book.

## mt5-developer sign-off
- **SIGNED (implementation correct).** Compile 0 err / exactly 38 warn (0 new; 0 warnings in either edited file). 3 edit sites verified against current source. CRiskMonitor is the single source of truth (start-of-day EQUITY baseline at Init + day-reset; `GetDailyPnL()` unchanged math now equity-vs-equity). CDailyLossHaltExit reads `GetDailyPnL()` via the injected `SetRiskMonitor` (forward-decl pattern mirroring Phase 4.2's CPositionCoordinator; resolves in the single-TU build), legacy baseline retained only as a NULL-fallback so the plugin is never broken standalone. The 3% threshold, reset-on-new-day, and halt/exit actions are all preserved — NO logic cut. Runtime: single-source confirmed (no second baseline; 10 unified halts; 0 CDailyLossHaltExit independent closes in A and B alike).

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** equity baseline over-triggers on overnight runners → net could drop → loop to stok (balance-baseline fallback).
- **Result: PASS — NOT tripped.** Halt-day count identical (10 vs 10, not MORE); net +5.29% / avg-R +0.0054 (UP, not down). The 3 newly-halted B-only days each prevented net-negative trading. No over-trigger regression.

## stok sign-off
- **SIGNED — verdict: ACCEPT-WITH-FOLLOWUP, baseline = SHIP-EQUITY** (binding). stok verified the spec in source (equity anchor at Init L78 + day-reset L247; unchanged PnL math L105; consumer reads GetDailyPnL on the production path L92; legacy balance reconstruction kept ONLY as NULL fallback). Ruling:
  - **(1) Equity is the dimensionally-CORRECT anchor for a runner-holding trend book**, not merely "more conservative." A daily-loss stop must answer "how much have I actually given back today?" — that includes floating P&L on overnight runners, which is real marked capital at risk. A BALANCE baseline is blind to it (only "sees" risk once a runner books red) — that is backwards for a strategy whose entire risk surface is the open position. The 2021.01.05 halt reads PROTECTIVE: B's equity hit −3.07% from the start-of-day high-water — a genuine 3% intraday give-back of real capital — and the rule stopped the day correctly; it halts NEW entries only, does NOT force-close runners (0 force-closes both runs), so the open thesis survives. Balance didn't halt only because it was structurally blind = the bug, not a feature.
  - **(2) The +5.29% is LUCK, not edge — accept does NOT rest on it.** The entire net delta is 3 B-only halt days that happened to precede losers on THIS path; which trades fell past the halts is path-dependent (4.1 lesson). What survives: halt count IDENTICAL 10=10 (directly falsifies the plan's over-trigger kill-criterion), all divergent years net-positive, maxDD flat. The backtest serves only as a falsification check (kill-criterion did NOT trip); the accept rests on the §1 + §3 CORRECTNESS argument.
  - **(3) Single source of truth is unambiguously correct** independent of the baseline choice — two halts on two baselines made the 3% line literally two numbers; unifying onto one CRiskMonitor-owned baseline ships either way.
  - **Caveat ON RECORD:** do NOT cite this commit as "equity baseline made us 5%." Defensible claim = "unified two disagreeing daily-stop baselines onto one dimensionally-correct equity anchor; net-neutral-to-protective in-sample; over-trigger fear falsified (10=10); OOS premature-halt risk pending."
  - **Verdict: ACCEPT-WITH-FOLLOWUP, SHIP-EQUITY. NOT fallback-to-balance** (balance is the wrong anchor; kill-criterion not tripped; no year harmed).

## Iteration-6 follow-up (logged by stok; does NOT block this commit)
- **(primary) OOS / regime guard.** The protective effect was measured on a 7.5-yr gold-uptrend path where runners are usually winners. In a CHOPPY / range / post-spike mean-reverting regime, a 3% equity give-back on an open runner is likelier to precede a recovery you're locked out of (the §1 risk case). Follow-up: on the next OOS slice / different-regime window, re-run A/B counting **B-only halts that preceded a RECOVERY (would-have-been winners B was locked out of)**, not just losers-avoided. KILL-SIGNAL for equity baseline: if OOS B-only halts systematically lock out recoveries and B net/avg-R drops below A there → revisit balance-from-one-place.
- **(secondary, only if primary flags it) config knob:** expose baseline source as `InpDailyLossBaseline` {equity/balance}, default equity — a one-line swap thanks to the single-source plumbing, not a re-architecture.

## Decision
- **DONE — both agents signed (mt5 implementation-correct; stok ACCEPT-WITH-FOLLOWUP / SHIP-EQUITY, binding, source-verified). Committed source-only on `feat/multi-strategy`.** Single-source unified, equity baseline shipped on CORRECTNESS grounds (dimensionally-correct anchor for a runner-holding book; over-trigger fear falsified 10=10); the +5.29% net is treated as luck not edge. OOS/regime premature-halt guard logged as Iter-6 follow-up. NEXT ACTION → Phase 4.4+4.5.

## Commit
- **Hash:** `<filled at commit>` (source-only: CRiskMonitor.mqh + CDailyLossHaltExit.mqh + UltimateTrader.mq5 + 4-3.md + 00-STATUS.md; `.ex5` intentionally uncommitted). Branch `feat/multi-strategy`.

## Artifacts
- A report: `725B…/p43_A.htm` (decoded `/tmp/p43/A/report.utf8.html`) | B report: `…/p43_B.htm` (decoded `/tmp/p43/B/report.utf8.html`)
- A/B inis: `725B…/backtest_p43_A.ini`, `backtest_p43_B.ini` (built from p42_B template; identical apart from `Report=`; 395 prod params, InpEnableMultiStrategy=false, full 2019.01.01–2026.06.27, Model=1)
- A CSVs: `/tmp/p43/A/{Stats,TradeEvents,Risk}.csv` (decoded) | B CSVs: `/tmp/p43/B/{...}.csv`
- Halt extracts: `/tmp/p43/A_halt_days.txt`, `/tmp/p43/B_halt_days.txt`, `/tmp/p43/{A,B}_halts_raw.txt`
- A binary `/tmp/p43/UT_A_head.ex5` (md5 `c75a310b` = HEAD 445baa0 source build) | B binary `/tmp/p43/UT_B_p43.ex5` = in-tree `UltimateTrader.ex5` (md5 `1a6e5404`)
- Analysis script: `/tmp/p43/analyze.py`
