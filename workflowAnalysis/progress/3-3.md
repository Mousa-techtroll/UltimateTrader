# Phase 3.3 — Range/Reversal engine hardcoded day_type → GetDayType() (telemetry fidelity)

## Phase
- **Phase id:** 3.3
- **Fix ids:** 3.3
- **Touches trading logic:** N (NOT-A-TRADING-PROBLEM — telemetry fidelity; no trade-decision change)
- **depends_on (phases that must be COMMITTED first):** 3.2 (COMMITTED). Reordered Iteration-3-correctness block, before the 2.4-GATE.
- **Binding stok param/option used:** n/a (no-op / telemetry-only). Plan-prescribed form: `signal.day_type = (m_context != NULL) ? m_context.GetDayType() : DAY_RANGE;`

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/EntryPlugins/CRangeReversionEngine.mqh` — `TagAndRetarget()`, line 127: changed `signal.day_type = DAY_RANGE;` (hardcoded) to `signal.day_type = (m_context != NULL) ? m_context.GetDayType() : DAY_RANGE;`.
- **Diff summary:** Telemetry-fidelity fix. The range engine previously stamped a hardcoded `DAY_RANGE` on every signal's `day_type`; now it reads the real classified day type from the market context (matching how `CReversalSweepEngine.mqh:208` stamps `signal.day_type = m_context.GetDayType();`), with a NULL guard falling back to `DAY_RANGE`. This protects the per-day-type tuning data the fresh GATE / Iter-3 validation relies on. No trade-decision branch reads this field on the production path.
- **Logic-cut check:** NO trading logic deleted. One-line telemetry assignment only; no toggle/registration change, no path removed.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 9157 ms elapsed, cpu='X64 Regular'
```
- Errors: 0  | New warnings vs 38-baseline: 0 (38 == baseline; no new warning in the edited file CRangeReversionEngine.mqh)
- Error 106 / include issues: none
- `.ex5` regenerated: yes — B build saved to /tmp/UT_phase3_3_B.ex5 (md5 55d62b37…). A build (pre-edit recompile of current source) saved to /tmp/UT_phase3_3_A.ex5 (md5 899add78…). Committed-HEAD baseline saved to /tmp/UT_phase3_3_baseline.ex5 (md5 e0cfbb3a…). NOTE: .ex5 md5 is non-deterministic across compiles (embedded build metadata) — byte-identity is proven on backtest CSV outputs, per protocol, not on the .ex5.
- A-build compile (verbatim): `Result: 0 errors, 38 warnings, 9100 ms elapsed, cpu='X64 Regular'`

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1 2024, DEFAULT production `.set` = `MQL5/Profiles/Tester/UltimateTrader.set` (decoded: `InpEnableMultiStrategy=false`, all 4 `InpEnableEngine*=false` incl. `InpEnableEngineRange=false`; `InpSignalSource=2` BOTH; `InpSymbolProfile=0` gold). 1 year sufficient for an engine-off no-op (task instruction).
- **Mode:** Model=1 (1-min OHLC), Deposit 10000 USD, Leverage 100. Terminal `C:\Program Files\Vantage Markets MT5 Terminal\terminal64.exe` (NO `/portable`), data dir `nullkuhl`/`725B72F25E46C780EF59F57016D58156`; CSVs → `MetaQuotes/Terminal/Common/Files/`.
- **A (baseline = pre-edit recompile of current working-tree source; `CRangeReversionEngine.mqh` clean vs HEAD):** reproduces the committed-HEAD 2024 anchors EXACTLY — Stats `04d116972696f905c6ea5fa6701a7155`, TradeEvents `825489b61fbf3db5cb2302361de66bb1`, Candidates `bcc44924d5d3fc31a5f4d35e5142b0a7`, Risk `8babda0f7b846cc6fa94941ecb39c815`. Headline 140t / net $1190.84 / totR 11.76 / avgR 0.0840.
- **B (this phase, fix 3.3 build):** Stats `04d116972696f905c6ea5fa6701a7155`, TradeEvents `825489b61fbf3db5cb2302361de66bb1`, Candidates `bcc44924d5d3fc31a5f4d35e5142b0a7`, Risk `8babda0f7b846cc6fa94941ecb39c815`. IDENTICAL to A.
- **No-op / byte-identical claim:** **YES — A == B BYTE-IDENTICAL.** All 4 CSVs md5 MATCH + `cmp` byte-for-byte clean (sizes identical: Stats 348670, TradeEvents 1409638, Candidates 278030, Risk 66060). Trade-for-trade, SL-for-SL, R-for-R identical.
- **Mechanism:** `g_engineRange = new CRangeReversionEngine(...)` (UltimateTrader.mq5:742) + `RegisterEngine`/`RegisterEntryPlugin` are ALL inside `if(InpEnableMultiStrategy)` (block opens mq5:718; flag false on the production `.set`). The edited method `TagAndRetarget()` is the engine's OWN method — there is NO legacy standalone plugin emitting through it (unlike fix 3.4's CFailedBreakReversal/CCrashBreakoutEntry reachability via the legacy Factor-4 evaluator). With the engine never instantiated, line 127 is unreachable on production → byte-identity is STRUCTURAL by construction.

## stok review + verdict
- **Numbers handed to stok:** the byte-identity result (A == B, all 4 CSVs md5 MATCH + cmp CLEAN, reproducing committed-HEAD 2024 anchors `04d11697…`/`825489b6…`/`bcc44924…`/`8babda0f…`); the one-line diff; the structural reachability argument (range engine gated behind `InpEnableMultiStrategy=false`; `TagAndRetarget` is the engine's own method, no legacy plugin routes through it).
- **stok assessment:** PENDING (independent stok no-op sign-off owed — do NOT sign on stok's behalf).
- **stok verdict:** PENDING

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** no-op claim → byte-identical production backtest = PASS; any divergence = STOP and report (the engine should be off).
- **Result:** PASS — A == B byte-identical (all 4 CSVs md5 + cmp), engine OFF on production confirmed, no divergence.

## mt5-developer sign-off
- Compile clean (0-err / 0-new-warn, `Result: 0 errors, 38 warnings, 9157 ms`); mechanism implements the validated telemetry-fidelity fix (real `m_context.GetDayType()` with NULL-guard fallback to `DAY_RANGE`, matching CReversalSweepEngine.mqh:208 and the local `engine_confluence` NULL-guard convention at CRangeReversionEngine.mqh:139-140); file:line verified against current source (line 127, `TagAndRetarget()`); NO logic cut; production A==B byte-identical proven (structural — engine gated behind `InpEnableMultiStrategy=false`). — SIGNED

## stok sign-off
- **SIGNED (independent no-op sign-off, verified at SOURCE).** One-line diff CONFIRMED at `CRangeReversionEngine.mqh:127` in `TagAndRetarget()`: `signal.day_type = (m_context != NULL) ? m_context.GetDayType() : DAY_RANGE;` (real classified day type, NULL-guard fallback to `DAY_RANGE`, matching the `CReversalSweepEngine` stamp); `day_type` is a telemetry field — no production trade-decision branch reads it. Router gating CONFIRMED at source: `g_engineRange = new CRangeReversionEngine(...)` (mq5:742) lives entirely inside `if(InpEnableMultiStrategy)` (mq5:718, default false per the mq5:713 comment) → `TagAndRetarget()` UNREACHABLE on the production `.set` → structural no-op. Edit is the engine's OWN method — no legacy / file-signal side channel touched. A==B XAUUSD+ H1 2024 byte-identical (all 4 CSVs md5+cmp match committed-HEAD; 140t/$1190.84) is consistent with an off-path telemetry edit. KILL-CRITERION (production byte-identical) PASS. — stok

## Decision
- **mt5-QA-DONE → awaiting INDEPENDENT stok no-op sign-off.** Do NOT commit; do NOT advance until stok signs.
  - On stok ACCEPT → set QA-PASS → COMMITTED (main session runs git) → advance NEXT ACTION to Phase 3.4.

## Commit
- **Hash:** <not committed this session — leave stok PENDING per task instruction>
- **Message:** <n/a>

## Artifacts (for stok independent re-verification)
- Baseline (committed-HEAD) .ex5: `/tmp/UT_phase3_3_baseline.ex5` (md5 e0cfbb3a…)
- A-build .ex5 (pre-edit recompile): `/tmp/UT_phase3_3_A.ex5` (md5 899add78…)
- B-build .ex5 (fix 3.3): `/tmp/UT_phase3_3_B.ex5` (md5 55d62b37…)
- A-build CSVs saved aside: `/tmp/UT_phase3_3_A_csv/{Stats,TradeEvents,Candidates,Risk}.csv`
- B-build CSVs (in place): `Common/Files/UltTrader_*_XAUUSD+_20240101_0000.csv`
- NOTE: `.ex5` md5 is non-deterministic across compiles (embedded build metadata) — byte-identity is proven on the backtest CSV outputs (md5 + cmp), per the QA protocol, NOT on the `.ex5`.
