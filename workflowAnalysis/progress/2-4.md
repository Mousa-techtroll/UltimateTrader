# Phase 2.4 — Scorer axis orthogonality: D1 dealing-range de-correlation from H1-swing SL anchor

## Phase
- **Phase id:** 2.4
- **Fix ids:** 2.4 (BETTER-OPTION). NOTE: this is fix 2.4 ONLY — the 2.4-GATE tier-threshold RE-DERIVATION is a SEPARATE PENDING phase, NOT done here.
- **Touches trading logic:** Y (but unreachable on the production `.set` — consumed ONLY by the scorer + router engines, OFF by default → byte-identical; see QA)
- **depends_on (phases that must be COMMITTED first):** 2.3 (objective spine; COMMITTED 42d8e34), 2.2 (spine freshness; COMMITTED b6228cb), 1.8 (closed-bar swing shift; COMMITTED), 1.2 (closed-bar BOS anchors; COMMITTED)
- **Binding stok param/option used:** `InpDealingRangeD1Lookback = 20` closed D1 bars (ICT IPDA 20-day window). De-correlate the scorer L1 LOCATION axis from the SL anchor: dealing range / equilibrium / premium-discount now derive from an HTF **D1** range; `GetSwingHigh/Low` (the SL anchor) STAYS the H1 closed swing.

## What changed
- **Files touched (verified file:line against current source):**
  - `Include/MarketAnalysis/CMarketContext.mqh`
    - New member `int m_dealing_range_d1_lookback;` (decl :86, after the H1 swing members; comment marks it the ICT IPDA 20-day window backing the L1 location axis).
    - Constructor: new defaulted param `int dealing_range_d1_lookback = 20` (appended after `smc_use_htf_confluence`); assigned `m_dealing_range_d1_lookback = (dealing_range_d1_lookback > 0) ? dealing_range_d1_lookback : 20;` (:147).
    - Added two `const` helpers `D1DealingRangeHigh()` (:744) / `D1DealingRangeLow()` (:757): highest-high / lowest-low over `iHigh/iLow(_Symbol, PERIOD_D1, i)` for `i = 1..m_dealing_range_d1_lookback` (CLOSED D1 bars, shift 1..lookback). Each skips un-available bars (`<=0`, warmup/gaps) so a short read can't seed a 0 extreme.
    - Re-sourced the getters: `GetDealingRangeHigh() override { return D1DealingRangeHigh(); }` (:771), `GetDealingRangeLow() override { return D1DealingRangeLow(); }` (:772); `GetEquilibrium() override` (:775) now = midpoint of the D1 dealing range (`(dr_high+dr_low)*0.5`, same 0/inverted guard as before).
    - `GetSwingHigh()` (:691) / `GetSwingLow()` (:696) **UNCHANGED** — still return `m_swing_high`/`m_swing_low` (the H1 closed swing = the STRUCTURAL SL anchor). THIS is the de-correlation.
    - `UpdateSwingPoints()` comment updated (the cached H1 swings now back the SL anchor ONLY; the L1 location axis was de-correlated onto the D1 IPDA window). The swing-detection CODE is unchanged (still H1 closed bars `[1..lookback]`, realized-count guarded, per fix 1.9).
  - `Include/Validation/CConfluenceScorer.mqh`
    - Axis 1 (HTF draw alignment): **DROPPED the nested `eq>0` sub-point.** Was `if(draw>0){ points+=1; if(GetEquilibrium()>0) points+=1; }` (0-2) → now `if(draw>0) points+=1;` (0-1). The dropped sub-point was derived from the SAME equilibrium/swing pair as axis 2 and the SL — not orthogonal. The independent draw-exists +1 stays.
    - Axis 2 (premium/discount, +2): **KEPT unchanged** — `IsInDiscount/IsInPremium` key off `GetEquilibrium`, which post-2.4 is the D1-range midpoint → this is now the SOLE, independent location axis.
    - Header comment updated (HTF-draw axis 1 → max 1; raw point ceiling now 9; the `if(points>10) points=10` clamp retained but unreachable — re-derived at the 2.4-GATE).
  - `UltimateTrader_Inputs.mqh`
    - Added `input int InpDealingRangeD1Lookback = 20;` in the **MULTI-STRATEGY ENGINES** group (:505), the natural sibling of the Phase-2.2 `InpBOSFreshnessBars` and Phase-2.3 `InpSpineMinConfluence` scorer inputs.
  - `UltimateTrader.mq5`
    - `new CMarketContext(...)` (:496-512): passed `InpDealingRangeD1Lookback` as the final positional argument (after `InpSMCUseHTFConfluence`), matching the new constructor param.
- **Diff summary:** The scorer's L1 LOCATION axis (dealing range / equilibrium / premium-discount) is de-correlated from the structural SL anchor: the dealing range + equilibrium now derive from an HTF D1 IPDA 20-bar window (closed D1 bars), while `GetSwingHigh/Low` stays the H1 closed swing that anchors stops; the scorer's non-orthogonal `eq>0` sub-point on axis 1 is dropped (axis 1: 0-2 → 0-1) and the +2 premium/discount remains as the sole, now-independent location axis. `git diff --stat`: 4 files, +87/-25.
- **Logic-cut check:** NO trading logic deleted. No sub-path removed, no toggle disabled, no engine cut. The dropped `eq>0` sub-point is an axis-orthogonality repair (it double-counted the same swing-derived signal as axis 2/the SL), explicitly mandated by the binding fix-2.4 spec ("DROP the eq>0 sub-point"). The dealing-range re-sourcing replaces the data SOURCE feeding the location axis (H1 swing → D1 IPDA), repairing the non-orthogonality finding — it does not remove a feature.

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 9088 ms elapsed, cpu='X64 Regular'
```
- Errors: **0**  | New warnings vs 38-baseline: **0** (all 38 are pre-existing — Logger/RecoveryManager/HealthMonitor/CVolatilityBreakoutEntry/Series/CEnhancedTradeExecutor + the 2 long-standing mq5:783/784 trailing-plugin `double→int` warnings; grep confirms NONE in CMarketContext / CConfluenceScorer / UltimateTrader_Inputs).
- Error 106 / include issues: none (no new include; `iHigh`/`iLow` are global MQL5 functions).
- `.ex5` regenerated: yes — `/mnt/c/Trading/UltimateTrader/UltimateTrader.ex5` 895382 bytes, md5 `8913894205772dd569fa089510baf3cc` (B-build; differs from the A-baseline `13fa4527…` because source changed, though dormant on the production `.set`).

## QA backtest (decoded, ACTUAL — never estimated)
- **Config:** XAUUSD+ H1, production `.set` = `UltimateTrader.set` (`Profiles/Tester`; `InpEnableMultiStrategy=false`, `InpSignalSource=2` BOTH, `InpSymbolProfile=0`; `InpEnableEngineTrend/Reversal/Range/Expansion=false`; `InpDealingRangeD1Lookback` not in `.set` → uses default 20). Years 2024 and 2023. Deposit 10000 USD, Leverage 100.
- **Mode:** 1-min OHLC (Model=1).
- **A (baseline = current committed HEAD source):** `/tmp/UT_phase2_4_baseline.ex5` (md5 `13fa45273181759a12a063df40bb7f00`), the working-tree `.ex5` copied aside BEFORE editing. It is the Phase-2.3 B-build = current committed HEAD source (tree clean at HEAD `42d8e34`; the committed `.ex5` blob `e0cfbb3a…` is a stale earlier build, but the SOURCE is HEAD and this `.ex5` is a faithful HEAD compile). PROVEN: A reproduces the committed Phase-2.3 trading anchors EXACTLY both years.
  - 2024: **140 trades / $1190.84 net / 11.76 totR / 0.0840 avg-R** (Stats md5 `04d116972696f905c6ea5fa6701a7155`).
  - 2023: **129 trades / $714.29 net / 3.40 totR / 0.0264 avg-R** (Stats md5 `b11e07675cf4d577e27663ffde0aca19`).
- **B (this phase):** `/mnt/c/Trading/UltimateTrader/UltimateTrader.ex5` (md5 `8913894205772dd569fa089510baf3cc`).
  - 2024: 140t / $1190.84 / 11.76 totR / 0.0840 avg-R — IDENTICAL to A.
  - 2023: 129t / $714.29 / 3.40 totR / 0.0264 avg-R — IDENTICAL to A.
- **Delta + explanation:** ZERO. On the production `.set`, `InpEnableMultiStrategy=false` → `g_confluenceScorer` is never `new`'d (mq5:719 is inside `if(InpEnableMultiStrategy)`) and the four router engines (CTrendContinuationEngine / CRangeReversionEngine / CReversalSweepEngine) stay OFF (`InpEnableEngine*=false`). `GetDealingRangeHigh/Low`, `GetEquilibrium`, `IsInDiscount/IsInPremium` and the scorer axes are consumed ONLY by those (scorer + the three engines) — a full repo grep confirms NO other consumer. So the D1 re-sourcing, the dropped axis-1 sub-point, and the new `InpDealingRangeD1Lookback`/Configure arg are all UNREACHABLE on the default path. Byte-identity is STRUCTURAL, not coincidental — same proof class signed for Phases 2.1/2.2/2.3. ExpansionEngine signals still appear in B-2024 (12 rows) via the LEGACY evaluator path (`routed_engine=false`, Phase 2.1) — unaffected by 2.4.
- **No-op / byte-identical claim:** YES — trade-for-trade, SL-for-SL, R-for-R IDENTICAL. **A==B BYTE-IDENTICAL across ALL 8 CSVs** (Stats/TradeEvents/Risk/Candidates × 2024+2023): raw md5 MATCH on all 8 AND `cmp` byte-for-byte clean on all 8 (8 PASS / 0 FAIL).
  - 2024: Stats `04d11697…` / TradeEvents `825489b6…` / Risk `8babda0f…` / Candidates `bcc44924…` (A==B).
  - 2023: Stats `b11e0767…` / TradeEvents `7ef40d5f…` / Risk `b21a2999…` / Candidates `2fba7df1…` (A==B).
- **Iter-3 note:** the D1-range / dropped-sub-point / re-derived-tier behavior is exercised only when `InpEnableMultiStrategy=true` (Iter 3, AFTER the 2.4-GATE tier re-derivation). The de-correlated SCORE DISTRIBUTION this phase produces is precisely what the 2.4-GATE (next phase) re-derives `InpPoints*` against (do NOT carry v18 forward).

## Inspection note (D1-range / H1-swing-SL de-correlation, for Iter 3 / the 2.4-GATE)
Confirmed at SOURCE that the de-correlation is wired correctly:
- **H1-swing SL anchor — UNCHANGED.** `GetSwingHigh()` (CMarketContext.mqh:691) / `GetSwingLow()` (:696) still return `m_swing_high`/`m_swing_low`, which `UpdateSwingPoints()` (:830+) computes from H1 CLOSED bars `[1..InpSwingLookback(=20)]` (fix-1.9 closed-bar shift intact, realized-count guarded). The structural stop continues to anchor on the H1 swing.
- **L1 LOCATION axis — re-sourced to HTF D1 IPDA.** `GetDealingRangeHigh/Low()` (:771-772) → `D1DealingRangeHigh/Low()` (:744/:757) scan `iHigh/iLow(_Symbol, PERIOD_D1, 1..InpDealingRangeD1Lookback(=20))` (CLOSED D1 bars — shift 1, never the forming D1 bar 0). `GetEquilibrium()` (:775) = midpoint of that D1 range. `IsInDiscount/IsInPremium` therefore key off the D1-range midpoint, NOT the H1 swing pair.
- **De-correlation achieved.** The dealing range / equilibrium / premium-discount (location) now live on the D1 timeframe; the SL anchor lives on the H1 swing. A change in the H1 swing moves the stop, not the location axis (and vice-versa) — the two axes are no longer the same swing pair. This is the orthogonality the fix-2.4 spec requires.
- **Scorer axis change.** Axis 1 (HTF draw) dropped the `eq>0` sub-point (which re-used the same equilibrium as axis 2 and was derived from the swing) → axis 1 is 0-1 (independent draw-exists test); axis 2 keeps the +2 premium/discount, now backed by the independent D1 equilibrium. Raw point ceiling 9 (clamp at 10 retained, unreachable).
- **`InpDealingRangeD1Lookback = 20`** added (Inputs:505, MULTI-STRATEGY ENGINES group) and wired into `new CMarketContext(...)` (mq5:511) as the final positional arg.
- **Consumers (full repo grep):** `GetDealingRangeHigh/Low`, `GetEquilibrium`, `IsInDiscount/IsInPremium` are read ONLY by CConfluenceScorer (scorer) and CTrendContinuationEngine / CRangeReversionEngine / CReversalSweepEngine (the three router engines). ALL OFF by default → byte-identical on production, behavior exercised in Iter 3.
- **2.4-GATE hand-off:** the de-correlated, less-correlated score distribution is what the next phase (2.4-GATE, separate PENDING) must re-derive A+/A/B+/B `InpPoints*` against on a 2019–2026 per-signal axis-breakdown log. The clamp/ceiling note (max 9) is flagged for that re-derivation. This phase deliberately does NOT touch `InpPoints*`.

## stok review + verdict
- **Numbers handed to stok:** A==B byte-identity across all 8 CSVs (md5 + cmp) on the production `.set` both years; A reproduces the committed Phase-2.3 anchors (2024 `04d11697…`/140t/$1190.84/0.0840avgR, 2023 `b11e0767…`/129t/$714.29/0.0264avgR); the D1-range vs H1-swing-SL de-correlation source wiring; the dropped axis-1 sub-point + kept +2 prem/disc; `InpDealingRangeD1Lookback=20`; the note that 2.4 produces the new distribution the 2.4-GATE re-derives tiers against.
- **stok assessment (verified at SOURCE, not the write-up):**
  1. **Byte-identity correct (consumers off) — CONFIRMED.** Production `.set` (decoded UTF-16LE) has `InpEnableMultiStrategy=false` AND all four `InpEnableEngine*=false`. Independent repo grep: `GetDealingRangeHigh/Low`, `GetEquilibrium`, `IsInDiscount/IsInPremium` are read ONLY by CConfluenceScorer (the scorer) and the three router engines (CReversalSweepEngine:110/114-115, CTrendContinuationEngine:351-352, CRangeReversionEngine:89/93-94) — NO other consumer. Scorer is constant-NULL on the default path; engines are OFF. Every 2.4 edit (D1 re-sourcing, dropped axis-1 sub-point, new `InpDealingRangeD1Lookback`/Configure arg) is therefore unreachable → A==B byte-identity is STRUCTURAL, not coincidental. The 8-CSV md5+cmp result is consistent with that structural claim.
  2. **D1-IPDA(20)-range-for-LOCATION vs H1-closed-swing-for-SL IS the intended, ICT-grounded de-correlation — CONFIRMED.** Verified `GetSwingHigh/Low` (CMarketContext.mqh:691/696) UNCHANGED = the H1 closed swing SL anchor; `GetDealingRangeHigh/Low` (:771-772) = highest-high/lowest-low over `iHigh/iLow(PERIOD_D1, 1..20)` CLOSED D1 bars; `GetEquilibrium` (:775) = midpoint of that D1 range. This is not just an orthogonality hack — it is the textbook top-down mapping: premium/discount is a macro BIAS/LOCATION concept and belongs on a slower HTF reference (ICT's IPDA 20-day window), while the SL is the H1 structural invalidation of the trade being managed inside that range. The prior coupling (one H1 swing pair driving dealing-range + equilibrium + premium/discount + the stop) WAS the defect: it forced "where price sits in the range" to move every time the executable structure shifted. Post-2.4 the two axes move independently — an H1 swing change moves the stop, not the premium/discount judgment, and a D1-range change moves the location bias, not the stop. Dropping the axis-1 `eq>0` sub-point is the correct de-double-count: it keyed off the same equilibrium axis 2 already consumes (the same swing-derived signal counted twice, co-moving with the SL a third time) — a phantom confluence removed, no genuine one lost.
  3. **Raw point ceiling 10→9 is a direct, mandatory input to the 2.4-GATE — CONFIRMED.** Axis maxes now sum to 1+2+2+1 (context) + 1+1+1 (trigger) = 9; the `if(points>10) points=10;` clamp (CConfluenceScorer.mqh:195) is retained but unreachable. The realizable max is 9, not 10. v18 `InpPoints*` were calibrated against a max-10 (and pre-2.2/2.3/2.4 differently-shaped) distribution; carrying them forward would mis-tier every engine signal at a now-unreachable ceiling and silently down-tier/mis-size A+ setups. The 2.4-GATE MUST re-derive A+/A/B+/B on the NEW max-9 per-signal axis-breakdown distribution over 2019–2026 (A+ = genuine top decile of realized expectancy), per Risk-Register items 2 & 6. This phase correctly did NOT touch `InpPoints*`.
- **Forward note for the GATE (non-blocking):** on XAUUSD's structural uptrend a D1-IPDA-20 range will sit in DISCOUNT for a large fraction of pullback bars, so the +2 long-discount axis (the sole, now-D1-backed location axis) will fire more often/more persistently than the old H1-swing version. That is expected and is precisely why the threshold re-derivation is mandatory — the score distribution genuinely shifted in both shape (max 9) and density (more frequent +2 longs). Tune on this distribution; do not anchor to v18.
- **stok verdict:** **SIGN-OFF.** De-correlation is wired correctly and is the right ICT design; byte-identity is structurally sound (consumers off); the max-9 ceiling and the shifted distribution are correctly handed to the 2.4-GATE, which MUST re-scale and must NOT carry v18 `InpPoints*` forward.

## Kill-criteria check
- **Phase kill-criterion (from plan/00-STATUS):** no-op claim on the production path → must be byte-identical; touches-logic correctness (the de-correlation + dropped sub-point) verified by inspection for Iter 3 / the 2.4-GATE.
- **Result:** PASS (mt5 side) — A==B byte-identical (raw md5 + cmp) across all 8 CSVs both years; the "unreachable on default path" claim holds (scorer constant-NULL + engines OFF when `InpEnableMultiStrategy=false`); de-correlation wired correctly per source inspection. Behavioral effect deferred to Iter 3 (router ON, post-2.4-GATE).

## mt5-developer sign-off
- Compile clean (0 errors / 0 new warnings, verbatim Result quoted); mechanism implements the validated fix exactly (D1 IPDA dealing range backing GetDealingRangeHigh/Low + GetEquilibrium; H1 closed swing kept as the SL anchor via GetSwingHigh/Low; axis-1 `eq>0` sub-point dropped; +2 prem/disc kept; `InpDealingRangeD1Lookback=20` added + wired); file:line verified against current source; NO logic cut; A==B byte-identical proven (md5 + cmp, both years). — **SIGNED**

## stok sign-off
- **SIGNED** (independent agent, every load-bearing claim verified at SOURCE not the write-up). (1) Byte-identity correct — consumers (scorer + 3 router engines) provably OFF on the production `.set`, grep-confirmed sole readers, edits unreachable → structural A==B. (2) D1-IPDA(20)-for-LOCATION vs H1-closed-swing-for-SL is the INTENDED de-correlation and is ICT-correct (HTF bias/location vs LTF structural stop; the prior shared-H1-swing coupling was the defect; the dropped axis-1 `eq>0` sub-point was a genuine de-double-count, no edge lost). (3) Raw ceiling 10→9 is a direct input to the 2.4-GATE — the GATE MUST re-derive A+/A/B+/B on the new max-9 distribution over 2019–2026 and MUST NOT carry v18 `InpPoints*` forward. This phase correctly did NOT touch `InpPoints*`.

## Decision
- **QA-PASS — BOTH agents signed (mt5-developer + INDEPENDENT stok).** Compile clean + mechanism correct + byte-identity proven (mt5); de-correlation design ICT-correct + byte-identity structurally sound + max-9 ceiling correctly handed to the GATE (stok). Ready to COMMIT. NEXT ACTION advances to Phase 2.4-GATE (tier-threshold re-derivation on the new max-9 distribution). Do NOT run git here (main session commits).

## Commit
- **Hash:** (NOT committed per task instruction — main session runs git; stok PENDING)
- **Message (proposed):** Phase 2.4: de-correlate scorer L1 location axis — D1 IPDA dealing range, H1-swing SL anchor (fix 2.4, InpDealingRangeD1Lookback=20)
