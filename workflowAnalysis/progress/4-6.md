# Phase 4.6 — Spread unit mismatch (shock detection + spread-stability gate)

## Phase
- **Phase id:** 4.6
- **Fix ids:** 4.6
- **Touches trading logic:** Y on the LIVE path (both gates are ON by default and consumed: DetectShock mq5:1735, SessionQualityGate mq5:1750, risk-reduce mq5:1880). Backtest impact = empirical (see A/B).
- **depends_on:** none (independent of 4.1–4.5; HEAD c858e6c)
- **Binding stok param/option:** n/a (FIX-SOUND dimensional fix; no tunable)

## Dimensional proof (verified at source by orchestrator before edit)
- **Samples are stored in POINTS:** `CEnhancedTradeExecutor.mqh:1905` `double spread = SymbolInfoInteger(_Symbol, SYMBOL_SPREAD);` → `:1909` `m_exec_metrics.spread_samples[size] = spread;` (raw integer SYMBOL_SPREAD = points, e.g. 20).
- **Site 1 — shock detection (`DetectShock`, :1938→1949):** buggy `current_spread = SYMBOL_SPREAD * _Point` (PRICE, e.g. 0.20) divided by `recent_avg` of samples (POINTS, e.g. 20) → `spread_ratio = 0.20/20 = 0.01` ≈ `_Point`, NOT ~1.0. The shock thresholds are `spread_ratio > 3.0` (extreme) / `> 2.0` (shock) (:1960/1968) → a 0.01 ratio can NEVER cross them → **spread-shock leg permanently dead** (shock then driven only by bar_range/m5_range).
- **Site 2 — spread-stability gate (`GetSessionExecutionQuality`, :2039→2052):** buggy `spike_ratio = current_spread(PRICE)/recent_avg(POINTS) = 0.01`; `spread_stability = MathMax(0, 1.0 - MathMax(0, spike_ratio-1.0)) = MathMax(0, 1 - MathMax(0,-0.99)) = 1.0` ALWAYS → **stability gate permanently pinned at 1.0** (never penalises a spread spike).
- **median_spread path (:2024–2033)** uses `spread_samples` directly (POINTS) ÷ 80.0 — already point-valued; **LEFT UNCHANGED** per plan.

## What changed
- **`Include/Execution/CEnhancedTradeExecutor.mqh:1938` and `:2039`** (both lines byte-identical → single replace_all): dropped `* _Point` so `current_spread` is in POINTS, matching the point-valued `spread_samples[]` denominator. Both ratios now compute points/points → ~1.0 at normal spread and scale correctly on a real spike.
  - Before: `double current_spread = (double)SymbolInfoInteger(_Symbol, SYMBOL_SPREAD) * _Point;`
  - After:  `double current_spread = (double)SymbolInfoInteger(_Symbol, SYMBOL_SPREAD);  // points (Fix 4.6: ...)`
- **Logic-cut check:** NO logic cut. Only the numerator's unit conversion removed; thresholds, OR-structure, median path, and both gate consumers untouched.

## Gate defaults (verified)
- `InpEnableSessionQualityGate = true` (Inputs:355) — ON.
- `InpEnableShockDetection = true` (Inputs:429) — ON.
- Both consumed on the live entry path (mq5:1735 DetectShock → shock_intensity risk cut; 1750 session-quality block/halve; 1880 risk *= g_session_quality_factor). So the corrected ratios DO execute in the backtest; byte-identity is therefore an EMPIRICAL question (constant tester spread → both ratios stay sub-threshold → identical; varying spread → corrected ratio may legitimately cross a threshold the buggy ×0.01 never would).

## Compile result (verbatim)
```
Result: 0 errors, 38 warnings, 10181 ms elapsed, cpu='X64 Regular'
```
- B (working tree = fix applied): `Result: 0 errors, 38 warnings, 10181 ms elapsed` — 0 errors, exactly 38-warn baseline (0 new). ex5 md5 `2882bbde08ebb8cb39899f58dcae64d4` → `/tmp/p46/UT_B.ex5`. Both sites confirmed NO `* _Point` (grep count 0 at 1938/2039).
- A (committed/buggy, via `git stash push -- Include/Execution/CEnhancedTradeExecutor.mqh`): `Result: 0 errors, 38 warnings, 10187 ms elapsed` — buggy `* _Point` restored at 1938/2039 (grep count 2), compiled, ex5 md5 `63d6c3061e5c130734341274472d933b` → `/tmp/p46/UT_A.ex5`. Then `git stash pop` → fix RESTORED (grep count 0 at 1938/2039; file again `M` vs HEAD). Working tree is back to the fix — confirmed.

## QA backtest A/B (decoded, ACTUAL)
- **Config:** XAUUSD+ H1, Model=1 (1-min OHLC), full 2019.01.01–2026.06.27, `[TesterInputs]` 395 production defaults (InpEnableMultiStrategy=false), deposit 10000 USD, lev 100. A/B inis byte-identical apart from `Expert=` (per-build ex5) + `Report=`. 2 terminal runs total (within the ≤2 bound). A = buggy `git stash`'d HEAD c858e6c build (ex5 `63d6c306`); B = post-fix build (ex5 `2882bbde`).
- **Decoded report headlines (p46_A.htm / p46_B.htm):**

| Metric | A (HEAD c858e6c, buggy) | B (post-fix) |
|---|---|---|
| Total Net Profit | $25,570.97 | $24,667.66 |
| Profit Factor | 1.31 | 1.32 |
| Expected Payoff | 12.72 | 12.95 |
| Total Trades | 2011 | 1905 |
| Sharpe Ratio | 2.51 | 2.63 |
| Recovery Factor | 5.66 | 5.60 |
| Bal-DD Max | $3,637.66 (9.28%) | $3,612.77 (9.44%) |
| Eq-DD Max | $4,513.87 (11.26%) | $4,405.55 (11.28%) |
| Profit Trades % | 70.66% | 70.76% |

- **A reproduces the established HEAD baseline EXACTLY:** A's 4 CSV md5s (Stats `a555618b`, TradeEvents `5dbe2666`, Risk `fa4b951e`, backtest_results `5a2b3cad`) are byte-identical to the recorded Phase-4.4/4.5 A baseline, and A's report (Net $25,570.97 / PF 1.31 / 2011 trades) matches it. Confirms the A/B harness is sound.
- **Delta:** B trades 1905 vs A 2011 (−106 trades, −5.3%); Net −$903.31 (−3.5%); PF +0.01, EP +0.23, Sharpe +0.12, Bal-DD essentially flat, Eq-DD −$108. Fewer trades, marginally better quality per trade.

## Byte-identity determination + mechanism
**DIVERGED — NOT byte-identical. The fix is LIVE-AND-BACKTEST-active (the earlier "live-only / constant tester spread" hypothesis is FALSIFIED).**

- **All 4 output CSVs DIFFER (md5 + cmp):**

| Output | A md5 | B md5 | cmp |
|---|---|---|---|
| Stats CSV | `a555618b7ef18c34aa502bafad854998` | `edeb9c9691c6dd1e05075615491aa313` | DIFFER @ line 232 |
| TradeEvents CSV | `5dbe2666ce66e839e8293ddf461bc0c3` | `ebee9ad3c30f5d2592ab99f84bde63bb` | DIFFER @ line 1274 |
| Risk CSV | `fa4b951e56aa9fdac1d6fb64941491c2` | `2847e38aa43f10a57de2827b3519ef93` | DIFFER @ line 125 |
| backtest_results CSV | `5a2b3cade25778986697702fa35ae643` | `44981cde0c74d841ad810a8a3dd30413` | DIFFER @ line 2 |

- **Shock event counts (real tags `[ShockDetector]`/`[ShockGate]`; A_seg vs B_seg of the daily tester journal, isolated by per-run `MetaTester 5 started/stopped` boundaries + zero p46A/p46B cross-leakage):**

| Event | A (buggy) | B (fix) |
|---|---|---|
| `[ShockDetector]` total | **0** | **3567** |
| `[ShockDetector] EXTREME` | 0 | 2308 |
| `[ShockDetector] MODERATE` | 0 | 1259 |
| `[ShockGate] EXTREME — ALL entries BLOCKED` | 0 | 2308 |
| `[ShockGate] Moderate — risk reduced` | 0 | 1259 |

- **Session/execution-quality gate (Site 2, `GetSessionExecutionQuality` → mq5:1750): ZERO runtime block/halve events in BOTH A and B.** No "execution quality"/"quality too low"/halve log line fired in either run. (`[SessionRisk]` lines A=213 / B=207 are a SEPARATE session-multiplier mechanism, not the spread-stability gate; the small delta is a downstream consequence of fewer surviving trades.)

- **Mechanism (matches the dimensional proof exactly):**
  - **Site 1 (shock, line 1938) is the entire driver of the divergence.** In A the buggy `current_spread = SYMBOL_SPREAD * _Point` (PRICE ~0.20) ÷ `recent_avg` of point-valued samples (~20) gave `spread_ratio ≈ 0.01`, which can never cross the 2.0/3.0 thresholds → spread-shock leg dead → 0 `[ShockDetector]` events. In B (points/points) the ratio computes correctly and the tester's modeled spread spikes DO cross the thresholds — B's `[ShockDetector]` lines log `Spread/Avg=3.23, 3.09, 2.07…`. Under Model=1 the intrabar legs are degenerate (`BarRange/ATR=0.00`, `M5/ATR=0.00`), so the spread ratio is the ONLY live shock signal — which is exactly why A saw 0 shocks and B sees 3567. The 2308 EXTREME-blocked bars + 1259 moderate risk-cut bars remove/shrink entries → 2011→1905 trades.
  - **Site 2 (spread-stability, line 2039) did NOT independently trip in the tester** — even with the corrected ratio the composite execution-quality never crossed `InpExecQualityBlockThresh=0.25` / `InpExecQualityReduceThresh=0.5` (the stability term is one of three components and moved the composite only modestly here). Its fix is dimensionally correct and now scales properly on a real spike, but it is empirically non-binding on this 2019–2026 XAUUSD+ tester path. So Site 1 = backtest-active; Site 2 = correct-but-non-binding-in-tester.
- **Conclusion:** the −106 trades / −$903 net are fully mechanism-attributable to the now-live spread-shock gate (2308 EXTREME blocks + 1259 moderate cuts), and the change is risk-reducing (PF/EP/Sharpe up, Eq-DD down). No unexplained divergence.

## mt5-developer sign-off
- **SIGNED (compile clean + mechanism correct).** Compile B: 0 err / exactly 38 warn (0 new). Negative-control implicit in A-build path (the `git stash`'d buggy version compiled separately to a distinct ex5, md5 `63d6c306` ≠ B `2882bbde` → real source delta). Working tree restored to the fix after A-build (grep count 0 of `* _Point` at 1938/2039; file `M` vs HEAD) — verified, not left reverted.
- Source change is the documented dimensional fix only (numerator unit conversion dropped at both sites; thresholds/OR-structure/median path/gate consumers untouched — NO logic cut).
- A reproduces the recorded HEAD baseline byte-for-byte (4 CSV md5-equal to the 4.4/4.5 A baseline) → harness sound. B DIVERGES on all 4 CSVs.
- Mechanism is fully explained and matches the pre-edit dimensional proof: Site 1 spread-shock leg went 0→3567 events (Spread/Avg now 2–3.2× crossing the 2.0/3.0 thresholds; under Model=1 it is the sole live shock signal), blocking 2308 EXTREME bars + cutting risk on 1259 → 2011→1905 trades, a risk-reducing change (PF 1.31→1.32, Sharpe 2.51→2.63, Eq-DD 11.26%→11.28% flat, Net −3.5%). Site 2 spread-stability gate is dimensionally corrected but empirically non-binding in this tester (0 quality-block/halve events in both). No runtime errors. Implementation correct; divergence is the intended, mechanism-attributable activation of a previously-dead safety gate. Handing decoded A/B + event counts to stok for trading-impact sign-off (do NOT self-sign stok).

## stok sign-off
- **SIGNED — verdict: ACCEPT-WITH-FOLLOWUP** (binding). stok verified the dimensional proof at source (samples points @1905/1909; both numerators now points; denominator point-valued). Ruling:
  - **(1) Fix CORRECT.** A spread-spike detector whose ratio is permanently ~0.01 vs 2.0/3.0 thresholds is DEAD CODE masquerading as a safety gate — strictly worse than no gate (system reports protection it doesn't have). Blocking new entries when spread is 2–3× its recent average is sound XAUUSD execution-risk control (spread is the cleanest real-time liquidity-withdrawal proxy; blows out around news/rollover/session-gaps where slippage & adverse selection are worst; a doubled spread eats a meaningful fraction of this book's ~+0.16R A+ edge). Gate belongs ON. Leaves position management untouched (blocks only NEW entries).
  - **(2) Trade-impact ACCEPTABLE.** Same shape as 4.2/4.3: risk-reducing gate, −$903 net (−3.5%) but EP +0.23, PF +0.01, Sharpe +0.12 on FLAT Eq-DD. Dispositive = Sharpe up on flat DD (same risk envelope, smoother path, higher quality/trade; survivors better per trade). The −$903 is the cost of declining 106 entries into degraded execution — NOT a defect. Rejecting a dimensionally-correct safety fix because risk control costs 3.5% gross would optimize backtest P&L over live-account survival (backwards).
  - **(3) MODEL-FIDELITY = the FOLLOWUP reason, not REJECT.** Under Model=1 the spread leg is the SOLE live shock signal (bar-range/M5 legs degenerate 0.00); Model=1 modeled per-bar spread is a coarse reconstruction. B's `Spread/Avg = 3.23/3.09/2.07` are plausible gold spike magnitudes, but calibration (are all 2308 EXTREME blocks genuine vs spread-model noise?) is UNPROVEN on synthetic spread = a confidence gap, NOT a correctness defect (direction unambiguously right). Crucially the Model=1 run is the CONSERVATIVE LOWER BOUND on gate activity — live fires all three legs, so the gate triggers ≥ as often live; residual is live behavior partially UNREPRESENTED, not MIS-represented. Dimensional correctness suffices to ship; real-tick is calibration confirmation, not a correctness gate.
  - **(4) EXTREME rate ~5% of bars (2308 / ~45,600 H1 bars ≈ 0.9/day) = upper edge of comfortable but plausible for gold (news/rollover/opens); did not distort the book (Eq-DD flat, −5.3% trades, quality up).** Thresholds (2.0/3.0) + 20-bar baseline window are TUNING SURFACE, not logic — any future adjustment is config, not a loop.
  - **(5) Site 2 correct-but-dormant** (one of 3 components @0.25 weight; pinned at 1.0 pre-fix = a lie, now scales correctly). No action.
  - **Verdict: ACCEPT-WITH-FOLLOWUP — COMMIT as-is; do NOT loop (no logic to change; thresholds are correct defaults).**

## Iteration-6 follow-ups (logged by stok; validation/optimization, NEITHER blocks this commit)
- **(1, validation) Real-tick spot-check:** ONE A/B on XAUUSD+ H1, **Model=4 (Every-tick based on real ticks)** over a spread-stress window (a quarter spanning ≥2 NFP/CPI/FOMC events). Confirm (i) `[ShockDetector] Spread/Avg` sits ~1.0 in calm and crosses 2.0/3.0 only around genuine wide-spread events; (ii) the bar-range + M5 legs come ALIVE (non-zero) as predicted; (iii) combined block rate stays sane (rough ceiling ~5–7% of bars). Confirms the calibration Model=1 structurally cannot.
- **(2, optimization, lower priority) Threshold sensitivity:** if the real-tick block rate exceeds ~7% of bars OR net degrades materially beyond −3.5%, sweep extreme/moderate thresholds (e.g. 2.5/3.5) and/or median-filter the 20-bar baseline. CONFIG ONLY — no logic change.

## Decision
- **DONE — both agents signed (mt5 implementation-correct; stok ACCEPT-WITH-FOLLOWUP, binding, source-verified). Committed source-only on `feat/multi-strategy`.** Fix activates the previously-dead spread-shock gate (DIVERGED: 2308 EXTREME-blocks + 1259 moderate-cuts → 2011→1905 trades, net −3.5% but Sharpe +0.12 / PF +0.01 / Eq-DD flat = risk-reducing). Shipped on proven dimensional correctness; Model=1 is the conservative lower bound on gate activity. Two Iter-6 follow-ups logged (real-tick calibration spot-check; threshold sensitivity). NOTE for resume: the first 4.6 mt5 agent hit a SPURIOUS content-filter false-positive on its final message and persisted NOTHING — orchestrator made the proven dimensional edit directly + a minimal mechanical agent ran the A/B. NEXT ACTION → Phase 4.7.

## Commit
- **Hash:** `397fa7b` (source-only: CEnhancedTradeExecutor.mqh + 4-6.md + 00-STATUS.md; `.ex5` intentionally uncommitted). Branch `feat/multi-strategy`.
